// LOOKED-UP RULES, NOT CARRIED RULES.
// Every step of a tool loop re-sends the whole system prompt. Measured on this agent
// 2026-08-05: 25,250 bytes of system plus 6,730 bytes of tool schemas on every one of nine
// steps of a single instruction — 287,820 bytes of identical text for one job. The sections
// below are situational: an article job never needs the leads pipeline, a leads job never
// needs the X format gate. They live here and are fetched with the `rules` tool when the
// work calls for them, so the constant cost of every call carries only what every call needs.
//
// THE TRADE, STATED PLAINLY: a rule that is always present is always obeyed; a rule that must
// be fetched can be skipped. This moves a class of failure from "too expensive" to "forgot to
// look". The index in the system prompt names every section and says that working from memory
// instead of fetching is itself the failure. Sections whose omission is unrecoverable — scope
// law, verification, refusals, never state a number you did not read — are NOT here. They stay
// in the prompt at full price.

export const RULES = {
  "article": "THE ARTICLE WRITE PATH HAS FOUR GATES AND EACH ONE REFUSES THE WHOLE WRITE. Satisfy all four on the FIRST put; discovering them one 422 at a time costs a full tool loop each.\n 1. CLAIM_LAW — claims:[{id,text,tier,source_ids,why_material}], count scales with words. The 422 states claims_required and claims_found.\n 2. SOURCE_QUOTE_LAW — every source is {id,url,quote,type} and the quote is the source\u2019s own verbatim words, 40 chars minimum. Never the title, never a summary, never a gloss, never composed by you. If you cannot retrieve the words, drop the source rather than decorate it.\n 3. editorial preflight (headline) — the title names the subject and its central event. A teaser, a statistic hook or an evidence tier in the headline is refused.\n 4. hero preflight — hero + hero_brief + editorial_review{headline_subject,hero_subject,visual_action,rationale,inspected:true,inspection_note}, and you must actually open the rendered image first.\nCLAIMS ARE MANDATORY OR THE WRITE IS REFUSED. ARTICLE_PUT answers 422 claim_law_refused without claims:[{id,text,tier,source_ids,why_material}], and the count required scales with word count — the refusal states claims_required and claims_found so you never have to guess. Tiers: human, rct, trial, animal, mechanistic, in-vitro, cell, case, observational, regulatory, expert, definition, unsourced. Every source_id must exist in sources[] or it is refused as dangling. THE HEADLINE IS GATED TOO: editorial_preflight_refused with headline_preflight means the title does not name the subject and its central event — a teaser, a statistic hook or an evidence tier in the headline is refused. Write both before the first PUT rather than discovering them as 422s.\nEDITING AN ARTICLE. Never send a shortened body: ARTICLE_PUT REPLACES the article, and the\nbuild's shrink guard answers 409 when the incoming body is smaller than what is there. To\nchange part of an article, first GET the whole body from\nhttps://miscsubjects.com/api/articles/<slug> (or the /bundle?format=markdown view), edit\nthat full text, then PUT the whole thing back. ART_PATCH merges fields (claims, sources,\ntags, title) without touching the body and is the right call when the body is not changing.\nARTICLE_PUT REQUIRES slug AND title IN THE PAYLOAD, ALWAYS, even when neither is changing.\nA body-only PUT answers 400 {\"error\":\"slug and title required\"} — verified against the live\nAPI 2026-08-05. Carry the existing title through from the GET rather than inventing one, and\nsend slug, title, body together with the other fields you read back (tags, category,\nregister, status, hero). Two ways in, both fine: capability ARTICLE_PUT, or a direct\nPUT https://miscsubjects.com/api/articles/<slug> whose auth header is x-terminal-key — a\nBearer token answers 401 on that route.\nSETTING A HERO IS GATED BY AN EDITORIAL PREFLIGHT, AND ART_PATCH ALONE WILL BE REFUSED.\nA patch that sets hero without the review answers 422 editorial_preflight_refused and lists\nwhat is missing. The write must carry hero, hero_brief, and editorial_review with ALL of\nheadline_subject, hero_subject, visual_action, rationale, inspected:true and inspection_note.\ninspected:true is a statement that you opened the actual rendered image and looked at it — do\nthat first and write what is visibly in the frame, because a prompt is not visual proof. The\ngate also refuses a brief that reuses another article's props (it named \"red string\" as a\nhouse motif on 2026-08-05): a reference image sets the level of craft, not the objects, so the\nimage has to be one this article earns on its own. When it refuses, fix the image or the\nbrief and resubmit — never the gate.\n\nARTICLE WORK IS NOT OUTREACH WORK. Before writing, rewriting, or fixing any ARTICLE\ncontent (body, hero, images), read ${BUILD}/.claude/skills/writing-law/SKILL.md and\n${BUILD}/.claude/skills/design-law/SKILL.md \u2014 not outreach-law. Outreach law governs cold\nemail only. Reading the wrong law file is the same defect as not reading one at all.\nEVERY ARTICLE CARRIES A FEATURED IMAGE. Before finishing any article write or rewrite,\ncheck the hero field. If it is null, generate one (ARCADS_GENERATE \u2192 ARCADS_TO_R2) and set\nit with ART_PATCH (args: slug|json_body). A finished article with hero:null is unfinished.\nCANONICAL WORK GETS MONUMENTAL EFFORT. When he names an article canonical, or says an idea\nis huge, the output must match: full-length body, every claim sourced, no stub sections,\nno /tmp write-and-forget. The work is saved to the article itself (ARTICLE_PUT of the full\nbody, read back after) before the turn ends. Writing the rewrite to a temp file and\nreporting it as done is the same failure as not writing it at all.\nA SAVE IS NOT DONE UNTIL READ BACK. After any ARTICLE_PUT or ART_PATCH, GET the article\nand confirm the field you set carries the value you sent. Report the read-back value, not\nthe write call.",
  "x": "POSTING TO X. X_POST takes one plain-text body, newlines and all, max 280 characters.\nTHE GATE IS `xFormatViolation` IN functions/_lib/fn_runners.js AND IT REJECTS SEVEN THINGS, NOT FOUR.\nThis section said four until 2026-08-05, when the agent read the function and found the summary had\ndrifted from the code. The seven, by the exact slug each returns:\n 1. machine_log_header — a [bracketed log header] on the first line. The headline goes there.\n 2. model_self_introduction — \"I am kimi/grok/claude\" anywhere in the copy. The account is his.\n 3. model_self_promotion — \"kimi shipped\", \"grok wrote\". Write the finding, not the author.\n 4. missing_signature — the last line must be an em dash, the model name, the surface in parentheses.\n 5. paragraph_blob — one body line over 180 chars after stripping URLs, signature and hashtag-only lines.\n 6. generic_hashtag — it names the offending tags back to you.\n 7. hashtag_spam — too many tags; it returns the count.\nWhat passes: a hook under about eight words, a blank line, one short beat per line, the signature last.\nReal line breaks in the argument. Read the slug it returns and fix that one thing; never guess twice.\nIF THIS SECTION AND THE FUNCTION EVER DISAGREE, THE FUNCTION IS RIGHT — go read it and fix this text.",

  "counts": "COUNTS COME FROM COUNT(*), NEVER FROM A LIST'S LENGTH. LEADS_LIST and every list\ncapability cap what they SHOW (the \"shown\" field is a display cap, not a total). The\nby_status block in LEADS_LIST carries the real totals. To answer \"how many\", read\nby_status or run D1_QUERY with COUNT(*) \u2014 a shown/limit number stated as a total is a\nwrong answer.",
  "db": "THE TWO DATABASES, AND HOW TO QUERY THEM. Do not go hunting for table names \u2014 they are here.\n - Content and capabilities: capability D1_QUERY with plain SQL as the body. Tables:\n   articles(slug, title, subject, published, created_at, updated_at, body, meta),\n   article_slots, oip_articles, directory(key, type, target, auth, content, category,\n   enabled, ...), settings, agents. Sources and claims live inside articles.meta as JSON,\n   so count them with json_array_length(json_extract(meta, \"$.sources\")).\n   Example: D1_QUERY | SELECT slug, updated_at FROM articles WHERE json_array_length(json_extract(meta, \"$.sources\")) = 0 ORDER BY updated_at ASC LIMIT 3\n - Events and receipts: capability LEDGER_QUERY against events (a different database).\nUse double quotes for SQL string literals; single quotes are eaten by the argument parser.\nIf a query errors twice, run SELECT name FROM pragma_table_info(\"<table>\") and read the\ncolumns rather than guessing a third time.",
  "money": "WHERE MONEY LIVES. The ledger has NO cost column \u2014 never try to answer a cost question from\nevents. Model spend is in the Cloudflare AI Gateway logs: capability CF_AI_GATEWAY_LIST_LOGS\n(gateway_id \"default\"), or CF_EXEC with\nGET /accounts/$CF_ACCOUNT/ai-gateway/gateways/default/logs?limit=100 \u2014 each entry carries\ncost, tokens_in, output_tokens, input_cached_tokens, model and created_at. Sum the cost field over\nthe entries to answer \"what did today cost\". Cached input bills at a fraction of fresh\ninput, so never price a turn without reading input_cached_tokens.",
  "ledger": "READING THE LEDGER: use LEDGER_QUERY with SQL against the events table\n(columns: id, ts, source, key, route, actor, action, status, trace_id, request_preview,\nresponse_preview, request_size, response_size), or LEDGER_ERRORS for recent failures.\nThe plain LEDGER key is admin-gated and answers 401 \u2014 do not use it.\nUse double quotes for string literals in that SQL \u2014 single quotes are eaten by the\ndispatch argument parser and the query comes back as an error.\nExample: LEDGER_QUERY | SELECT key, COUNT(*) n FROM events WHERE source = \"aig\" GROUP BY key ORDER BY n DESC LIMIT 20",
  "marketing": "MARKETING AND OUTREACH \u2014 THE REAL LOOP. This is a live wholesale business, not a demo.\n - The pipeline is leads in D1, one lead per business, with a status: new \u2192 enriched \u2192\n   drafted \u2192 sent. LEADS_LIST gives the counts and the leads themselves (body \"drafted\" or\n   \"status=drafted|limit=20\" filters). Each lead carries id, name, segment, city, website,\n   email, score and its saved draft.\n - LEADS_LIST PAGES ITS ROWS (default 20). Counting the rows it returned is NOT a count \u2014\n   two runs answered \"20\" and \"674\" to the same question that way (2026-07-27). A count\n   comes from its counts field, or from D1_QUERY with SELECT COUNT(*) FROM leads WHERE\n   status = \"enriched\". Always the count field, never the length of one page.\n - LEADS_DRAFT (id) writes and SAVES a draft on one lead. LEADS_DRAFT_AI drafts with live\n   grounding in the business's own site. Neither sends. LEADS_FOLLOWUPS adds touch 2 and 3.\n - LEADS_SEND sends ONE reviewed lead. NEVER send outreach he has not approved in this\n   conversation. Drafting, rewriting and saving are always allowed; sending is not.\n - LEADS_DISCOVER / _PLACES / _NPI find businesses, LEADS_ENRICH_BATCH finds their emails,\n   LEADS_SCORE_AI scores them. LEADS_RUN_CITY does discover-then-enrich in one call.\n - The drafts he reads are at https://miscsubjects.com/admin/marketing. That page is\n   key-gated: open it in the browser for him when he asks to SEE them, and read the data\n   itself with LEADS_LIST rather than trying to log in.\n - THE WRITING LAW IS A FILE, NOT A MEMORY. Before you write or rewrite any outreach copy,\n   read ${BUILD}/.claude/skills/outreach-law/SKILL.md, and cold-email/SKILL.md next to it.\n   Apply the clause ids. The commonest violations are: an opener that would fit any business\n   in the segment, the sender as the subject of the first sentence, a meeting as the ask,\n   and internal parameters (minimums, discounts) leaking into the copy.",
};

export const RULES_INDEX = {
  "article": "creating or editing an article: the PUT/PATCH contract, the shrink guard, which law file to read first, heroes, and the read-back rule",
  "x": "posting to X: the exact shape the format gate accepts and what it rejects",
  "counts": "answering \"how many\" without stating a wrong number",
  "db": "the two databases: table names, which capability queries which, and the SQL quoting trap",
  "money": "where model spend actually lives and how to price a turn",
  "ledger": "reading the event ledger with SQL",
  "marketing": "the leads pipeline end to end, what drafts, what sends, and the writing-law files to read first",
};

export function rulesIndexText() {
  return Object.entries(RULES_INDEX).map(([k, v]) => `- ${k} — ${v}`).join('\n');
}

// CARRIED, NOT LOOKED UP (2026-08-05, reversing the same day's change).
// The lookup saved 5,318 bytes on every step and cost the agent its ability to work: the
// first real job after the move fabricated two rules rather than fetching them, so a gate was
// added forcing the fetch, and the gate then consumed a 20-step budget on compulsory reading
// before any write could be attempted. Three mechanisms where one had done. Codex ships
// 36,223 bytes of system prompt; this one carries 8,160 bytes of contracts and is still
// smaller. The prefix is the constant term. It was never the thing to optimise.
export function rulesText() {
  return Object.entries(RULES).map(([k, v]) => `--- ${k} ---\n${v}`).join('\n\n');
}
