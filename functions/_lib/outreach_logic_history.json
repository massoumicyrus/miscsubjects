{"generated_at":"2026-07-25T19:58:09.362Z","sources":[{"id":"draft_rules","label":"The drafting instructions — what the writer is told to write, and what gets a draft thrown away","path":"functions/_lib/fn_runners.js","versions":[{"commit":"75edd3f14","date":"2026-07-25T11:56:15-07:00","what_changed":"owner facts into drafting rules: 50% of shown price with MOQ 3 units stated in high register, COAs available on request, ~2-day Dallas shipping, reply channel line in every email, prospect reply-to = support@leoresearch.com; Operational Logic central in the shared nav-hub header + footer beside writing/design #vault-approved","chars":10609,"added":["'3. State the wholesale model plainly, one line: every item is 50% of the price shown on leoresearch.com, they set their own price from there. Wholesale pricing applies from three units — state the minimum plainly and in high register, never as smallness or cheapness. That is the whole pricing story — no kits, no tiers, no volume ladder. White-label available.\\n' +","'5. One line: third-party COAs available on request; orders ship nationwide from Dallas in approximately two days. NEVER say Austin — the warehouse is Dallas. Do not volunteer samples — samples are discretionary, never the headline offer.\\n' +","'6. ONE soft CTA, phrased as a question they can answer in one word, offering the smallest credible first step in high register — the smallness is conveyed as their convenience, never as price or cheapness. Never state dollar amounts, \"just\", \"only\", or \"try\" in the CTA: e.g. \"Would it be useful to run a first order alongside your current supply and review the COAs yourself?\" End with one short line telling them how to respond: replies to this email reach us directly; support@leoresearch.com also works. NEVER \"reply wholesale\" / \"reply for the sheet\" / \"send the sheet\" — the list is already here.\\n' +"],"removed":["'3. State the wholesale model plainly, one line: every item is 50% of the price listed on leoresearch.com, they set their own price from there. That is the whole pricing story — no kits, no tiers, no minimums, no volume ladder. White-label available.\\n' +","'5. One line: third-party COA on every lot, two-day shipping nationwide from Dallas. NEVER say Austin — the warehouse is Dallas. Do not volunteer samples — samples are discretionary, never the headline offer.\\n' +","'6. ONE soft CTA, phrased as a question they can answer in one word, offering the smallest credible first step in high register — the smallness is conveyed as their convenience, never as price or unit count. Never state dollar amounts, unit minimums, \"just\", \"only\", or \"try\" in the CTA: e.g. \"If it would be useful, we can start with a small first order alongside your current supply — you can run the COAs yourself.\" NEVER \"reply wholesale\" / \"reply for the sheet\" / \"send the sheet\" — the list is already here. (Offer architecture, never voiced: first order can be as small as three units / ~$75 — this informs what the operator will accept, not what the message says.)\\n' +"],"text":"async leadsDraftAI(env, leadId, venture) {\n    const id = Number(leadId);\n    if (!id) return JSON.stringify({ error: 'lead_id required' });\n    const lead = await env.DB.prepare('SELECT id,name,segment,city,email,website,context,status,score,COALESCE(notes,\\'\\') notes FROM leads WHERE id=?').bind(id).first();\n    if (!lead) return JSON.stringify({ error: 'lead not found: ' + id });\n    if (!lead.email) return JSON.stringify({ blocked: true, error: 'verified_email_required', note: 'Nothing drafted. Enrich the lead first.' });\n    if (!String(lead.notes).includes('mx:ok')) return JSON.stringify({ blocked: true, error: 'mx_verification_required', note: 'Nothing drafted. Verify MX first.' });\n    if (!String(lead.notes).includes('icp:') || Number(lead.score || 0) < 65) return JSON.stringify({ blocked: true, error: 'icp_threshold_not_met', score: Number(lead.score || 0), minimum: 65, note: 'Nothing drafted. Only verified high-fit leads enter copy review.' });\n    if (String(lead.context || '').trim().length < 40) return JSON.stringify({ blocked: true, error: 'grounding_context_required', note: 'Nothing drafted. The business site context is too thin for truthful personalization.' });\n    const suppressed = await env.DB.prepare('SELECT reason FROM lead_suppressions WHERE lower(email)=lower(?)').bind(lead.email).first();\n    if (suppressed) return JSON.stringify({ blocked: true, error: 'recipient_suppressed', reason: suppressed.reason || 'suppressed' });\n    const brand = String(venture || 'LeoResearch');\n    const dr = await env.DB.prepare(\"SELECT content FROM directory WHERE key='OUTREACH_DOSSIER'\").first();\n    const dossier = dr ? String(dr.content || '') : '';\n    if (!dossier) return JSON.stringify({ error: 'OUTREACH_DOSSIER row is empty — set the brand/team/offer knowledge first' });\n    const cr = await env.DB.prepare(\"SELECT content FROM directory WHERE key='OUTREACH_CATALOG'\").first();\n    const catalog = cr ? String(cr.content || '') : '';\n    const segWord = ({ medspa: 'med spa', clinic: 'clinic', wellness: 'wellness practice', gym: 'gym', supplement: 'supplement brand' })[lead.segment] || 'business';\n    const user =\n      'Recipient business: ' + lead.name + ' — a ' + segWord + ' in ' + (lead.city || 'their city') + '.\\n' +\n      'What they do (from their own site): ' + (lead.context || lead.website || 'unknown') + '.\\n\\n' +\n      'WHOLESALE CATALOG (include this exact list in the email, formatted as a clean list — do not invent items or prices):\\n' + catalog + '\\n\\n' +\n      'Write ONE B2B wholesale email from ' + brand + '. It must read like one competent operator wrote it to another — not marketing, not a blast. Follow exactly:\\n' +\n      '1. SALUTATION: \"Hello,\" on its own line. NEVER \"Hi <business> team\" / \"team at\" / any use of the word \"team\". If a real person first name is known, \"Hi <First>,\".\\n' +\n      '2. FIRST LINE = ONE TRUE, SPECIFIC OBSERVATION drawn ONLY from \"What they do\" above, connected to supply. Name a real thing from their site: a compound or service they actually list, a second location, their booking tool. Examples of the SHAPE (do not copy): \"You already list peptides on the site — we supply the raw compounds wholesale.\" / \"You run hormone therapy and medical weight loss — the peptides behind those usually come from a supplier you can switch.\" The observation must be a plain fact you could point to on their page, then pivot to supply in the same or next sentence. It is FORBIDDEN to compliment, admire, congratulate, or infer (\"positioned\", \"perfectly positioned\", \"focus on\", \"committed to\", \"dedicated to\", \"expand into\", \"with your emphasis on\"). If \"What they do\" contains NO concrete hook, skip the observation entirely and open plainly: \"We supply research peptides wholesale.\" A generic or invented observation is worse than none.\\n' +\n      '3. State the wholesale model plainly, one line: every item is 50% of the price shown on leoresearch.com, they set their own price from there. Wholesale pricing applies from three units — state the minimum plainly and in high register, never as smallness or cheapness. That is the whole pricing story — no kits, no tiers, no volume ladder. White-label available.\\n' +\n      '4. INCLUDE THE FULL CATALOG LIST above, verbatim, as a clean list — every item WITH its listed price, its wholesale price, and its product link exactly as given. This is the reference the owner requires in the email.\\n' +\n      '5. One line: third-party COAs available on request; orders ship nationwide from Dallas in approximately two days. NEVER say Austin — the warehouse is Dallas. Do not volunteer samples — samples are discretionary, never the headline offer.\\n' +\n      '6. ONE soft CTA, phrased as a question they can answer in one word, offering the smallest credible first step in high register — the smallness is conveyed as their convenience, never as price or cheapness. Never state dollar amounts, \"just\", \"only\", or \"try\" in the CTA: e.g. \"Would it be useful to run a first order alongside your current supply and review the COAs yourself?\" End with one short line telling them how to respond: replies to this email reach us directly; support@leoresearch.com also works. NEVER \"reply wholesale\" / \"reply for the sheet\" / \"send the sheet\" — the list is already here.\\n' +\n      '7. No clinical, efficacy, treatment, recovery, dosing, or outcome language. Commerce facts only.\\n' +\n      '8. NO compliance boilerplate in the body — the send system appends the opt-out footer.\\n' +\n      '9. Sign off with exactly one line: LeoResearch. NEVER a personal name. NEVER the word \"team\".\\n' +\n      '10. BREVITY: outside the catalog list, the whole email is under 90 words. Every non-list sentence earns its place or is cut. Plain human register, contractions welcome. BANNED words: cutting-edge, elevate, seamless, revolutionize, unlock, empower, streamline, robust, world-class, game-changer, thrilled, excited, \"hope this finds you\", \"wanted to reach out\", \"reaching out\", \"positioned\", \"perfectly\", \"expand into\", \"focus on\", \"committed to\", \"dedicated to\", \"team\". No exclamation points. At most one em-dash. If a line sounds like AI, cut it.\\n' +\n      'SUBJECT: 2-4 words, all lowercase, boring and internal-looking like a note from a colleague — e.g. \"peptide wholesale\", \"reorder costs\", \"your supplier\", \"wholesale + coa\". NEVER title case, NEVER the word \"pricing\"/\"offer\"/\"partnership\", NEVER a business name, NEVER \"Came across\".\\n' +\n      'Return ONLY strict JSON: {\"subject\": \"...\", \"body\": \"...\"} with \\\\n for line breaks in body.';\n    const g = await callGateway(env, dossier, user, 1200);\n    if (g.err) return JSON.stringify({ error: 'model call failed: ' + g.err });\n    let parsed = pipeJson(g.text);\n    if (!parsed) { try { parsed = JSON.parse(g.text); } catch {} }\n    if (!parsed || !parsed.subject || !parsed.body) {\n      const mm = String(g.text).match(/\\{[\\s\\S]*\\}/);\n      if (mm) { try { parsed = JSON.parse(mm[0]); } catch {} }\n    }\n    if (!parsed || !parsed.subject || !parsed.body) return JSON.stringify({ error: 'model did not return subject/body', raw: String(g.text).slice(0, 300) });\n    // Scrub any personal name AND the banned word \"team\" from the signoff/body → brand only.\n    parsed.body = String(parsed.body)\n      .replace(/\\bOWNER_FIRST_NAME\\s+[OWNER_SURNAME]\\b/g, 'LeoResearch')\n      .replace(/\\bOWNER_FIRST_NAME\\b/g, 'LeoResearch')\n      .replace(/\\bthe\\s+LeoResearch\\s+team\\b/gi, 'LeoResearch')\n      .replace(/\\bLeoResearch\\s+team\\b/gi, 'LeoResearch')\n      .replace(/\\bteam\\s+at\\s+[A-Z][^\\n,.]{0,40}/g, 'Hello')\n      .replace(/\\bHi\\s+[^\\n,]{0,50}\\bteam\\b/gi, 'Hello');\n    const draft = { subject: String(parsed.subject), body: String(parsed.body), venture: brand, model: g.fallback ? 'gemini-2.5-flash(fallback)' : 'grok/grok-4.3', by: 'leadsDraftAI' };\n    const unsafe = /commonly used|use peptides|used in|support recovery|for recovery|clinical efficacy|treat(?:ment|ing)?|patient outcomes?|healing|therapeutic/i.test(draft.body);\n    if (unsafe) return JSON.stringify({ blocked: true, error: 'unsafe_copy_claim', note: 'Nothing saved. The model introduced a clinical/use claim; retry the draft.' });\n    // HARD SLOP REJECT: AI-tell phrases and any surviving \"team\" → do not save; force a retry.\n    const slop = /\\bteam\\b|positioned|perfectly|\\bexpand(?:ing)? into\\b|with your focus|your focus on|focus on (?:your|hormone|wellness|providing)|committed to|dedicated to|cutting[- ]edge|elevate|seamless|revolutioni[sz]e|unlock|empower|streamline|world[- ]class|game[- ]chang|thrilled|excited to|hope this (?:finds|email)|wanted to reach out|reaching out|as a (?:leading|premier)|your commitment/i.test(draft.body + ' ' + draft.subject);\n    if (slop) return JSON.stringify({ blocked: true, error: 'slop_or_banned_phrase', note: 'Nothing saved. Draft contained an AI-tell / banned phrase (team, positioned, focus on, committed to, etc.); retry.' });\n    // SUBJECT contract: 2-4 words, lowercase, internal-looking. Reject title-case, salesy words, business name.\n    const subj = String(draft.subject || '').trim();\n    const subjWords = subj.split(/\\s+/).filter(Boolean);\n    const subjBad = subjWords.length > 5 || /pricing|offer|partnership|introduction|opportunity|solution|premium|exclusive/i.test(subj) || /[A-Z]/.test(subj.replace(/COA|RUO/g, '')) || (lead.name && subj.toLowerCase().includes(String(lead.name).toLowerCase().split(/\\s+/)[0].toLowerCase()));\n    if (subjBad) return JSON.stringify({ blocked: true, error: 'subject_contract', note: 'Nothing saved. Subject must be 2-4 lowercase internal words (e.g. \"peptide wholesale\"), no title case / salesy words / business name; retry.' });\n    // The reply-no stop line lives in the send-time footer (leadsSend appends it) — requiring it\n    // in the body doubled it and read as boilerplate. Body contract = store mention only.\n    if (!/leoresearch\\.com/i.test(draft.body)) return JSON.stringify({ blocked: true, error: 'copy_contract_missing', note: 'Nothing saved. Draft must mention the store (leoresearch.com).' });\n    await env.DB.prepare(\"UPDATE leads SET draft=?, status='drafted' WHERE id=?\").bind(JSON.stringify(draft), id).run();\n    return JSON.stringify({ id, to: lead.email || '(no email yet)', subject: draft.subject, body: draft.body, model: draft.model });\n  },\n  // Follow-up sequence for a drafted lead. 40-60% of positive replies come from touches 2-3, not\n  // the first send — but each must carry a NEW angle on the SAME thread, never \"just checking in\".\n  // Generates touch 2 (new angle) + touch 3 (honest break-up). $1=lead_id, $2=brand(default LeoResearch).\n  ","version":12,"of":12},{"commit":"9712944d5","date":"2026-07-25T11:39:48-07:00","what_changed":"outreach-law + drafting rules: architecture-is-not-copy (risk parameters inform design, never voiced — state the asymmetry, never the arithmetic), status law (never reduce perceived status for accessibility; cordial-not-familiar register table), parameter-leak and register-substitution added as failure classes; CTA rules now high-register small-first-order, no dollar amounts or unit counts in copy #vault-approved","chars":10536,"added":["'6. ONE soft CTA, phrased as a question they can answer in one word, offering the smallest credible first step in high register — the smallness is conveyed as their convenience, never as price or unit count. Never state dollar amounts, unit minimums, \"just\", \"only\", or \"try\" in the CTA: e.g. \"If it would be useful, we can start with a small first order alongside your current supply — you can run the COAs yourself.\" NEVER \"reply wholesale\" / \"reply for the sheet\" / \"send the sheet\" — the list is already here. (Offer architecture, never voiced: first order can be as small as three units / ~$75 — this informs what the operator will accept, not what the message says.)\\n' +"],"removed":["'6. ONE soft CTA, phrased as a question they can answer in one word, offering the smallest credible first step — a first order can be three units, about $75: e.g. \"Want to try three units first?\" NEVER \"reply wholesale\" / \"reply for the sheet\" / \"send the sheet\" — the list is already here.\\n' +"],"text":"async leadsDraftAI(env, leadId, venture) {\n    const id = Number(leadId);\n    if (!id) return JSON.stringify({ error: 'lead_id required' });\n    const lead = await env.DB.prepare('SELECT id,name,segment,city,email,website,context,status,score,COALESCE(notes,\\'\\') notes FROM leads WHERE id=?').bind(id).first();\n    if (!lead) return JSON.stringify({ error: 'lead not found: ' + id });\n    if (!lead.email) return JSON.stringify({ blocked: true, error: 'verified_email_required', note: 'Nothing drafted. Enrich the lead first.' });\n    if (!String(lead.notes).includes('mx:ok')) return JSON.stringify({ blocked: true, error: 'mx_verification_required', note: 'Nothing drafted. Verify MX first.' });\n    if (!String(lead.notes).includes('icp:') || Number(lead.score || 0) < 65) return JSON.stringify({ blocked: true, error: 'icp_threshold_not_met', score: Number(lead.score || 0), minimum: 65, note: 'Nothing drafted. Only verified high-fit leads enter copy review.' });\n    if (String(lead.context || '').trim().length < 40) return JSON.stringify({ blocked: true, error: 'grounding_context_required', note: 'Nothing drafted. The business site context is too thin for truthful personalization.' });\n    const suppressed = await env.DB.prepare('SELECT reason FROM lead_suppressions WHERE lower(email)=lower(?)').bind(lead.email).first();\n    if (suppressed) return JSON.stringify({ blocked: true, error: 'recipient_suppressed', reason: suppressed.reason || 'suppressed' });\n    const brand = String(venture || 'LeoResearch');\n    const dr = await env.DB.prepare(\"SELECT content FROM directory WHERE key='OUTREACH_DOSSIER'\").first();\n    const dossier = dr ? String(dr.content || '') : '';\n    if (!dossier) return JSON.stringify({ error: 'OUTREACH_DOSSIER row is empty — set the brand/team/offer knowledge first' });\n    const cr = await env.DB.prepare(\"SELECT content FROM directory WHERE key='OUTREACH_CATALOG'\").first();\n    const catalog = cr ? String(cr.content || '') : '';\n    const segWord = ({ medspa: 'med spa', clinic: 'clinic', wellness: 'wellness practice', gym: 'gym', supplement: 'supplement brand' })[lead.segment] || 'business';\n    const user =\n      'Recipient business: ' + lead.name + ' — a ' + segWord + ' in ' + (lead.city || 'their city') + '.\\n' +\n      'What they do (from their own site): ' + (lead.context || lead.website || 'unknown') + '.\\n\\n' +\n      'WHOLESALE CATALOG (include this exact list in the email, formatted as a clean list — do not invent items or prices):\\n' + catalog + '\\n\\n' +\n      'Write ONE B2B wholesale email from ' + brand + '. It must read like one competent operator wrote it to another — not marketing, not a blast. Follow exactly:\\n' +\n      '1. SALUTATION: \"Hello,\" on its own line. NEVER \"Hi <business> team\" / \"team at\" / any use of the word \"team\". If a real person first name is known, \"Hi <First>,\".\\n' +\n      '2. FIRST LINE = ONE TRUE, SPECIFIC OBSERVATION drawn ONLY from \"What they do\" above, connected to supply. Name a real thing from their site: a compound or service they actually list, a second location, their booking tool. Examples of the SHAPE (do not copy): \"You already list peptides on the site — we supply the raw compounds wholesale.\" / \"You run hormone therapy and medical weight loss — the peptides behind those usually come from a supplier you can switch.\" The observation must be a plain fact you could point to on their page, then pivot to supply in the same or next sentence. It is FORBIDDEN to compliment, admire, congratulate, or infer (\"positioned\", \"perfectly positioned\", \"focus on\", \"committed to\", \"dedicated to\", \"expand into\", \"with your emphasis on\"). If \"What they do\" contains NO concrete hook, skip the observation entirely and open plainly: \"We supply research peptides wholesale.\" A generic or invented observation is worse than none.\\n' +\n      '3. State the wholesale model plainly, one line: every item is 50% of the price listed on leoresearch.com, they set their own price from there. That is the whole pricing story — no kits, no tiers, no minimums, no volume ladder. White-label available.\\n' +\n      '4. INCLUDE THE FULL CATALOG LIST above, verbatim, as a clean list — every item WITH its listed price, its wholesale price, and its product link exactly as given. This is the reference the owner requires in the email.\\n' +\n      '5. One line: third-party COA on every lot, two-day shipping nationwide from Dallas. NEVER say Austin — the warehouse is Dallas. Do not volunteer samples — samples are discretionary, never the headline offer.\\n' +\n      '6. ONE soft CTA, phrased as a question they can answer in one word, offering the smallest credible first step in high register — the smallness is conveyed as their convenience, never as price or unit count. Never state dollar amounts, unit minimums, \"just\", \"only\", or \"try\" in the CTA: e.g. \"If it would be useful, we can start with a small first order alongside your current supply — you can run the COAs yourself.\" NEVER \"reply wholesale\" / \"reply for the sheet\" / \"send the sheet\" — the list is already here. (Offer architecture, never voiced: first order can be as small as three units / ~$75 — this informs what the operator will accept, not what the message says.)\\n' +\n      '7. No clinical, efficacy, treatment, recovery, dosing, or outcome language. Commerce facts only.\\n' +\n      '8. NO compliance boilerplate in the body — the send system appends the opt-out footer.\\n' +\n      '9. Sign off with exactly one line: LeoResearch. NEVER a personal name. NEVER the word \"team\".\\n' +\n      '10. BREVITY: outside the catalog list, the whole email is under 90 words. Every non-list sentence earns its place or is cut. Plain human register, contractions welcome. BANNED words: cutting-edge, elevate, seamless, revolutionize, unlock, empower, streamline, robust, world-class, game-changer, thrilled, excited, \"hope this finds you\", \"wanted to reach out\", \"reaching out\", \"positioned\", \"perfectly\", \"expand into\", \"focus on\", \"committed to\", \"dedicated to\", \"team\". No exclamation points. At most one em-dash. If a line sounds like AI, cut it.\\n' +\n      'SUBJECT: 2-4 words, all lowercase, boring and internal-looking like a note from a colleague — e.g. \"peptide wholesale\", \"reorder costs\", \"your supplier\", \"wholesale + coa\". NEVER title case, NEVER the word \"pricing\"/\"offer\"/\"partnership\", NEVER a business name, NEVER \"Came across\".\\n' +\n      'Return ONLY strict JSON: {\"subject\": \"...\", \"body\": \"...\"} with \\\\n for line breaks in body.';\n    const g = await callGateway(env, dossier, user, 1200);\n    if (g.err) return JSON.stringify({ error: 'model call failed: ' + g.err });\n    let parsed = pipeJson(g.text);\n    if (!parsed) { try { parsed = JSON.parse(g.text); } catch {} }\n    if (!parsed || !parsed.subject || !parsed.body) {\n      const mm = String(g.text).match(/\\{[\\s\\S]*\\}/);\n      if (mm) { try { parsed = JSON.parse(mm[0]); } catch {} }\n    }\n    if (!parsed || !parsed.subject || !parsed.body) return JSON.stringify({ error: 'model did not return subject/body', raw: String(g.text).slice(0, 300) });\n    // Scrub any personal name AND the banned word \"team\" from the signoff/body → brand only.\n    parsed.body = String(parsed.body)\n      .replace(/\\bOWNER_FIRST_NAME\\s+[OWNER_SURNAME]\\b/g, 'LeoResearch')\n      .replace(/\\bOWNER_FIRST_NAME\\b/g, 'LeoResearch')\n      .replace(/\\bthe\\s+LeoResearch\\s+team\\b/gi, 'LeoResearch')\n      .replace(/\\bLeoResearch\\s+team\\b/gi, 'LeoResearch')\n      .replace(/\\bteam\\s+at\\s+[A-Z][^\\n,.]{0,40}/g, 'Hello')\n      .replace(/\\bHi\\s+[^\\n,]{0,50}\\bteam\\b/gi, 'Hello');\n    const draft = { subject: String(parsed.subject), body: String(parsed.body), venture: brand, model: g.fallback ? 'gemini-2.5-flash(fallback)' : 'grok/grok-4.3', by: 'leadsDraftAI' };\n    const unsafe = /commonly used|use peptides|used in|support recovery|for recovery|clinical efficacy|treat(?:ment|ing)?|patient outcomes?|healing|therapeutic/i.test(draft.body);\n    if (unsafe) return JSON.stringify({ blocked: true, error: 'unsafe_copy_claim', note: 'Nothing saved. The model introduced a clinical/use claim; retry the draft.' });\n    // HARD SLOP REJECT: AI-tell phrases and any surviving \"team\" → do not save; force a retry.\n    const slop = /\\bteam\\b|positioned|perfectly|\\bexpand(?:ing)? into\\b|with your focus|your focus on|focus on (?:your|hormone|wellness|providing)|committed to|dedicated to|cutting[- ]edge|elevate|seamless|revolutioni[sz]e|unlock|empower|streamline|world[- ]class|game[- ]chang|thrilled|excited to|hope this (?:finds|email)|wanted to reach out|reaching out|as a (?:leading|premier)|your commitment/i.test(draft.body + ' ' + draft.subject);\n    if (slop) return JSON.stringify({ blocked: true, error: 'slop_or_banned_phrase', note: 'Nothing saved. Draft contained an AI-tell / banned phrase (team, positioned, focus on, committed to, etc.); retry.' });\n    // SUBJECT contract: 2-4 words, lowercase, internal-looking. Reject title-case, salesy words, business name.\n    const subj = String(draft.subject || '').trim();\n    const subjWords = subj.split(/\\s+/).filter(Boolean);\n    const subjBad = subjWords.length > 5 || /pricing|offer|partnership|introduction|opportunity|solution|premium|exclusive/i.test(subj) || /[A-Z]/.test(subj.replace(/COA|RUO/g, '')) || (lead.name && subj.toLowerCase().includes(String(lead.name).toLowerCase().split(/\\s+/)[0].toLowerCase()));\n    if (subjBad) return JSON.stringify({ blocked: true, error: 'subject_contract', note: 'Nothing saved. Subject must be 2-4 lowercase internal words (e.g. \"peptide wholesale\"), no title case / salesy words / business name; retry.' });\n    // The reply-no stop line lives in the send-time footer (leadsSend appends it) — requiring it\n    // in the body doubled it and read as boilerplate. Body contract = store mention only.\n    if (!/leoresearch\\.com/i.test(draft.body)) return JSON.stringify({ blocked: true, error: 'copy_contract_missing', note: 'Nothing saved. Draft must mention the store (leoresearch.com).' });\n    await env.DB.prepare(\"UPDATE leads SET draft=?, status='drafted' WHERE id=?\").bind(JSON.stringify(draft), id).run();\n    return JSON.stringify({ id, to: lead.email || '(no email yet)', subject: draft.subject, body: draft.body, model: draft.model });\n  },\n  // Follow-up sequence for a drafted lead. 40-60% of positive replies come from touches 2-3, not\n  // the first send — but each must carry a NEW angle on the SAME thread, never \"just checking in\".\n  // Generates touch 2 (new angle) + touch 3 (honest break-up). $1=lead_id, $2=brand(default LeoResearch).\n  ","version":11,"of":12},{"commit":"c169d223f","date":"2026-07-25T11:31:39-07:00","what_changed":"drafting rules realigned to amended outreach-law: exposure/mechanism/outcome contrast (evidence-backed only, never invented competitor defects), small-entry CTA (three units, about $75), samples discretionary never volunteered — dossier + runner prompt, existing approved language preserved #vault-approved","chars":10154,"added":["'5. One line: third-party COA on every lot, two-day shipping nationwide from Dallas. NEVER say Austin — the warehouse is Dallas. Do not volunteer samples — samples are discretionary, never the headline offer.\\n' +","'6. ONE soft CTA, phrased as a question they can answer in one word, offering the smallest credible first step — a first order can be three units, about $75: e.g. \"Want to try three units first?\" NEVER \"reply wholesale\" / \"reply for the sheet\" / \"send the sheet\" — the list is already here.\\n' +"],"removed":["'5. One line: third-party COA on every lot, two-day shipping nationwide from Dallas, samples available. NEVER say Austin — the warehouse is Dallas.\\n' +","'6. ONE soft CTA, phrased as a question they can answer in one word, offering a SAMPLE: e.g. \"Want me to send a sample of the ones you already run?\" NEVER \"reply wholesale\" / \"reply for the sheet\" / \"send the sheet\" — the list is already here.\\n' +"],"text":"async leadsDraftAI(env, leadId, venture) {\n    const id = Number(leadId);\n    if (!id) return JSON.stringify({ error: 'lead_id required' });\n    const lead = await env.DB.prepare('SELECT id,name,segment,city,email,website,context,status,score,COALESCE(notes,\\'\\') notes FROM leads WHERE id=?').bind(id).first();\n    if (!lead) return JSON.stringify({ error: 'lead not found: ' + id });\n    if (!lead.email) return JSON.stringify({ blocked: true, error: 'verified_email_required', note: 'Nothing drafted. Enrich the lead first.' });\n    if (!String(lead.notes).includes('mx:ok')) return JSON.stringify({ blocked: true, error: 'mx_verification_required', note: 'Nothing drafted. Verify MX first.' });\n    if (!String(lead.notes).includes('icp:') || Number(lead.score || 0) < 65) return JSON.stringify({ blocked: true, error: 'icp_threshold_not_met', score: Number(lead.score || 0), minimum: 65, note: 'Nothing drafted. Only verified high-fit leads enter copy review.' });\n    if (String(lead.context || '').trim().length < 40) return JSON.stringify({ blocked: true, error: 'grounding_context_required', note: 'Nothing drafted. The business site context is too thin for truthful personalization.' });\n    const suppressed = await env.DB.prepare('SELECT reason FROM lead_suppressions WHERE lower(email)=lower(?)').bind(lead.email).first();\n    if (suppressed) return JSON.stringify({ blocked: true, error: 'recipient_suppressed', reason: suppressed.reason || 'suppressed' });\n    const brand = String(venture || 'LeoResearch');\n    const dr = await env.DB.prepare(\"SELECT content FROM directory WHERE key='OUTREACH_DOSSIER'\").first();\n    const dossier = dr ? String(dr.content || '') : '';\n    if (!dossier) return JSON.stringify({ error: 'OUTREACH_DOSSIER row is empty — set the brand/team/offer knowledge first' });\n    const cr = await env.DB.prepare(\"SELECT content FROM directory WHERE key='OUTREACH_CATALOG'\").first();\n    const catalog = cr ? String(cr.content || '') : '';\n    const segWord = ({ medspa: 'med spa', clinic: 'clinic', wellness: 'wellness practice', gym: 'gym', supplement: 'supplement brand' })[lead.segment] || 'business';\n    const user =\n      'Recipient business: ' + lead.name + ' — a ' + segWord + ' in ' + (lead.city || 'their city') + '.\\n' +\n      'What they do (from their own site): ' + (lead.context || lead.website || 'unknown') + '.\\n\\n' +\n      'WHOLESALE CATALOG (include this exact list in the email, formatted as a clean list — do not invent items or prices):\\n' + catalog + '\\n\\n' +\n      'Write ONE B2B wholesale email from ' + brand + '. It must read like one competent operator wrote it to another — not marketing, not a blast. Follow exactly:\\n' +\n      '1. SALUTATION: \"Hello,\" on its own line. NEVER \"Hi <business> team\" / \"team at\" / any use of the word \"team\". If a real person first name is known, \"Hi <First>,\".\\n' +\n      '2. FIRST LINE = ONE TRUE, SPECIFIC OBSERVATION drawn ONLY from \"What they do\" above, connected to supply. Name a real thing from their site: a compound or service they actually list, a second location, their booking tool. Examples of the SHAPE (do not copy): \"You already list peptides on the site — we supply the raw compounds wholesale.\" / \"You run hormone therapy and medical weight loss — the peptides behind those usually come from a supplier you can switch.\" The observation must be a plain fact you could point to on their page, then pivot to supply in the same or next sentence. It is FORBIDDEN to compliment, admire, congratulate, or infer (\"positioned\", \"perfectly positioned\", \"focus on\", \"committed to\", \"dedicated to\", \"expand into\", \"with your emphasis on\"). If \"What they do\" contains NO concrete hook, skip the observation entirely and open plainly: \"We supply research peptides wholesale.\" A generic or invented observation is worse than none.\\n' +\n      '3. State the wholesale model plainly, one line: every item is 50% of the price listed on leoresearch.com, they set their own price from there. That is the whole pricing story — no kits, no tiers, no minimums, no volume ladder. White-label available.\\n' +\n      '4. INCLUDE THE FULL CATALOG LIST above, verbatim, as a clean list — every item WITH its listed price, its wholesale price, and its product link exactly as given. This is the reference the owner requires in the email.\\n' +\n      '5. One line: third-party COA on every lot, two-day shipping nationwide from Dallas. NEVER say Austin — the warehouse is Dallas. Do not volunteer samples — samples are discretionary, never the headline offer.\\n' +\n      '6. ONE soft CTA, phrased as a question they can answer in one word, offering the smallest credible first step — a first order can be three units, about $75: e.g. \"Want to try three units first?\" NEVER \"reply wholesale\" / \"reply for the sheet\" / \"send the sheet\" — the list is already here.\\n' +\n      '7. No clinical, efficacy, treatment, recovery, dosing, or outcome language. Commerce facts only.\\n' +\n      '8. NO compliance boilerplate in the body — the send system appends the opt-out footer.\\n' +\n      '9. Sign off with exactly one line: LeoResearch. NEVER a personal name. NEVER the word \"team\".\\n' +\n      '10. BREVITY: outside the catalog list, the whole email is under 90 words. Every non-list sentence earns its place or is cut. Plain human register, contractions welcome. BANNED words: cutting-edge, elevate, seamless, revolutionize, unlock, empower, streamline, robust, world-class, game-changer, thrilled, excited, \"hope this finds you\", \"wanted to reach out\", \"reaching out\", \"positioned\", \"perfectly\", \"expand into\", \"focus on\", \"committed to\", \"dedicated to\", \"team\". No exclamation points. At most one em-dash. If a line sounds like AI, cut it.\\n' +\n      'SUBJECT: 2-4 words, all lowercase, boring and internal-looking like a note from a colleague — e.g. \"peptide wholesale\", \"reorder costs\", \"your supplier\", \"wholesale + coa\". NEVER title case, NEVER the word \"pricing\"/\"offer\"/\"partnership\", NEVER a business name, NEVER \"Came across\".\\n' +\n      'Return ONLY strict JSON: {\"subject\": \"...\", \"body\": \"...\"} with \\\\n for line breaks in body.';\n    const g = await callGateway(env, dossier, user, 1200);\n    if (g.err) return JSON.stringify({ error: 'model call failed: ' + g.err });\n    let parsed = pipeJson(g.text);\n    if (!parsed) { try { parsed = JSON.parse(g.text); } catch {} }\n    if (!parsed || !parsed.subject || !parsed.body) {\n      const mm = String(g.text).match(/\\{[\\s\\S]*\\}/);\n      if (mm) { try { parsed = JSON.parse(mm[0]); } catch {} }\n    }\n    if (!parsed || !parsed.subject || !parsed.body) return JSON.stringify({ error: 'model did not return subject/body', raw: String(g.text).slice(0, 300) });\n    // Scrub any personal name AND the banned word \"team\" from the signoff/body → brand only.\n    parsed.body = String(parsed.body)\n      .replace(/\\bOWNER_FIRST_NAME\\s+[OWNER_SURNAME]\\b/g, 'LeoResearch')\n      .replace(/\\bOWNER_FIRST_NAME\\b/g, 'LeoResearch')\n      .replace(/\\bthe\\s+LeoResearch\\s+team\\b/gi, 'LeoResearch')\n      .replace(/\\bLeoResearch\\s+team\\b/gi, 'LeoResearch')\n      .replace(/\\bteam\\s+at\\s+[A-Z][^\\n,.]{0,40}/g, 'Hello')\n      .replace(/\\bHi\\s+[^\\n,]{0,50}\\bteam\\b/gi, 'Hello');\n    const draft = { subject: String(parsed.subject), body: String(parsed.body), venture: brand, model: g.fallback ? 'gemini-2.5-flash(fallback)' : 'grok/grok-4.3', by: 'leadsDraftAI' };\n    const unsafe = /commonly used|use peptides|used in|support recovery|for recovery|clinical efficacy|treat(?:ment|ing)?|patient outcomes?|healing|therapeutic/i.test(draft.body);\n    if (unsafe) return JSON.stringify({ blocked: true, error: 'unsafe_copy_claim', note: 'Nothing saved. The model introduced a clinical/use claim; retry the draft.' });\n    // HARD SLOP REJECT: AI-tell phrases and any surviving \"team\" → do not save; force a retry.\n    const slop = /\\bteam\\b|positioned|perfectly|\\bexpand(?:ing)? into\\b|with your focus|your focus on|focus on (?:your|hormone|wellness|providing)|committed to|dedicated to|cutting[- ]edge|elevate|seamless|revolutioni[sz]e|unlock|empower|streamline|world[- ]class|game[- ]chang|thrilled|excited to|hope this (?:finds|email)|wanted to reach out|reaching out|as a (?:leading|premier)|your commitment/i.test(draft.body + ' ' + draft.subject);\n    if (slop) return JSON.stringify({ blocked: true, error: 'slop_or_banned_phrase', note: 'Nothing saved. Draft contained an AI-tell / banned phrase (team, positioned, focus on, committed to, etc.); retry.' });\n    // SUBJECT contract: 2-4 words, lowercase, internal-looking. Reject title-case, salesy words, business name.\n    const subj = String(draft.subject || '').trim();\n    const subjWords = subj.split(/\\s+/).filter(Boolean);\n    const subjBad = subjWords.length > 5 || /pricing|offer|partnership|introduction|opportunity|solution|premium|exclusive/i.test(subj) || /[A-Z]/.test(subj.replace(/COA|RUO/g, '')) || (lead.name && subj.toLowerCase().includes(String(lead.name).toLowerCase().split(/\\s+/)[0].toLowerCase()));\n    if (subjBad) return JSON.stringify({ blocked: true, error: 'subject_contract', note: 'Nothing saved. Subject must be 2-4 lowercase internal words (e.g. \"peptide wholesale\"), no title case / salesy words / business name; retry.' });\n    // The reply-no stop line lives in the send-time footer (leadsSend appends it) — requiring it\n    // in the body doubled it and read as boilerplate. Body contract = store mention only.\n    if (!/leoresearch\\.com/i.test(draft.body)) return JSON.stringify({ blocked: true, error: 'copy_contract_missing', note: 'Nothing saved. Draft must mention the store (leoresearch.com).' });\n    await env.DB.prepare(\"UPDATE leads SET draft=?, status='drafted' WHERE id=?\").bind(JSON.stringify(draft), id).run();\n    return JSON.stringify({ id, to: lead.email || '(no email yet)', subject: draft.subject, body: draft.body, model: draft.model });\n  },\n  // Follow-up sequence for a drafted lead. 40-60% of positive replies come from touches 2-3, not\n  // the first send — but each must carry a NEW angle on the SAME thread, never \"just checking in\".\n  // Generates touch 2 (new angle) + touch 3 (honest break-up). $1=lead_id, $2=brand(default LeoResearch).\n  ","version":10,"of":12},{"commit":"98d812c5c","date":"2026-07-24T18:06:39-07:00","what_changed":"outreach loop to owner spec: 50%-of-listed pricing (no kits/tiers/minimums), Dallas never Austin, samples CTA, catalog carries listed+wholesale price and product link per item; /admin/outreach audit page (outbox+drafts+rules, ?format=text portable dump) #vault-approved","chars":10046,"added":["'3. State the wholesale model plainly, one line: every item is 50% of the price listed on leoresearch.com, they set their own price from there. That is the whole pricing story — no kits, no tiers, no minimums, no volume ladder. White-label available.\\n' +","'4. INCLUDE THE FULL CATALOG LIST above, verbatim, as a clean list — every item WITH its listed price, its wholesale price, and its product link exactly as given. This is the reference the owner requires in the email.\\n' +","'5. One line: third-party COA on every lot, two-day shipping nationwide from Dallas, samples available. NEVER say Austin — the warehouse is Dallas.\\n' +","'6. ONE soft CTA, phrased as a question they can answer in one word, offering a SAMPLE: e.g. \"Want me to send a sample of the ones you already run?\" NEVER \"reply wholesale\" / \"reply for the sheet\" / \"send the sheet\" — the list is already here.\\n' +"],"removed":["'3. State the wholesale model plainly, one line: kits of 10 per item, 20% off to start, up to 50% off at volume, they set their own price. White-label available.\\n' +","'4. INCLUDE THE FULL CATALOG LIST above, verbatim, as a clean list. Every item. This is the reference the owner requires in the email.\\n' +","'5. One line: third-party COA per lot, ships from Austin, ~2-day restock. leoresearch.com once.\\n' +","'6. ONE soft CTA, phrased as a question they can answer in one word: e.g. \"Want me to put together a comparison against what you pay now?\" or \"Worth sending a sample of the ones you already run?\" NEVER \"reply wholesale\" / \"reply for the sheet\" / \"send the sheet\" — the list is already here. A first order can be a single kit of 10; say the bar is that low in the same line.\\n' +"],"text":"async leadsDraftAI(env, leadId, venture) {\n    const id = Number(leadId);\n    if (!id) return JSON.stringify({ error: 'lead_id required' });\n    const lead = await env.DB.prepare('SELECT id,name,segment,city,email,website,context,status,score,COALESCE(notes,\\'\\') notes FROM leads WHERE id=?').bind(id).first();\n    if (!lead) return JSON.stringify({ error: 'lead not found: ' + id });\n    if (!lead.email) return JSON.stringify({ blocked: true, error: 'verified_email_required', note: 'Nothing drafted. Enrich the lead first.' });\n    if (!String(lead.notes).includes('mx:ok')) return JSON.stringify({ blocked: true, error: 'mx_verification_required', note: 'Nothing drafted. Verify MX first.' });\n    if (!String(lead.notes).includes('icp:') || Number(lead.score || 0) < 65) return JSON.stringify({ blocked: true, error: 'icp_threshold_not_met', score: Number(lead.score || 0), minimum: 65, note: 'Nothing drafted. Only verified high-fit leads enter copy review.' });\n    if (String(lead.context || '').trim().length < 40) return JSON.stringify({ blocked: true, error: 'grounding_context_required', note: 'Nothing drafted. The business site context is too thin for truthful personalization.' });\n    const suppressed = await env.DB.prepare('SELECT reason FROM lead_suppressions WHERE lower(email)=lower(?)').bind(lead.email).first();\n    if (suppressed) return JSON.stringify({ blocked: true, error: 'recipient_suppressed', reason: suppressed.reason || 'suppressed' });\n    const brand = String(venture || 'LeoResearch');\n    const dr = await env.DB.prepare(\"SELECT content FROM directory WHERE key='OUTREACH_DOSSIER'\").first();\n    const dossier = dr ? String(dr.content || '') : '';\n    if (!dossier) return JSON.stringify({ error: 'OUTREACH_DOSSIER row is empty — set the brand/team/offer knowledge first' });\n    const cr = await env.DB.prepare(\"SELECT content FROM directory WHERE key='OUTREACH_CATALOG'\").first();\n    const catalog = cr ? String(cr.content || '') : '';\n    const segWord = ({ medspa: 'med spa', clinic: 'clinic', wellness: 'wellness practice', gym: 'gym', supplement: 'supplement brand' })[lead.segment] || 'business';\n    const user =\n      'Recipient business: ' + lead.name + ' — a ' + segWord + ' in ' + (lead.city || 'their city') + '.\\n' +\n      'What they do (from their own site): ' + (lead.context || lead.website || 'unknown') + '.\\n\\n' +\n      'WHOLESALE CATALOG (include this exact list in the email, formatted as a clean list — do not invent items or prices):\\n' + catalog + '\\n\\n' +\n      'Write ONE B2B wholesale email from ' + brand + '. It must read like one competent operator wrote it to another — not marketing, not a blast. Follow exactly:\\n' +\n      '1. SALUTATION: \"Hello,\" on its own line. NEVER \"Hi <business> team\" / \"team at\" / any use of the word \"team\". If a real person first name is known, \"Hi <First>,\".\\n' +\n      '2. FIRST LINE = ONE TRUE, SPECIFIC OBSERVATION drawn ONLY from \"What they do\" above, connected to supply. Name a real thing from their site: a compound or service they actually list, a second location, their booking tool. Examples of the SHAPE (do not copy): \"You already list peptides on the site — we supply the raw compounds wholesale.\" / \"You run hormone therapy and medical weight loss — the peptides behind those usually come from a supplier you can switch.\" The observation must be a plain fact you could point to on their page, then pivot to supply in the same or next sentence. It is FORBIDDEN to compliment, admire, congratulate, or infer (\"positioned\", \"perfectly positioned\", \"focus on\", \"committed to\", \"dedicated to\", \"expand into\", \"with your emphasis on\"). If \"What they do\" contains NO concrete hook, skip the observation entirely and open plainly: \"We supply research peptides wholesale.\" A generic or invented observation is worse than none.\\n' +\n      '3. State the wholesale model plainly, one line: every item is 50% of the price listed on leoresearch.com, they set their own price from there. That is the whole pricing story — no kits, no tiers, no minimums, no volume ladder. White-label available.\\n' +\n      '4. INCLUDE THE FULL CATALOG LIST above, verbatim, as a clean list — every item WITH its listed price, its wholesale price, and its product link exactly as given. This is the reference the owner requires in the email.\\n' +\n      '5. One line: third-party COA on every lot, two-day shipping nationwide from Dallas, samples available. NEVER say Austin — the warehouse is Dallas.\\n' +\n      '6. ONE soft CTA, phrased as a question they can answer in one word, offering a SAMPLE: e.g. \"Want me to send a sample of the ones you already run?\" NEVER \"reply wholesale\" / \"reply for the sheet\" / \"send the sheet\" — the list is already here.\\n' +\n      '7. No clinical, efficacy, treatment, recovery, dosing, or outcome language. Commerce facts only.\\n' +\n      '8. NO compliance boilerplate in the body — the send system appends the opt-out footer.\\n' +\n      '9. Sign off with exactly one line: LeoResearch. NEVER a personal name. NEVER the word \"team\".\\n' +\n      '10. BREVITY: outside the catalog list, the whole email is under 90 words. Every non-list sentence earns its place or is cut. Plain human register, contractions welcome. BANNED words: cutting-edge, elevate, seamless, revolutionize, unlock, empower, streamline, robust, world-class, game-changer, thrilled, excited, \"hope this finds you\", \"wanted to reach out\", \"reaching out\", \"positioned\", \"perfectly\", \"expand into\", \"focus on\", \"committed to\", \"dedicated to\", \"team\". No exclamation points. At most one em-dash. If a line sounds like AI, cut it.\\n' +\n      'SUBJECT: 2-4 words, all lowercase, boring and internal-looking like a note from a colleague — e.g. \"peptide wholesale\", \"reorder costs\", \"your supplier\", \"wholesale + coa\". NEVER title case, NEVER the word \"pricing\"/\"offer\"/\"partnership\", NEVER a business name, NEVER \"Came across\".\\n' +\n      'Return ONLY strict JSON: {\"subject\": \"...\", \"body\": \"...\"} with \\\\n for line breaks in body.';\n    const g = await callGateway(env, dossier, user, 1200);\n    if (g.err) return JSON.stringify({ error: 'model call failed: ' + g.err });\n    let parsed = pipeJson(g.text);\n    if (!parsed) { try { parsed = JSON.parse(g.text); } catch {} }\n    if (!parsed || !parsed.subject || !parsed.body) {\n      const mm = String(g.text).match(/\\{[\\s\\S]*\\}/);\n      if (mm) { try { parsed = JSON.parse(mm[0]); } catch {} }\n    }\n    if (!parsed || !parsed.subject || !parsed.body) return JSON.stringify({ error: 'model did not return subject/body', raw: String(g.text).slice(0, 300) });\n    // Scrub any personal name AND the banned word \"team\" from the signoff/body → brand only.\n    parsed.body = String(parsed.body)\n      .replace(/\\bOWNER_FIRST_NAME\\s+[OWNER_SURNAME]\\b/g, 'LeoResearch')\n      .replace(/\\bOWNER_FIRST_NAME\\b/g, 'LeoResearch')\n      .replace(/\\bthe\\s+LeoResearch\\s+team\\b/gi, 'LeoResearch')\n      .replace(/\\bLeoResearch\\s+team\\b/gi, 'LeoResearch')\n      .replace(/\\bteam\\s+at\\s+[A-Z][^\\n,.]{0,40}/g, 'Hello')\n      .replace(/\\bHi\\s+[^\\n,]{0,50}\\bteam\\b/gi, 'Hello');\n    const draft = { subject: String(parsed.subject), body: String(parsed.body), venture: brand, model: g.fallback ? 'gemini-2.5-flash(fallback)' : 'grok/grok-4.3', by: 'leadsDraftAI' };\n    const unsafe = /commonly used|use peptides|used in|support recovery|for recovery|clinical efficacy|treat(?:ment|ing)?|patient outcomes?|healing|therapeutic/i.test(draft.body);\n    if (unsafe) return JSON.stringify({ blocked: true, error: 'unsafe_copy_claim', note: 'Nothing saved. The model introduced a clinical/use claim; retry the draft.' });\n    // HARD SLOP REJECT: AI-tell phrases and any surviving \"team\" → do not save; force a retry.\n    const slop = /\\bteam\\b|positioned|perfectly|\\bexpand(?:ing)? into\\b|with your focus|your focus on|focus on (?:your|hormone|wellness|providing)|committed to|dedicated to|cutting[- ]edge|elevate|seamless|revolutioni[sz]e|unlock|empower|streamline|world[- ]class|game[- ]chang|thrilled|excited to|hope this (?:finds|email)|wanted to reach out|reaching out|as a (?:leading|premier)|your commitment/i.test(draft.body + ' ' + draft.subject);\n    if (slop) return JSON.stringify({ blocked: true, error: 'slop_or_banned_phrase', note: 'Nothing saved. Draft contained an AI-tell / banned phrase (team, positioned, focus on, committed to, etc.); retry.' });\n    // SUBJECT contract: 2-4 words, lowercase, internal-looking. Reject title-case, salesy words, business name.\n    const subj = String(draft.subject || '').trim();\n    const subjWords = subj.split(/\\s+/).filter(Boolean);\n    const subjBad = subjWords.length > 5 || /pricing|offer|partnership|introduction|opportunity|solution|premium|exclusive/i.test(subj) || /[A-Z]/.test(subj.replace(/COA|RUO/g, '')) || (lead.name && subj.toLowerCase().includes(String(lead.name).toLowerCase().split(/\\s+/)[0].toLowerCase()));\n    if (subjBad) return JSON.stringify({ blocked: true, error: 'subject_contract', note: 'Nothing saved. Subject must be 2-4 lowercase internal words (e.g. \"peptide wholesale\"), no title case / salesy words / business name; retry.' });\n    // The reply-no stop line lives in the send-time footer (leadsSend appends it) — requiring it\n    // in the body doubled it and read as boilerplate. Body contract = store mention only.\n    if (!/leoresearch\\.com/i.test(draft.body)) return JSON.stringify({ blocked: true, error: 'copy_contract_missing', note: 'Nothing saved. Draft must mention the store (leoresearch.com).' });\n    await env.DB.prepare(\"UPDATE leads SET draft=?, status='drafted' WHERE id=?\").bind(JSON.stringify(draft), id).run();\n    return JSON.stringify({ id, to: lead.email || '(no email yet)', subject: draft.subject, body: draft.body, model: draft.model });\n  },\n  // Follow-up sequence for a drafted lead. 40-60% of positive replies come from touches 2-3, not\n  // the first send — but each must carry a NEW angle on the SAME thread, never \"just checking in\".\n  // Generates touch 2 (new angle) + touch 3 (honest break-up). $1=lead_id, $2=brand(default LeoResearch).\n  ","version":9,"of":12},{"commit":"dcb477bd6","date":"2026-07-23T12:41:46-07:00","what_changed":"outreach loop stronger: real-signal observation opener (uses captured services/booking/location), lowercase internal subject-line guard, 90-word brevity, soft-question CTA, + LEADS_FOLLOWUPS 3-touch sequence (mig 0326) #vault-approved","chars":9952,"added":["'Write ONE B2B wholesale email from ' + brand + '. It must read like one competent operator wrote it to another — not marketing, not a blast. Follow exactly:\\n' +","'2. FIRST LINE = ONE TRUE, SPECIFIC OBSERVATION drawn ONLY from \"What they do\" above, connected to supply. Name a real thing from their site: a compound or service they actually list, a second location, their booking tool. Examples of the SHAPE (do not copy): \"You already list peptides on the site — we supply the raw compounds wholesale.\" / \"You run hormone therapy and medical weight loss — the peptides behind those usually come from a supplier you can switch.\" The observation must be a plain fact you could point to on their page, then pivot to supply in the same or next sentence. It is FORBIDDEN to compliment, admire, congratulate, or infer (\"positioned\", \"perfectly positioned\", \"focus on\", \"committed to\", \"dedicated to\", \"expand into\", \"with your emphasis on\"). If \"What they do\" contains NO concrete hook, skip the observation entirely and open plainly: \"We supply research peptides wholesale.\" A generic or invented observation is worse than none.\\n' +","'3. State the wholesale model plainly, one line: kits of 10 per item, 20% off to start, up to 50% off at volume, they set their own price. White-label available.\\n' +","'4. INCLUDE THE FULL CATALOG LIST above, verbatim, as a clean list. Every item. This is the reference the owner requires in the email.\\n' +","'5. One line: third-party COA per lot, ships from Austin, ~2-day restock. leoresearch.com once.\\n' +","'6. ONE soft CTA, phrased as a question they can answer in one word: e.g. \"Want me to put together a comparison against what you pay now?\" or \"Worth sending a sample of the ones you already run?\" NEVER \"reply wholesale\" / \"reply for the sheet\" / \"send the sheet\" — the list is already here. A first order can be a single kit of 10; say the bar is that low in the same line.\\n' +","'10. BREVITY: outside the catalog list, the whole email is under 90 words. Every non-list sentence earns its place or is cut. Plain human register, contractions welcome. BANNED words: cutting-edge, elevate, seamless, revolutionize, unlock, empower, streamline, robust, world-class, game-changer, thrilled, excited, \"hope this finds you\", \"wanted to reach out\", \"reaching out\", \"positioned\", \"perfectly\", \"expand into\", \"focus on\", \"committed to\", \"dedicated to\", \"team\". No exclamation points. At most one em-dash. If a line sounds like AI, cut it.\\n' +","'SUBJECT: 2-4 words, all lowercase, boring and internal-looking like a note from a colleague — e.g. \"peptide wholesale\", \"reorder costs\", \"your supplier\", \"wholesale + coa\". NEVER title case, NEVER the word \"pricing\"/\"offer\"/\"partnership\", NEVER a business name, NEVER \"Came across\".\\n' +","const slop = /\\bteam\\b|positioned|perfectly|\\bexpand(?:ing)? into\\b|with your focus|your focus on|focus on (?:your|hormone|wellness|providing)|committed to|dedicated to|cutting[- ]edge|elevate|seamless|revolutioni[sz]e|unlock|empower|streamline|world[- ]class|game[- ]chang|thrilled|excited to|hope this (?:finds|email)|wanted to reach out|reaching out|as a (?:leading|premier)|your commitment/i.test(draft.body + ' ' + draft.subject);","if (slop) return JSON.stringify({ blocked: true, error: 'slop_or_banned_phrase', note: 'Nothing saved. Draft contained an AI-tell / banned phrase (team, positioned, focus on, committed to, etc.); retry.' });","// SUBJECT contract: 2-4 words, lowercase, internal-looking. Reject title-case, salesy words, business name.","const subj = String(draft.subject || '').trim();","const subjWords = subj.split(/\\s+/).filter(Boolean);","const subjBad = subjWords.length > 5 || /pricing|offer|partnership|introduction|opportunity|solution|premium|exclusive/i.test(subj) || /[A-Z]/.test(subj.replace(/COA|RUO/g, '')) || (lead.name && subj.toLowerCase().includes(String(lead.name).toLowerCase().split(/\\s+/)[0].toLowerCase()));","if (subjBad) return JSON.stringify({ blocked: true, error: 'subject_contract', note: 'Nothing saved. Subject must be 2-4 lowercase internal words (e.g. \"peptide wholesale\"), no title case / salesy words / business name; retry.' });","// Follow-up sequence for a drafted lead. 40-60% of positive replies come from touches 2-3, not","// the first send — but each must carry a NEW angle on the SAME thread, never \"just checking in\".","// Generates touch 2 (new angle) + touch 3 (honest break-up). $1=lead_id, $2=brand(default LeoResearch)."],"removed":["'Write ONE professional B2B wholesale email from ' + brand + '. Voice = a competent operator writing to a business owner. Follow exactly:\\n' +","'2. DO NOT open with a sentence about the recipient. No compliment, no describing their business, no \"with your focus on\", no \"positioned\"/\"perfectly positioned\"/\"positioned to expand\", no \"expand into new offerings\". You do not know their business. The FIRST line states plainly what you do: you supply research peptides wholesale. Nothing about them.\\n' +","'3. State the wholesale model plainly, one line: kits of 10 per item, 20% off to start, up to 50% off at volume, they set their own price.\\n' +","'4. INCLUDE THE FULL CATALOG LIST above, verbatim, as a clean list. Every item. This IS the email.\\n' +","'5. One line: COA per lot, ships from Austin, ~2-day restock. leoresearch.com once.\\n' +","'6. Low bar, one line: a first order can be a single kit of 10; happy to answer questions or send a sample. NEVER \"reply wholesale\" / \"reply for the sheet\" / \"send the sheet\" — the list is already here.\\n' +","'10. Plain human register. BANNED words: cutting-edge, elevate, seamless, revolutionize, unlock, empower, streamline, robust, world-class, game-changer, thrilled, excited, \"hope this finds you\", \"wanted to reach out\", \"reaching out\", \"positioned\", \"perfectly\", \"expand into\", \"focus on\", \"team\". No exclamation points. At most one em-dash. If a line sounds like AI, cut it.\\n' +","'Subject: plain, e.g. \"Wholesale research peptide pricing\" or \"Research peptide wholesale + white-label\". No \"Came across\".\\n' +","const slop = /\\bteam\\b|positioned|perfectly|\\bexpand(?:ing)? into\\b|with your focus|your focus on|focus on (?:your|hormone|wellness|providing)|cutting[- ]edge|elevate|seamless|revolutioni[sz]e|unlock|empower|streamline|world[- ]class|game[- ]chang|thrilled|excited to|hope this (?:finds|email)|wanted to reach out|reaching out|as a (?:leading|premier)|your commitment/i.test(draft.body + ' ' + draft.subject);","if (slop) return JSON.stringify({ blocked: true, error: 'slop_or_banned_phrase', note: 'Nothing saved. Draft contained an AI-tell / banned phrase (team, positioned, focus on, etc.); retry.' });","// List leads by status. $1=status (default all).","async leadsList(env, status) {","const st = String(status || '').trim();","const rows = st","? (await env.DB.prepare('SELECT id,name,segment,city,website,email,phone,status,score FROM leads WHERE status=? ORDER BY score DESC, id DESC LIMIT 100').bind(st).all()).results",": (await env.DB.prepare('SELECT id,name,segment,city,website,email,phone,status,score FROM leads ORDER BY score DESC, id DESC LIMIT 100').all()).results;","const counts = (await env.DB.prepare('SELECT status, COUNT(*) n FROM leads GROUP BY status').all()).results || [];","return JSON.stringify({ filter: st || 'all', shown: (rows || []).length, by_status: counts, leads: rows || [] });","// MX verification over enriched emails via DNS-over-HTTPS. A domain with no MX record","// cannot receive mail — those leads are tagged mx:none and (if not yet drafted) parked","// as no_mx so drafting/sending never wastes a slot on a dead mailbox. $1 = max leads (default 25).","async leadsVerifyMx(env, limitArg) {","const lim = Math.min(50, Math.max(1, parseInt(limitArg || '25', 10) || 25));","const rows = (await env.DB.prepare(","\"SELECT id, email, status, COALESCE(notes,'') notes FROM leads WHERE email IS NOT NULL AND email != '' AND COALESCE(notes,'') NOT LIKE '%mx:%' ORDER BY id LIMIT ?\"",").bind(lim).all()).results || [];","if (!rows.length) return JSON.stringify({ checked: 0, note: 'no unverified emails left' });","const domainCache = {};","let ok = 0, none = 0, parked = 0;","for (const l of rows) {","const domain = String(l.email).split('@')[1] || '';","if (!domain) continue;","if (!(domain in domainCache)) {","try {","const r = await fetch('https://cloudflare-dns.com/dns-query?name=' + encodeURIComponent(domain) + '&type=MX', { headers: { accept: 'application/dns-json' } });","const j = await r.json();","domainCache[domain] = Array.isArray(j.Answer) && j.Answer.some(a => a.type === 15);","} catch { domainCache[domain] = null; }","const hasMx = domainCache[domain];","if (hasMx === null) continue;","const tag = hasMx ? ' mx:ok' : ' mx:none';","if (hasMx) ok++; else none++;","if (!hasMx && (l.status === 'enriched' || l.status === 'new')) {","await env.DB.prepare('UPDATE leads SET notes=?, status=? WHERE id=?').bind((l.notes + tag).trim(), 'no_mx', l.id).run();","parked++;","} else {","await env.DB.prepare('UPDATE leads SET notes=? WHERE id=?').bind((l.notes + tag).trim(), l.id).run();","return JSON.stringify({ checked: rows.length, mx_ok: ok, mx_none: none, parked_no_mx: parked, domains_queried: Object.keys(domainCache).length });","// AI ICP scoring: one Grok call scores a batch of enriched leads on wholesale/white-label fit","// grounded in the OUTREACH_DOSSIER. Writes score (0-100) + an icp: note with buyer type and","// reason, so LEADS_LIST ordering becomes commercial fit instead of has-a-website. $1 = batch size (default 8).","async leadsScoreAI(env, countArg) {","const n = Math.min(10, Math.max(1, parseInt(countArg || '8', 10) || 8));","\"SELECT id, name, segment, city, substr(COALESCE(context,''),1,400) context, COALESCE(notes,'') notes FROM leads WHERE status IN ('enriched','drafted') AND email IS NOT NULL AND email!='' AND COALESCE(notes,'') LIKE '%mx:ok%' AND COALESCE(notes,'') NOT LIKE '%icp:%' ORDER BY score DESC,id LIMIT ?\"",").bind(n).all()).results || [];","if (!rows.length) return JSON.stringify({ scored: 0, note: 'no unscored enriched leads left' });","'Score each business below as a wholesale/white-label peptide buyer for the venture in the dossier.\\n' +","'Leads: ' + JSON.stringify(rows.map(r => ({ id: r.id, name: r.name, segment: r.segment, city: r.city, about: r.context }))) + '\\n\\n' +","'For each, judge: does this business plausibly BUY peptides at volume (clinic protocols, med-spa services, resale), ' +","'who the buyer is, and how much volume is realistic. Penalize pure consumer gyms, marketing-agency pages, and anything not a real operating business. ' +","'Return ONLY strict JSON: [{\"id\":<lead id>,\"icp_fit\":<0-100>,\"buyer\":\"clinic_buyer|medspa_owner|practitioner|reseller|gym_owner|other\",\"volume\":\"low|med|high\",\"reason\":\"<max 120 chars, concrete>\"}]';","const g = await callGateway(env, dossier, user, 1600);","if (!parsed) { const mm = String(g.text).match(/\\[[\\s\\S]*\\]/); if (mm) { try { parsed = JSON.parse(mm[0]); } catch {} } }","if (!Array.isArray(parsed)) return JSON.stringify({ error: 'model did not return a JSON array', raw: String(g.text).slice(0, 300) });","const byId = Object.fromEntries(rows.map(r => [r.id, r]));","let scored = 0;","const results = [];","for (const s of parsed) {","const lead = byId[Number(s.id)];","if (!lead || typeof s.icp_fit !== 'number') continue;","const fit = Math.min(100, Math.max(0, Math.round(s.icp_fit)));","const tag = ' icp:' + fit + ' buyer:' + String(s.buyer || 'other').slice(0, 20) + ' vol:' + String(s.volume || '?').slice(0, 4) + ' why:' + String(s.reason || '').replace(/\\s+/g, ' ').slice(0, 120);","await env.DB.prepare('UPDATE leads SET score=?, notes=? WHERE id=?').bind(fit, (lead.notes + tag).trim(), lead.id).run();","scored++;","results.push({ id: lead.id, name: lead.name, icp_fit: fit, buyer: s.buyer, volume: s.volume, reason: s.reason });","return JSON.stringify({ scored, model: 'grok/grok-4.3', results });","// Send ONE lead's drafted email via the miscsubjects email channel.","// HARD-GATED: $1 must equal CONFIRM, $2=lead_id, $3=from_local.","// Send a tracked email (open pixel + wrapped click links) and record it in email_sends for","// full visibility + engagement. $1 = JSON {to, subject, body, kind?, lead_id?, from?, from_name?, reply_to?}.","// Links in the body are rewritten to /api/t/c/<id> and a 1x1 /api/t/o/<id>.gif pixel is appended.","async emailSendTracked(env, raw) {","let p; try { p = JSON.parse(String(raw || '{}')); } catch { return JSON.stringify({ error: 'body must be JSON {to,subject,body,...}' }); }","const to = String(p.to || '').trim();","const subject = String(p.subject || '').trim();","const body = String(p.body || '');","if (!to || !subject || !body) return JSON.stringify({ error: 'to, subject, body required' });","const kind = String(p.kind || 'outreach');","const leadId = p.lead_id != null ? Number(p.lead_id) : null;","const fromLocal = String(p.from || 'wholesale').replace(/[^a-z0-9._-]/gi, '') || 'wholesale';","const id = 'es_' + (crypto.randomUUID ? crypto.randomUUID().replace(/-/g, '').slice(0, 20) : (Date.now().toString(36) + Math.random().toString(36).slice(2, 10)));","const base = 'https://miscsubjects.com/api/t';","const wrap = (url) => base + '/c/' + id + '?u=' + encodeURIComponent(btoa(url));","const urlRe = /(https?:\\/\\/[^\\s<>()\"']+)/g;","const textTracked = body.replace(urlRe, (m) => wrap(m));","const htmlBody = body.replace(/&/g, '&amp;').replace(/</g, '&lt;').replace(/>/g, '&gt;')",".replace(urlRe, (m) => '<a href=\"' + wrap(m) + '\">' + m + '</a>').replace(/\\n/g, '<br>\\n');","const html = htmlBody + '<img src=\"' + base + '/o/' + id + '.gif\" width=\"1\" height=\"1\" style=\"display:none\" alt=\"\">';","let status = 0, resp = '';","const r = await fetch('https://miscsubjects.com/api/email/send', {","method: 'POST', headers: { 'Content-Type': 'application/json', 'x-terminal-key': env.TERMINAL_KEY },","body: JSON.stringify({ to, subject, text: textTracked, html, from: fromLocal + '@miscsubjects.com', from_name: p.from_name || 'LeoResearch', reply_to: p.reply_to || 'build@miscsubjects.com' }),","});","status = r.status; resp = (await r.text()).slice(0, 200);","} catch (e) { resp = 'fetch:' + (e && e.message || e); }","await env.DB.prepare(","'INSERT INTO email_sends (id,lead_id,to_email,from_email,subject,body,kind,sent_at,send_status) VALUES (?,?,?,?,?,?,?,?,?)'",").bind(id, leadId, to, fromLocal + '@miscsubjects.com', subject, textTracked, kind, buildNowIso(), status).run();","} catch (e) { return JSON.stringify({ id, to, subject, send_status: status, warn: 'sent but not recorded: ' + (e && e.message || e), response: resp }); }","return JSON.stringify({ id, to, subject, kind, send_status: status, open_pixel: base + '/o/' + id + '.gif', response: resp });","// Leads exposed as canonical CONTACT objects (person/business contact records) — the object","// logic fits the OIP shape without making each lead a directory row. A contact is a first-class","// object with channels (email/phone/instagram), fit score, signals, and provenance.","async contactGet(env, idArg) {","const id = Number(String(idArg || '').replace(/^contact:/, ''));","if (!id) return JSON.stringify({ error: 'contact id required (lead id or contact:<id>)' });","const l = await env.DB.prepare(\"SELECT id,name,segment,city,website,email,phone,status,score,source,context,COALESCE(notes,'') notes FROM leads WHERE id=?\").bind(id).first();","if (!l) return JSON.stringify({ error: 'contact not found: ' + id });","const igm = String(l.context || '').match(/ig:@([A-Za-z0-9_.]+)/);","const booking = (String(l.context || '').match(/booking:([a-z+-]+)/) || [])[1] || null;","return JSON.stringify({","object_type: 'contact', id: 'contact:' + l.id, lead_id: l.id,","name: l.name, business: l.name, segment: l.segment, city: l.city,","channels: { email: l.email || null, phone: l.phone || null, website: l.website || null, instagram: igm ? igm[1] : null },","fit_score: l.score, status: l.status, source: l.source,","signals: { booking_software: booking, mx: /mx:ok/.test(l.notes) ? 'ok' : (/mx:none/.test(l.notes) ? 'none' : null), icp: (String(l.notes).match(/icp:[^,]*/) || [])[0] || null },","context: l.context || null,","// List contact objects with optional status/segment/city filter. $1 = JSON {status?,segment?,city?,limit?} or a bare status.","async contactsList(env, raw) {","let f = {}; const s = String(raw || '').trim();","if (s.startsWith('{')) { try { f = JSON.parse(s); } catch {} } else if (s) { f.status = s; }","const lim = Math.min(200, Math.max(1, parseInt(f.limit || '50', 10) || 50));","const where = []; const bind = [];","if (f.status) { where.push('status=?'); bind.push(String(f.status)); }","if (f.segment) { where.push('segment=?'); bind.push(String(f.segment)); }","if (f.city) { where.push('city=?'); bind.push(String(f.city)); }","const sql = 'SELECT id,name,segment,city,email,phone,status,score FROM leads' + (where.length ? ' WHERE ' + where.join(' AND ') : '') + ' ORDER BY score DESC, id DESC LIMIT ?';","bind.push(lim);","const rows = (await env.DB.prepare(sql).bind(...bind).all()).results || [];","return JSON.stringify({ object_type: 'contact', filter: f, by_status: counts, shown: rows.length, contacts: rows.map(r => ({ id: 'contact:' + r.id, lead_id: r.id, name: r.name, segment: r.segment, city: r.city, email: r.email, phone: r.phone, status: r.status, fit_score: r.score })) });","// Visibility: list recent sent emails with open/click engagement + totals. $1 = limit (default 50).","async emailsSent(env, limitArg) {","const lim = Math.min(300, Math.max(1, parseInt(limitArg || '50', 10) || 50));","'SELECT id,lead_id,to_email,subject,kind,sent_at,send_status,opens,first_open_at,clicks,first_click_at FROM email_sends ORDER BY sent_at DESC LIMIT ?'","const agg = (await env.DB.prepare(","'SELECT COUNT(*) sent, SUM(CASE WHEN opens>0 THEN 1 ELSE 0 END) opened, SUM(CASE WHEN cli"],"text":"async leadsDraftAI(env, leadId, venture) {\n    const id = Number(leadId);\n    if (!id) return JSON.stringify({ error: 'lead_id required' });\n    const lead = await env.DB.prepare('SELECT id,name,segment,city,email,website,context,status,score,COALESCE(notes,\\'\\') notes FROM leads WHERE id=?').bind(id).first();\n    if (!lead) return JSON.stringify({ error: 'lead not found: ' + id });\n    if (!lead.email) return JSON.stringify({ blocked: true, error: 'verified_email_required', note: 'Nothing drafted. Enrich the lead first.' });\n    if (!String(lead.notes).includes('mx:ok')) return JSON.stringify({ blocked: true, error: 'mx_verification_required', note: 'Nothing drafted. Verify MX first.' });\n    if (!String(lead.notes).includes('icp:') || Number(lead.score || 0) < 65) return JSON.stringify({ blocked: true, error: 'icp_threshold_not_met', score: Number(lead.score || 0), minimum: 65, note: 'Nothing drafted. Only verified high-fit leads enter copy review.' });\n    if (String(lead.context || '').trim().length < 40) return JSON.stringify({ blocked: true, error: 'grounding_context_required', note: 'Nothing drafted. The business site context is too thin for truthful personalization.' });\n    const suppressed = await env.DB.prepare('SELECT reason FROM lead_suppressions WHERE lower(email)=lower(?)').bind(lead.email).first();\n    if (suppressed) return JSON.stringify({ blocked: true, error: 'recipient_suppressed', reason: suppressed.reason || 'suppressed' });\n    const brand = String(venture || 'LeoResearch');\n    const dr = await env.DB.prepare(\"SELECT content FROM directory WHERE key='OUTREACH_DOSSIER'\").first();\n    const dossier = dr ? String(dr.content || '') : '';\n    if (!dossier) return JSON.stringify({ error: 'OUTREACH_DOSSIER row is empty — set the brand/team/offer knowledge first' });\n    const cr = await env.DB.prepare(\"SELECT content FROM directory WHERE key='OUTREACH_CATALOG'\").first();\n    const catalog = cr ? String(cr.content || '') : '';\n    const segWord = ({ medspa: 'med spa', clinic: 'clinic', wellness: 'wellness practice', gym: 'gym', supplement: 'supplement brand' })[lead.segment] || 'business';\n    const user =\n      'Recipient business: ' + lead.name + ' — a ' + segWord + ' in ' + (lead.city || 'their city') + '.\\n' +\n      'What they do (from their own site): ' + (lead.context || lead.website || 'unknown') + '.\\n\\n' +\n      'WHOLESALE CATALOG (include this exact list in the email, formatted as a clean list — do not invent items or prices):\\n' + catalog + '\\n\\n' +\n      'Write ONE B2B wholesale email from ' + brand + '. It must read like one competent operator wrote it to another — not marketing, not a blast. Follow exactly:\\n' +\n      '1. SALUTATION: \"Hello,\" on its own line. NEVER \"Hi <business> team\" / \"team at\" / any use of the word \"team\". If a real person first name is known, \"Hi <First>,\".\\n' +\n      '2. FIRST LINE = ONE TRUE, SPECIFIC OBSERVATION drawn ONLY from \"What they do\" above, connected to supply. Name a real thing from their site: a compound or service they actually list, a second location, their booking tool. Examples of the SHAPE (do not copy): \"You already list peptides on the site — we supply the raw compounds wholesale.\" / \"You run hormone therapy and medical weight loss — the peptides behind those usually come from a supplier you can switch.\" The observation must be a plain fact you could point to on their page, then pivot to supply in the same or next sentence. It is FORBIDDEN to compliment, admire, congratulate, or infer (\"positioned\", \"perfectly positioned\", \"focus on\", \"committed to\", \"dedicated to\", \"expand into\", \"with your emphasis on\"). If \"What they do\" contains NO concrete hook, skip the observation entirely and open plainly: \"We supply research peptides wholesale.\" A generic or invented observation is worse than none.\\n' +\n      '3. State the wholesale model plainly, one line: kits of 10 per item, 20% off to start, up to 50% off at volume, they set their own price. White-label available.\\n' +\n      '4. INCLUDE THE FULL CATALOG LIST above, verbatim, as a clean list. Every item. This is the reference the owner requires in the email.\\n' +\n      '5. One line: third-party COA per lot, ships from Austin, ~2-day restock. leoresearch.com once.\\n' +\n      '6. ONE soft CTA, phrased as a question they can answer in one word: e.g. \"Want me to put together a comparison against what you pay now?\" or \"Worth sending a sample of the ones you already run?\" NEVER \"reply wholesale\" / \"reply for the sheet\" / \"send the sheet\" — the list is already here. A first order can be a single kit of 10; say the bar is that low in the same line.\\n' +\n      '7. No clinical, efficacy, treatment, recovery, dosing, or outcome language. Commerce facts only.\\n' +\n      '8. NO compliance boilerplate in the body — the send system appends the opt-out footer.\\n' +\n      '9. Sign off with exactly one line: LeoResearch. NEVER a personal name. NEVER the word \"team\".\\n' +\n      '10. BREVITY: outside the catalog list, the whole email is under 90 words. Every non-list sentence earns its place or is cut. Plain human register, contractions welcome. BANNED words: cutting-edge, elevate, seamless, revolutionize, unlock, empower, streamline, robust, world-class, game-changer, thrilled, excited, \"hope this finds you\", \"wanted to reach out\", \"reaching out\", \"positioned\", \"perfectly\", \"expand into\", \"focus on\", \"committed to\", \"dedicated to\", \"team\". No exclamation points. At most one em-dash. If a line sounds like AI, cut it.\\n' +\n      'SUBJECT: 2-4 words, all lowercase, boring and internal-looking like a note from a colleague — e.g. \"peptide wholesale\", \"reorder costs\", \"your supplier\", \"wholesale + coa\". NEVER title case, NEVER the word \"pricing\"/\"offer\"/\"partnership\", NEVER a business name, NEVER \"Came across\".\\n' +\n      'Return ONLY strict JSON: {\"subject\": \"...\", \"body\": \"...\"} with \\\\n for line breaks in body.';\n    const g = await callGateway(env, dossier, user, 1200);\n    if (g.err) return JSON.stringify({ error: 'model call failed: ' + g.err });\n    let parsed = pipeJson(g.text);\n    if (!parsed) { try { parsed = JSON.parse(g.text); } catch {} }\n    if (!parsed || !parsed.subject || !parsed.body) {\n      const mm = String(g.text).match(/\\{[\\s\\S]*\\}/);\n      if (mm) { try { parsed = JSON.parse(mm[0]); } catch {} }\n    }\n    if (!parsed || !parsed.subject || !parsed.body) return JSON.stringify({ error: 'model did not return subject/body', raw: String(g.text).slice(0, 300) });\n    // Scrub any personal name AND the banned word \"team\" from the signoff/body → brand only.\n    parsed.body = String(parsed.body)\n      .replace(/\\bOWNER_FIRST_NAME\\s+[OWNER_SURNAME]\\b/g, 'LeoResearch')\n      .replace(/\\bOWNER_FIRST_NAME\\b/g, 'LeoResearch')\n      .replace(/\\bthe\\s+LeoResearch\\s+team\\b/gi, 'LeoResearch')\n      .replace(/\\bLeoResearch\\s+team\\b/gi, 'LeoResearch')\n      .replace(/\\bteam\\s+at\\s+[A-Z][^\\n,.]{0,40}/g, 'Hello')\n      .replace(/\\bHi\\s+[^\\n,]{0,50}\\bteam\\b/gi, 'Hello');\n    const draft = { subject: String(parsed.subject), body: String(parsed.body), venture: brand, model: g.fallback ? 'gemini-2.5-flash(fallback)' : 'grok/grok-4.3', by: 'leadsDraftAI' };\n    const unsafe = /commonly used|use peptides|used in|support recovery|for recovery|clinical efficacy|treat(?:ment|ing)?|patient outcomes?|healing|therapeutic/i.test(draft.body);\n    if (unsafe) return JSON.stringify({ blocked: true, error: 'unsafe_copy_claim', note: 'Nothing saved. The model introduced a clinical/use claim; retry the draft.' });\n    // HARD SLOP REJECT: AI-tell phrases and any surviving \"team\" → do not save; force a retry.\n    const slop = /\\bteam\\b|positioned|perfectly|\\bexpand(?:ing)? into\\b|with your focus|your focus on|focus on (?:your|hormone|wellness|providing)|committed to|dedicated to|cutting[- ]edge|elevate|seamless|revolutioni[sz]e|unlock|empower|streamline|world[- ]class|game[- ]chang|thrilled|excited to|hope this (?:finds|email)|wanted to reach out|reaching out|as a (?:leading|premier)|your commitment/i.test(draft.body + ' ' + draft.subject);\n    if (slop) return JSON.stringify({ blocked: true, error: 'slop_or_banned_phrase', note: 'Nothing saved. Draft contained an AI-tell / banned phrase (team, positioned, focus on, committed to, etc.); retry.' });\n    // SUBJECT contract: 2-4 words, lowercase, internal-looking. Reject title-case, salesy words, business name.\n    const subj = String(draft.subject || '').trim();\n    const subjWords = subj.split(/\\s+/).filter(Boolean);\n    const subjBad = subjWords.length > 5 || /pricing|offer|partnership|introduction|opportunity|solution|premium|exclusive/i.test(subj) || /[A-Z]/.test(subj.replace(/COA|RUO/g, '')) || (lead.name && subj.toLowerCase().includes(String(lead.name).toLowerCase().split(/\\s+/)[0].toLowerCase()));\n    if (subjBad) return JSON.stringify({ blocked: true, error: 'subject_contract', note: 'Nothing saved. Subject must be 2-4 lowercase internal words (e.g. \"peptide wholesale\"), no title case / salesy words / business name; retry.' });\n    // The reply-no stop line lives in the send-time footer (leadsSend appends it) — requiring it\n    // in the body doubled it and read as boilerplate. Body contract = store mention only.\n    if (!/leoresearch\\.com/i.test(draft.body)) return JSON.stringify({ blocked: true, error: 'copy_contract_missing', note: 'Nothing saved. Draft must mention the store (leoresearch.com).' });\n    await env.DB.prepare(\"UPDATE leads SET draft=?, status='drafted' WHERE id=?\").bind(JSON.stringify(draft), id).run();\n    return JSON.stringify({ id, to: lead.email || '(no email yet)', subject: draft.subject, body: draft.body, model: draft.model });\n  },\n  // Follow-up sequence for a drafted lead. 40-60% of positive replies come from touches 2-3, not\n  // the first send — but each must carry a NEW angle on the SAME thread, never \"just checking in\".\n  // Generates touch 2 (new angle) + touch 3 (honest break-up). $1=lead_id, $2=brand(default LeoResearch).\n  ","version":8,"of":12},{"commit":"ab0f19645","date":"2026-07-23T11:38:32-07:00","what_changed":"outreach draft prompt: no-recipient-flattery opener, kits-of-10 model, full catalog, hard slop reject, brand-only signoff #vault-approved","chars":20000,"added":["'1. SALUTATION: \"Hello,\" on its own line. NEVER \"Hi <business> team\" / \"team at\" / any use of the word \"team\". If a real person first name is known, \"Hi <First>,\".\\n' +","'2. DO NOT open with a sentence about the recipient. No compliment, no describing their business, no \"with your focus on\", no \"positioned\"/\"perfectly positioned\"/\"positioned to expand\", no \"expand into new offerings\". You do not know their business. The FIRST line states plainly what you do: you supply research peptides wholesale. Nothing about them.\\n' +","'3. State the wholesale model plainly, one line: kits of 10 per item, 20% off to start, up to 50% off at volume, they set their own price.\\n' +","'4. INCLUDE THE FULL CATALOG LIST above, verbatim, as a clean list. Every item. This IS the email.\\n' +","'5. One line: COA per lot, ships from Austin, ~2-day restock. leoresearch.com once.\\n' +","'6. Low bar, one line: a first order can be a single kit of 10; happy to answer questions or send a sample. NEVER \"reply wholesale\" / \"reply for the sheet\" / \"send the sheet\" — the list is already here.\\n' +","'7. No clinical, efficacy, treatment, recovery, dosing, or outcome language. Commerce facts only.\\n' +","'8. NO compliance boilerplate in the body — the send system appends the opt-out footer.\\n' +","'9. Sign off with exactly one line: LeoResearch. NEVER a personal name. NEVER the word \"team\".\\n' +","'10. Plain human register. BANNED words: cutting-edge, elevate, seamless, revolutionize, unlock, empower, streamline, robust, world-class, game-changer, thrilled, excited, \"hope this finds you\", \"wanted to reach out\", \"reaching out\", \"positioned\", \"perfectly\", \"expand into\", \"focus on\", \"team\". No exclamation points. At most one em-dash. If a line sounds like AI, cut it.\\n' +","'Subject: plain, e.g. \"Wholesale research peptide pricing\" or \"Research peptide wholesale + white-label\". No \"Came across\".\\n' +","// Scrub any personal name AND the banned word \"team\" from the signoff/body → brand only.",".replace(/\\bOWNER_FIRST_NAME\\s+[OWNER_SURNAME]\\b/g, 'LeoResearch')",".replace(/\\bOWNER_FIRST_NAME\\b/g, 'LeoResearch')",".replace(/\\bthe\\s+LeoResearch\\s+team\\b/gi, 'LeoResearch')",".replace(/\\bLeoResearch\\s+team\\b/gi, 'LeoResearch')",".replace(/\\bteam\\s+at\\s+[A-Z][^\\n,.]{0,40}/g, 'Hello')",".replace(/\\bHi\\s+[^\\n,]{0,50}\\bteam\\b/gi, 'Hello');","// HARD SLOP REJECT: AI-tell phrases and any surviving \"team\" → do not save; force a retry.","const slop = /\\bteam\\b|positioned|perfectly|\\bexpand(?:ing)? into\\b|with your focus|your focus on|focus on (?:your|hormone|wellness|providing)|cutting[- ]edge|elevate|seamless|revolutioni[sz]e|unlock|empower|streamline|world[- ]class|game[- ]chang|thrilled|excited to|hope this (?:finds|email)|wanted to reach out|reaching out|as a (?:leading|premier)|your commitment/i.test(draft.body + ' ' + draft.subject);","if (slop) return JSON.stringify({ blocked: true, error: 'slop_or_banned_phrase', note: 'Nothing saved. Draft contained an AI-tell / banned phrase (team, positioned, focus on, etc.); retry.' });","'SELECT COUNT(*) sent, SUM(CASE WHEN opens>0 THEN 1 ELSE 0 END) opened, SUM(CASE WHEN cli"],"removed":["'1. SALUTATION: \"Hello,\" on its own line. NEVER \"Hi <business name> team\" or \"Hi <business> team\" — that is not a real salutation and reads as a mail-merge. If and only if a real person first name is known, \"Hi <First>,\" is allowed; otherwise \"Hello,\".\\n' +","'2. One-line opener stating why you are writing to a ' + segWord + ' specifically — a buyer angle, not a summary of their website, no flattery.\\n' +","'3. State the wholesale model plainly: 50-vial minimum per item, wholesale runs about half of public retail, they set their own price. One line.\\n' +","'4. INCLUDE THE FULL CATALOG LIST above, verbatim, as a clean list (peptide — retail / wholesale). List every item. Do NOT trim to 2-3 items. This IS the value of the email.\\n' +","'5. Logistics said once, plainly: Dallas warehouse, ~2-day restock, COA per lot. Public catalog at leoresearch.com once.\\n' +","'6. THE BAR IS LOW — say so: a first order can be as small as a few units, there is no large commitment to start. Invite them to place a small first order, ask a question, or set up a short call. NEVER say \"reply wholesale\" or \"reply for the sheet\" or offer to \"send the sheet\" — the list is already in the email.\\n' +","'7. No clinical, efficacy, treatment, recovery, or outcome language. Commerce facts only. Do not claim the recipient uses peptides.\\n' +","'8. NO compliance boilerplate in the body — the send system appends the opt-out footer automatically.\\n' +","'9. Sign \"The LeoResearch team\" on its own line. NEVER a personal human name — no \"the owner\", no first name, no owner name anywhere.\\n' +","'10. Professional register. No hype words (cutting-edge, revolutionize, elevate, seamless), no exclamation points, at most one em-dash. It should read like real business correspondence.\\n' +","'Subject: a plain professional line, e.g. \"LeoResearch — wholesale research peptide catalog\" or \"Wholesale peptide pricing for <city> clinics\". No \"Came across ...\".\\n' +","// Belt-and-suspenders on top of prompt rule 9: the owner's personal name must never appear","// in outbound copy. Swap any personal-name sign-off for the brand.",".replace(/\\b(Best|Thanks|Thank you|Regards|Warm(?:ly| regards)?|Sincerely|Cheers)[,\\s]*\\n+\\s*the owner(?:\\s+[OWNER_SURNAME])?\\b/gi, '$1,\\nThe LeoResearch team')",".replace(/\\bOWNER_FIRST_NAME\\s+[OWNER_SURNAME]\\b/g, 'The LeoResearch team')",".replace(/\\bOWNER_FIRST_NAME\\b/g, 'LeoResearch');","'SELECT COUNT(*) sent, SUM(CASE WHEN opens>0 THEN 1 ELSE 0 END) opened, SUM(CASE WHEN clicks>0 THEN 1 ELSE 0 END) clicked FROM email_sends'",").first()) || {};","return JSON.stringify({ totals: agg, shown: rows.length, sends: rows });","async leadsSend(env, confirm, leadId, fromLocal) {","if (String(confirm || '').trim().toUpperCase() !== 'CONFIRM') {","blocked: true,","error: 'explicit_confirmation_required',","note: 'Nothing sent. LEADS_SEND requires CONFIRM|lead_id|from_local.',","const lead = await env.DB.prepare(\"SELECT id,name,email,status,draft,score,COALESCE(note"],"text":"async leadsDraftAI(env, leadId, venture) {\n    const id = Number(leadId);\n    if (!id) return JSON.stringify({ error: 'lead_id required' });\n    const lead = await env.DB.prepare('SELECT id,name,segment,city,email,website,context,status,score,COALESCE(notes,\\'\\') notes FROM leads WHERE id=?').bind(id).first();\n    if (!lead) return JSON.stringify({ error: 'lead not found: ' + id });\n    if (!lead.email) return JSON.stringify({ blocked: true, error: 'verified_email_required', note: 'Nothing drafted. Enrich the lead first.' });\n    if (!String(lead.notes).includes('mx:ok')) return JSON.stringify({ blocked: true, error: 'mx_verification_required', note: 'Nothing drafted. Verify MX first.' });\n    if (!String(lead.notes).includes('icp:') || Number(lead.score || 0) < 65) return JSON.stringify({ blocked: true, error: 'icp_threshold_not_met', score: Number(lead.score || 0), minimum: 65, note: 'Nothing drafted. Only verified high-fit leads enter copy review.' });\n    if (String(lead.context || '').trim().length < 40) return JSON.stringify({ blocked: true, error: 'grounding_context_required', note: 'Nothing drafted. The business site context is too thin for truthful personalization.' });\n    const suppressed = await env.DB.prepare('SELECT reason FROM lead_suppressions WHERE lower(email)=lower(?)').bind(lead.email).first();\n    if (suppressed) return JSON.stringify({ blocked: true, error: 'recipient_suppressed', reason: suppressed.reason || 'suppressed' });\n    const brand = String(venture || 'LeoResearch');\n    const dr = await env.DB.prepare(\"SELECT content FROM directory WHERE key='OUTREACH_DOSSIER'\").first();\n    const dossier = dr ? String(dr.content || '') : '';\n    if (!dossier) return JSON.stringify({ error: 'OUTREACH_DOSSIER row is empty — set the brand/team/offer knowledge first' });\n    const cr = await env.DB.prepare(\"SELECT content FROM directory WHERE key='OUTREACH_CATALOG'\").first();\n    const catalog = cr ? String(cr.content || '') : '';\n    const segWord = ({ medspa: 'med spa', clinic: 'clinic', wellness: 'wellness practice', gym: 'gym', supplement: 'supplement brand' })[lead.segment] || 'business';\n    const user =\n      'Recipient business: ' + lead.name + ' — a ' + segWord + ' in ' + (lead.city || 'their city') + '.\\n' +\n      'What they do (from their own site): ' + (lead.context || lead.website || 'unknown') + '.\\n\\n' +\n      'WHOLESALE CATALOG (include this exact list in the email, formatted as a clean list — do not invent items or prices):\\n' + catalog + '\\n\\n' +\n      'Write ONE professional B2B wholesale email from ' + brand + '. Voice = a competent operator writing to a business owner. Follow exactly:\\n' +\n      '1. SALUTATION: \"Hello,\" on its own line. NEVER \"Hi <business> team\" / \"team at\" / any use of the word \"team\". If a real person first name is known, \"Hi <First>,\".\\n' +\n      '2. DO NOT open with a sentence about the recipient. No compliment, no describing their business, no \"with your focus on\", no \"positioned\"/\"perfectly positioned\"/\"positioned to expand\", no \"expand into new offerings\". You do not know their business. The FIRST line states plainly what you do: you supply research peptides wholesale. Nothing about them.\\n' +\n      '3. State the wholesale model plainly, one line: kits of 10 per item, 20% off to start, up to 50% off at volume, they set their own price.\\n' +\n      '4. INCLUDE THE FULL CATALOG LIST above, verbatim, as a clean list. Every item. This IS the email.\\n' +\n      '5. One line: COA per lot, ships from Austin, ~2-day restock. leoresearch.com once.\\n' +\n      '6. Low bar, one line: a first order can be a single kit of 10; happy to answer questions or send a sample. NEVER \"reply wholesale\" / \"reply for the sheet\" / \"send the sheet\" — the list is already here.\\n' +\n      '7. No clinical, efficacy, treatment, recovery, dosing, or outcome language. Commerce facts only.\\n' +\n      '8. NO compliance boilerplate in the body — the send system appends the opt-out footer.\\n' +\n      '9. Sign off with exactly one line: LeoResearch. NEVER a personal name. NEVER the word \"team\".\\n' +\n      '10. Plain human register. BANNED words: cutting-edge, elevate, seamless, revolutionize, unlock, empower, streamline, robust, world-class, game-changer, thrilled, excited, \"hope this finds you\", \"wanted to reach out\", \"reaching out\", \"positioned\", \"perfectly\", \"expand into\", \"focus on\", \"team\". No exclamation points. At most one em-dash. If a line sounds like AI, cut it.\\n' +\n      'Subject: plain, e.g. \"Wholesale research peptide pricing\" or \"Research peptide wholesale + white-label\". No \"Came across\".\\n' +\n      'Return ONLY strict JSON: {\"subject\": \"...\", \"body\": \"...\"} with \\\\n for line breaks in body.';\n    const g = await callGateway(env, dossier, user, 1200);\n    if (g.err) return JSON.stringify({ error: 'model call failed: ' + g.err });\n    let parsed = pipeJson(g.text);\n    if (!parsed) { try { parsed = JSON.parse(g.text); } catch {} }\n    if (!parsed || !parsed.subject || !parsed.body) {\n      const mm = String(g.text).match(/\\{[\\s\\S]*\\}/);\n      if (mm) { try { parsed = JSON.parse(mm[0]); } catch {} }\n    }\n    if (!parsed || !parsed.subject || !parsed.body) return JSON.stringify({ error: 'model did not return subject/body', raw: String(g.text).slice(0, 300) });\n    // Scrub any personal name AND the banned word \"team\" from the signoff/body → brand only.\n    parsed.body = String(parsed.body)\n      .replace(/\\bOWNER_FIRST_NAME\\s+[OWNER_SURNAME]\\b/g, 'LeoResearch')\n      .replace(/\\bOWNER_FIRST_NAME\\b/g, 'LeoResearch')\n      .replace(/\\bthe\\s+LeoResearch\\s+team\\b/gi, 'LeoResearch')\n      .replace(/\\bLeoResearch\\s+team\\b/gi, 'LeoResearch')\n      .replace(/\\bteam\\s+at\\s+[A-Z][^\\n,.]{0,40}/g, 'Hello')\n      .replace(/\\bHi\\s+[^\\n,]{0,50}\\bteam\\b/gi, 'Hello');\n    const draft = { subject: String(parsed.subject), body: String(parsed.body), venture: brand, model: g.fallback ? 'gemini-2.5-flash(fallback)' : 'grok/grok-4.3', by: 'leadsDraftAI' };\n    const unsafe = /commonly used|use peptides|used in|support recovery|for recovery|clinical efficacy|treat(?:ment|ing)?|patient outcomes?|healing|therapeutic/i.test(draft.body);\n    if (unsafe) return JSON.stringify({ blocked: true, error: 'unsafe_copy_claim', note: 'Nothing saved. The model introduced a clinical/use claim; retry the draft.' });\n    // HARD SLOP REJECT: AI-tell phrases and any surviving \"team\" → do not save; force a retry.\n    const slop = /\\bteam\\b|positioned|perfectly|\\bexpand(?:ing)? into\\b|with your focus|your focus on|focus on (?:your|hormone|wellness|providing)|cutting[- ]edge|elevate|seamless|revolutioni[sz]e|unlock|empower|streamline|world[- ]class|game[- ]chang|thrilled|excited to|hope this (?:finds|email)|wanted to reach out|reaching out|as a (?:leading|premier)|your commitment/i.test(draft.body + ' ' + draft.subject);\n    if (slop) return JSON.stringify({ blocked: true, error: 'slop_or_banned_phrase', note: 'Nothing saved. Draft contained an AI-tell / banned phrase (team, positioned, focus on, etc.); retry.' });\n    // The reply-no stop line lives in the send-time footer (leadsSend appends it) — requiring it\n    // in the body doubled it and read as boilerplate. Body contract = store mention only.\n    if (!/leoresearch\\.com/i.test(draft.body)) return JSON.stringify({ blocked: true, error: 'copy_contract_missing', note: 'Nothing saved. Draft must mention the store (leoresearch.com).' });\n    await env.DB.prepare(\"UPDATE leads SET draft=?, status='drafted' WHERE id=?\").bind(JSON.stringify(draft), id).run();\n    return JSON.stringify({ id, to: lead.email || '(no email yet)', subject: draft.subject, body: draft.body, model: draft.model });\n  },\n  // List leads by status. $1=status (default all).\n  async leadsList(env, status) {\n    const st = String(status || '').trim();\n    const rows = st\n      ? (await env.DB.prepare('SELECT id,name,segment,city,website,email,phone,status,score FROM leads WHERE status=? ORDER BY score DESC, id DESC LIMIT 100').bind(st).all()).results\n      : (await env.DB.prepare('SELECT id,name,segment,city,website,email,phone,status,score FROM leads ORDER BY score DESC, id DESC LIMIT 100').all()).results;\n    const counts = (await env.DB.prepare('SELECT status, COUNT(*) n FROM leads GROUP BY status').all()).results || [];\n    return JSON.stringify({ filter: st || 'all', shown: (rows || []).length, by_status: counts, leads: rows || [] });\n  },\n  // MX verification over enriched emails via DNS-over-HTTPS. A domain with no MX record\n  // cannot receive mail — those leads are tagged mx:none and (if not yet drafted) parked\n  // as no_mx so drafting/sending never wastes a slot on a dead mailbox. $1 = max leads (default 25).\n  async leadsVerifyMx(env, limitArg) {\n    const lim = Math.min(50, Math.max(1, parseInt(limitArg || '25', 10) || 25));\n    const rows = (await env.DB.prepare(\n      \"SELECT id, email, status, COALESCE(notes,'') notes FROM leads WHERE email IS NOT NULL AND email != '' AND COALESCE(notes,'') NOT LIKE '%mx:%' ORDER BY id LIMIT ?\"\n    ).bind(lim).all()).results || [];\n    if (!rows.length) return JSON.stringify({ checked: 0, note: 'no unverified emails left' });\n    const domainCache = {};\n    let ok = 0, none = 0, parked = 0;\n    for (const l of rows) {\n      const domain = String(l.email).split('@')[1] || '';\n      if (!domain) continue;\n      if (!(domain in domainCache)) {\n        try {\n          const r = await fetch('https://cloudflare-dns.com/dns-query?name=' + encodeURIComponent(domain) + '&type=MX', { headers: { accept: 'application/dns-json' } });\n          const j = await r.json();\n          domainCache[domain] = Array.isArray(j.Answer) && j.Answer.some(a => a.type === 15);\n        } catch { domainCache[domain] = null; }\n      }\n      const hasMx = domainCache[domain];\n      if (hasMx === null) continue;\n      const tag = hasMx ? ' mx:ok' : ' mx:none';\n      if (hasMx) ok++; else none++;\n      if (!hasMx && (l.status === 'enriched' || l.status === 'new')) {\n        await env.DB.prepare('UPDATE leads SET notes=?, status=? WHERE id=?').bind((l.notes + tag).trim(), 'no_mx', l.id).run();\n        parked++;\n      } else {\n        await env.DB.prepare('UPDATE leads SET notes=? WHERE id=?').bind((l.notes + tag).trim(), l.id).run();\n      }\n    }\n    return JSON.stringify({ checked: rows.length, mx_ok: ok, mx_none: none, parked_no_mx: parked, domains_queried: Object.keys(domainCache).length });\n  },\n  // AI ICP scoring: one Grok call scores a batch of enriched leads on wholesale/white-label fit\n  // grounded in the OUTREACH_DOSSIER. Writes score (0-100) + an icp: note with buyer type and\n  // reason, so LEADS_LIST ordering becomes commercial fit instead of has-a-website. $1 = batch size (default 8).\n  async leadsScoreAI(env, countArg) {\n    const n = Math.min(10, Math.max(1, parseInt(countArg || '8', 10) || 8));\n    const rows = (await env.DB.prepare(\n      \"SELECT id, name, segment, city, substr(COALESCE(context,''),1,400) context, COALESCE(notes,'') notes FROM leads WHERE status IN ('enriched','drafted') AND email IS NOT NULL AND email!='' AND COALESCE(notes,'') LIKE '%mx:ok%' AND COALESCE(notes,'') NOT LIKE '%icp:%' ORDER BY score DESC,id LIMIT ?\"\n    ).bind(n).all()).results || [];\n    if (!rows.length) return JSON.stringify({ scored: 0, note: 'no unscored enriched leads left' });\n    const dr = await env.DB.prepare(\"SELECT content FROM directory WHERE key='OUTREACH_DOSSIER'\").first();\n    const dossier = dr ? String(dr.content || '') : '';\n    if (!dossier) return JSON.stringify({ error: 'OUTREACH_DOSSIER row is empty — set the brand/team/offer knowledge first' });\n    const user =\n      'Score each business below as a wholesale/white-label peptide buyer for the venture in the dossier.\\n' +\n      'Leads: ' + JSON.stringify(rows.map(r => ({ id: r.id, name: r.name, segment: r.segment, city: r.city, about: r.context }))) + '\\n\\n' +\n      'For each, judge: does this business plausibly BUY peptides at volume (clinic protocols, med-spa services, resale), ' +\n      'who the buyer is, and how much volume is realistic. Penalize pure consumer gyms, marketing-agency pages, and anything not a real operating business. ' +\n      'Return ONLY strict JSON: [{\"id\":<lead id>,\"icp_fit\":<0-100>,\"buyer\":\"clinic_buyer|medspa_owner|practitioner|reseller|gym_owner|other\",\"volume\":\"low|med|high\",\"reason\":\"<max 120 chars, concrete>\"}]';\n    const g = await callGateway(env, dossier, user, 1600);\n    if (g.err) return JSON.stringify({ error: 'model call failed: ' + g.err });\n    let parsed = pipeJson(g.text);\n    if (!parsed) { const mm = String(g.text).match(/\\[[\\s\\S]*\\]/); if (mm) { try { parsed = JSON.parse(mm[0]); } catch {} } }\n    if (!Array.isArray(parsed)) return JSON.stringify({ error: 'model did not return a JSON array', raw: String(g.text).slice(0, 300) });\n    const byId = Object.fromEntries(rows.map(r => [r.id, r]));\n    let scored = 0;\n    const results = [];\n    for (const s of parsed) {\n      const lead = byId[Number(s.id)];\n      if (!lead || typeof s.icp_fit !== 'number') continue;\n      const fit = Math.min(100, Math.max(0, Math.round(s.icp_fit)));\n      const tag = ' icp:' + fit + ' buyer:' + String(s.buyer || 'other').slice(0, 20) + ' vol:' + String(s.volume || '?').slice(0, 4) + ' why:' + String(s.reason || '').replace(/\\s+/g, ' ').slice(0, 120);\n      await env.DB.prepare('UPDATE leads SET score=?, notes=? WHERE id=?').bind(fit, (lead.notes + tag).trim(), lead.id).run();\n      scored++;\n      results.push({ id: lead.id, name: lead.name, icp_fit: fit, buyer: s.buyer, volume: s.volume, reason: s.reason });\n    }\n    return JSON.stringify({ scored, model: 'grok/grok-4.3', results });\n  },\n  // Send ONE lead's drafted email via the miscsubjects email channel.\n  // HARD-GATED: $1 must equal CONFIRM, $2=lead_id, $3=from_local.\n  // Send a tracked email (open pixel + wrapped click links) and record it in email_sends for\n  // full visibility + engagement. $1 = JSON {to, subject, body, kind?, lead_id?, from?, from_name?, reply_to?}.\n  // Links in the body are rewritten to /api/t/c/<id> and a 1x1 /api/t/o/<id>.gif pixel is appended.\n  async emailSendTracked(env, raw) {\n    let p; try { p = JSON.parse(String(raw || '{}')); } catch { return JSON.stringify({ error: 'body must be JSON {to,subject,body,...}' }); }\n    const to = String(p.to || '').trim();\n    const subject = String(p.subject || '').trim();\n    const body = String(p.body || '');\n    if (!to || !subject || !body) return JSON.stringify({ error: 'to, subject, body required' });\n    const kind = String(p.kind || 'outreach');\n    const leadId = p.lead_id != null ? Number(p.lead_id) : null;\n    const fromLocal = String(p.from || 'wholesale').replace(/[^a-z0-9._-]/gi, '') || 'wholesale';\n    const id = 'es_' + (crypto.randomUUID ? crypto.randomUUID().replace(/-/g, '').slice(0, 20) : (Date.now().toString(36) + Math.random().toString(36).slice(2, 10)));\n    const base = 'https://miscsubjects.com/api/t';\n    const wrap = (url) => base + '/c/' + id + '?u=' + encodeURIComponent(btoa(url));\n    const urlRe = /(https?:\\/\\/[^\\s<>()\"']+)/g;\n    const textTracked = body.replace(urlRe, (m) => wrap(m));\n    const htmlBody = body.replace(/&/g, '&amp;').replace(/</g, '&lt;').replace(/>/g, '&gt;')\n      .replace(urlRe, (m) => '<a href=\"' + wrap(m) + '\">' + m + '</a>').replace(/\\n/g, '<br>\\n');\n    const html = htmlBody + '<img src=\"' + base + '/o/' + id + '.gif\" width=\"1\" height=\"1\" style=\"display:none\" alt=\"\">';\n    let status = 0, resp = '';\n    try {\n      const r = await fetch('https://miscsubjects.com/api/email/send', {\n        method: 'POST', headers: { 'Content-Type': 'application/json', 'x-terminal-key': env.TERMINAL_KEY },\n        body: JSON.stringify({ to, subject, text: textTracked, html, from: fromLocal + '@miscsubjects.com', from_name: p.from_name || 'LeoResearch', reply_to: p.reply_to || 'build@miscsubjects.com' }),\n      });\n      status = r.status; resp = (await r.text()).slice(0, 200);\n    } catch (e) { resp = 'fetch:' + (e && e.message || e); }\n    try {\n      await env.DB.prepare(\n        'INSERT INTO email_sends (id,lead_id,to_email,from_email,subject,body,kind,sent_at,send_status) VALUES (?,?,?,?,?,?,?,?,?)'\n      ).bind(id, leadId, to, fromLocal + '@miscsubjects.com', subject, textTracked, kind, buildNowIso(), status).run();\n    } catch (e) { return JSON.stringify({ id, to, subject, send_status: status, warn: 'sent but not recorded: ' + (e && e.message || e), response: resp }); }\n    return JSON.stringify({ id, to, subject, kind, send_status: status, open_pixel: base + '/o/' + id + '.gif', response: resp });\n  },\n  // Leads exposed as canonical CONTACT objects (person/business contact records) — the object\n  // logic fits the OIP shape without making each lead a directory row. A contact is a first-class\n  // object with channels (email/phone/instagram), fit score, signals, and provenance.\n  async contactGet(env, idArg) {\n    const id = Number(String(idArg || '').replace(/^contact:/, ''));\n    if (!id) return JSON.stringify({ error: 'contact id required (lead id or contact:<id>)' });\n    const l = await env.DB.prepare(\"SELECT id,name,segment,city,website,email,phone,status,score,source,context,COALESCE(notes,'') notes FROM leads WHERE id=?\").bind(id).first();\n    if (!l) return JSON.stringify({ error: 'contact not found: ' + id });\n    const igm = String(l.context || '').match(/ig:@([A-Za-z0-9_.]+)/);\n    const booking = (String(l.context || '').match(/booking:([a-z+-]+)/) || [])[1] || null;\n    return JSON.stringify({\n      object_type: 'contact', id: 'contact:' + l.id, lead_id: l.id,\n      name: l.name, business: l.name, segment: l.segment, city: l.city,\n      channels: { email: l.email || null, phone: l.phone || null, website: l.website || null, instagram: igm ? igm[1] : null },\n      fit_score: l.score, status: l.status, source: l.source,\n      signals: { booking_software: booking, mx: /mx:ok/.test(l.notes) ? 'ok' : (/mx:none/.test(l.notes) ? 'none' : null), icp: (String(l.notes).match(/icp:[^,]*/) || [])[0] || null },\n      context: l.context || null,\n    });\n  },\n  // List contact objects with optional status/segment/city filter. $1 = JSON {status?,segment?,city?,limit?} or a bare status.\n  async contactsList(env, raw) {\n    let f = {}; const s = String(raw || '').trim();\n    if (s.startsWith('{')) { try { f = JSON.parse(s); } catch {} } else if (s) { f.status = s; }\n    const lim = Math.min(200, Math.max(1, parseInt(f.limit || '50', 10) || 50));\n    const where = []; const bind = [];\n    if (f.status) { where.push('status=?'); bind.push(String(f.status)); }\n    if (f.segment) { where.push('segment=?'); bind.push(String(f.segment)); }\n    if (f.city) { where.push('city=?'); bind.push(String(f.city)); }\n    const sql = 'SELECT id,name,segment,city,email,phone,status,score FROM leads' + (where.length ? ' WHERE ' + where.join(' AND ') : '') + ' ORDER BY score DESC, id DESC LIMIT ?';\n    bind.push(lim);\n    const rows = (await env.DB.prepare(sql).bind(...bind).all()).results || [];\n    const counts = (await env.DB.prepare('SELECT status, COUNT(*) n FROM leads GROUP BY status').all()).results || [];\n    return JSON.stringify({ object_type: 'contact', filter: f, by_status: counts, shown: rows.length, contacts: rows.map(r => ({ id: 'contact:' + r.id, lead_id: r.id, name: r.name, segment: r.segment, city: r.city, email: r.email, phone: r.phone, status: r.status, fit_score: r.score })) });\n  },\n  // Visibility: list recent sent emails with open/click engagement + totals. $1 = limit (default 50).\n  async emailsSent(env, limitArg) {\n    const lim = Math.min(300, Math.max(1, parseInt(limitArg || '50', 10) || 50));\n    const rows = (await env.DB.prepare(\n      'SELECT id,lead_id,to_email,subject,kind,sent_at,send_status,opens,first_open_at,clicks,first_click_at FROM email_sends ORDER BY sent_at DESC LIMIT ?'\n    ).bind(lim).all()).results || [];\n    const agg = (await env.DB.prepare(\n      'SELECT COUNT(*) sent, SUM(CASE WHEN opens>0 THEN 1 ELSE 0 END) opened, SUM(CASE WHEN cli","version":7,"of":12},{"commit":"118bf673e","date":"2026-07-23T09:34:54-07:00","what_changed":"outreach copy overhaul: professional Hello salutation (kill 'Hi <biz> team'), full catalog listed in-email (kill 'reply wholesale'), low-bar CTA (a few units to start), OUTREACH_CATALOG source-of-truth; skill+generator+template aligned #vault-approved","chars":20000,"added":["const cr = await env.DB.prepare(\"SELECT content FROM directory WHERE key='OUTREACH_CATALOG'\").first();","const catalog = cr ? String(cr.content || '') : '';","'WHOLESALE CATALOG (include this exact list in the email, formatted as a clean list — do not invent items or prices):\\n' + catalog + '\\n\\n' +","'Write ONE professional B2B wholesale email from ' + brand + '. Voice = a competent operator writing to a business owner. Follow exactly:\\n' +","'1. SALUTATION: \"Hello,\" on its own line. NEVER \"Hi <business name> team\" or \"Hi <business> team\" — that is not a real salutation and reads as a mail-merge. If and only if a real person first name is known, \"Hi <First>,\" is allowed; otherwise \"Hello,\".\\n' +","'2. One-line opener stating why you are writing to a ' + segWord + ' specifically — a buyer angle, not a summary of their website, no flattery.\\n' +","'3. State the wholesale model plainly: 50-vial minimum per item, wholesale runs about half of public retail, they set their own price. One line.\\n' +","'4. INCLUDE THE FULL CATALOG LIST above, verbatim, as a clean list (peptide — retail / wholesale). List every item. Do NOT trim to 2-3 items. This IS the value of the email.\\n' +","'5. Logistics said once, plainly: Dallas warehouse, ~2-day restock, COA per lot. Public catalog at leoresearch.com once.\\n' +","'6. THE BAR IS LOW — say so: a first order can be as small as a few units, there is no large commitment to start. Invite them to place a small first order, ask a question, or set up a short call. NEVER say \"reply wholesale\" or \"reply for the sheet\" or offer to \"send the sheet\" — the list is already in the email.\\n' +","'7. No clinical, efficacy, treatment, recovery, or outcome language. Commerce facts only. Do not claim the recipient uses peptides.\\n' +","'9. Sign \"The LeoResearch team\" on its own line. NEVER a personal human name — no \"the owner\", no first name, no owner name anywhere.\\n' +","'10. Professional register. No hype words (cutting-edge, revolutionize, elevate, seamless), no exclamation points, at most one em-dash. It should read like real business correspondence.\\n' +","'Subject: a plain professional line, e.g. \"LeoResearch — wholesale research peptide catalog\" or \"Wholesale peptide pricing for <city> clinics\". No \"Came across ...\".\\n' +","const g = await callGateway(env, dossier, user, 1200);","const lead = await env.DB.prepare(\"SELECT id,name,email,status,draft,score,COALESCE(note"],"removed":["'Write ONE cold B2B email from ' + brand + ', voice = the founder writing person-to-person. OWNER-APPROVED STYLE (2026-07-12, replaced the rejected mail-merge template) — follow exactly:\\n' +","'1. NEVER open by reciting their homepage back at them. Open with why THEY specifically make sense as a buyer — an angle, not a summary of their site.\\n' +","'2. Lead the substance with THEIR economics, one real number: BPC-157 is $49 public sticker, about $24.50 a vial at 50 units; they set their own retail, so their margin roughly doubles.\\n' +","'3. Name at most 2-3 products RELEVANT to their practice type (aesthetics/skin -> GHK-Cu; hormone/TRT/primary care -> BPC-157, Tesamorelin, MOTS-C; IV bars -> GHK-Cu, MOTS-C). NEVER dump the whole catalog with milligrams.\\n' +","'4. Dallas warehouse, two-day restock, COA per lot — said once, plainly. For Dallas-area businesses say we are local.\\n' +","'5. Mention leoresearch.com once, naturally, as the public sticker/catalog.\\n' +","'6. One soft ask: a one-line reply gets the wholesale sheet, or a short call. Under 120 words. Vary structure — no two emails alike.\\n' +","'7. Do not claim the recipient uses peptides. No clinical, efficacy, treatment, recovery, or outcome language of any kind. Commerce facts only.\\n' +","'9. NEVER sign with a personal human name. Sign only as \"LeoResearch\" or \"the LeoResearch team\". No first name, no full name, no \"the owner\", no owner name anywhere in the body.\\n' +","'Subject: \"Came across <their name> — <short concrete angle>\".\\n' +","const g = await callGateway(env, dossier, user, 700);","const lead = await env.DB.prepare(\"SELECT id,name,email,status,draft,score,COALESCE(notes,'') notes FROM leads WHERE id=?\").bind(id).first();","if (!lead.email) return JSON.stringify({ error: 'no email for lead ' + id });","if (lead.status !== 'drafted') return JSON.stringify({ error: 'lead ' + id + ' is ' + lead.status + ', not drafted — draft it first' });","if (!String(lead.notes).includes('mx:ok')) return JSON.stringify({ blocked: true, error: 'mx_verification_required', note: 'Nothing sent.' });","if (!String(lead.notes).includes('icp:') || Number(lead.score || 0) < 65) return JSON.stringify({ blocked: true, error: 'icp_threshold_not_met', score: Number(lead.score || 0), minimum: 65, note: 'Nothing sent.' });","const suppressed = await env.DB.prepare('SELECT re"],"text":"async leadsDraftAI(env, leadId, venture) {\n    const id = Number(leadId);\n    if (!id) return JSON.stringify({ error: 'lead_id required' });\n    const lead = await env.DB.prepare('SELECT id,name,segment,city,email,website,context,status,score,COALESCE(notes,\\'\\') notes FROM leads WHERE id=?').bind(id).first();\n    if (!lead) return JSON.stringify({ error: 'lead not found: ' + id });\n    if (!lead.email) return JSON.stringify({ blocked: true, error: 'verified_email_required', note: 'Nothing drafted. Enrich the lead first.' });\n    if (!String(lead.notes).includes('mx:ok')) return JSON.stringify({ blocked: true, error: 'mx_verification_required', note: 'Nothing drafted. Verify MX first.' });\n    if (!String(lead.notes).includes('icp:') || Number(lead.score || 0) < 65) return JSON.stringify({ blocked: true, error: 'icp_threshold_not_met', score: Number(lead.score || 0), minimum: 65, note: 'Nothing drafted. Only verified high-fit leads enter copy review.' });\n    if (String(lead.context || '').trim().length < 40) return JSON.stringify({ blocked: true, error: 'grounding_context_required', note: 'Nothing drafted. The business site context is too thin for truthful personalization.' });\n    const suppressed = await env.DB.prepare('SELECT reason FROM lead_suppressions WHERE lower(email)=lower(?)').bind(lead.email).first();\n    if (suppressed) return JSON.stringify({ blocked: true, error: 'recipient_suppressed', reason: suppressed.reason || 'suppressed' });\n    const brand = String(venture || 'LeoResearch');\n    const dr = await env.DB.prepare(\"SELECT content FROM directory WHERE key='OUTREACH_DOSSIER'\").first();\n    const dossier = dr ? String(dr.content || '') : '';\n    if (!dossier) return JSON.stringify({ error: 'OUTREACH_DOSSIER row is empty — set the brand/team/offer knowledge first' });\n    const cr = await env.DB.prepare(\"SELECT content FROM directory WHERE key='OUTREACH_CATALOG'\").first();\n    const catalog = cr ? String(cr.content || '') : '';\n    const segWord = ({ medspa: 'med spa', clinic: 'clinic', wellness: 'wellness practice', gym: 'gym', supplement: 'supplement brand' })[lead.segment] || 'business';\n    const user =\n      'Recipient business: ' + lead.name + ' — a ' + segWord + ' in ' + (lead.city || 'their city') + '.\\n' +\n      'What they do (from their own site): ' + (lead.context || lead.website || 'unknown') + '.\\n\\n' +\n      'WHOLESALE CATALOG (include this exact list in the email, formatted as a clean list — do not invent items or prices):\\n' + catalog + '\\n\\n' +\n      'Write ONE professional B2B wholesale email from ' + brand + '. Voice = a competent operator writing to a business owner. Follow exactly:\\n' +\n      '1. SALUTATION: \"Hello,\" on its own line. NEVER \"Hi <business name> team\" or \"Hi <business> team\" — that is not a real salutation and reads as a mail-merge. If and only if a real person first name is known, \"Hi <First>,\" is allowed; otherwise \"Hello,\".\\n' +\n      '2. One-line opener stating why you are writing to a ' + segWord + ' specifically — a buyer angle, not a summary of their website, no flattery.\\n' +\n      '3. State the wholesale model plainly: 50-vial minimum per item, wholesale runs about half of public retail, they set their own price. One line.\\n' +\n      '4. INCLUDE THE FULL CATALOG LIST above, verbatim, as a clean list (peptide — retail / wholesale). List every item. Do NOT trim to 2-3 items. This IS the value of the email.\\n' +\n      '5. Logistics said once, plainly: Dallas warehouse, ~2-day restock, COA per lot. Public catalog at leoresearch.com once.\\n' +\n      '6. THE BAR IS LOW — say so: a first order can be as small as a few units, there is no large commitment to start. Invite them to place a small first order, ask a question, or set up a short call. NEVER say \"reply wholesale\" or \"reply for the sheet\" or offer to \"send the sheet\" — the list is already in the email.\\n' +\n      '7. No clinical, efficacy, treatment, recovery, or outcome language. Commerce facts only. Do not claim the recipient uses peptides.\\n' +\n      '8. NO compliance boilerplate in the body — the send system appends the opt-out footer automatically.\\n' +\n      '9. Sign \"The LeoResearch team\" on its own line. NEVER a personal human name — no \"the owner\", no first name, no owner name anywhere.\\n' +\n      '10. Professional register. No hype words (cutting-edge, revolutionize, elevate, seamless), no exclamation points, at most one em-dash. It should read like real business correspondence.\\n' +\n      'Subject: a plain professional line, e.g. \"LeoResearch — wholesale research peptide catalog\" or \"Wholesale peptide pricing for <city> clinics\". No \"Came across ...\".\\n' +\n      'Return ONLY strict JSON: {\"subject\": \"...\", \"body\": \"...\"} with \\\\n for line breaks in body.';\n    const g = await callGateway(env, dossier, user, 1200);\n    if (g.err) return JSON.stringify({ error: 'model call failed: ' + g.err });\n    let parsed = pipeJson(g.text);\n    if (!parsed) { try { parsed = JSON.parse(g.text); } catch {} }\n    if (!parsed || !parsed.subject || !parsed.body) {\n      const mm = String(g.text).match(/\\{[\\s\\S]*\\}/);\n      if (mm) { try { parsed = JSON.parse(mm[0]); } catch {} }\n    }\n    if (!parsed || !parsed.subject || !parsed.body) return JSON.stringify({ error: 'model did not return subject/body', raw: String(g.text).slice(0, 300) });\n    // Belt-and-suspenders on top of prompt rule 9: the owner's personal name must never appear\n    // in outbound copy. Swap any personal-name sign-off for the brand.\n    parsed.body = String(parsed.body)\n      .replace(/\\b(Best|Thanks|Thank you|Regards|Warm(?:ly| regards)?|Sincerely|Cheers)[,\\s]*\\n+\\s*the owner(?:\\s+[OWNER_SURNAME])?\\b/gi, '$1,\\nThe LeoResearch team')\n      .replace(/\\bOWNER_FIRST_NAME\\s+[OWNER_SURNAME]\\b/g, 'The LeoResearch team')\n      .replace(/\\bOWNER_FIRST_NAME\\b/g, 'LeoResearch');\n    const draft = { subject: String(parsed.subject), body: String(parsed.body), venture: brand, model: g.fallback ? 'gemini-2.5-flash(fallback)' : 'grok/grok-4.3', by: 'leadsDraftAI' };\n    const unsafe = /commonly used|use peptides|used in|support recovery|for recovery|clinical efficacy|treat(?:ment|ing)?|patient outcomes?|healing|therapeutic/i.test(draft.body);\n    if (unsafe) return JSON.stringify({ blocked: true, error: 'unsafe_copy_claim', note: 'Nothing saved. The model introduced a clinical/use claim; retry the draft.' });\n    // The reply-no stop line lives in the send-time footer (leadsSend appends it) — requiring it\n    // in the body doubled it and read as boilerplate. Body contract = store mention only.\n    if (!/leoresearch\\.com/i.test(draft.body)) return JSON.stringify({ blocked: true, error: 'copy_contract_missing', note: 'Nothing saved. Draft must mention the store (leoresearch.com).' });\n    await env.DB.prepare(\"UPDATE leads SET draft=?, status='drafted' WHERE id=?\").bind(JSON.stringify(draft), id).run();\n    return JSON.stringify({ id, to: lead.email || '(no email yet)', subject: draft.subject, body: draft.body, model: draft.model });\n  },\n  // List leads by status. $1=status (default all).\n  async leadsList(env, status) {\n    const st = String(status || '').trim();\n    const rows = st\n      ? (await env.DB.prepare('SELECT id,name,segment,city,website,email,phone,status,score FROM leads WHERE status=? ORDER BY score DESC, id DESC LIMIT 100').bind(st).all()).results\n      : (await env.DB.prepare('SELECT id,name,segment,city,website,email,phone,status,score FROM leads ORDER BY score DESC, id DESC LIMIT 100').all()).results;\n    const counts = (await env.DB.prepare('SELECT status, COUNT(*) n FROM leads GROUP BY status').all()).results || [];\n    return JSON.stringify({ filter: st || 'all', shown: (rows || []).length, by_status: counts, leads: rows || [] });\n  },\n  // MX verification over enriched emails via DNS-over-HTTPS. A domain with no MX record\n  // cannot receive mail — those leads are tagged mx:none and (if not yet drafted) parked\n  // as no_mx so drafting/sending never wastes a slot on a dead mailbox. $1 = max leads (default 25).\n  async leadsVerifyMx(env, limitArg) {\n    const lim = Math.min(50, Math.max(1, parseInt(limitArg || '25', 10) || 25));\n    const rows = (await env.DB.prepare(\n      \"SELECT id, email, status, COALESCE(notes,'') notes FROM leads WHERE email IS NOT NULL AND email != '' AND COALESCE(notes,'') NOT LIKE '%mx:%' ORDER BY id LIMIT ?\"\n    ).bind(lim).all()).results || [];\n    if (!rows.length) return JSON.stringify({ checked: 0, note: 'no unverified emails left' });\n    const domainCache = {};\n    let ok = 0, none = 0, parked = 0;\n    for (const l of rows) {\n      const domain = String(l.email).split('@')[1] || '';\n      if (!domain) continue;\n      if (!(domain in domainCache)) {\n        try {\n          const r = await fetch('https://cloudflare-dns.com/dns-query?name=' + encodeURIComponent(domain) + '&type=MX', { headers: { accept: 'application/dns-json' } });\n          const j = await r.json();\n          domainCache[domain] = Array.isArray(j.Answer) && j.Answer.some(a => a.type === 15);\n        } catch { domainCache[domain] = null; }\n      }\n      const hasMx = domainCache[domain];\n      if (hasMx === null) continue;\n      const tag = hasMx ? ' mx:ok' : ' mx:none';\n      if (hasMx) ok++; else none++;\n      if (!hasMx && (l.status === 'enriched' || l.status === 'new')) {\n        await env.DB.prepare('UPDATE leads SET notes=?, status=? WHERE id=?').bind((l.notes + tag).trim(), 'no_mx', l.id).run();\n        parked++;\n      } else {\n        await env.DB.prepare('UPDATE leads SET notes=? WHERE id=?').bind((l.notes + tag).trim(), l.id).run();\n      }\n    }\n    return JSON.stringify({ checked: rows.length, mx_ok: ok, mx_none: none, parked_no_mx: parked, domains_queried: Object.keys(domainCache).length });\n  },\n  // AI ICP scoring: one Grok call scores a batch of enriched leads on wholesale/white-label fit\n  // grounded in the OUTREACH_DOSSIER. Writes score (0-100) + an icp: note with buyer type and\n  // reason, so LEADS_LIST ordering becomes commercial fit instead of has-a-website. $1 = batch size (default 8).\n  async leadsScoreAI(env, countArg) {\n    const n = Math.min(10, Math.max(1, parseInt(countArg || '8', 10) || 8));\n    const rows = (await env.DB.prepare(\n      \"SELECT id, name, segment, city, substr(COALESCE(context,''),1,400) context, COALESCE(notes,'') notes FROM leads WHERE status IN ('enriched','drafted') AND email IS NOT NULL AND email!='' AND COALESCE(notes,'') LIKE '%mx:ok%' AND COALESCE(notes,'') NOT LIKE '%icp:%' ORDER BY score DESC,id LIMIT ?\"\n    ).bind(n).all()).results || [];\n    if (!rows.length) return JSON.stringify({ scored: 0, note: 'no unscored enriched leads left' });\n    const dr = await env.DB.prepare(\"SELECT content FROM directory WHERE key='OUTREACH_DOSSIER'\").first();\n    const dossier = dr ? String(dr.content || '') : '';\n    if (!dossier) return JSON.stringify({ error: 'OUTREACH_DOSSIER row is empty — set the brand/team/offer knowledge first' });\n    const user =\n      'Score each business below as a wholesale/white-label peptide buyer for the venture in the dossier.\\n' +\n      'Leads: ' + JSON.stringify(rows.map(r => ({ id: r.id, name: r.name, segment: r.segment, city: r.city, about: r.context }))) + '\\n\\n' +\n      'For each, judge: does this business plausibly BUY peptides at volume (clinic protocols, med-spa services, resale), ' +\n      'who the buyer is, and how much volume is realistic. Penalize pure consumer gyms, marketing-agency pages, and anything not a real operating business. ' +\n      'Return ONLY strict JSON: [{\"id\":<lead id>,\"icp_fit\":<0-100>,\"buyer\":\"clinic_buyer|medspa_owner|practitioner|reseller|gym_owner|other\",\"volume\":\"low|med|high\",\"reason\":\"<max 120 chars, concrete>\"}]';\n    const g = await callGateway(env, dossier, user, 1600);\n    if (g.err) return JSON.stringify({ error: 'model call failed: ' + g.err });\n    let parsed = pipeJson(g.text);\n    if (!parsed) { const mm = String(g.text).match(/\\[[\\s\\S]*\\]/); if (mm) { try { parsed = JSON.parse(mm[0]); } catch {} } }\n    if (!Array.isArray(parsed)) return JSON.stringify({ error: 'model did not return a JSON array', raw: String(g.text).slice(0, 300) });\n    const byId = Object.fromEntries(rows.map(r => [r.id, r]));\n    let scored = 0;\n    const results = [];\n    for (const s of parsed) {\n      const lead = byId[Number(s.id)];\n      if (!lead || typeof s.icp_fit !== 'number') continue;\n      const fit = Math.min(100, Math.max(0, Math.round(s.icp_fit)));\n      const tag = ' icp:' + fit + ' buyer:' + String(s.buyer || 'other').slice(0, 20) + ' vol:' + String(s.volume || '?').slice(0, 4) + ' why:' + String(s.reason || '').replace(/\\s+/g, ' ').slice(0, 120);\n      await env.DB.prepare('UPDATE leads SET score=?, notes=? WHERE id=?').bind(fit, (lead.notes + tag).trim(), lead.id).run();\n      scored++;\n      results.push({ id: lead.id, name: lead.name, icp_fit: fit, buyer: s.buyer, volume: s.volume, reason: s.reason });\n    }\n    return JSON.stringify({ scored, model: 'grok/grok-4.3', results });\n  },\n  // Send ONE lead's drafted email via the miscsubjects email channel.\n  // HARD-GATED: $1 must equal CONFIRM, $2=lead_id, $3=from_local.\n  // Send a tracked email (open pixel + wrapped click links) and record it in email_sends for\n  // full visibility + engagement. $1 = JSON {to, subject, body, kind?, lead_id?, from?, from_name?, reply_to?}.\n  // Links in the body are rewritten to /api/t/c/<id> and a 1x1 /api/t/o/<id>.gif pixel is appended.\n  async emailSendTracked(env, raw) {\n    let p; try { p = JSON.parse(String(raw || '{}')); } catch { return JSON.stringify({ error: 'body must be JSON {to,subject,body,...}' }); }\n    const to = String(p.to || '').trim();\n    const subject = String(p.subject || '').trim();\n    const body = String(p.body || '');\n    if (!to || !subject || !body) return JSON.stringify({ error: 'to, subject, body required' });\n    const kind = String(p.kind || 'outreach');\n    const leadId = p.lead_id != null ? Number(p.lead_id) : null;\n    const fromLocal = String(p.from || 'wholesale').replace(/[^a-z0-9._-]/gi, '') || 'wholesale';\n    const id = 'es_' + (crypto.randomUUID ? crypto.randomUUID().replace(/-/g, '').slice(0, 20) : (Date.now().toString(36) + Math.random().toString(36).slice(2, 10)));\n    const base = 'https://miscsubjects.com/api/t';\n    const wrap = (url) => base + '/c/' + id + '?u=' + encodeURIComponent(btoa(url));\n    const urlRe = /(https?:\\/\\/[^\\s<>()\"']+)/g;\n    const textTracked = body.replace(urlRe, (m) => wrap(m));\n    const htmlBody = body.replace(/&/g, '&amp;').replace(/</g, '&lt;').replace(/>/g, '&gt;')\n      .replace(urlRe, (m) => '<a href=\"' + wrap(m) + '\">' + m + '</a>').replace(/\\n/g, '<br>\\n');\n    const html = htmlBody + '<img src=\"' + base + '/o/' + id + '.gif\" width=\"1\" height=\"1\" style=\"display:none\" alt=\"\">';\n    let status = 0, resp = '';\n    try {\n      const r = await fetch('https://miscsubjects.com/api/email/send', {\n        method: 'POST', headers: { 'Content-Type': 'application/json', 'x-terminal-key': env.TERMINAL_KEY },\n        body: JSON.stringify({ to, subject, text: textTracked, html, from: fromLocal + '@miscsubjects.com', from_name: p.from_name || 'LeoResearch', reply_to: p.reply_to || 'build@miscsubjects.com' }),\n      });\n      status = r.status; resp = (await r.text()).slice(0, 200);\n    } catch (e) { resp = 'fetch:' + (e && e.message || e); }\n    try {\n      await env.DB.prepare(\n        'INSERT INTO email_sends (id,lead_id,to_email,from_email,subject,body,kind,sent_at,send_status) VALUES (?,?,?,?,?,?,?,?,?)'\n      ).bind(id, leadId, to, fromLocal + '@miscsubjects.com', subject, textTracked, kind, buildNowIso(), status).run();\n    } catch (e) { return JSON.stringify({ id, to, subject, send_status: status, warn: 'sent but not recorded: ' + (e && e.message || e), response: resp }); }\n    return JSON.stringify({ id, to, subject, kind, send_status: status, open_pixel: base + '/o/' + id + '.gif', response: resp });\n  },\n  // Leads exposed as canonical CONTACT objects (person/business contact records) — the object\n  // logic fits the OIP shape without making each lead a directory row. A contact is a first-class\n  // object with channels (email/phone/instagram), fit score, signals, and provenance.\n  async contactGet(env, idArg) {\n    const id = Number(String(idArg || '').replace(/^contact:/, ''));\n    if (!id) return JSON.stringify({ error: 'contact id required (lead id or contact:<id>)' });\n    const l = await env.DB.prepare(\"SELECT id,name,segment,city,website,email,phone,status,score,source,context,COALESCE(notes,'') notes FROM leads WHERE id=?\").bind(id).first();\n    if (!l) return JSON.stringify({ error: 'contact not found: ' + id });\n    const igm = String(l.context || '').match(/ig:@([A-Za-z0-9_.]+)/);\n    const booking = (String(l.context || '').match(/booking:([a-z+-]+)/) || [])[1] || null;\n    return JSON.stringify({\n      object_type: 'contact', id: 'contact:' + l.id, lead_id: l.id,\n      name: l.name, business: l.name, segment: l.segment, city: l.city,\n      channels: { email: l.email || null, phone: l.phone || null, website: l.website || null, instagram: igm ? igm[1] : null },\n      fit_score: l.score, status: l.status, source: l.source,\n      signals: { booking_software: booking, mx: /mx:ok/.test(l.notes) ? 'ok' : (/mx:none/.test(l.notes) ? 'none' : null), icp: (String(l.notes).match(/icp:[^,]*/) || [])[0] || null },\n      context: l.context || null,\n    });\n  },\n  // List contact objects with optional status/segment/city filter. $1 = JSON {status?,segment?,city?,limit?} or a bare status.\n  async contactsList(env, raw) {\n    let f = {}; const s = String(raw || '').trim();\n    if (s.startsWith('{')) { try { f = JSON.parse(s); } catch {} } else if (s) { f.status = s; }\n    const lim = Math.min(200, Math.max(1, parseInt(f.limit || '50', 10) || 50));\n    const where = []; const bind = [];\n    if (f.status) { where.push('status=?'); bind.push(String(f.status)); }\n    if (f.segment) { where.push('segment=?'); bind.push(String(f.segment)); }\n    if (f.city) { where.push('city=?'); bind.push(String(f.city)); }\n    const sql = 'SELECT id,name,segment,city,email,phone,status,score FROM leads' + (where.length ? ' WHERE ' + where.join(' AND ') : '') + ' ORDER BY score DESC, id DESC LIMIT ?';\n    bind.push(lim);\n    const rows = (await env.DB.prepare(sql).bind(...bind).all()).results || [];\n    const counts = (await env.DB.prepare('SELECT status, COUNT(*) n FROM leads GROUP BY status').all()).results || [];\n    return JSON.stringify({ object_type: 'contact', filter: f, by_status: counts, shown: rows.length, contacts: rows.map(r => ({ id: 'contact:' + r.id, lead_id: r.id, name: r.name, segment: r.segment, city: r.city, email: r.email, phone: r.phone, status: r.status, fit_score: r.score })) });\n  },\n  // Visibility: list recent sent emails with open/click engagement + totals. $1 = limit (default 50).\n  async emailsSent(env, limitArg) {\n    const lim = Math.min(300, Math.max(1, parseInt(limitArg || '50', 10) || 50));\n    const rows = (await env.DB.prepare(\n      'SELECT id,lead_id,to_email,subject,kind,sent_at,send_status,opens,first_open_at,clicks,first_click_at FROM email_sends ORDER BY sent_at DESC LIMIT ?'\n    ).bind(lim).all()).results || [];\n    const agg = (await env.DB.prepare(\n      'SELECT COUNT(*) sent, SUM(CASE WHEN opens>0 THEN 1 ELSE 0 END) opened, SUM(CASE WHEN clicks>0 THEN 1 ELSE 0 END) clicked FROM email_sends'\n    ).first()) || {};\n    return JSON.stringify({ totals: agg, shown: rows.length, sends: rows });\n  },\n  async leadsSend(env, confirm, leadId, fromLocal) {\n    if (String(confirm || '').trim().toUpperCase() !== 'CONFIRM') {\n      return JSON.stringify({\n        blocked: true,\n        error: 'explicit_confirmation_required',\n        note: 'Nothing sent. LEADS_SEND requires CONFIRM|lead_id|from_local.',\n      });\n    }\n    const id = Number(leadId);\n    if (!id) return JSON.stringify({ error: 'lead_id required' });\n    const lead = await env.DB.prepare(\"SELECT id,name,email,status,draft,score,COALESCE(note","version":6,"of":12},{"commit":"b38498c7b","date":"2026-07-23T00:15:58-07:00","what_changed":"leads as contact objects: CONTACT_GET + CONTACTS_LIST canonical contact accessors (channels/fit/signals) #vault-approved","chars":20000,"added":["// Leads exposed as canonical CONTACT objects (person/business contact records) — the object","// logic fits the OIP shape without making each lead a directory row. A contact is a first-class","// object with channels (email/phone/instagram), fit score, signals, and provenance.","async contactGet(env, idArg) {","const id = Number(String(idArg || '').replace(/^contact:/, ''));","if (!id) return JSON.stringify({ error: 'contact id required (lead id or contact:<id>)' });","const l = await env.DB.prepare(\"SELECT id,name,segment,city,website,email,phone,status,score,source,context,COALESCE(notes,'') notes FROM leads WHERE id=?\").bind(id).first();","if (!l) return JSON.stringify({ error: 'contact not found: ' + id });","const igm = String(l.context || '').match(/ig:@([A-Za-z0-9_.]+)/);","const booking = (String(l.context || '').match(/booking:([a-z+-]+)/) || [])[1] || null;","object_type: 'contact', id: 'contact:' + l.id, lead_id: l.id,","name: l.name, business: l.name, segment: l.segment, city: l.city,","channels: { email: l.email || null, phone: l.phone || null, website: l.website || null, instagram: igm ? igm[1] : null },","fit_score: l.score, status: l.status, source: l.source,","signals: { booking_software: booking, mx: /mx:ok/.test(l.notes) ? 'ok' : (/mx:none/.test(l.notes) ? 'none' : null), icp: (String(l.notes).match(/icp:[^,]*/) || [])[0] || null },","context: l.context || null,","// List contact objects with optional status/segment/city filter. $1 = JSON {status?,segment?,city?,limit?} or a bare status.","async contactsList(env, raw) {","let f = {}; const s = String(raw || '').trim();","if (s.startsWith('{')) { try { f = JSON.parse(s); } catch {} } else if (s) { f.status = s; }","const lim = Math.min(200, Math.max(1, parseInt(f.limit || '50', 10) || 50));","const where = []; const bind = [];","if (f.status) { where.push('status=?'); bind.push(String(f.status)); }","if (f.segment) { where.push('segment=?'); bind.push(String(f.segment)); }","if (f.city) { where.push('city=?'); bind.push(String(f.city)); }","const sql = 'SELECT id,name,segment,city,email,phone,status,score FROM leads' + (where.length ? ' WHERE ' + where.join(' AND ') : '') + ' ORDER BY score DESC, id DESC LIMIT ?';","bind.push(lim);","const rows = (await env.DB.prepare(sql).bind(...bind).all()).results || [];","return JSON.stringify({ object_type: 'contact', filter: f, by_status: counts, shown: rows.length, contacts: rows.map(r => ({ id: 'contact:' + r.id, lead_id: r.id, name: r.name, segment: r.segment, city: r.city, email: r.email, phone: r.phone, status: r.status, fit_score: r.score })) });","const suppressed = await env.DB.prepare('SELECT re"],"removed":["if (suppressed) return JSON.stringify({ blocked: true, error: 'recipient_suppressed', reason: suppressed.reason || 'suppressed', note: 'Nothing sent.' });","const settings = await env.DB.prepare(\"SELECT key,value FROM settings WHERE key IN ('outreach_postal_address','outreach_sending_domain_ready')\").all();","const cfg = Object.fromEntries((settings.results || []).map(r => [r.key, r.value]));","if (!String(cfg.outreach_postal_address || '').trim()) return JSON.stringify({ blocked: true, error: 'postal_address_required', note: 'Nothing sent. Set settings.outreach_postal_address to the valid business postal address required for commercial email.' });","if (String(cfg.outreach_sending_domain_ready || '') !== '1') return JSON.stringify({ blocked: true, error: 'sending_domain_not_ready', note: 'Nothing sent. Set settings.outreach_sending_domain_ready=1 only after From-domain SPF/DKIM/DMARC alignment is proven.' });","let d = {}; try { d = JSON.parse(lead.draft || '{}'); } catch {}","if (!d.subject || !d.body) return JSON.stringify({ error: 'lead ' + id + ' has no usable draft' });","const prior = await env.DB.prepare(","\"SELECT id,name FROM leads WHERE id<>? AND status='sent' AND LOWER(email)=LOWER(?) ORDER BY id LIMIT 1\"",").bind(id, lead.email).first();","if (prior) {","error: 'recipient_already_sent',","note: 'Nothing sent. This email address was already used by lead ' + prior.id + ' (' + prior.name + ').',","prior_lead_id: prior.id,","const local = String(fromLocal || 'wholesale').replace(/[^a-z0-9._-]/gi, '');","let res, err;","body: JSON.stringify({ to: lead.email, subject: d.subject, text: d.body.replace(/\\s+$/,'') + '\\n\\nAdvertisement from LeoResearch\\n' + cfg.outreach_postal_address + '\\nIf this is not relevant, reply no and I will not follow up.', from: local + '@miscsubjects.com', from_name: d.venture || 'LeoResearch', reply_to: 'build@miscsubjects.com' }),","res = await r.text(); if (!r.ok) err = 'HTTP ' + r.status;","} catch (e) { err = 'fetch:' + (e && e.message || e); }","if (err) return JSON.stringify({ id, error: 'send failed: ' + err, detail: String(res).slice(0, 200) });","await env.DB.prepare(\"UPDATE leads SET status='sent' WHERE id=?\").bind(id).run();","return JSON.stringify({ id, to: lead.email, subject: d.subject, status: 'sent', response: String(res).slice(0, 200) });","// Send a batch of drafted leads. HARD-GATED: $1 must equal the word CONFIRM,"],"text":"async leadsDraftAI(env, leadId, venture) {\n    const id = Number(leadId);\n    if (!id) return JSON.stringify({ error: 'lead_id required' });\n    const lead = await env.DB.prepare('SELECT id,name,segment,city,email,website,context,status,score,COALESCE(notes,\\'\\') notes FROM leads WHERE id=?').bind(id).first();\n    if (!lead) return JSON.stringify({ error: 'lead not found: ' + id });\n    if (!lead.email) return JSON.stringify({ blocked: true, error: 'verified_email_required', note: 'Nothing drafted. Enrich the lead first.' });\n    if (!String(lead.notes).includes('mx:ok')) return JSON.stringify({ blocked: true, error: 'mx_verification_required', note: 'Nothing drafted. Verify MX first.' });\n    if (!String(lead.notes).includes('icp:') || Number(lead.score || 0) < 65) return JSON.stringify({ blocked: true, error: 'icp_threshold_not_met', score: Number(lead.score || 0), minimum: 65, note: 'Nothing drafted. Only verified high-fit leads enter copy review.' });\n    if (String(lead.context || '').trim().length < 40) return JSON.stringify({ blocked: true, error: 'grounding_context_required', note: 'Nothing drafted. The business site context is too thin for truthful personalization.' });\n    const suppressed = await env.DB.prepare('SELECT reason FROM lead_suppressions WHERE lower(email)=lower(?)').bind(lead.email).first();\n    if (suppressed) return JSON.stringify({ blocked: true, error: 'recipient_suppressed', reason: suppressed.reason || 'suppressed' });\n    const brand = String(venture || 'LeoResearch');\n    const dr = await env.DB.prepare(\"SELECT content FROM directory WHERE key='OUTREACH_DOSSIER'\").first();\n    const dossier = dr ? String(dr.content || '') : '';\n    if (!dossier) return JSON.stringify({ error: 'OUTREACH_DOSSIER row is empty — set the brand/team/offer knowledge first' });\n    const segWord = ({ medspa: 'med spa', clinic: 'clinic', wellness: 'wellness practice', gym: 'gym', supplement: 'supplement brand' })[lead.segment] || 'business';\n    const user =\n      'Recipient business: ' + lead.name + ' — a ' + segWord + ' in ' + (lead.city || 'their city') + '.\\n' +\n      'What they do (from their own site): ' + (lead.context || lead.website || 'unknown') + '.\\n\\n' +\n      'Write ONE cold B2B email from ' + brand + ', voice = the founder writing person-to-person. OWNER-APPROVED STYLE (2026-07-12, replaced the rejected mail-merge template) — follow exactly:\\n' +\n      '1. NEVER open by reciting their homepage back at them. Open with why THEY specifically make sense as a buyer — an angle, not a summary of their site.\\n' +\n      '2. Lead the substance with THEIR economics, one real number: BPC-157 is $49 public sticker, about $24.50 a vial at 50 units; they set their own retail, so their margin roughly doubles.\\n' +\n      '3. Name at most 2-3 products RELEVANT to their practice type (aesthetics/skin -> GHK-Cu; hormone/TRT/primary care -> BPC-157, Tesamorelin, MOTS-C; IV bars -> GHK-Cu, MOTS-C). NEVER dump the whole catalog with milligrams.\\n' +\n      '4. Dallas warehouse, two-day restock, COA per lot — said once, plainly. For Dallas-area businesses say we are local.\\n' +\n      '5. Mention leoresearch.com once, naturally, as the public sticker/catalog.\\n' +\n      '6. One soft ask: a one-line reply gets the wholesale sheet, or a short call. Under 120 words. Vary structure — no two emails alike.\\n' +\n      '7. Do not claim the recipient uses peptides. No clinical, efficacy, treatment, recovery, or outcome language of any kind. Commerce facts only.\\n' +\n      '8. NO compliance boilerplate in the body — the send system appends the opt-out footer automatically.\\n' +\n      '9. NEVER sign with a personal human name. Sign only as \"LeoResearch\" or \"the LeoResearch team\". No first name, no full name, no \"the owner\", no owner name anywhere in the body.\\n' +\n      'Subject: \"Came across <their name> — <short concrete angle>\".\\n' +\n      'Return ONLY strict JSON: {\"subject\": \"...\", \"body\": \"...\"} with \\\\n for line breaks in body.';\n    const g = await callGateway(env, dossier, user, 700);\n    if (g.err) return JSON.stringify({ error: 'model call failed: ' + g.err });\n    let parsed = pipeJson(g.text);\n    if (!parsed) { try { parsed = JSON.parse(g.text); } catch {} }\n    if (!parsed || !parsed.subject || !parsed.body) {\n      const mm = String(g.text).match(/\\{[\\s\\S]*\\}/);\n      if (mm) { try { parsed = JSON.parse(mm[0]); } catch {} }\n    }\n    if (!parsed || !parsed.subject || !parsed.body) return JSON.stringify({ error: 'model did not return subject/body', raw: String(g.text).slice(0, 300) });\n    // Belt-and-suspenders on top of prompt rule 9: the owner's personal name must never appear\n    // in outbound copy. Swap any personal-name sign-off for the brand.\n    parsed.body = String(parsed.body)\n      .replace(/\\b(Best|Thanks|Thank you|Regards|Warm(?:ly| regards)?|Sincerely|Cheers)[,\\s]*\\n+\\s*the owner(?:\\s+[OWNER_SURNAME])?\\b/gi, '$1,\\nThe LeoResearch team')\n      .replace(/\\bOWNER_FIRST_NAME\\s+[OWNER_SURNAME]\\b/g, 'The LeoResearch team')\n      .replace(/\\bOWNER_FIRST_NAME\\b/g, 'LeoResearch');\n    const draft = { subject: String(parsed.subject), body: String(parsed.body), venture: brand, model: g.fallback ? 'gemini-2.5-flash(fallback)' : 'grok/grok-4.3', by: 'leadsDraftAI' };\n    const unsafe = /commonly used|use peptides|used in|support recovery|for recovery|clinical efficacy|treat(?:ment|ing)?|patient outcomes?|healing|therapeutic/i.test(draft.body);\n    if (unsafe) return JSON.stringify({ blocked: true, error: 'unsafe_copy_claim', note: 'Nothing saved. The model introduced a clinical/use claim; retry the draft.' });\n    // The reply-no stop line lives in the send-time footer (leadsSend appends it) — requiring it\n    // in the body doubled it and read as boilerplate. Body contract = store mention only.\n    if (!/leoresearch\\.com/i.test(draft.body)) return JSON.stringify({ blocked: true, error: 'copy_contract_missing', note: 'Nothing saved. Draft must mention the store (leoresearch.com).' });\n    await env.DB.prepare(\"UPDATE leads SET draft=?, status='drafted' WHERE id=?\").bind(JSON.stringify(draft), id).run();\n    return JSON.stringify({ id, to: lead.email || '(no email yet)', subject: draft.subject, body: draft.body, model: draft.model });\n  },\n  // List leads by status. $1=status (default all).\n  async leadsList(env, status) {\n    const st = String(status || '').trim();\n    const rows = st\n      ? (await env.DB.prepare('SELECT id,name,segment,city,website,email,phone,status,score FROM leads WHERE status=? ORDER BY score DESC, id DESC LIMIT 100').bind(st).all()).results\n      : (await env.DB.prepare('SELECT id,name,segment,city,website,email,phone,status,score FROM leads ORDER BY score DESC, id DESC LIMIT 100').all()).results;\n    const counts = (await env.DB.prepare('SELECT status, COUNT(*) n FROM leads GROUP BY status').all()).results || [];\n    return JSON.stringify({ filter: st || 'all', shown: (rows || []).length, by_status: counts, leads: rows || [] });\n  },\n  // MX verification over enriched emails via DNS-over-HTTPS. A domain with no MX record\n  // cannot receive mail — those leads are tagged mx:none and (if not yet drafted) parked\n  // as no_mx so drafting/sending never wastes a slot on a dead mailbox. $1 = max leads (default 25).\n  async leadsVerifyMx(env, limitArg) {\n    const lim = Math.min(50, Math.max(1, parseInt(limitArg || '25', 10) || 25));\n    const rows = (await env.DB.prepare(\n      \"SELECT id, email, status, COALESCE(notes,'') notes FROM leads WHERE email IS NOT NULL AND email != '' AND COALESCE(notes,'') NOT LIKE '%mx:%' ORDER BY id LIMIT ?\"\n    ).bind(lim).all()).results || [];\n    if (!rows.length) return JSON.stringify({ checked: 0, note: 'no unverified emails left' });\n    const domainCache = {};\n    let ok = 0, none = 0, parked = 0;\n    for (const l of rows) {\n      const domain = String(l.email).split('@')[1] || '';\n      if (!domain) continue;\n      if (!(domain in domainCache)) {\n        try {\n          const r = await fetch('https://cloudflare-dns.com/dns-query?name=' + encodeURIComponent(domain) + '&type=MX', { headers: { accept: 'application/dns-json' } });\n          const j = await r.json();\n          domainCache[domain] = Array.isArray(j.Answer) && j.Answer.some(a => a.type === 15);\n        } catch { domainCache[domain] = null; }\n      }\n      const hasMx = domainCache[domain];\n      if (hasMx === null) continue;\n      const tag = hasMx ? ' mx:ok' : ' mx:none';\n      if (hasMx) ok++; else none++;\n      if (!hasMx && (l.status === 'enriched' || l.status === 'new')) {\n        await env.DB.prepare('UPDATE leads SET notes=?, status=? WHERE id=?').bind((l.notes + tag).trim(), 'no_mx', l.id).run();\n        parked++;\n      } else {\n        await env.DB.prepare('UPDATE leads SET notes=? WHERE id=?').bind((l.notes + tag).trim(), l.id).run();\n      }\n    }\n    return JSON.stringify({ checked: rows.length, mx_ok: ok, mx_none: none, parked_no_mx: parked, domains_queried: Object.keys(domainCache).length });\n  },\n  // AI ICP scoring: one Grok call scores a batch of enriched leads on wholesale/white-label fit\n  // grounded in the OUTREACH_DOSSIER. Writes score (0-100) + an icp: note with buyer type and\n  // reason, so LEADS_LIST ordering becomes commercial fit instead of has-a-website. $1 = batch size (default 8).\n  async leadsScoreAI(env, countArg) {\n    const n = Math.min(10, Math.max(1, parseInt(countArg || '8', 10) || 8));\n    const rows = (await env.DB.prepare(\n      \"SELECT id, name, segment, city, substr(COALESCE(context,''),1,400) context, COALESCE(notes,'') notes FROM leads WHERE status IN ('enriched','drafted') AND email IS NOT NULL AND email!='' AND COALESCE(notes,'') LIKE '%mx:ok%' AND COALESCE(notes,'') NOT LIKE '%icp:%' ORDER BY score DESC,id LIMIT ?\"\n    ).bind(n).all()).results || [];\n    if (!rows.length) return JSON.stringify({ scored: 0, note: 'no unscored enriched leads left' });\n    const dr = await env.DB.prepare(\"SELECT content FROM directory WHERE key='OUTREACH_DOSSIER'\").first();\n    const dossier = dr ? String(dr.content || '') : '';\n    if (!dossier) return JSON.stringify({ error: 'OUTREACH_DOSSIER row is empty — set the brand/team/offer knowledge first' });\n    const user =\n      'Score each business below as a wholesale/white-label peptide buyer for the venture in the dossier.\\n' +\n      'Leads: ' + JSON.stringify(rows.map(r => ({ id: r.id, name: r.name, segment: r.segment, city: r.city, about: r.context }))) + '\\n\\n' +\n      'For each, judge: does this business plausibly BUY peptides at volume (clinic protocols, med-spa services, resale), ' +\n      'who the buyer is, and how much volume is realistic. Penalize pure consumer gyms, marketing-agency pages, and anything not a real operating business. ' +\n      'Return ONLY strict JSON: [{\"id\":<lead id>,\"icp_fit\":<0-100>,\"buyer\":\"clinic_buyer|medspa_owner|practitioner|reseller|gym_owner|other\",\"volume\":\"low|med|high\",\"reason\":\"<max 120 chars, concrete>\"}]';\n    const g = await callGateway(env, dossier, user, 1600);\n    if (g.err) return JSON.stringify({ error: 'model call failed: ' + g.err });\n    let parsed = pipeJson(g.text);\n    if (!parsed) { const mm = String(g.text).match(/\\[[\\s\\S]*\\]/); if (mm) { try { parsed = JSON.parse(mm[0]); } catch {} } }\n    if (!Array.isArray(parsed)) return JSON.stringify({ error: 'model did not return a JSON array', raw: String(g.text).slice(0, 300) });\n    const byId = Object.fromEntries(rows.map(r => [r.id, r]));\n    let scored = 0;\n    const results = [];\n    for (const s of parsed) {\n      const lead = byId[Number(s.id)];\n      if (!lead || typeof s.icp_fit !== 'number') continue;\n      const fit = Math.min(100, Math.max(0, Math.round(s.icp_fit)));\n      const tag = ' icp:' + fit + ' buyer:' + String(s.buyer || 'other').slice(0, 20) + ' vol:' + String(s.volume || '?').slice(0, 4) + ' why:' + String(s.reason || '').replace(/\\s+/g, ' ').slice(0, 120);\n      await env.DB.prepare('UPDATE leads SET score=?, notes=? WHERE id=?').bind(fit, (lead.notes + tag).trim(), lead.id).run();\n      scored++;\n      results.push({ id: lead.id, name: lead.name, icp_fit: fit, buyer: s.buyer, volume: s.volume, reason: s.reason });\n    }\n    return JSON.stringify({ scored, model: 'grok/grok-4.3', results });\n  },\n  // Send ONE lead's drafted email via the miscsubjects email channel.\n  // HARD-GATED: $1 must equal CONFIRM, $2=lead_id, $3=from_local.\n  // Send a tracked email (open pixel + wrapped click links) and record it in email_sends for\n  // full visibility + engagement. $1 = JSON {to, subject, body, kind?, lead_id?, from?, from_name?, reply_to?}.\n  // Links in the body are rewritten to /api/t/c/<id> and a 1x1 /api/t/o/<id>.gif pixel is appended.\n  async emailSendTracked(env, raw) {\n    let p; try { p = JSON.parse(String(raw || '{}')); } catch { return JSON.stringify({ error: 'body must be JSON {to,subject,body,...}' }); }\n    const to = String(p.to || '').trim();\n    const subject = String(p.subject || '').trim();\n    const body = String(p.body || '');\n    if (!to || !subject || !body) return JSON.stringify({ error: 'to, subject, body required' });\n    const kind = String(p.kind || 'outreach');\n    const leadId = p.lead_id != null ? Number(p.lead_id) : null;\n    const fromLocal = String(p.from || 'wholesale').replace(/[^a-z0-9._-]/gi, '') || 'wholesale';\n    const id = 'es_' + (crypto.randomUUID ? crypto.randomUUID().replace(/-/g, '').slice(0, 20) : (Date.now().toString(36) + Math.random().toString(36).slice(2, 10)));\n    const base = 'https://miscsubjects.com/api/t';\n    const wrap = (url) => base + '/c/' + id + '?u=' + encodeURIComponent(btoa(url));\n    const urlRe = /(https?:\\/\\/[^\\s<>()\"']+)/g;\n    const textTracked = body.replace(urlRe, (m) => wrap(m));\n    const htmlBody = body.replace(/&/g, '&amp;').replace(/</g, '&lt;').replace(/>/g, '&gt;')\n      .replace(urlRe, (m) => '<a href=\"' + wrap(m) + '\">' + m + '</a>').replace(/\\n/g, '<br>\\n');\n    const html = htmlBody + '<img src=\"' + base + '/o/' + id + '.gif\" width=\"1\" height=\"1\" style=\"display:none\" alt=\"\">';\n    let status = 0, resp = '';\n    try {\n      const r = await fetch('https://miscsubjects.com/api/email/send', {\n        method: 'POST', headers: { 'Content-Type': 'application/json', 'x-terminal-key': env.TERMINAL_KEY },\n        body: JSON.stringify({ to, subject, text: textTracked, html, from: fromLocal + '@miscsubjects.com', from_name: p.from_name || 'LeoResearch', reply_to: p.reply_to || 'build@miscsubjects.com' }),\n      });\n      status = r.status; resp = (await r.text()).slice(0, 200);\n    } catch (e) { resp = 'fetch:' + (e && e.message || e); }\n    try {\n      await env.DB.prepare(\n        'INSERT INTO email_sends (id,lead_id,to_email,from_email,subject,body,kind,sent_at,send_status) VALUES (?,?,?,?,?,?,?,?,?)'\n      ).bind(id, leadId, to, fromLocal + '@miscsubjects.com', subject, textTracked, kind, buildNowIso(), status).run();\n    } catch (e) { return JSON.stringify({ id, to, subject, send_status: status, warn: 'sent but not recorded: ' + (e && e.message || e), response: resp }); }\n    return JSON.stringify({ id, to, subject, kind, send_status: status, open_pixel: base + '/o/' + id + '.gif', response: resp });\n  },\n  // Leads exposed as canonical CONTACT objects (person/business contact records) — the object\n  // logic fits the OIP shape without making each lead a directory row. A contact is a first-class\n  // object with channels (email/phone/instagram), fit score, signals, and provenance.\n  async contactGet(env, idArg) {\n    const id = Number(String(idArg || '').replace(/^contact:/, ''));\n    if (!id) return JSON.stringify({ error: 'contact id required (lead id or contact:<id>)' });\n    const l = await env.DB.prepare(\"SELECT id,name,segment,city,website,email,phone,status,score,source,context,COALESCE(notes,'') notes FROM leads WHERE id=?\").bind(id).first();\n    if (!l) return JSON.stringify({ error: 'contact not found: ' + id });\n    const igm = String(l.context || '').match(/ig:@([A-Za-z0-9_.]+)/);\n    const booking = (String(l.context || '').match(/booking:([a-z+-]+)/) || [])[1] || null;\n    return JSON.stringify({\n      object_type: 'contact', id: 'contact:' + l.id, lead_id: l.id,\n      name: l.name, business: l.name, segment: l.segment, city: l.city,\n      channels: { email: l.email || null, phone: l.phone || null, website: l.website || null, instagram: igm ? igm[1] : null },\n      fit_score: l.score, status: l.status, source: l.source,\n      signals: { booking_software: booking, mx: /mx:ok/.test(l.notes) ? 'ok' : (/mx:none/.test(l.notes) ? 'none' : null), icp: (String(l.notes).match(/icp:[^,]*/) || [])[0] || null },\n      context: l.context || null,\n    });\n  },\n  // List contact objects with optional status/segment/city filter. $1 = JSON {status?,segment?,city?,limit?} or a bare status.\n  async contactsList(env, raw) {\n    let f = {}; const s = String(raw || '').trim();\n    if (s.startsWith('{')) { try { f = JSON.parse(s); } catch {} } else if (s) { f.status = s; }\n    const lim = Math.min(200, Math.max(1, parseInt(f.limit || '50', 10) || 50));\n    const where = []; const bind = [];\n    if (f.status) { where.push('status=?'); bind.push(String(f.status)); }\n    if (f.segment) { where.push('segment=?'); bind.push(String(f.segment)); }\n    if (f.city) { where.push('city=?'); bind.push(String(f.city)); }\n    const sql = 'SELECT id,name,segment,city,email,phone,status,score FROM leads' + (where.length ? ' WHERE ' + where.join(' AND ') : '') + ' ORDER BY score DESC, id DESC LIMIT ?';\n    bind.push(lim);\n    const rows = (await env.DB.prepare(sql).bind(...bind).all()).results || [];\n    const counts = (await env.DB.prepare('SELECT status, COUNT(*) n FROM leads GROUP BY status').all()).results || [];\n    return JSON.stringify({ object_type: 'contact', filter: f, by_status: counts, shown: rows.length, contacts: rows.map(r => ({ id: 'contact:' + r.id, lead_id: r.id, name: r.name, segment: r.segment, city: r.city, email: r.email, phone: r.phone, status: r.status, fit_score: r.score })) });\n  },\n  // Visibility: list recent sent emails with open/click engagement + totals. $1 = limit (default 50).\n  async emailsSent(env, limitArg) {\n    const lim = Math.min(300, Math.max(1, parseInt(limitArg || '50', 10) || 50));\n    const rows = (await env.DB.prepare(\n      'SELECT id,lead_id,to_email,subject,kind,sent_at,send_status,opens,first_open_at,clicks,first_click_at FROM email_sends ORDER BY sent_at DESC LIMIT ?'\n    ).bind(lim).all()).results || [];\n    const agg = (await env.DB.prepare(\n      'SELECT COUNT(*) sent, SUM(CASE WHEN opens>0 THEN 1 ELSE 0 END) opened, SUM(CASE WHEN clicks>0 THEN 1 ELSE 0 END) clicked FROM email_sends'\n    ).first()) || {};\n    return JSON.stringify({ totals: agg, shown: rows.length, sends: rows });\n  },\n  async leadsSend(env, confirm, leadId, fromLocal) {\n    if (String(confirm || '').trim().toUpperCase() !== 'CONFIRM') {\n      return JSON.stringify({\n        blocked: true,\n        error: 'explicit_confirmation_required',\n        note: 'Nothing sent. LEADS_SEND requires CONFIRM|lead_id|from_local.',\n      });\n    }\n    const id = Number(leadId);\n    if (!id) return JSON.stringify({ error: 'lead_id required' });\n    const lead = await env.DB.prepare(\"SELECT id,name,email,status,draft,score,COALESCE(notes,'') notes FROM leads WHERE id=?\").bind(id).first();\n    if (!lead) return JSON.stringify({ error: 'lead not found: ' + id });\n    if (!lead.email) return JSON.stringify({ error: 'no email for lead ' + id });\n    if (lead.status !== 'drafted') return JSON.stringify({ error: 'lead ' + id + ' is ' + lead.status + ', not drafted — draft it first' });\n    if (!String(lead.notes).includes('mx:ok')) return JSON.stringify({ blocked: true, error: 'mx_verification_required', note: 'Nothing sent.' });\n    if (!String(lead.notes).includes('icp:') || Number(lead.score || 0) < 65) return JSON.stringify({ blocked: true, error: 'icp_threshold_not_met', score: Number(lead.score || 0), minimum: 65, note: 'Nothing sent.' });\n    const suppressed = await env.DB.prepare('SELECT re","version":5,"of":12},{"commit":"40e18f884","date":"2026-07-23T00:07:42-07:00","what_changed":"email tracking: open pixel + click redirect endpoint, tracked-send + sent-visibility tools, email_sends table (migration 0325); write-human/business-email/cold-outreach-craft skills #vault-approved","chars":20000,"added":["// Send a tracked email (open pixel + wrapped click links) and record it in email_sends for","// full visibility + engagement. $1 = JSON {to, subject, body, kind?, lead_id?, from?, from_name?, reply_to?}.","// Links in the body are rewritten to /api/t/c/<id> and a 1x1 /api/t/o/<id>.gif pixel is appended.","async emailSendTracked(env, raw) {","let p; try { p = JSON.parse(String(raw || '{}')); } catch { return JSON.stringify({ error: 'body must be JSON {to,subject,body,...}' }); }","const to = String(p.to || '').trim();","const subject = String(p.subject || '').trim();","const body = String(p.body || '');","if (!to || !subject || !body) return JSON.stringify({ error: 'to, subject, body required' });","const kind = String(p.kind || 'outreach');","const leadId = p.lead_id != null ? Number(p.lead_id) : null;","const fromLocal = String(p.from || 'wholesale').replace(/[^a-z0-9._-]/gi, '') || 'wholesale';","const id = 'es_' + (crypto.randomUUID ? crypto.randomUUID().replace(/-/g, '').slice(0, 20) : (Date.now().toString(36) + Math.random().toString(36).slice(2, 10)));","const base = 'https://miscsubjects.com/api/t';","const wrap = (url) => base + '/c/' + id + '?u=' + encodeURIComponent(btoa(url));","const urlRe = /(https?:\\/\\/[^\\s<>()\"']+)/g;","const textTracked = body.replace(urlRe, (m) => wrap(m));","const htmlBody = body.replace(/&/g, '&amp;').replace(/</g, '&lt;').replace(/>/g, '&gt;')",".replace(urlRe, (m) => '<a href=\"' + wrap(m) + '\">' + m + '</a>').replace(/\\n/g, '<br>\\n');","const html = htmlBody + '<img src=\"' + base + '/o/' + id + '.gif\" width=\"1\" height=\"1\" style=\"display:none\" alt=\"\">';","let status = 0, resp = '';","body: JSON.stringify({ to, subject, text: textTracked, html, from: fromLocal + '@miscsubjects.com', from_name: p.from_name || 'LeoResearch', reply_to: p.reply_to || 'build@miscsubjects.com' }),","status = r.status; resp = (await r.text()).slice(0, 200);","} catch (e) { resp = 'fetch:' + (e && e.message || e); }","await env.DB.prepare(","'INSERT INTO email_sends (id,lead_id,to_email,from_email,subject,body,kind,sent_at,send_status) VALUES (?,?,?,?,?,?,?,?,?)'",").bind(id, leadId, to, fromLocal + '@miscsubjects.com', subject, textTracked, kind, buildNowIso(), status).run();","} catch (e) { return JSON.stringify({ id, to, subject, send_status: status, warn: 'sent but not recorded: ' + (e && e.message || e), response: resp }); }","return JSON.stringify({ id, to, subject, kind, send_status: status, open_pixel: base + '/o/' + id + '.gif', response: resp });","// Visibility: list recent sent emails with open/click engagement + totals. $1 = limit (default 50).","async emailsSent(env, limitArg) {","const lim = Math.min(300, Math.max(1, parseInt(limitArg || '50', 10) || 50));","'SELECT id,lead_id,to_email,subject,kind,sent_at,send_status,opens,first_open_at,clicks,first_click_at FROM email_sends ORDER BY sent_at DESC LIMIT ?'","const agg = (await env.DB.prepare(","'SELECT COUNT(*) sent, SUM(CASE WHEN opens>0 THEN 1 ELSE 0 END) opened, SUM(CASE WHEN clicks>0 THEN 1 ELSE 0 END) clicked FROM email_sends'",").first()) || {};","return JSON.stringify({ totals: agg, shown: rows.length, sends: rows });","// Send a batch of drafted leads. HARD-GATED: $1 must equal the word CONFIRM,"],"removed":["// Send a batch of drafted leads. HARD-GATED: $1 must equal the word CONFIRM, $2 = max count (cap 25).","// Refuses without CONFIRM so it can never accidentally blast.","async leadsSendBatch(env, confirm, maxArg) {","const ready = await env.DB.prepare(\"SELECT COUNT(*) n FROM leads WHERE status='drafted' AND email IS NOT NULL\").first();","return JSON.stringify({ blocked: true, note: 'Refusing to send without CONFIRM. Call LEADS_SEND_BATCH CONFIRM|N to send N.', drafted_ready: ready ? ready.n : 0 });","const n = Math.min(25, Math.max(1, parseInt(maxArg || '10', 10) || 10));","const rows = (await env.DB.prepare(\"SELECT id FROM leads WHERE status='drafted' AND email IS NOT NULL AND score>=65 AND COALESCE(notes,'') LIKE '%mx:ok%' ORDER BY score DESC,id DESC LIMIT ?\").bind(n).all()).results || [];","const sent = [];","for (const r of rows) { try { sent.push(JSON.parse(await FN_MAP.leadsSend(env, 'CONFIRM', r.id))); } catch (e) { sent.push({ id: r.id, error: String(e && e.message || e) }); } }","return JSON.stringify({ sent_count: sent.filter(s => s.status === 'sent').length, attempted: sent.length, results: sent });","// SCRAPER 2: Grok live web search discovers businesses OSM does not have.","// $1=segment, $2=city, $3=count(default 20, cap 30). Inserts source='grok-live-search'.","async leadsDiscoverAI(env, segment, city, countArg) {","const seg = String(segment || 'medspa').toLowerCase().trim();","const town = String(city || '').trim();","if (!town) return JSON.stringify({ error: 'city required, e.g. leadsDiscoverAI medspa|Scottsdale' });","const n = Math.min(30, Math.max(5, parseInt(countArg || '20', 10) || 20));","const segWord = ({ medspa: 'med spas / aesthetic clinics', clinic: 'medical clinics', wellness: 'wellness centers', gym: 'gyms / fitness studios', supplement: 'supplement stores', chiro: 'chiropractic / physical therapy practices', massage: 'massage / recovery studios', weightloss: 'weight-loss / nutrition clinics', longevity: 'longevity / IV-therapy / functional-medicine clinics' })[seg] || seg + ' businesses';","const g = await xaiSearch(env,","'You are a lead researcher. You output ONLY a strict JSON array, no prose, no markdown fence.',","'Find ' + n + ' real, currently-operating ' + segWord + ' in ' + town + '. Independent/local businesses preferred over national chains. For each return its exact business name and official website. Output ONLY: [{\"name\":\"...\",\"website\":\"https://...\"}]. Skip any business whose official website you cannot verify.',","3000);","if (g.err) return JSON.stringify({ error: 'live search failed: ' + g.err });","let arr = pipeJson(g.text);","if (!Array.isArray(arr)) { const mm = String(g.text).match(/\\[[\\s\\S]*\\]/); if (mm) { try { arr = JSON.parse(mm[0]); } catch {} } }","if (!Array.isArray(arr)) return JSON.stringify({ error: 'model did not return a JSON array', raw: String(g.text).slice(0, 200) });","let inserted = 0, skipped = 0;","for (const b of arr) {","const name = String(b && b.name || '').trim();","let site = String(b && b.website || '').trim();","if (!name || !/^https?:\\/\\/|^www\\./i.test(site)) { skipped++; continue; }","if (!/^https?:/i.test(site)) site = 'https://' + site;","const res = await env.DB.prepare(","\"INSERT INTO leads (created_at,name,segment,city,website,source,status,score) VALUES (?,?,?,?,?,?,?,?) ON CONFLICT(name,city) DO NOTHING\"",").bind(buildNowIso(), name, seg, town, site, 'grok-live-search', 'new', 2).run();"],"text":"async leadsDraftAI(env, leadId, venture) {\n    const id = Number(leadId);\n    if (!id) return JSON.stringify({ error: 'lead_id required' });\n    const lead = await env.DB.prepare('SELECT id,name,segment,city,email,website,context,status,score,COALESCE(notes,\\'\\') notes FROM leads WHERE id=?').bind(id).first();\n    if (!lead) return JSON.stringify({ error: 'lead not found: ' + id });\n    if (!lead.email) return JSON.stringify({ blocked: true, error: 'verified_email_required', note: 'Nothing drafted. Enrich the lead first.' });\n    if (!String(lead.notes).includes('mx:ok')) return JSON.stringify({ blocked: true, error: 'mx_verification_required', note: 'Nothing drafted. Verify MX first.' });\n    if (!String(lead.notes).includes('icp:') || Number(lead.score || 0) < 65) return JSON.stringify({ blocked: true, error: 'icp_threshold_not_met', score: Number(lead.score || 0), minimum: 65, note: 'Nothing drafted. Only verified high-fit leads enter copy review.' });\n    if (String(lead.context || '').trim().length < 40) return JSON.stringify({ blocked: true, error: 'grounding_context_required', note: 'Nothing drafted. The business site context is too thin for truthful personalization.' });\n    const suppressed = await env.DB.prepare('SELECT reason FROM lead_suppressions WHERE lower(email)=lower(?)').bind(lead.email).first();\n    if (suppressed) return JSON.stringify({ blocked: true, error: 'recipient_suppressed', reason: suppressed.reason || 'suppressed' });\n    const brand = String(venture || 'LeoResearch');\n    const dr = await env.DB.prepare(\"SELECT content FROM directory WHERE key='OUTREACH_DOSSIER'\").first();\n    const dossier = dr ? String(dr.content || '') : '';\n    if (!dossier) return JSON.stringify({ error: 'OUTREACH_DOSSIER row is empty — set the brand/team/offer knowledge first' });\n    const segWord = ({ medspa: 'med spa', clinic: 'clinic', wellness: 'wellness practice', gym: 'gym', supplement: 'supplement brand' })[lead.segment] || 'business';\n    const user =\n      'Recipient business: ' + lead.name + ' — a ' + segWord + ' in ' + (lead.city || 'their city') + '.\\n' +\n      'What they do (from their own site): ' + (lead.context || lead.website || 'unknown') + '.\\n\\n' +\n      'Write ONE cold B2B email from ' + brand + ', voice = the founder writing person-to-person. OWNER-APPROVED STYLE (2026-07-12, replaced the rejected mail-merge template) — follow exactly:\\n' +\n      '1. NEVER open by reciting their homepage back at them. Open with why THEY specifically make sense as a buyer — an angle, not a summary of their site.\\n' +\n      '2. Lead the substance with THEIR economics, one real number: BPC-157 is $49 public sticker, about $24.50 a vial at 50 units; they set their own retail, so their margin roughly doubles.\\n' +\n      '3. Name at most 2-3 products RELEVANT to their practice type (aesthetics/skin -> GHK-Cu; hormone/TRT/primary care -> BPC-157, Tesamorelin, MOTS-C; IV bars -> GHK-Cu, MOTS-C). NEVER dump the whole catalog with milligrams.\\n' +\n      '4. Dallas warehouse, two-day restock, COA per lot — said once, plainly. For Dallas-area businesses say we are local.\\n' +\n      '5. Mention leoresearch.com once, naturally, as the public sticker/catalog.\\n' +\n      '6. One soft ask: a one-line reply gets the wholesale sheet, or a short call. Under 120 words. Vary structure — no two emails alike.\\n' +\n      '7. Do not claim the recipient uses peptides. No clinical, efficacy, treatment, recovery, or outcome language of any kind. Commerce facts only.\\n' +\n      '8. NO compliance boilerplate in the body — the send system appends the opt-out footer automatically.\\n' +\n      '9. NEVER sign with a personal human name. Sign only as \"LeoResearch\" or \"the LeoResearch team\". No first name, no full name, no \"the owner\", no owner name anywhere in the body.\\n' +\n      'Subject: \"Came across <their name> — <short concrete angle>\".\\n' +\n      'Return ONLY strict JSON: {\"subject\": \"...\", \"body\": \"...\"} with \\\\n for line breaks in body.';\n    const g = await callGateway(env, dossier, user, 700);\n    if (g.err) return JSON.stringify({ error: 'model call failed: ' + g.err });\n    let parsed = pipeJson(g.text);\n    if (!parsed) { try { parsed = JSON.parse(g.text); } catch {} }\n    if (!parsed || !parsed.subject || !parsed.body) {\n      const mm = String(g.text).match(/\\{[\\s\\S]*\\}/);\n      if (mm) { try { parsed = JSON.parse(mm[0]); } catch {} }\n    }\n    if (!parsed || !parsed.subject || !parsed.body) return JSON.stringify({ error: 'model did not return subject/body', raw: String(g.text).slice(0, 300) });\n    // Belt-and-suspenders on top of prompt rule 9: the owner's personal name must never appear\n    // in outbound copy. Swap any personal-name sign-off for the brand.\n    parsed.body = String(parsed.body)\n      .replace(/\\b(Best|Thanks|Thank you|Regards|Warm(?:ly| regards)?|Sincerely|Cheers)[,\\s]*\\n+\\s*the owner(?:\\s+[OWNER_SURNAME])?\\b/gi, '$1,\\nThe LeoResearch team')\n      .replace(/\\bOWNER_FIRST_NAME\\s+[OWNER_SURNAME]\\b/g, 'The LeoResearch team')\n      .replace(/\\bOWNER_FIRST_NAME\\b/g, 'LeoResearch');\n    const draft = { subject: String(parsed.subject), body: String(parsed.body), venture: brand, model: g.fallback ? 'gemini-2.5-flash(fallback)' : 'grok/grok-4.3', by: 'leadsDraftAI' };\n    const unsafe = /commonly used|use peptides|used in|support recovery|for recovery|clinical efficacy|treat(?:ment|ing)?|patient outcomes?|healing|therapeutic/i.test(draft.body);\n    if (unsafe) return JSON.stringify({ blocked: true, error: 'unsafe_copy_claim', note: 'Nothing saved. The model introduced a clinical/use claim; retry the draft.' });\n    // The reply-no stop line lives in the send-time footer (leadsSend appends it) — requiring it\n    // in the body doubled it and read as boilerplate. Body contract = store mention only.\n    if (!/leoresearch\\.com/i.test(draft.body)) return JSON.stringify({ blocked: true, error: 'copy_contract_missing', note: 'Nothing saved. Draft must mention the store (leoresearch.com).' });\n    await env.DB.prepare(\"UPDATE leads SET draft=?, status='drafted' WHERE id=?\").bind(JSON.stringify(draft), id).run();\n    return JSON.stringify({ id, to: lead.email || '(no email yet)', subject: draft.subject, body: draft.body, model: draft.model });\n  },\n  // List leads by status. $1=status (default all).\n  async leadsList(env, status) {\n    const st = String(status || '').trim();\n    const rows = st\n      ? (await env.DB.prepare('SELECT id,name,segment,city,website,email,phone,status,score FROM leads WHERE status=? ORDER BY score DESC, id DESC LIMIT 100').bind(st).all()).results\n      : (await env.DB.prepare('SELECT id,name,segment,city,website,email,phone,status,score FROM leads ORDER BY score DESC, id DESC LIMIT 100').all()).results;\n    const counts = (await env.DB.prepare('SELECT status, COUNT(*) n FROM leads GROUP BY status').all()).results || [];\n    return JSON.stringify({ filter: st || 'all', shown: (rows || []).length, by_status: counts, leads: rows || [] });\n  },\n  // MX verification over enriched emails via DNS-over-HTTPS. A domain with no MX record\n  // cannot receive mail — those leads are tagged mx:none and (if not yet drafted) parked\n  // as no_mx so drafting/sending never wastes a slot on a dead mailbox. $1 = max leads (default 25).\n  async leadsVerifyMx(env, limitArg) {\n    const lim = Math.min(50, Math.max(1, parseInt(limitArg || '25', 10) || 25));\n    const rows = (await env.DB.prepare(\n      \"SELECT id, email, status, COALESCE(notes,'') notes FROM leads WHERE email IS NOT NULL AND email != '' AND COALESCE(notes,'') NOT LIKE '%mx:%' ORDER BY id LIMIT ?\"\n    ).bind(lim).all()).results || [];\n    if (!rows.length) return JSON.stringify({ checked: 0, note: 'no unverified emails left' });\n    const domainCache = {};\n    let ok = 0, none = 0, parked = 0;\n    for (const l of rows) {\n      const domain = String(l.email).split('@')[1] || '';\n      if (!domain) continue;\n      if (!(domain in domainCache)) {\n        try {\n          const r = await fetch('https://cloudflare-dns.com/dns-query?name=' + encodeURIComponent(domain) + '&type=MX', { headers: { accept: 'application/dns-json' } });\n          const j = await r.json();\n          domainCache[domain] = Array.isArray(j.Answer) && j.Answer.some(a => a.type === 15);\n        } catch { domainCache[domain] = null; }\n      }\n      const hasMx = domainCache[domain];\n      if (hasMx === null) continue;\n      const tag = hasMx ? ' mx:ok' : ' mx:none';\n      if (hasMx) ok++; else none++;\n      if (!hasMx && (l.status === 'enriched' || l.status === 'new')) {\n        await env.DB.prepare('UPDATE leads SET notes=?, status=? WHERE id=?').bind((l.notes + tag).trim(), 'no_mx', l.id).run();\n        parked++;\n      } else {\n        await env.DB.prepare('UPDATE leads SET notes=? WHERE id=?').bind((l.notes + tag).trim(), l.id).run();\n      }\n    }\n    return JSON.stringify({ checked: rows.length, mx_ok: ok, mx_none: none, parked_no_mx: parked, domains_queried: Object.keys(domainCache).length });\n  },\n  // AI ICP scoring: one Grok call scores a batch of enriched leads on wholesale/white-label fit\n  // grounded in the OUTREACH_DOSSIER. Writes score (0-100) + an icp: note with buyer type and\n  // reason, so LEADS_LIST ordering becomes commercial fit instead of has-a-website. $1 = batch size (default 8).\n  async leadsScoreAI(env, countArg) {\n    const n = Math.min(10, Math.max(1, parseInt(countArg || '8', 10) || 8));\n    const rows = (await env.DB.prepare(\n      \"SELECT id, name, segment, city, substr(COALESCE(context,''),1,400) context, COALESCE(notes,'') notes FROM leads WHERE status IN ('enriched','drafted') AND email IS NOT NULL AND email!='' AND COALESCE(notes,'') LIKE '%mx:ok%' AND COALESCE(notes,'') NOT LIKE '%icp:%' ORDER BY score DESC,id LIMIT ?\"\n    ).bind(n).all()).results || [];\n    if (!rows.length) return JSON.stringify({ scored: 0, note: 'no unscored enriched leads left' });\n    const dr = await env.DB.prepare(\"SELECT content FROM directory WHERE key='OUTREACH_DOSSIER'\").first();\n    const dossier = dr ? String(dr.content || '') : '';\n    if (!dossier) return JSON.stringify({ error: 'OUTREACH_DOSSIER row is empty — set the brand/team/offer knowledge first' });\n    const user =\n      'Score each business below as a wholesale/white-label peptide buyer for the venture in the dossier.\\n' +\n      'Leads: ' + JSON.stringify(rows.map(r => ({ id: r.id, name: r.name, segment: r.segment, city: r.city, about: r.context }))) + '\\n\\n' +\n      'For each, judge: does this business plausibly BUY peptides at volume (clinic protocols, med-spa services, resale), ' +\n      'who the buyer is, and how much volume is realistic. Penalize pure consumer gyms, marketing-agency pages, and anything not a real operating business. ' +\n      'Return ONLY strict JSON: [{\"id\":<lead id>,\"icp_fit\":<0-100>,\"buyer\":\"clinic_buyer|medspa_owner|practitioner|reseller|gym_owner|other\",\"volume\":\"low|med|high\",\"reason\":\"<max 120 chars, concrete>\"}]';\n    const g = await callGateway(env, dossier, user, 1600);\n    if (g.err) return JSON.stringify({ error: 'model call failed: ' + g.err });\n    let parsed = pipeJson(g.text);\n    if (!parsed) { const mm = String(g.text).match(/\\[[\\s\\S]*\\]/); if (mm) { try { parsed = JSON.parse(mm[0]); } catch {} } }\n    if (!Array.isArray(parsed)) return JSON.stringify({ error: 'model did not return a JSON array', raw: String(g.text).slice(0, 300) });\n    const byId = Object.fromEntries(rows.map(r => [r.id, r]));\n    let scored = 0;\n    const results = [];\n    for (const s of parsed) {\n      const lead = byId[Number(s.id)];\n      if (!lead || typeof s.icp_fit !== 'number') continue;\n      const fit = Math.min(100, Math.max(0, Math.round(s.icp_fit)));\n      const tag = ' icp:' + fit + ' buyer:' + String(s.buyer || 'other').slice(0, 20) + ' vol:' + String(s.volume || '?').slice(0, 4) + ' why:' + String(s.reason || '').replace(/\\s+/g, ' ').slice(0, 120);\n      await env.DB.prepare('UPDATE leads SET score=?, notes=? WHERE id=?').bind(fit, (lead.notes + tag).trim(), lead.id).run();\n      scored++;\n      results.push({ id: lead.id, name: lead.name, icp_fit: fit, buyer: s.buyer, volume: s.volume, reason: s.reason });\n    }\n    return JSON.stringify({ scored, model: 'grok/grok-4.3', results });\n  },\n  // Send ONE lead's drafted email via the miscsubjects email channel.\n  // HARD-GATED: $1 must equal CONFIRM, $2=lead_id, $3=from_local.\n  // Send a tracked email (open pixel + wrapped click links) and record it in email_sends for\n  // full visibility + engagement. $1 = JSON {to, subject, body, kind?, lead_id?, from?, from_name?, reply_to?}.\n  // Links in the body are rewritten to /api/t/c/<id> and a 1x1 /api/t/o/<id>.gif pixel is appended.\n  async emailSendTracked(env, raw) {\n    let p; try { p = JSON.parse(String(raw || '{}')); } catch { return JSON.stringify({ error: 'body must be JSON {to,subject,body,...}' }); }\n    const to = String(p.to || '').trim();\n    const subject = String(p.subject || '').trim();\n    const body = String(p.body || '');\n    if (!to || !subject || !body) return JSON.stringify({ error: 'to, subject, body required' });\n    const kind = String(p.kind || 'outreach');\n    const leadId = p.lead_id != null ? Number(p.lead_id) : null;\n    const fromLocal = String(p.from || 'wholesale').replace(/[^a-z0-9._-]/gi, '') || 'wholesale';\n    const id = 'es_' + (crypto.randomUUID ? crypto.randomUUID().replace(/-/g, '').slice(0, 20) : (Date.now().toString(36) + Math.random().toString(36).slice(2, 10)));\n    const base = 'https://miscsubjects.com/api/t';\n    const wrap = (url) => base + '/c/' + id + '?u=' + encodeURIComponent(btoa(url));\n    const urlRe = /(https?:\\/\\/[^\\s<>()\"']+)/g;\n    const textTracked = body.replace(urlRe, (m) => wrap(m));\n    const htmlBody = body.replace(/&/g, '&amp;').replace(/</g, '&lt;').replace(/>/g, '&gt;')\n      .replace(urlRe, (m) => '<a href=\"' + wrap(m) + '\">' + m + '</a>').replace(/\\n/g, '<br>\\n');\n    const html = htmlBody + '<img src=\"' + base + '/o/' + id + '.gif\" width=\"1\" height=\"1\" style=\"display:none\" alt=\"\">';\n    let status = 0, resp = '';\n    try {\n      const r = await fetch('https://miscsubjects.com/api/email/send', {\n        method: 'POST', headers: { 'Content-Type': 'application/json', 'x-terminal-key': env.TERMINAL_KEY },\n        body: JSON.stringify({ to, subject, text: textTracked, html, from: fromLocal + '@miscsubjects.com', from_name: p.from_name || 'LeoResearch', reply_to: p.reply_to || 'build@miscsubjects.com' }),\n      });\n      status = r.status; resp = (await r.text()).slice(0, 200);\n    } catch (e) { resp = 'fetch:' + (e && e.message || e); }\n    try {\n      await env.DB.prepare(\n        'INSERT INTO email_sends (id,lead_id,to_email,from_email,subject,body,kind,sent_at,send_status) VALUES (?,?,?,?,?,?,?,?,?)'\n      ).bind(id, leadId, to, fromLocal + '@miscsubjects.com', subject, textTracked, kind, buildNowIso(), status).run();\n    } catch (e) { return JSON.stringify({ id, to, subject, send_status: status, warn: 'sent but not recorded: ' + (e && e.message || e), response: resp }); }\n    return JSON.stringify({ id, to, subject, kind, send_status: status, open_pixel: base + '/o/' + id + '.gif', response: resp });\n  },\n  // Visibility: list recent sent emails with open/click engagement + totals. $1 = limit (default 50).\n  async emailsSent(env, limitArg) {\n    const lim = Math.min(300, Math.max(1, parseInt(limitArg || '50', 10) || 50));\n    const rows = (await env.DB.prepare(\n      'SELECT id,lead_id,to_email,subject,kind,sent_at,send_status,opens,first_open_at,clicks,first_click_at FROM email_sends ORDER BY sent_at DESC LIMIT ?'\n    ).bind(lim).all()).results || [];\n    const agg = (await env.DB.prepare(\n      'SELECT COUNT(*) sent, SUM(CASE WHEN opens>0 THEN 1 ELSE 0 END) opened, SUM(CASE WHEN clicks>0 THEN 1 ELSE 0 END) clicked FROM email_sends'\n    ).first()) || {};\n    return JSON.stringify({ totals: agg, shown: rows.length, sends: rows });\n  },\n  async leadsSend(env, confirm, leadId, fromLocal) {\n    if (String(confirm || '').trim().toUpperCase() !== 'CONFIRM') {\n      return JSON.stringify({\n        blocked: true,\n        error: 'explicit_confirmation_required',\n        note: 'Nothing sent. LEADS_SEND requires CONFIRM|lead_id|from_local.',\n      });\n    }\n    const id = Number(leadId);\n    if (!id) return JSON.stringify({ error: 'lead_id required' });\n    const lead = await env.DB.prepare(\"SELECT id,name,email,status,draft,score,COALESCE(notes,'') notes FROM leads WHERE id=?\").bind(id).first();\n    if (!lead) return JSON.stringify({ error: 'lead not found: ' + id });\n    if (!lead.email) return JSON.stringify({ error: 'no email for lead ' + id });\n    if (lead.status !== 'drafted') return JSON.stringify({ error: 'lead ' + id + ' is ' + lead.status + ', not drafted — draft it first' });\n    if (!String(lead.notes).includes('mx:ok')) return JSON.stringify({ blocked: true, error: 'mx_verification_required', note: 'Nothing sent.' });\n    if (!String(lead.notes).includes('icp:') || Number(lead.score || 0) < 65) return JSON.stringify({ blocked: true, error: 'icp_threshold_not_met', score: Number(lead.score || 0), minimum: 65, note: 'Nothing sent.' });\n    const suppressed = await env.DB.prepare('SELECT reason FROM lead_suppressions WHERE lower(email)=lower(?)').bind(lead.email).first();\n    if (suppressed) return JSON.stringify({ blocked: true, error: 'recipient_suppressed', reason: suppressed.reason || 'suppressed', note: 'Nothing sent.' });\n    const settings = await env.DB.prepare(\"SELECT key,value FROM settings WHERE key IN ('outreach_postal_address','outreach_sending_domain_ready')\").all();\n    const cfg = Object.fromEntries((settings.results || []).map(r => [r.key, r.value]));\n    if (!String(cfg.outreach_postal_address || '').trim()) return JSON.stringify({ blocked: true, error: 'postal_address_required', note: 'Nothing sent. Set settings.outreach_postal_address to the valid business postal address required for commercial email.' });\n    if (String(cfg.outreach_sending_domain_ready || '') !== '1') return JSON.stringify({ blocked: true, error: 'sending_domain_not_ready', note: 'Nothing sent. Set settings.outreach_sending_domain_ready=1 only after From-domain SPF/DKIM/DMARC alignment is proven.' });\n    let d = {}; try { d = JSON.parse(lead.draft || '{}'); } catch {}\n    if (!d.subject || !d.body) return JSON.stringify({ error: 'lead ' + id + ' has no usable draft' });\n    const prior = await env.DB.prepare(\n      \"SELECT id,name FROM leads WHERE id<>? AND status='sent' AND LOWER(email)=LOWER(?) ORDER BY id LIMIT 1\"\n    ).bind(id, lead.email).first();\n    if (prior) {\n      return JSON.stringify({\n        blocked: true,\n        error: 'recipient_already_sent',\n        note: 'Nothing sent. This email address was already used by lead ' + prior.id + ' (' + prior.name + ').',\n        prior_lead_id: prior.id,\n      });\n    }\n    const local = String(fromLocal || 'wholesale').replace(/[^a-z0-9._-]/gi, '');\n    let res, err;\n    try {\n      const r = await fetch('https://miscsubjects.com/api/email/send', {\n        method: 'POST', headers: { 'Content-Type': 'application/json', 'x-terminal-key': env.TERMINAL_KEY },\n        body: JSON.stringify({ to: lead.email, subject: d.subject, text: d.body.replace(/\\s+$/,'') + '\\n\\nAdvertisement from LeoResearch\\n' + cfg.outreach_postal_address + '\\nIf this is not relevant, reply no and I will not follow up.', from: local + '@miscsubjects.com', from_name: d.venture || 'LeoResearch', reply_to: 'build@miscsubjects.com' }),\n      });\n      res = await r.text(); if (!r.ok) err = 'HTTP ' + r.status;\n    } catch (e) { err = 'fetch:' + (e && e.message || e); }\n    if (err) return JSON.stringify({ id, error: 'send failed: ' + err, detail: String(res).slice(0, 200) });\n    await env.DB.prepare(\"UPDATE leads SET status='sent' WHERE id=?\").bind(id).run();\n    return JSON.stringify({ id, to: lead.email, subject: d.subject, status: 'sent', response: String(res).slice(0, 200) });\n  },\n  // Send a batch of drafted leads. HARD-GATED: $1 must equal the word CONFIRM,","version":4,"of":12},{"commit":"6667d40d1","date":"2026-07-22T22:32:36-07:00","what_changed":"leads draft: never sign the owner personal name — prompt rule + hard body scrub to brand signature #vault-approved","chars":20000,"added":["'9. NEVER sign with a personal human name. Sign only as \"LeoResearch\" or \"the LeoResearch team\". No first name, no full name, no \"the owner\", no owner name anywhere in the body.\\n' +","// Belt-and-suspenders on top of prompt rule 9: the owner's personal name must never appear","// in outbound copy. Swap any personal-name sign-off for the brand.","parsed.body = String(parsed.body)",".replace(/\\b(Best|Thanks|Thank you|Regards|Warm(?:ly| regards)?|Sincerely|Cheers)[,\\s]*\\n+\\s*the owner(?:\\s+[OWNER_SURNAME])?\\b/gi, '$1,\\nThe LeoResearch team')",".replace(/\\bOWNER_FIRST_NAME\\s+[OWNER_SURNAME]\\b/g, 'The LeoResearch team')",".replace(/\\bOWNER_FIRST_NAME\\b/g, 'LeoResearch');"],"removed":["if (res.meta && res.meta.changes) inserted++; else skipped++;","} catch (e) { skipped++; }","return JSON.stringify({ segment: seg, city: town, model_returned: arr.length, inserted_new: inserted, skipped_dup_or_bad: skipped, source: 'grok-live-search' });","// Rescue websiteless leads: ONE Grok live-search call finds official websites for a batch of","// leads that have a name+city but no site (OSM often lacks the website tag). $1=count(default 8, cap 10).","async leadsFindSites(env, countArg) {","const rows = (await env.DB.prepare(\"SELECT id,name"],"text":"async leadsDraftAI(env, leadId, venture) {\n    const id = Number(leadId);\n    if (!id) return JSON.stringify({ error: 'lead_id required' });\n    const lead = await env.DB.prepare('SELECT id,name,segment,city,email,website,context,status,score,COALESCE(notes,\\'\\') notes FROM leads WHERE id=?').bind(id).first();\n    if (!lead) return JSON.stringify({ error: 'lead not found: ' + id });\n    if (!lead.email) return JSON.stringify({ blocked: true, error: 'verified_email_required', note: 'Nothing drafted. Enrich the lead first.' });\n    if (!String(lead.notes).includes('mx:ok')) return JSON.stringify({ blocked: true, error: 'mx_verification_required', note: 'Nothing drafted. Verify MX first.' });\n    if (!String(lead.notes).includes('icp:') || Number(lead.score || 0) < 65) return JSON.stringify({ blocked: true, error: 'icp_threshold_not_met', score: Number(lead.score || 0), minimum: 65, note: 'Nothing drafted. Only verified high-fit leads enter copy review.' });\n    if (String(lead.context || '').trim().length < 40) return JSON.stringify({ blocked: true, error: 'grounding_context_required', note: 'Nothing drafted. The business site context is too thin for truthful personalization.' });\n    const suppressed = await env.DB.prepare('SELECT reason FROM lead_suppressions WHERE lower(email)=lower(?)').bind(lead.email).first();\n    if (suppressed) return JSON.stringify({ blocked: true, error: 'recipient_suppressed', reason: suppressed.reason || 'suppressed' });\n    const brand = String(venture || 'LeoResearch');\n    const dr = await env.DB.prepare(\"SELECT content FROM directory WHERE key='OUTREACH_DOSSIER'\").first();\n    const dossier = dr ? String(dr.content || '') : '';\n    if (!dossier) return JSON.stringify({ error: 'OUTREACH_DOSSIER row is empty — set the brand/team/offer knowledge first' });\n    const segWord = ({ medspa: 'med spa', clinic: 'clinic', wellness: 'wellness practice', gym: 'gym', supplement: 'supplement brand' })[lead.segment] || 'business';\n    const user =\n      'Recipient business: ' + lead.name + ' — a ' + segWord + ' in ' + (lead.city || 'their city') + '.\\n' +\n      'What they do (from their own site): ' + (lead.context || lead.website || 'unknown') + '.\\n\\n' +\n      'Write ONE cold B2B email from ' + brand + ', voice = the founder writing person-to-person. OWNER-APPROVED STYLE (2026-07-12, replaced the rejected mail-merge template) — follow exactly:\\n' +\n      '1. NEVER open by reciting their homepage back at them. Open with why THEY specifically make sense as a buyer — an angle, not a summary of their site.\\n' +\n      '2. Lead the substance with THEIR economics, one real number: BPC-157 is $49 public sticker, about $24.50 a vial at 50 units; they set their own retail, so their margin roughly doubles.\\n' +\n      '3. Name at most 2-3 products RELEVANT to their practice type (aesthetics/skin -> GHK-Cu; hormone/TRT/primary care -> BPC-157, Tesamorelin, MOTS-C; IV bars -> GHK-Cu, MOTS-C). NEVER dump the whole catalog with milligrams.\\n' +\n      '4. Dallas warehouse, two-day restock, COA per lot — said once, plainly. For Dallas-area businesses say we are local.\\n' +\n      '5. Mention leoresearch.com once, naturally, as the public sticker/catalog.\\n' +\n      '6. One soft ask: a one-line reply gets the wholesale sheet, or a short call. Under 120 words. Vary structure — no two emails alike.\\n' +\n      '7. Do not claim the recipient uses peptides. No clinical, efficacy, treatment, recovery, or outcome language of any kind. Commerce facts only.\\n' +\n      '8. NO compliance boilerplate in the body — the send system appends the opt-out footer automatically.\\n' +\n      '9. NEVER sign with a personal human name. Sign only as \"LeoResearch\" or \"the LeoResearch team\". No first name, no full name, no \"the owner\", no owner name anywhere in the body.\\n' +\n      'Subject: \"Came across <their name> — <short concrete angle>\".\\n' +\n      'Return ONLY strict JSON: {\"subject\": \"...\", \"body\": \"...\"} with \\\\n for line breaks in body.';\n    const g = await callGateway(env, dossier, user, 700);\n    if (g.err) return JSON.stringify({ error: 'model call failed: ' + g.err });\n    let parsed = pipeJson(g.text);\n    if (!parsed) { try { parsed = JSON.parse(g.text); } catch {} }\n    if (!parsed || !parsed.subject || !parsed.body) {\n      const mm = String(g.text).match(/\\{[\\s\\S]*\\}/);\n      if (mm) { try { parsed = JSON.parse(mm[0]); } catch {} }\n    }\n    if (!parsed || !parsed.subject || !parsed.body) return JSON.stringify({ error: 'model did not return subject/body', raw: String(g.text).slice(0, 300) });\n    // Belt-and-suspenders on top of prompt rule 9: the owner's personal name must never appear\n    // in outbound copy. Swap any personal-name sign-off for the brand.\n    parsed.body = String(parsed.body)\n      .replace(/\\b(Best|Thanks|Thank you|Regards|Warm(?:ly| regards)?|Sincerely|Cheers)[,\\s]*\\n+\\s*the owner(?:\\s+[OWNER_SURNAME])?\\b/gi, '$1,\\nThe LeoResearch team')\n      .replace(/\\bOWNER_FIRST_NAME\\s+[OWNER_SURNAME]\\b/g, 'The LeoResearch team')\n      .replace(/\\bOWNER_FIRST_NAME\\b/g, 'LeoResearch');\n    const draft = { subject: String(parsed.subject), body: String(parsed.body), venture: brand, model: g.fallback ? 'gemini-2.5-flash(fallback)' : 'grok/grok-4.3', by: 'leadsDraftAI' };\n    const unsafe = /commonly used|use peptides|used in|support recovery|for recovery|clinical efficacy|treat(?:ment|ing)?|patient outcomes?|healing|therapeutic/i.test(draft.body);\n    if (unsafe) return JSON.stringify({ blocked: true, error: 'unsafe_copy_claim', note: 'Nothing saved. The model introduced a clinical/use claim; retry the draft.' });\n    // The reply-no stop line lives in the send-time footer (leadsSend appends it) — requiring it\n    // in the body doubled it and read as boilerplate. Body contract = store mention only.\n    if (!/leoresearch\\.com/i.test(draft.body)) return JSON.stringify({ blocked: true, error: 'copy_contract_missing', note: 'Nothing saved. Draft must mention the store (leoresearch.com).' });\n    await env.DB.prepare(\"UPDATE leads SET draft=?, status='drafted' WHERE id=?\").bind(JSON.stringify(draft), id).run();\n    return JSON.stringify({ id, to: lead.email || '(no email yet)', subject: draft.subject, body: draft.body, model: draft.model });\n  },\n  // List leads by status. $1=status (default all).\n  async leadsList(env, status) {\n    const st = String(status || '').trim();\n    const rows = st\n      ? (await env.DB.prepare('SELECT id,name,segment,city,website,email,phone,status,score FROM leads WHERE status=? ORDER BY score DESC, id DESC LIMIT 100').bind(st).all()).results\n      : (await env.DB.prepare('SELECT id,name,segment,city,website,email,phone,status,score FROM leads ORDER BY score DESC, id DESC LIMIT 100').all()).results;\n    const counts = (await env.DB.prepare('SELECT status, COUNT(*) n FROM leads GROUP BY status').all()).results || [];\n    return JSON.stringify({ filter: st || 'all', shown: (rows || []).length, by_status: counts, leads: rows || [] });\n  },\n  // MX verification over enriched emails via DNS-over-HTTPS. A domain with no MX record\n  // cannot receive mail — those leads are tagged mx:none and (if not yet drafted) parked\n  // as no_mx so drafting/sending never wastes a slot on a dead mailbox. $1 = max leads (default 25).\n  async leadsVerifyMx(env, limitArg) {\n    const lim = Math.min(50, Math.max(1, parseInt(limitArg || '25', 10) || 25));\n    const rows = (await env.DB.prepare(\n      \"SELECT id, email, status, COALESCE(notes,'') notes FROM leads WHERE email IS NOT NULL AND email != '' AND COALESCE(notes,'') NOT LIKE '%mx:%' ORDER BY id LIMIT ?\"\n    ).bind(lim).all()).results || [];\n    if (!rows.length) return JSON.stringify({ checked: 0, note: 'no unverified emails left' });\n    const domainCache = {};\n    let ok = 0, none = 0, parked = 0;\n    for (const l of rows) {\n      const domain = String(l.email).split('@')[1] || '';\n      if (!domain) continue;\n      if (!(domain in domainCache)) {\n        try {\n          const r = await fetch('https://cloudflare-dns.com/dns-query?name=' + encodeURIComponent(domain) + '&type=MX', { headers: { accept: 'application/dns-json' } });\n          const j = await r.json();\n          domainCache[domain] = Array.isArray(j.Answer) && j.Answer.some(a => a.type === 15);\n        } catch { domainCache[domain] = null; }\n      }\n      const hasMx = domainCache[domain];\n      if (hasMx === null) continue;\n      const tag = hasMx ? ' mx:ok' : ' mx:none';\n      if (hasMx) ok++; else none++;\n      if (!hasMx && (l.status === 'enriched' || l.status === 'new')) {\n        await env.DB.prepare('UPDATE leads SET notes=?, status=? WHERE id=?').bind((l.notes + tag).trim(), 'no_mx', l.id).run();\n        parked++;\n      } else {\n        await env.DB.prepare('UPDATE leads SET notes=? WHERE id=?').bind((l.notes + tag).trim(), l.id).run();\n      }\n    }\n    return JSON.stringify({ checked: rows.length, mx_ok: ok, mx_none: none, parked_no_mx: parked, domains_queried: Object.keys(domainCache).length });\n  },\n  // AI ICP scoring: one Grok call scores a batch of enriched leads on wholesale/white-label fit\n  // grounded in the OUTREACH_DOSSIER. Writes score (0-100) + an icp: note with buyer type and\n  // reason, so LEADS_LIST ordering becomes commercial fit instead of has-a-website. $1 = batch size (default 8).\n  async leadsScoreAI(env, countArg) {\n    const n = Math.min(10, Math.max(1, parseInt(countArg || '8', 10) || 8));\n    const rows = (await env.DB.prepare(\n      \"SELECT id, name, segment, city, substr(COALESCE(context,''),1,400) context, COALESCE(notes,'') notes FROM leads WHERE status IN ('enriched','drafted') AND email IS NOT NULL AND email!='' AND COALESCE(notes,'') LIKE '%mx:ok%' AND COALESCE(notes,'') NOT LIKE '%icp:%' ORDER BY score DESC,id LIMIT ?\"\n    ).bind(n).all()).results || [];\n    if (!rows.length) return JSON.stringify({ scored: 0, note: 'no unscored enriched leads left' });\n    const dr = await env.DB.prepare(\"SELECT content FROM directory WHERE key='OUTREACH_DOSSIER'\").first();\n    const dossier = dr ? String(dr.content || '') : '';\n    if (!dossier) return JSON.stringify({ error: 'OUTREACH_DOSSIER row is empty — set the brand/team/offer knowledge first' });\n    const user =\n      'Score each business below as a wholesale/white-label peptide buyer for the venture in the dossier.\\n' +\n      'Leads: ' + JSON.stringify(rows.map(r => ({ id: r.id, name: r.name, segment: r.segment, city: r.city, about: r.context }))) + '\\n\\n' +\n      'For each, judge: does this business plausibly BUY peptides at volume (clinic protocols, med-spa services, resale), ' +\n      'who the buyer is, and how much volume is realistic. Penalize pure consumer gyms, marketing-agency pages, and anything not a real operating business. ' +\n      'Return ONLY strict JSON: [{\"id\":<lead id>,\"icp_fit\":<0-100>,\"buyer\":\"clinic_buyer|medspa_owner|practitioner|reseller|gym_owner|other\",\"volume\":\"low|med|high\",\"reason\":\"<max 120 chars, concrete>\"}]';\n    const g = await callGateway(env, dossier, user, 1600);\n    if (g.err) return JSON.stringify({ error: 'model call failed: ' + g.err });\n    let parsed = pipeJson(g.text);\n    if (!parsed) { const mm = String(g.text).match(/\\[[\\s\\S]*\\]/); if (mm) { try { parsed = JSON.parse(mm[0]); } catch {} } }\n    if (!Array.isArray(parsed)) return JSON.stringify({ error: 'model did not return a JSON array', raw: String(g.text).slice(0, 300) });\n    const byId = Object.fromEntries(rows.map(r => [r.id, r]));\n    let scored = 0;\n    const results = [];\n    for (const s of parsed) {\n      const lead = byId[Number(s.id)];\n      if (!lead || typeof s.icp_fit !== 'number') continue;\n      const fit = Math.min(100, Math.max(0, Math.round(s.icp_fit)));\n      const tag = ' icp:' + fit + ' buyer:' + String(s.buyer || 'other').slice(0, 20) + ' vol:' + String(s.volume || '?').slice(0, 4) + ' why:' + String(s.reason || '').replace(/\\s+/g, ' ').slice(0, 120);\n      await env.DB.prepare('UPDATE leads SET score=?, notes=? WHERE id=?').bind(fit, (lead.notes + tag).trim(), lead.id).run();\n      scored++;\n      results.push({ id: lead.id, name: lead.name, icp_fit: fit, buyer: s.buyer, volume: s.volume, reason: s.reason });\n    }\n    return JSON.stringify({ scored, model: 'grok/grok-4.3', results });\n  },\n  // Send ONE lead's drafted email via the miscsubjects email channel.\n  // HARD-GATED: $1 must equal CONFIRM, $2=lead_id, $3=from_local.\n  async leadsSend(env, confirm, leadId, fromLocal) {\n    if (String(confirm || '').trim().toUpperCase() !== 'CONFIRM') {\n      return JSON.stringify({\n        blocked: true,\n        error: 'explicit_confirmation_required',\n        note: 'Nothing sent. LEADS_SEND requires CONFIRM|lead_id|from_local.',\n      });\n    }\n    const id = Number(leadId);\n    if (!id) return JSON.stringify({ error: 'lead_id required' });\n    const lead = await env.DB.prepare(\"SELECT id,name,email,status,draft,score,COALESCE(notes,'') notes FROM leads WHERE id=?\").bind(id).first();\n    if (!lead) return JSON.stringify({ error: 'lead not found: ' + id });\n    if (!lead.email) return JSON.stringify({ error: 'no email for lead ' + id });\n    if (lead.status !== 'drafted') return JSON.stringify({ error: 'lead ' + id + ' is ' + lead.status + ', not drafted — draft it first' });\n    if (!String(lead.notes).includes('mx:ok')) return JSON.stringify({ blocked: true, error: 'mx_verification_required', note: 'Nothing sent.' });\n    if (!String(lead.notes).includes('icp:') || Number(lead.score || 0) < 65) return JSON.stringify({ blocked: true, error: 'icp_threshold_not_met', score: Number(lead.score || 0), minimum: 65, note: 'Nothing sent.' });\n    const suppressed = await env.DB.prepare('SELECT reason FROM lead_suppressions WHERE lower(email)=lower(?)').bind(lead.email).first();\n    if (suppressed) return JSON.stringify({ blocked: true, error: 'recipient_suppressed', reason: suppressed.reason || 'suppressed', note: 'Nothing sent.' });\n    const settings = await env.DB.prepare(\"SELECT key,value FROM settings WHERE key IN ('outreach_postal_address','outreach_sending_domain_ready')\").all();\n    const cfg = Object.fromEntries((settings.results || []).map(r => [r.key, r.value]));\n    if (!String(cfg.outreach_postal_address || '').trim()) return JSON.stringify({ blocked: true, error: 'postal_address_required', note: 'Nothing sent. Set settings.outreach_postal_address to the valid business postal address required for commercial email.' });\n    if (String(cfg.outreach_sending_domain_ready || '') !== '1') return JSON.stringify({ blocked: true, error: 'sending_domain_not_ready', note: 'Nothing sent. Set settings.outreach_sending_domain_ready=1 only after From-domain SPF/DKIM/DMARC alignment is proven.' });\n    let d = {}; try { d = JSON.parse(lead.draft || '{}'); } catch {}\n    if (!d.subject || !d.body) return JSON.stringify({ error: 'lead ' + id + ' has no usable draft' });\n    const prior = await env.DB.prepare(\n      \"SELECT id,name FROM leads WHERE id<>? AND status='sent' AND LOWER(email)=LOWER(?) ORDER BY id LIMIT 1\"\n    ).bind(id, lead.email).first();\n    if (prior) {\n      return JSON.stringify({\n        blocked: true,\n        error: 'recipient_already_sent',\n        note: 'Nothing sent. This email address was already used by lead ' + prior.id + ' (' + prior.name + ').',\n        prior_lead_id: prior.id,\n      });\n    }\n    const local = String(fromLocal || 'wholesale').replace(/[^a-z0-9._-]/gi, '');\n    let res, err;\n    try {\n      const r = await fetch('https://miscsubjects.com/api/email/send', {\n        method: 'POST', headers: { 'Content-Type': 'application/json', 'x-terminal-key': env.TERMINAL_KEY },\n        body: JSON.stringify({ to: lead.email, subject: d.subject, text: d.body.replace(/\\s+$/,'') + '\\n\\nAdvertisement from LeoResearch\\n' + cfg.outreach_postal_address + '\\nIf this is not relevant, reply no and I will not follow up.', from: local + '@miscsubjects.com', from_name: d.venture || 'LeoResearch', reply_to: 'build@miscsubjects.com' }),\n      });\n      res = await r.text(); if (!r.ok) err = 'HTTP ' + r.status;\n    } catch (e) { err = 'fetch:' + (e && e.message || e); }\n    if (err) return JSON.stringify({ id, error: 'send failed: ' + err, detail: String(res).slice(0, 200) });\n    await env.DB.prepare(\"UPDATE leads SET status='sent' WHERE id=?\").bind(id).run();\n    return JSON.stringify({ id, to: lead.email, subject: d.subject, status: 'sent', response: String(res).slice(0, 200) });\n  },\n  // Send a batch of drafted leads. HARD-GATED: $1 must equal the word CONFIRM, $2 = max count (cap 25).\n  // Refuses without CONFIRM so it can never accidentally blast.\n  async leadsSendBatch(env, confirm, maxArg) {\n    if (String(confirm || '').trim().toUpperCase() !== 'CONFIRM') {\n      const ready = await env.DB.prepare(\"SELECT COUNT(*) n FROM leads WHERE status='drafted' AND email IS NOT NULL\").first();\n      return JSON.stringify({ blocked: true, note: 'Refusing to send without CONFIRM. Call LEADS_SEND_BATCH CONFIRM|N to send N.', drafted_ready: ready ? ready.n : 0 });\n    }\n    const n = Math.min(25, Math.max(1, parseInt(maxArg || '10', 10) || 10));\n    const rows = (await env.DB.prepare(\"SELECT id FROM leads WHERE status='drafted' AND email IS NOT NULL AND score>=65 AND COALESCE(notes,'') LIKE '%mx:ok%' ORDER BY score DESC,id DESC LIMIT ?\").bind(n).all()).results || [];\n    const sent = [];\n    for (const r of rows) { try { sent.push(JSON.parse(await FN_MAP.leadsSend(env, 'CONFIRM', r.id))); } catch (e) { sent.push({ id: r.id, error: String(e && e.message || e) }); } }\n    return JSON.stringify({ sent_count: sent.filter(s => s.status === 'sent').length, attempted: sent.length, results: sent });\n  },\n  // SCRAPER 2: Grok live web search discovers businesses OSM does not have.\n  // $1=segment, $2=city, $3=count(default 20, cap 30). Inserts source='grok-live-search'.\n  async leadsDiscoverAI(env, segment, city, countArg) {\n    const seg = String(segment || 'medspa').toLowerCase().trim();\n    const town = String(city || '').trim();\n    if (!town) return JSON.stringify({ error: 'city required, e.g. leadsDiscoverAI medspa|Scottsdale' });\n    const n = Math.min(30, Math.max(5, parseInt(countArg || '20', 10) || 20));\n    const segWord = ({ medspa: 'med spas / aesthetic clinics', clinic: 'medical clinics', wellness: 'wellness centers', gym: 'gyms / fitness studios', supplement: 'supplement stores', chiro: 'chiropractic / physical therapy practices', massage: 'massage / recovery studios', weightloss: 'weight-loss / nutrition clinics', longevity: 'longevity / IV-therapy / functional-medicine clinics' })[seg] || seg + ' businesses';\n    const g = await xaiSearch(env,\n      'You are a lead researcher. You output ONLY a strict JSON array, no prose, no markdown fence.',\n      'Find ' + n + ' real, currently-operating ' + segWord + ' in ' + town + '. Independent/local businesses preferred over national chains. For each return its exact business name and official website. Output ONLY: [{\"name\":\"...\",\"website\":\"https://...\"}]. Skip any business whose official website you cannot verify.',\n      3000);\n    if (g.err) return JSON.stringify({ error: 'live search failed: ' + g.err });\n    let arr = pipeJson(g.text);\n    if (!Array.isArray(arr)) { const mm = String(g.text).match(/\\[[\\s\\S]*\\]/); if (mm) { try { arr = JSON.parse(mm[0]); } catch {} } }\n    if (!Array.isArray(arr)) return JSON.stringify({ error: 'model did not return a JSON array', raw: String(g.text).slice(0, 200) });\n    let inserted = 0, skipped = 0;\n    for (const b of arr) {\n      const name = String(b && b.name || '').trim();\n      let site = String(b && b.website || '').trim();\n      if (!name || !/^https?:\\/\\/|^www\\./i.test(site)) { skipped++; continue; }\n      if (!/^https?:/i.test(site)) site = 'https://' + site;\n      try {\n        const res = await env.DB.prepare(\n          \"INSERT INTO leads (created_at,name,segment,city,website,source,status,score) VALUES (?,?,?,?,?,?,?,?) ON CONFLICT(name,city) DO NOTHING\"\n        ).bind(buildNowIso(), name, seg, town, site, 'grok-live-search', 'new', 2).run();\n","version":3,"of":12},{"commit":"35a8e0b3f","date":"2026-07-22T22:16:11-07:00","what_changed":"gateway: Gemini fallback when the primary model returns empty — xAI/Grok billing hit its limit and every drafting/scoring/writing lane was silently returning nothing #vault-approved","chars":20000,"added":["const draft = { subject: String(parsed.subject), body: String(parsed.body), venture: brand, model: g.fallback ? 'gemini-2.5-flash(fallback)' : 'grok/grok-4.3', by: 'leadsDraftAI' };","const rows = (await env.DB.prepare(\"SELECT id,name"],"removed":["const draft = { subject: String(parsed.subject), body: String(parsed.body), venture: brand, model: 'grok/grok-4.3', by: 'leadsDraftAI' };","const rows = (await env.DB.prepare(\"SELECT id,name,city,segment FROM leads WHERE status='new'"],"text":"async leadsDraftAI(env, leadId, venture) {\n    const id = Number(leadId);\n    if (!id) return JSON.stringify({ error: 'lead_id required' });\n    const lead = await env.DB.prepare('SELECT id,name,segment,city,email,website,context,status,score,COALESCE(notes,\\'\\') notes FROM leads WHERE id=?').bind(id).first();\n    if (!lead) return JSON.stringify({ error: 'lead not found: ' + id });\n    if (!lead.email) return JSON.stringify({ blocked: true, error: 'verified_email_required', note: 'Nothing drafted. Enrich the lead first.' });\n    if (!String(lead.notes).includes('mx:ok')) return JSON.stringify({ blocked: true, error: 'mx_verification_required', note: 'Nothing drafted. Verify MX first.' });\n    if (!String(lead.notes).includes('icp:') || Number(lead.score || 0) < 65) return JSON.stringify({ blocked: true, error: 'icp_threshold_not_met', score: Number(lead.score || 0), minimum: 65, note: 'Nothing drafted. Only verified high-fit leads enter copy review.' });\n    if (String(lead.context || '').trim().length < 40) return JSON.stringify({ blocked: true, error: 'grounding_context_required', note: 'Nothing drafted. The business site context is too thin for truthful personalization.' });\n    const suppressed = await env.DB.prepare('SELECT reason FROM lead_suppressions WHERE lower(email)=lower(?)').bind(lead.email).first();\n    if (suppressed) return JSON.stringify({ blocked: true, error: 'recipient_suppressed', reason: suppressed.reason || 'suppressed' });\n    const brand = String(venture || 'LeoResearch');\n    const dr = await env.DB.prepare(\"SELECT content FROM directory WHERE key='OUTREACH_DOSSIER'\").first();\n    const dossier = dr ? String(dr.content || '') : '';\n    if (!dossier) return JSON.stringify({ error: 'OUTREACH_DOSSIER row is empty — set the brand/team/offer knowledge first' });\n    const segWord = ({ medspa: 'med spa', clinic: 'clinic', wellness: 'wellness practice', gym: 'gym', supplement: 'supplement brand' })[lead.segment] || 'business';\n    const user =\n      'Recipient business: ' + lead.name + ' — a ' + segWord + ' in ' + (lead.city || 'their city') + '.\\n' +\n      'What they do (from their own site): ' + (lead.context || lead.website || 'unknown') + '.\\n\\n' +\n      'Write ONE cold B2B email from ' + brand + ', voice = the founder writing person-to-person. OWNER-APPROVED STYLE (2026-07-12, replaced the rejected mail-merge template) — follow exactly:\\n' +\n      '1. NEVER open by reciting their homepage back at them. Open with why THEY specifically make sense as a buyer — an angle, not a summary of their site.\\n' +\n      '2. Lead the substance with THEIR economics, one real number: BPC-157 is $49 public sticker, about $24.50 a vial at 50 units; they set their own retail, so their margin roughly doubles.\\n' +\n      '3. Name at most 2-3 products RELEVANT to their practice type (aesthetics/skin -> GHK-Cu; hormone/TRT/primary care -> BPC-157, Tesamorelin, MOTS-C; IV bars -> GHK-Cu, MOTS-C). NEVER dump the whole catalog with milligrams.\\n' +\n      '4. Dallas warehouse, two-day restock, COA per lot — said once, plainly. For Dallas-area businesses say we are local.\\n' +\n      '5. Mention leoresearch.com once, naturally, as the public sticker/catalog.\\n' +\n      '6. One soft ask: a one-line reply gets the wholesale sheet, or a short call. Under 120 words. Vary structure — no two emails alike.\\n' +\n      '7. Do not claim the recipient uses peptides. No clinical, efficacy, treatment, recovery, or outcome language of any kind. Commerce facts only.\\n' +\n      '8. NO compliance boilerplate in the body — the send system appends the opt-out footer automatically.\\n' +\n      'Subject: \"Came across <their name> — <short concrete angle>\".\\n' +\n      'Return ONLY strict JSON: {\"subject\": \"...\", \"body\": \"...\"} with \\\\n for line breaks in body.';\n    const g = await callGateway(env, dossier, user, 700);\n    if (g.err) return JSON.stringify({ error: 'model call failed: ' + g.err });\n    let parsed = pipeJson(g.text);\n    if (!parsed) { try { parsed = JSON.parse(g.text); } catch {} }\n    if (!parsed || !parsed.subject || !parsed.body) {\n      const mm = String(g.text).match(/\\{[\\s\\S]*\\}/);\n      if (mm) { try { parsed = JSON.parse(mm[0]); } catch {} }\n    }\n    if (!parsed || !parsed.subject || !parsed.body) return JSON.stringify({ error: 'model did not return subject/body', raw: String(g.text).slice(0, 300) });\n    const draft = { subject: String(parsed.subject), body: String(parsed.body), venture: brand, model: g.fallback ? 'gemini-2.5-flash(fallback)' : 'grok/grok-4.3', by: 'leadsDraftAI' };\n    const unsafe = /commonly used|use peptides|used in|support recovery|for recovery|clinical efficacy|treat(?:ment|ing)?|patient outcomes?|healing|therapeutic/i.test(draft.body);\n    if (unsafe) return JSON.stringify({ blocked: true, error: 'unsafe_copy_claim', note: 'Nothing saved. The model introduced a clinical/use claim; retry the draft.' });\n    // The reply-no stop line lives in the send-time footer (leadsSend appends it) — requiring it\n    // in the body doubled it and read as boilerplate. Body contract = store mention only.\n    if (!/leoresearch\\.com/i.test(draft.body)) return JSON.stringify({ blocked: true, error: 'copy_contract_missing', note: 'Nothing saved. Draft must mention the store (leoresearch.com).' });\n    await env.DB.prepare(\"UPDATE leads SET draft=?, status='drafted' WHERE id=?\").bind(JSON.stringify(draft), id).run();\n    return JSON.stringify({ id, to: lead.email || '(no email yet)', subject: draft.subject, body: draft.body, model: draft.model });\n  },\n  // List leads by status. $1=status (default all).\n  async leadsList(env, status) {\n    const st = String(status || '').trim();\n    const rows = st\n      ? (await env.DB.prepare('SELECT id,name,segment,city,website,email,phone,status,score FROM leads WHERE status=? ORDER BY score DESC, id DESC LIMIT 100').bind(st).all()).results\n      : (await env.DB.prepare('SELECT id,name,segment,city,website,email,phone,status,score FROM leads ORDER BY score DESC, id DESC LIMIT 100').all()).results;\n    const counts = (await env.DB.prepare('SELECT status, COUNT(*) n FROM leads GROUP BY status').all()).results || [];\n    return JSON.stringify({ filter: st || 'all', shown: (rows || []).length, by_status: counts, leads: rows || [] });\n  },\n  // MX verification over enriched emails via DNS-over-HTTPS. A domain with no MX record\n  // cannot receive mail — those leads are tagged mx:none and (if not yet drafted) parked\n  // as no_mx so drafting/sending never wastes a slot on a dead mailbox. $1 = max leads (default 25).\n  async leadsVerifyMx(env, limitArg) {\n    const lim = Math.min(50, Math.max(1, parseInt(limitArg || '25', 10) || 25));\n    const rows = (await env.DB.prepare(\n      \"SELECT id, email, status, COALESCE(notes,'') notes FROM leads WHERE email IS NOT NULL AND email != '' AND COALESCE(notes,'') NOT LIKE '%mx:%' ORDER BY id LIMIT ?\"\n    ).bind(lim).all()).results || [];\n    if (!rows.length) return JSON.stringify({ checked: 0, note: 'no unverified emails left' });\n    const domainCache = {};\n    let ok = 0, none = 0, parked = 0;\n    for (const l of rows) {\n      const domain = String(l.email).split('@')[1] || '';\n      if (!domain) continue;\n      if (!(domain in domainCache)) {\n        try {\n          const r = await fetch('https://cloudflare-dns.com/dns-query?name=' + encodeURIComponent(domain) + '&type=MX', { headers: { accept: 'application/dns-json' } });\n          const j = await r.json();\n          domainCache[domain] = Array.isArray(j.Answer) && j.Answer.some(a => a.type === 15);\n        } catch { domainCache[domain] = null; }\n      }\n      const hasMx = domainCache[domain];\n      if (hasMx === null) continue;\n      const tag = hasMx ? ' mx:ok' : ' mx:none';\n      if (hasMx) ok++; else none++;\n      if (!hasMx && (l.status === 'enriched' || l.status === 'new')) {\n        await env.DB.prepare('UPDATE leads SET notes=?, status=? WHERE id=?').bind((l.notes + tag).trim(), 'no_mx', l.id).run();\n        parked++;\n      } else {\n        await env.DB.prepare('UPDATE leads SET notes=? WHERE id=?').bind((l.notes + tag).trim(), l.id).run();\n      }\n    }\n    return JSON.stringify({ checked: rows.length, mx_ok: ok, mx_none: none, parked_no_mx: parked, domains_queried: Object.keys(domainCache).length });\n  },\n  // AI ICP scoring: one Grok call scores a batch of enriched leads on wholesale/white-label fit\n  // grounded in the OUTREACH_DOSSIER. Writes score (0-100) + an icp: note with buyer type and\n  // reason, so LEADS_LIST ordering becomes commercial fit instead of has-a-website. $1 = batch size (default 8).\n  async leadsScoreAI(env, countArg) {\n    const n = Math.min(10, Math.max(1, parseInt(countArg || '8', 10) || 8));\n    const rows = (await env.DB.prepare(\n      \"SELECT id, name, segment, city, substr(COALESCE(context,''),1,400) context, COALESCE(notes,'') notes FROM leads WHERE status IN ('enriched','drafted') AND email IS NOT NULL AND email!='' AND COALESCE(notes,'') LIKE '%mx:ok%' AND COALESCE(notes,'') NOT LIKE '%icp:%' ORDER BY score DESC,id LIMIT ?\"\n    ).bind(n).all()).results || [];\n    if (!rows.length) return JSON.stringify({ scored: 0, note: 'no unscored enriched leads left' });\n    const dr = await env.DB.prepare(\"SELECT content FROM directory WHERE key='OUTREACH_DOSSIER'\").first();\n    const dossier = dr ? String(dr.content || '') : '';\n    if (!dossier) return JSON.stringify({ error: 'OUTREACH_DOSSIER row is empty — set the brand/team/offer knowledge first' });\n    const user =\n      'Score each business below as a wholesale/white-label peptide buyer for the venture in the dossier.\\n' +\n      'Leads: ' + JSON.stringify(rows.map(r => ({ id: r.id, name: r.name, segment: r.segment, city: r.city, about: r.context }))) + '\\n\\n' +\n      'For each, judge: does this business plausibly BUY peptides at volume (clinic protocols, med-spa services, resale), ' +\n      'who the buyer is, and how much volume is realistic. Penalize pure consumer gyms, marketing-agency pages, and anything not a real operating business. ' +\n      'Return ONLY strict JSON: [{\"id\":<lead id>,\"icp_fit\":<0-100>,\"buyer\":\"clinic_buyer|medspa_owner|practitioner|reseller|gym_owner|other\",\"volume\":\"low|med|high\",\"reason\":\"<max 120 chars, concrete>\"}]';\n    const g = await callGateway(env, dossier, user, 1600);\n    if (g.err) return JSON.stringify({ error: 'model call failed: ' + g.err });\n    let parsed = pipeJson(g.text);\n    if (!parsed) { const mm = String(g.text).match(/\\[[\\s\\S]*\\]/); if (mm) { try { parsed = JSON.parse(mm[0]); } catch {} } }\n    if (!Array.isArray(parsed)) return JSON.stringify({ error: 'model did not return a JSON array', raw: String(g.text).slice(0, 300) });\n    const byId = Object.fromEntries(rows.map(r => [r.id, r]));\n    let scored = 0;\n    const results = [];\n    for (const s of parsed) {\n      const lead = byId[Number(s.id)];\n      if (!lead || typeof s.icp_fit !== 'number') continue;\n      const fit = Math.min(100, Math.max(0, Math.round(s.icp_fit)));\n      const tag = ' icp:' + fit + ' buyer:' + String(s.buyer || 'other').slice(0, 20) + ' vol:' + String(s.volume || '?').slice(0, 4) + ' why:' + String(s.reason || '').replace(/\\s+/g, ' ').slice(0, 120);\n      await env.DB.prepare('UPDATE leads SET score=?, notes=? WHERE id=?').bind(fit, (lead.notes + tag).trim(), lead.id).run();\n      scored++;\n      results.push({ id: lead.id, name: lead.name, icp_fit: fit, buyer: s.buyer, volume: s.volume, reason: s.reason });\n    }\n    return JSON.stringify({ scored, model: 'grok/grok-4.3', results });\n  },\n  // Send ONE lead's drafted email via the miscsubjects email channel.\n  // HARD-GATED: $1 must equal CONFIRM, $2=lead_id, $3=from_local.\n  async leadsSend(env, confirm, leadId, fromLocal) {\n    if (String(confirm || '').trim().toUpperCase() !== 'CONFIRM') {\n      return JSON.stringify({\n        blocked: true,\n        error: 'explicit_confirmation_required',\n        note: 'Nothing sent. LEADS_SEND requires CONFIRM|lead_id|from_local.',\n      });\n    }\n    const id = Number(leadId);\n    if (!id) return JSON.stringify({ error: 'lead_id required' });\n    const lead = await env.DB.prepare(\"SELECT id,name,email,status,draft,score,COALESCE(notes,'') notes FROM leads WHERE id=?\").bind(id).first();\n    if (!lead) return JSON.stringify({ error: 'lead not found: ' + id });\n    if (!lead.email) return JSON.stringify({ error: 'no email for lead ' + id });\n    if (lead.status !== 'drafted') return JSON.stringify({ error: 'lead ' + id + ' is ' + lead.status + ', not drafted — draft it first' });\n    if (!String(lead.notes).includes('mx:ok')) return JSON.stringify({ blocked: true, error: 'mx_verification_required', note: 'Nothing sent.' });\n    if (!String(lead.notes).includes('icp:') || Number(lead.score || 0) < 65) return JSON.stringify({ blocked: true, error: 'icp_threshold_not_met', score: Number(lead.score || 0), minimum: 65, note: 'Nothing sent.' });\n    const suppressed = await env.DB.prepare('SELECT reason FROM lead_suppressions WHERE lower(email)=lower(?)').bind(lead.email).first();\n    if (suppressed) return JSON.stringify({ blocked: true, error: 'recipient_suppressed', reason: suppressed.reason || 'suppressed', note: 'Nothing sent.' });\n    const settings = await env.DB.prepare(\"SELECT key,value FROM settings WHERE key IN ('outreach_postal_address','outreach_sending_domain_ready')\").all();\n    const cfg = Object.fromEntries((settings.results || []).map(r => [r.key, r.value]));\n    if (!String(cfg.outreach_postal_address || '').trim()) return JSON.stringify({ blocked: true, error: 'postal_address_required', note: 'Nothing sent. Set settings.outreach_postal_address to the valid business postal address required for commercial email.' });\n    if (String(cfg.outreach_sending_domain_ready || '') !== '1') return JSON.stringify({ blocked: true, error: 'sending_domain_not_ready', note: 'Nothing sent. Set settings.outreach_sending_domain_ready=1 only after From-domain SPF/DKIM/DMARC alignment is proven.' });\n    let d = {}; try { d = JSON.parse(lead.draft || '{}'); } catch {}\n    if (!d.subject || !d.body) return JSON.stringify({ error: 'lead ' + id + ' has no usable draft' });\n    const prior = await env.DB.prepare(\n      \"SELECT id,name FROM leads WHERE id<>? AND status='sent' AND LOWER(email)=LOWER(?) ORDER BY id LIMIT 1\"\n    ).bind(id, lead.email).first();\n    if (prior) {\n      return JSON.stringify({\n        blocked: true,\n        error: 'recipient_already_sent',\n        note: 'Nothing sent. This email address was already used by lead ' + prior.id + ' (' + prior.name + ').',\n        prior_lead_id: prior.id,\n      });\n    }\n    const local = String(fromLocal || 'wholesale').replace(/[^a-z0-9._-]/gi, '');\n    let res, err;\n    try {\n      const r = await fetch('https://miscsubjects.com/api/email/send', {\n        method: 'POST', headers: { 'Content-Type': 'application/json', 'x-terminal-key': env.TERMINAL_KEY },\n        body: JSON.stringify({ to: lead.email, subject: d.subject, text: d.body.replace(/\\s+$/,'') + '\\n\\nAdvertisement from LeoResearch\\n' + cfg.outreach_postal_address + '\\nIf this is not relevant, reply no and I will not follow up.', from: local + '@miscsubjects.com', from_name: d.venture || 'LeoResearch', reply_to: 'build@miscsubjects.com' }),\n      });\n      res = await r.text(); if (!r.ok) err = 'HTTP ' + r.status;\n    } catch (e) { err = 'fetch:' + (e && e.message || e); }\n    if (err) return JSON.stringify({ id, error: 'send failed: ' + err, detail: String(res).slice(0, 200) });\n    await env.DB.prepare(\"UPDATE leads SET status='sent' WHERE id=?\").bind(id).run();\n    return JSON.stringify({ id, to: lead.email, subject: d.subject, status: 'sent', response: String(res).slice(0, 200) });\n  },\n  // Send a batch of drafted leads. HARD-GATED: $1 must equal the word CONFIRM, $2 = max count (cap 25).\n  // Refuses without CONFIRM so it can never accidentally blast.\n  async leadsSendBatch(env, confirm, maxArg) {\n    if (String(confirm || '').trim().toUpperCase() !== 'CONFIRM') {\n      const ready = await env.DB.prepare(\"SELECT COUNT(*) n FROM leads WHERE status='drafted' AND email IS NOT NULL\").first();\n      return JSON.stringify({ blocked: true, note: 'Refusing to send without CONFIRM. Call LEADS_SEND_BATCH CONFIRM|N to send N.', drafted_ready: ready ? ready.n : 0 });\n    }\n    const n = Math.min(25, Math.max(1, parseInt(maxArg || '10', 10) || 10));\n    const rows = (await env.DB.prepare(\"SELECT id FROM leads WHERE status='drafted' AND email IS NOT NULL AND score>=65 AND COALESCE(notes,'') LIKE '%mx:ok%' ORDER BY score DESC,id DESC LIMIT ?\").bind(n).all()).results || [];\n    const sent = [];\n    for (const r of rows) { try { sent.push(JSON.parse(await FN_MAP.leadsSend(env, 'CONFIRM', r.id))); } catch (e) { sent.push({ id: r.id, error: String(e && e.message || e) }); } }\n    return JSON.stringify({ sent_count: sent.filter(s => s.status === 'sent').length, attempted: sent.length, results: sent });\n  },\n  // SCRAPER 2: Grok live web search discovers businesses OSM does not have.\n  // $1=segment, $2=city, $3=count(default 20, cap 30). Inserts source='grok-live-search'.\n  async leadsDiscoverAI(env, segment, city, countArg) {\n    const seg = String(segment || 'medspa').toLowerCase().trim();\n    const town = String(city || '').trim();\n    if (!town) return JSON.stringify({ error: 'city required, e.g. leadsDiscoverAI medspa|Scottsdale' });\n    const n = Math.min(30, Math.max(5, parseInt(countArg || '20', 10) || 20));\n    const segWord = ({ medspa: 'med spas / aesthetic clinics', clinic: 'medical clinics', wellness: 'wellness centers', gym: 'gyms / fitness studios', supplement: 'supplement stores', chiro: 'chiropractic / physical therapy practices', massage: 'massage / recovery studios', weightloss: 'weight-loss / nutrition clinics', longevity: 'longevity / IV-therapy / functional-medicine clinics' })[seg] || seg + ' businesses';\n    const g = await xaiSearch(env,\n      'You are a lead researcher. You output ONLY a strict JSON array, no prose, no markdown fence.',\n      'Find ' + n + ' real, currently-operating ' + segWord + ' in ' + town + '. Independent/local businesses preferred over national chains. For each return its exact business name and official website. Output ONLY: [{\"name\":\"...\",\"website\":\"https://...\"}]. Skip any business whose official website you cannot verify.',\n      3000);\n    if (g.err) return JSON.stringify({ error: 'live search failed: ' + g.err });\n    let arr = pipeJson(g.text);\n    if (!Array.isArray(arr)) { const mm = String(g.text).match(/\\[[\\s\\S]*\\]/); if (mm) { try { arr = JSON.parse(mm[0]); } catch {} } }\n    if (!Array.isArray(arr)) return JSON.stringify({ error: 'model did not return a JSON array', raw: String(g.text).slice(0, 200) });\n    let inserted = 0, skipped = 0;\n    for (const b of arr) {\n      const name = String(b && b.name || '').trim();\n      let site = String(b && b.website || '').trim();\n      if (!name || !/^https?:\\/\\/|^www\\./i.test(site)) { skipped++; continue; }\n      if (!/^https?:/i.test(site)) site = 'https://' + site;\n      try {\n        const res = await env.DB.prepare(\n          \"INSERT INTO leads (created_at,name,segment,city,website,source,status,score) VALUES (?,?,?,?,?,?,?,?) ON CONFLICT(name,city) DO NOTHING\"\n        ).bind(buildNowIso(), name, seg, town, site, 'grok-live-search', 'new', 2).run();\n        if (res.meta && res.meta.changes) inserted++; else skipped++;\n      } catch (e) { skipped++; }\n    }\n    return JSON.stringify({ segment: seg, city: town, model_returned: arr.length, inserted_new: inserted, skipped_dup_or_bad: skipped, source: 'grok-live-search' });\n  },\n  // Rescue websiteless leads: ONE Grok live-search call finds official websites for a batch of\n  // leads that have a name+city but no site (OSM often lacks the website tag). $1=count(default 8, cap 10).\n  async leadsFindSites(env, countArg) {\n    const n = Math.min(10, Math.max(1, parseInt(countArg || '8', 10) || 8));\n    const rows = (await env.DB.prepare(\"SELECT id,name","version":2,"of":12},{"commit":"2c0572816","date":"2026-07-12T23:05:20-07:00","what_changed":"OIP density: extract 224-fn FN_MAP plane from dispatch.js into _lib/fn_runners.js","chars":20000,"added":["async leadsDraftAI(env, leadId, venture) {","const id = Number(leadId);","if (!id) return JSON.stringify({ error: 'lead_id required' });","const lead = await env.DB.prepare('SELECT id,name,segment,city,email,website,context,status,score,COALESCE(notes,\\'\\') notes FROM leads WHERE id=?').bind(id).first();","if (!lead) return JSON.stringify({ error: 'lead not found: ' + id });","if (!lead.email) return JSON.stringify({ blocked: true, error: 'verified_email_required', note: 'Nothing drafted. Enrich the lead first.' });","if (!String(lead.notes).includes('mx:ok')) return JSON.stringify({ blocked: true, error: 'mx_verification_required', note: 'Nothing drafted. Verify MX first.' });","if (!String(lead.notes).includes('icp:') || Number(lead.score || 0) < 65) return JSON.stringify({ blocked: true, error: 'icp_threshold_not_met', score: Number(lead.score || 0), minimum: 65, note: 'Nothing drafted. Only verified high-fit leads enter copy review.' });","if (String(lead.context || '').trim().length < 40) return JSON.stringify({ blocked: true, error: 'grounding_context_required', note: 'Nothing drafted. The business site context is too thin for truthful personalization.' });","const suppressed = await env.DB.prepare('SELECT reason FROM lead_suppressions WHERE lower(email)=lower(?)').bind(lead.email).first();","if (suppressed) return JSON.stringify({ blocked: true, error: 'recipient_suppressed', reason: suppressed.reason || 'suppressed' });","const brand = String(venture || 'LeoResearch');","const dr = await env.DB.prepare(\"SELECT content FROM directory WHERE key='OUTREACH_DOSSIER'\").first();","const dossier = dr ? String(dr.content || '') : '';","if (!dossier) return JSON.stringify({ error: 'OUTREACH_DOSSIER row is empty — set the brand/team/offer knowledge first' });","const segWord = ({ medspa: 'med spa', clinic: 'clinic', wellness: 'wellness practice', gym: 'gym', supplement: 'supplement brand' })[lead.segment] || 'business';","const user =","'Recipient business: ' + lead.name + ' — a ' + segWord + ' in ' + (lead.city || 'their city') + '.\\n' +","'What they do (from their own site): ' + (lead.context || lead.website || 'unknown') + '.\\n\\n' +","'Write ONE cold B2B email from ' + brand + ', voice = the founder writing person-to-person. OWNER-APPROVED STYLE (2026-07-12, replaced the rejected mail-merge template) — follow exactly:\\n' +","'1. NEVER open by reciting their homepage back at them. Open with why THEY specifically make sense as a buyer — an angle, not a summary of their site.\\n' +","'2. Lead the substance with THEIR economics, one real number: BPC-157 is $49 public sticker, about $24.50 a vial at 50 units; they set their own retail, so their margin roughly doubles.\\n' +","'3. Name at most 2-3 products RELEVANT to their practice type (aesthetics/skin -> GHK-Cu; hormone/TRT/primary care -> BPC-157, Tesamorelin, MOTS-C; IV bars -> GHK-Cu, MOTS-C). NEVER dump the whole catalog with milligrams.\\n' +","'4. Dallas warehouse, two-day restock, COA per lot — said once, plainly. For Dallas-area businesses say we are local.\\n' +","'5. Mention leoresearch.com once, naturally, as the public sticker/catalog.\\n' +","'6. One soft ask: a one-line reply gets the wholesale sheet, or a short call. Under 120 words. Vary structure — no two emails alike.\\n' +","'7. Do not claim the recipient uses peptides. No clinical, efficacy, treatment, recovery, or outcome language of any kind. Commerce facts only.\\n' +","'8. NO compliance boilerplate in the body — the send system appends the opt-out footer automatically.\\n' +","'Subject: \"Came across <their name> — <short concrete angle>\".\\n' +","'Return ONLY strict JSON: {\"subject\": \"...\", \"body\": \"...\"} with \\\\n for line breaks in body.';","const g = await callGateway(env, dossier, user, 700);","if (g.err) return JSON.stringify({ error: 'model call failed: ' + g.err });","let parsed = pipeJson(g.text);","if (!parsed) { try { parsed = JSON.parse(g.text); } catch {} }","if (!parsed || !parsed.subject || !parsed.body) {","const mm = String(g.text).match(/\\{[\\s\\S]*\\}/);","if (mm) { try { parsed = JSON.parse(mm[0]); } catch {} }","}","if (!parsed || !parsed.subject || !parsed.body) return JSON.stringify({ error: 'model did not return subject/body', raw: String(g.text).slice(0, 300) });","const draft = { subject: String(parsed.subject), body: String(parsed.body), venture: brand, model: 'grok/grok-4.3', by: 'leadsDraftAI' };","const unsafe = /commonly used|use peptides|used in|support recovery|for recovery|clinical efficacy|treat(?:ment|ing)?|patient outcomes?|healing|therapeutic/i.test(draft.body);","if (unsafe) return JSON.stringify({ blocked: true, error: 'unsafe_copy_claim', note: 'Nothing saved. The model introduced a clinical/use claim; retry the draft.' });","// The reply-no stop line lives in the send-time footer (leadsSend appends it) — requiring it","// in the body doubled it and read as boilerplate. Body contract = store mention only.","if (!/leoresearch\\.com/i.test(draft.body)) return JSON.stringify({ blocked: true, error: 'copy_contract_missing', note: 'Nothing saved. Draft must mention the store (leoresearch.com).' });","await env.DB.prepare(\"UPDATE leads SET draft=?, status='drafted' WHERE id=?\").bind(JSON.stringify(draft), id).run();","return JSON.stringify({ id, to: lead.email || '(no email yet)', subject: draft.subject, body: draft.body, model: draft.model });","},","// List leads by status. $1=status (default all).","async leadsList(env, status) {","const st = String(status || '').trim();","const rows = st","? (await env.DB.prepare('SELECT id,name,segment,city,website,email,phone,status,score FROM leads WHERE status=? ORDER BY score DESC, id DESC LIMIT 100').bind(st).all()).results",": (await env.DB.prepare('SELECT id,name,segment,city,website,email,phone,status,score FROM leads ORDER BY score DESC, id DESC LIMIT 100').all()).results;","const counts = (await env.DB.prepare('SELECT status, COUNT(*) n FROM leads GROUP BY status').all()).results || [];","return JSON.stringify({ filter: st || 'all', shown: (rows || []).length, by_status: counts, leads: rows || [] });","// MX verification over enriched emails via DNS-over-HTTPS. A domain with no MX record","// cannot receive mail — those leads are tagged mx:none and (if not yet drafted) parked","// as no_mx so drafting/sending never wastes a slot on a dead mailbox. $1 = max leads (default 25).","async leadsVerifyMx(env, limitArg) {","const lim = Math.min(50, Math.max(1, parseInt(limitArg || '25', 10) || 25));","const rows = (await env.DB.prepare(","\"SELECT id, email, status, COALESCE(notes,'') notes FROM leads WHERE email IS NOT NULL AND email != '' AND COALESCE(notes,'') NOT LIKE '%mx:%' ORDER BY id LIMIT ?\"",").bind(lim).all()).results || [];","if (!rows.length) return JSON.stringify({ checked: 0, note: 'no unverified emails left' });","const domainCache = {};","let ok = 0, none = 0, parked = 0;","for (const l of rows) {","const domain = String(l.email).split('@')[1] || '';","if (!domain) continue;","if (!(domain in domainCache)) {","try {","const r = await fetch('https://cloudflare-dns.com/dns-query?name=' + encodeURIComponent(domain) + '&type=MX', { headers: { accept: 'application/dns-json' } });","const j = await r.json();","domainCache[domain] = Array.isArray(j.Answer) && j.Answer.some(a => a.type === 15);","} catch { domainCache[domain] = null; }","const hasMx = domainCache[domain];","if (hasMx === null) continue;","const tag = hasMx ? ' mx:ok' : ' mx:none';","if (hasMx) ok++; else none++;","if (!hasMx && (l.status === 'enriched' || l.status === 'new')) {","await env.DB.prepare('UPDATE leads SET notes=?, status=? WHERE id=?').bind((l.notes + tag).trim(), 'no_mx', l.id).run();","parked++;","} else {","await env.DB.prepare('UPDATE leads SET notes=? WHERE id=?').bind((l.notes + tag).trim(), l.id).run();","return JSON.stringify({ checked: rows.length, mx_ok: ok, mx_none: none, parked_no_mx: parked, domains_queried: Object.keys(domainCache).length });","// AI ICP scoring: one Grok call scores a batch of enriched leads on wholesale/white-label fit","// grounded in the OUTREACH_DOSSIER. Writes score (0-100) + an icp: note with buyer type and","// reason, so LEADS_LIST ordering becomes commercial fit instead of has-a-website. $1 = batch size (default 8).","async leadsScoreAI(env, countArg) {","const n = Math.min(10, Math.max(1, parseInt(countArg || '8', 10) || 8));","\"SELECT id, name, segment, city, substr(COALESCE(context,''),1,400) context, COALESCE(notes,'') notes FROM leads WHERE status IN ('enriched','drafted') AND email IS NOT NULL AND email!='' AND COALESCE(notes,'') LIKE '%mx:ok%' AND COALESCE(notes,'') NOT LIKE '%icp:%' ORDER BY score DESC,id LIMIT ?\"",").bind(n).all()).results || [];","if (!rows.length) return JSON.stringify({ scored: 0, note: 'no unscored enriched leads left' });","'Score each business below as a wholesale/white-label peptide buyer for the venture in the dossier.\\n' +","'Leads: ' + JSON.stringify(rows.map(r => ({ id: r.id, name: r.name, segment: r.segment, city: r.city, about: r.context }))) + '\\n\\n' +","'For each, judge: does this business plausibly BUY peptides at volume (clinic protocols, med-spa services, resale), ' +","'who the buyer is, and how much volume is realistic. Penalize pure consumer gyms, marketing-agency pages, and anything not a real operating business. ' +","'Return ONLY strict JSON: [{\"id\":<lead id>,\"icp_fit\":<0-100>,\"buyer\":\"clinic_buyer|medspa_owner|practitioner|reseller|gym_owner|other\",\"volume\":\"low|med|high\",\"reason\":\"<max 120 chars, concrete>\"}]';","const g = await callGateway(env, dossier, user, 1600);","if (!parsed) { const mm = String(g.text).match(/\\[[\\s\\S]*\\]/); if (mm) { try { parsed = JSON.parse(mm[0]); } catch {} } }","if (!Array.isArray(parsed)) return JSON.stringify({ error: 'model did not return a JSON array', raw: String(g.text).slice(0, 300) });","const byId = Object.fromEntries(rows.map(r => [r.id, r]));","let scored = 0;","const results = [];","for (const s of parsed) {","const lead = byId[Number(s.id)];","if (!lead || typeof s.icp_fit !== 'number') continue;","const fit = Math.min(100, Math.max(0, Math.round(s.icp_fit)));","const tag = ' icp:' + fit + ' buyer:' + String(s.buyer || 'other').slice(0, 20) + ' vol:' + String(s.volume || '?').slice(0, 4) + ' why:' + String(s.reason || '').replace(/\\s+/g, ' ').slice(0, 120);","await env.DB.prepare('UPDATE leads SET score=?, notes=? WHERE id=?').bind(fit, (lead.notes + tag).trim(), lead.id).run();","scored++;","results.push({ id: lead.id, name: lead.name, icp_fit: fit, buyer: s.buyer, volume: s.volume, reason: s.reason });","return JSON.stringify({ scored, model: 'grok/grok-4.3', results });","// Send ONE lead's drafted email via the miscsubjects email channel.","// HARD-GATED: $1 must equal CONFIRM, $2=lead_id, $3=from_local.","async leadsSend(env, confirm, leadId, fromLocal) {","if (String(confirm || '').trim().toUpperCase() !== 'CONFIRM') {","return JSON.stringify({","blocked: true,","error: 'explicit_confirmation_required',","note: 'Nothing sent. LEADS_SEND requires CONFIRM|lead_id|from_local.',","});","const lead = await env.DB.prepare(\"SELECT id,name,email,status,draft,score,COALESCE(notes,'') notes FROM leads WHERE id=?\").bind(id).first();","if (!lead.email) return JSON.stringify({ error: 'no email for lead ' + id });","if (lead.status !== 'drafted') return JSON.stringify({ error: 'lead ' + id + ' is ' + lead.status + ', not drafted — draft it first' });","if (!String(lead.notes).includes('mx:ok')) return JSON.stringify({ blocked: true, error: 'mx_verification_required', note: 'Nothing sent.' });","if (!String(lead.notes).includes('icp:') || Number(lead.score || 0) < 65) return JSON.stringify({ blocked: true, error: 'icp_threshold_not_met', score: Number(lead.score || 0), minimum: 65, note: 'Nothing sent.' });","if (suppressed) return JSON.stringify({ blocked: true, error: 'recipient_suppressed', reason: suppressed.reason || 'suppressed', note: 'Nothing sent.' });","const settings = await env.DB.prepare(\"SELECT key,value FROM settings WHERE key IN ('outreach_postal_address','outreach_sending_domain_ready')\").all();","const cfg = Object.fromEntries((settings.results || []).map(r => [r.key, r.value]));","if (!String(cfg.outreach_postal_address || '').trim()) return JSON.stringify({ blocked: true, error: 'postal_address_required', note: 'Nothing sent. Set settings.outreach_postal_address to the valid business postal address required for commercial email.' });","if (String(cfg.outreach_sending_domain_ready || '') !== '1') return JSON.stringify({ blocked: true, error: 'sending_domain_not_ready', note: 'Nothing sent. Set settings.outreach_sending_domain_ready=1 only after From-domain SPF/DKIM/DMARC alignment is proven.' });","let d = {}; try { d = JSON.parse(lead.draft || '{}'); } catch {}","if (!d.subject || !d.body) return JSON.stringify({ error: 'lead ' + id + ' has no usable draft' });","const prior = await env.DB.prepare(","\"SELECT id,name FROM leads WHERE id<>? AND status='sent' AND LOWER(email)=LOWER(?) ORDER BY id LIMIT 1\"",").bind(id, lead.email).first();","if (prior) {","error: 'recipient_already_sent',","note: 'Nothing sent. This email address was already used by lead ' + prior.id + ' (' + prior.name + ').',","prior_lead_id: prior.id,","const local = String(fromLocal || 'wholesale').replace(/[^a-z0-9._-]/gi, '');","let res, err;","const r = await fetch('https://miscsubjects.com/api/email/send', {","method: 'POST', headers: { 'Content-Type': 'application/json', 'x-terminal-key': env.TERMINAL_KEY },","body: JSON.stringify({ to: lead.email, subject: d.subject, text: d.body.replace(/\\s+$/,'') + '\\n\\nAdvertisement from LeoResearch\\n' + cfg.outreach_postal_address + '\\nIf this is not relevant, reply no and I will not follow up.', from: local + '@miscsubjects.com', from_name: d.venture || 'LeoResearch', reply_to: 'build@miscsubjects.com' }),","res = await r.text(); if (!r.ok) err = 'HTTP ' + r.status;","} catch (e) { err = 'fetch:' + (e && e.message || e); }","if (err) return JSON.stringify({ id, error: 'send failed: ' + err, detail: String(res).slice(0, 200) });","await env.DB.prepare(\"UPDATE leads SET status='sent' WHERE id=?\").bind(id).run();","return JSON.stringify({ id, to: lead.email, subject: d.subject, status: 'sent', response: String(res).slice(0, 200) });","// Send a batch of drafted leads. HARD-GATED: $1 must equal the word CONFIRM, $2 = max count (cap 25).","// Refuses without CONFIRM so it can never accidentally blast.","async leadsSendBatch(env, confirm, maxArg) {","const ready = await env.DB.prepare(\"SELECT COUNT(*) n FROM leads WHERE status='drafted' AND email IS NOT NULL\").first();","return JSON.stringify({ blocked: true, note: 'Refusing to send without CONFIRM. Call LEADS_SEND_BATCH CONFIRM|N to send N.', drafted_ready: ready ? ready.n : 0 });","const n = Math.min(25, Math.max(1, parseInt(maxArg || '10', 10) || 10));","const rows = (await env.DB.prepare(\"SELECT id FROM leads WHERE status='drafted' AND email IS NOT NULL AND score>=65 AND COALESCE(notes,'') LIKE '%mx:ok%' ORDER BY score DESC,id DESC LIMIT ?\").bind(n).all()).results || [];","const sent = [];","for (const r of rows) { try { sent.push(JSON.parse(await FN_MAP.leadsSend(env, 'CONFIRM', r.id))); } catch (e) { sent.push({ id: r.id, error: String(e && e.message || e) }); } }","return JSON.stringify({ sent_count: sent.filter(s => s.status === 'sent').length, attempted: sent.length, results: sent });","// SCRAPER 2: Grok live web search discovers businesses OSM does not have.","// $1=segment, $2=city, $3=count(default 20, cap 30). Inserts source='grok-live-search'.","async leadsDiscoverAI(env, segment, city, countArg) {","const seg = String(segment || 'medspa').toLowerCase().trim();","const town = String(city || '').trim();","if (!town) return JSON.stringify({ error: 'city required, e.g. leadsDiscoverAI medspa|Scottsdale' });","const n = Math.min(30, Math.max(5, parseInt(countArg || '20', 10) || 20));","const segWord = ({ medspa: 'med spas / aesthetic clinics', clinic: 'medical clinics', wellness: 'wellness centers', gym: 'gyms / fitness studios', supplement: 'supplement stores', chiro: 'chiropractic / physical therapy practices', massage: 'massage / recovery studios', weightloss: 'weight-loss / nutrition clinics', longevity: 'longevity / IV-therapy / functional-medicine clinics' })[seg] || seg + ' businesses';","const g = await xaiSearch(env,","'You are a lead researcher. You output ONLY a strict JSON array, no prose, no markdown fence.',","'Find ' + n + ' real, currently-operating ' + segWord + ' in ' + town + '. Independent/local businesses preferred over national chains. For each return its exact business name and official website. Output ONLY: [{\"name\":\"...\",\"website\":\"https://...\"}]. Skip any business whose official website you cannot verify.',","3000);","if (g.err) return JSON.stringify({ error: 'live search failed: ' + g.err });","let arr = pipeJson(g.text);","if (!Array.isArray(arr)) { const mm = String(g.text).match(/\\[[\\s\\S]*\\]/); if (mm) { try { arr = JSON.parse(mm[0]); } catch {} } }","if (!Array.isArray(arr)) return JSON.stringify({ error: 'model did not return a JSON array', raw: String(g.text).slice(0, 200) });","let inserted = 0, skipped = 0;","for (const b of arr) {","const name = String(b && b.name || '').trim();","let site = String(b && b.website || '').trim();","if (!name || !/^https?:\\/\\/|^www\\./i.test(site)) { skipped++; continue; }","if (!/^https?:/i.test(site)) site = 'https://' + site;","const res = await env.DB.prepare(","\"INSERT INTO leads (created_at,name,segment,city,website,source,status,score) VALUES (?,?,?,?,?,?,?,?) ON CONFLICT(name,city) DO NOTHING\"",").bind(buildNowIso(), name, seg, town, site, 'grok-live-search', 'new', 2).run();","if (res.meta && res.meta.changes) inserted++; else skipped++;","} catch (e) { skipped++; }","return JSON.stringify({ segment: seg, city: town, model_returned: arr.length, inserted_new: inserted, skipped_dup_or_bad: skipped, source: 'grok-live-search' });","// Rescue websiteless leads: ONE Grok live-search call finds official websites for a batch of","// leads that have a name+city but no site (OSM often lacks the website tag). $1=count(default 8, cap 10).","async leadsFindSites(env, countArg) {","const rows = (await env.DB.prepare(\"SELECT id,name,city,segment FROM leads WHERE status='new'"],"removed":[],"text":"async leadsDraftAI(env, leadId, venture) {\n    const id = Number(leadId);\n    if (!id) return JSON.stringify({ error: 'lead_id required' });\n    const lead = await env.DB.prepare('SELECT id,name,segment,city,email,website,context,status,score,COALESCE(notes,\\'\\') notes FROM leads WHERE id=?').bind(id).first();\n    if (!lead) return JSON.stringify({ error: 'lead not found: ' + id });\n    if (!lead.email) return JSON.stringify({ blocked: true, error: 'verified_email_required', note: 'Nothing drafted. Enrich the lead first.' });\n    if (!String(lead.notes).includes('mx:ok')) return JSON.stringify({ blocked: true, error: 'mx_verification_required', note: 'Nothing drafted. Verify MX first.' });\n    if (!String(lead.notes).includes('icp:') || Number(lead.score || 0) < 65) return JSON.stringify({ blocked: true, error: 'icp_threshold_not_met', score: Number(lead.score || 0), minimum: 65, note: 'Nothing drafted. Only verified high-fit leads enter copy review.' });\n    if (String(lead.context || '').trim().length < 40) return JSON.stringify({ blocked: true, error: 'grounding_context_required', note: 'Nothing drafted. The business site context is too thin for truthful personalization.' });\n    const suppressed = await env.DB.prepare('SELECT reason FROM lead_suppressions WHERE lower(email)=lower(?)').bind(lead.email).first();\n    if (suppressed) return JSON.stringify({ blocked: true, error: 'recipient_suppressed', reason: suppressed.reason || 'suppressed' });\n    const brand = String(venture || 'LeoResearch');\n    const dr = await env.DB.prepare(\"SELECT content FROM directory WHERE key='OUTREACH_DOSSIER'\").first();\n    const dossier = dr ? String(dr.content || '') : '';\n    if (!dossier) return JSON.stringify({ error: 'OUTREACH_DOSSIER row is empty — set the brand/team/offer knowledge first' });\n    const segWord = ({ medspa: 'med spa', clinic: 'clinic', wellness: 'wellness practice', gym: 'gym', supplement: 'supplement brand' })[lead.segment] || 'business';\n    const user =\n      'Recipient business: ' + lead.name + ' — a ' + segWord + ' in ' + (lead.city || 'their city') + '.\\n' +\n      'What they do (from their own site): ' + (lead.context || lead.website || 'unknown') + '.\\n\\n' +\n      'Write ONE cold B2B email from ' + brand + ', voice = the founder writing person-to-person. OWNER-APPROVED STYLE (2026-07-12, replaced the rejected mail-merge template) — follow exactly:\\n' +\n      '1. NEVER open by reciting their homepage back at them. Open with why THEY specifically make sense as a buyer — an angle, not a summary of their site.\\n' +\n      '2. Lead the substance with THEIR economics, one real number: BPC-157 is $49 public sticker, about $24.50 a vial at 50 units; they set their own retail, so their margin roughly doubles.\\n' +\n      '3. Name at most 2-3 products RELEVANT to their practice type (aesthetics/skin -> GHK-Cu; hormone/TRT/primary care -> BPC-157, Tesamorelin, MOTS-C; IV bars -> GHK-Cu, MOTS-C). NEVER dump the whole catalog with milligrams.\\n' +\n      '4. Dallas warehouse, two-day restock, COA per lot — said once, plainly. For Dallas-area businesses say we are local.\\n' +\n      '5. Mention leoresearch.com once, naturally, as the public sticker/catalog.\\n' +\n      '6. One soft ask: a one-line reply gets the wholesale sheet, or a short call. Under 120 words. Vary structure — no two emails alike.\\n' +\n      '7. Do not claim the recipient uses peptides. No clinical, efficacy, treatment, recovery, or outcome language of any kind. Commerce facts only.\\n' +\n      '8. NO compliance boilerplate in the body — the send system appends the opt-out footer automatically.\\n' +\n      'Subject: \"Came across <their name> — <short concrete angle>\".\\n' +\n      'Return ONLY strict JSON: {\"subject\": \"...\", \"body\": \"...\"} with \\\\n for line breaks in body.';\n    const g = await callGateway(env, dossier, user, 700);\n    if (g.err) return JSON.stringify({ error: 'model call failed: ' + g.err });\n    let parsed = pipeJson(g.text);\n    if (!parsed) { try { parsed = JSON.parse(g.text); } catch {} }\n    if (!parsed || !parsed.subject || !parsed.body) {\n      const mm = String(g.text).match(/\\{[\\s\\S]*\\}/);\n      if (mm) { try { parsed = JSON.parse(mm[0]); } catch {} }\n    }\n    if (!parsed || !parsed.subject || !parsed.body) return JSON.stringify({ error: 'model did not return subject/body', raw: String(g.text).slice(0, 300) });\n    const draft = { subject: String(parsed.subject), body: String(parsed.body), venture: brand, model: 'grok/grok-4.3', by: 'leadsDraftAI' };\n    const unsafe = /commonly used|use peptides|used in|support recovery|for recovery|clinical efficacy|treat(?:ment|ing)?|patient outcomes?|healing|therapeutic/i.test(draft.body);\n    if (unsafe) return JSON.stringify({ blocked: true, error: 'unsafe_copy_claim', note: 'Nothing saved. The model introduced a clinical/use claim; retry the draft.' });\n    // The reply-no stop line lives in the send-time footer (leadsSend appends it) — requiring it\n    // in the body doubled it and read as boilerplate. Body contract = store mention only.\n    if (!/leoresearch\\.com/i.test(draft.body)) return JSON.stringify({ blocked: true, error: 'copy_contract_missing', note: 'Nothing saved. Draft must mention the store (leoresearch.com).' });\n    await env.DB.prepare(\"UPDATE leads SET draft=?, status='drafted' WHERE id=?\").bind(JSON.stringify(draft), id).run();\n    return JSON.stringify({ id, to: lead.email || '(no email yet)', subject: draft.subject, body: draft.body, model: draft.model });\n  },\n  // List leads by status. $1=status (default all).\n  async leadsList(env, status) {\n    const st = String(status || '').trim();\n    const rows = st\n      ? (await env.DB.prepare('SELECT id,name,segment,city,website,email,phone,status,score FROM leads WHERE status=? ORDER BY score DESC, id DESC LIMIT 100').bind(st).all()).results\n      : (await env.DB.prepare('SELECT id,name,segment,city,website,email,phone,status,score FROM leads ORDER BY score DESC, id DESC LIMIT 100').all()).results;\n    const counts = (await env.DB.prepare('SELECT status, COUNT(*) n FROM leads GROUP BY status').all()).results || [];\n    return JSON.stringify({ filter: st || 'all', shown: (rows || []).length, by_status: counts, leads: rows || [] });\n  },\n  // MX verification over enriched emails via DNS-over-HTTPS. A domain with no MX record\n  // cannot receive mail — those leads are tagged mx:none and (if not yet drafted) parked\n  // as no_mx so drafting/sending never wastes a slot on a dead mailbox. $1 = max leads (default 25).\n  async leadsVerifyMx(env, limitArg) {\n    const lim = Math.min(50, Math.max(1, parseInt(limitArg || '25', 10) || 25));\n    const rows = (await env.DB.prepare(\n      \"SELECT id, email, status, COALESCE(notes,'') notes FROM leads WHERE email IS NOT NULL AND email != '' AND COALESCE(notes,'') NOT LIKE '%mx:%' ORDER BY id LIMIT ?\"\n    ).bind(lim).all()).results || [];\n    if (!rows.length) return JSON.stringify({ checked: 0, note: 'no unverified emails left' });\n    const domainCache = {};\n    let ok = 0, none = 0, parked = 0;\n    for (const l of rows) {\n      const domain = String(l.email).split('@')[1] || '';\n      if (!domain) continue;\n      if (!(domain in domainCache)) {\n        try {\n          const r = await fetch('https://cloudflare-dns.com/dns-query?name=' + encodeURIComponent(domain) + '&type=MX', { headers: { accept: 'application/dns-json' } });\n          const j = await r.json();\n          domainCache[domain] = Array.isArray(j.Answer) && j.Answer.some(a => a.type === 15);\n        } catch { domainCache[domain] = null; }\n      }\n      const hasMx = domainCache[domain];\n      if (hasMx === null) continue;\n      const tag = hasMx ? ' mx:ok' : ' mx:none';\n      if (hasMx) ok++; else none++;\n      if (!hasMx && (l.status === 'enriched' || l.status === 'new')) {\n        await env.DB.prepare('UPDATE leads SET notes=?, status=? WHERE id=?').bind((l.notes + tag).trim(), 'no_mx', l.id).run();\n        parked++;\n      } else {\n        await env.DB.prepare('UPDATE leads SET notes=? WHERE id=?').bind((l.notes + tag).trim(), l.id).run();\n      }\n    }\n    return JSON.stringify({ checked: rows.length, mx_ok: ok, mx_none: none, parked_no_mx: parked, domains_queried: Object.keys(domainCache).length });\n  },\n  // AI ICP scoring: one Grok call scores a batch of enriched leads on wholesale/white-label fit\n  // grounded in the OUTREACH_DOSSIER. Writes score (0-100) + an icp: note with buyer type and\n  // reason, so LEADS_LIST ordering becomes commercial fit instead of has-a-website. $1 = batch size (default 8).\n  async leadsScoreAI(env, countArg) {\n    const n = Math.min(10, Math.max(1, parseInt(countArg || '8', 10) || 8));\n    const rows = (await env.DB.prepare(\n      \"SELECT id, name, segment, city, substr(COALESCE(context,''),1,400) context, COALESCE(notes,'') notes FROM leads WHERE status IN ('enriched','drafted') AND email IS NOT NULL AND email!='' AND COALESCE(notes,'') LIKE '%mx:ok%' AND COALESCE(notes,'') NOT LIKE '%icp:%' ORDER BY score DESC,id LIMIT ?\"\n    ).bind(n).all()).results || [];\n    if (!rows.length) return JSON.stringify({ scored: 0, note: 'no unscored enriched leads left' });\n    const dr = await env.DB.prepare(\"SELECT content FROM directory WHERE key='OUTREACH_DOSSIER'\").first();\n    const dossier = dr ? String(dr.content || '') : '';\n    if (!dossier) return JSON.stringify({ error: 'OUTREACH_DOSSIER row is empty — set the brand/team/offer knowledge first' });\n    const user =\n      'Score each business below as a wholesale/white-label peptide buyer for the venture in the dossier.\\n' +\n      'Leads: ' + JSON.stringify(rows.map(r => ({ id: r.id, name: r.name, segment: r.segment, city: r.city, about: r.context }))) + '\\n\\n' +\n      'For each, judge: does this business plausibly BUY peptides at volume (clinic protocols, med-spa services, resale), ' +\n      'who the buyer is, and how much volume is realistic. Penalize pure consumer gyms, marketing-agency pages, and anything not a real operating business. ' +\n      'Return ONLY strict JSON: [{\"id\":<lead id>,\"icp_fit\":<0-100>,\"buyer\":\"clinic_buyer|medspa_owner|practitioner|reseller|gym_owner|other\",\"volume\":\"low|med|high\",\"reason\":\"<max 120 chars, concrete>\"}]';\n    const g = await callGateway(env, dossier, user, 1600);\n    if (g.err) return JSON.stringify({ error: 'model call failed: ' + g.err });\n    let parsed = pipeJson(g.text);\n    if (!parsed) { const mm = String(g.text).match(/\\[[\\s\\S]*\\]/); if (mm) { try { parsed = JSON.parse(mm[0]); } catch {} } }\n    if (!Array.isArray(parsed)) return JSON.stringify({ error: 'model did not return a JSON array', raw: String(g.text).slice(0, 300) });\n    const byId = Object.fromEntries(rows.map(r => [r.id, r]));\n    let scored = 0;\n    const results = [];\n    for (const s of parsed) {\n      const lead = byId[Number(s.id)];\n      if (!lead || typeof s.icp_fit !== 'number') continue;\n      const fit = Math.min(100, Math.max(0, Math.round(s.icp_fit)));\n      const tag = ' icp:' + fit + ' buyer:' + String(s.buyer || 'other').slice(0, 20) + ' vol:' + String(s.volume || '?').slice(0, 4) + ' why:' + String(s.reason || '').replace(/\\s+/g, ' ').slice(0, 120);\n      await env.DB.prepare('UPDATE leads SET score=?, notes=? WHERE id=?').bind(fit, (lead.notes + tag).trim(), lead.id).run();\n      scored++;\n      results.push({ id: lead.id, name: lead.name, icp_fit: fit, buyer: s.buyer, volume: s.volume, reason: s.reason });\n    }\n    return JSON.stringify({ scored, model: 'grok/grok-4.3', results });\n  },\n  // Send ONE lead's drafted email via the miscsubjects email channel.\n  // HARD-GATED: $1 must equal CONFIRM, $2=lead_id, $3=from_local.\n  async leadsSend(env, confirm, leadId, fromLocal) {\n    if (String(confirm || '').trim().toUpperCase() !== 'CONFIRM') {\n      return JSON.stringify({\n        blocked: true,\n        error: 'explicit_confirmation_required',\n        note: 'Nothing sent. LEADS_SEND requires CONFIRM|lead_id|from_local.',\n      });\n    }\n    const id = Number(leadId);\n    if (!id) return JSON.stringify({ error: 'lead_id required' });\n    const lead = await env.DB.prepare(\"SELECT id,name,email,status,draft,score,COALESCE(notes,'') notes FROM leads WHERE id=?\").bind(id).first();\n    if (!lead) return JSON.stringify({ error: 'lead not found: ' + id });\n    if (!lead.email) return JSON.stringify({ error: 'no email for lead ' + id });\n    if (lead.status !== 'drafted') return JSON.stringify({ error: 'lead ' + id + ' is ' + lead.status + ', not drafted — draft it first' });\n    if (!String(lead.notes).includes('mx:ok')) return JSON.stringify({ blocked: true, error: 'mx_verification_required', note: 'Nothing sent.' });\n    if (!String(lead.notes).includes('icp:') || Number(lead.score || 0) < 65) return JSON.stringify({ blocked: true, error: 'icp_threshold_not_met', score: Number(lead.score || 0), minimum: 65, note: 'Nothing sent.' });\n    const suppressed = await env.DB.prepare('SELECT reason FROM lead_suppressions WHERE lower(email)=lower(?)').bind(lead.email).first();\n    if (suppressed) return JSON.stringify({ blocked: true, error: 'recipient_suppressed', reason: suppressed.reason || 'suppressed', note: 'Nothing sent.' });\n    const settings = await env.DB.prepare(\"SELECT key,value FROM settings WHERE key IN ('outreach_postal_address','outreach_sending_domain_ready')\").all();\n    const cfg = Object.fromEntries((settings.results || []).map(r => [r.key, r.value]));\n    if (!String(cfg.outreach_postal_address || '').trim()) return JSON.stringify({ blocked: true, error: 'postal_address_required', note: 'Nothing sent. Set settings.outreach_postal_address to the valid business postal address required for commercial email.' });\n    if (String(cfg.outreach_sending_domain_ready || '') !== '1') return JSON.stringify({ blocked: true, error: 'sending_domain_not_ready', note: 'Nothing sent. Set settings.outreach_sending_domain_ready=1 only after From-domain SPF/DKIM/DMARC alignment is proven.' });\n    let d = {}; try { d = JSON.parse(lead.draft || '{}'); } catch {}\n    if (!d.subject || !d.body) return JSON.stringify({ error: 'lead ' + id + ' has no usable draft' });\n    const prior = await env.DB.prepare(\n      \"SELECT id,name FROM leads WHERE id<>? AND status='sent' AND LOWER(email)=LOWER(?) ORDER BY id LIMIT 1\"\n    ).bind(id, lead.email).first();\n    if (prior) {\n      return JSON.stringify({\n        blocked: true,\n        error: 'recipient_already_sent',\n        note: 'Nothing sent. This email address was already used by lead ' + prior.id + ' (' + prior.name + ').',\n        prior_lead_id: prior.id,\n      });\n    }\n    const local = String(fromLocal || 'wholesale').replace(/[^a-z0-9._-]/gi, '');\n    let res, err;\n    try {\n      const r = await fetch('https://miscsubjects.com/api/email/send', {\n        method: 'POST', headers: { 'Content-Type': 'application/json', 'x-terminal-key': env.TERMINAL_KEY },\n        body: JSON.stringify({ to: lead.email, subject: d.subject, text: d.body.replace(/\\s+$/,'') + '\\n\\nAdvertisement from LeoResearch\\n' + cfg.outreach_postal_address + '\\nIf this is not relevant, reply no and I will not follow up.', from: local + '@miscsubjects.com', from_name: d.venture || 'LeoResearch', reply_to: 'build@miscsubjects.com' }),\n      });\n      res = await r.text(); if (!r.ok) err = 'HTTP ' + r.status;\n    } catch (e) { err = 'fetch:' + (e && e.message || e); }\n    if (err) return JSON.stringify({ id, error: 'send failed: ' + err, detail: String(res).slice(0, 200) });\n    await env.DB.prepare(\"UPDATE leads SET status='sent' WHERE id=?\").bind(id).run();\n    return JSON.stringify({ id, to: lead.email, subject: d.subject, status: 'sent', response: String(res).slice(0, 200) });\n  },\n  // Send a batch of drafted leads. HARD-GATED: $1 must equal the word CONFIRM, $2 = max count (cap 25).\n  // Refuses without CONFIRM so it can never accidentally blast.\n  async leadsSendBatch(env, confirm, maxArg) {\n    if (String(confirm || '').trim().toUpperCase() !== 'CONFIRM') {\n      const ready = await env.DB.prepare(\"SELECT COUNT(*) n FROM leads WHERE status='drafted' AND email IS NOT NULL\").first();\n      return JSON.stringify({ blocked: true, note: 'Refusing to send without CONFIRM. Call LEADS_SEND_BATCH CONFIRM|N to send N.', drafted_ready: ready ? ready.n : 0 });\n    }\n    const n = Math.min(25, Math.max(1, parseInt(maxArg || '10', 10) || 10));\n    const rows = (await env.DB.prepare(\"SELECT id FROM leads WHERE status='drafted' AND email IS NOT NULL AND score>=65 AND COALESCE(notes,'') LIKE '%mx:ok%' ORDER BY score DESC,id DESC LIMIT ?\").bind(n).all()).results || [];\n    const sent = [];\n    for (const r of rows) { try { sent.push(JSON.parse(await FN_MAP.leadsSend(env, 'CONFIRM', r.id))); } catch (e) { sent.push({ id: r.id, error: String(e && e.message || e) }); } }\n    return JSON.stringify({ sent_count: sent.filter(s => s.status === 'sent').length, attempted: sent.length, results: sent });\n  },\n  // SCRAPER 2: Grok live web search discovers businesses OSM does not have.\n  // $1=segment, $2=city, $3=count(default 20, cap 30). Inserts source='grok-live-search'.\n  async leadsDiscoverAI(env, segment, city, countArg) {\n    const seg = String(segment || 'medspa').toLowerCase().trim();\n    const town = String(city || '').trim();\n    if (!town) return JSON.stringify({ error: 'city required, e.g. leadsDiscoverAI medspa|Scottsdale' });\n    const n = Math.min(30, Math.max(5, parseInt(countArg || '20', 10) || 20));\n    const segWord = ({ medspa: 'med spas / aesthetic clinics', clinic: 'medical clinics', wellness: 'wellness centers', gym: 'gyms / fitness studios', supplement: 'supplement stores', chiro: 'chiropractic / physical therapy practices', massage: 'massage / recovery studios', weightloss: 'weight-loss / nutrition clinics', longevity: 'longevity / IV-therapy / functional-medicine clinics' })[seg] || seg + ' businesses';\n    const g = await xaiSearch(env,\n      'You are a lead researcher. You output ONLY a strict JSON array, no prose, no markdown fence.',\n      'Find ' + n + ' real, currently-operating ' + segWord + ' in ' + town + '. Independent/local businesses preferred over national chains. For each return its exact business name and official website. Output ONLY: [{\"name\":\"...\",\"website\":\"https://...\"}]. Skip any business whose official website you cannot verify.',\n      3000);\n    if (g.err) return JSON.stringify({ error: 'live search failed: ' + g.err });\n    let arr = pipeJson(g.text);\n    if (!Array.isArray(arr)) { const mm = String(g.text).match(/\\[[\\s\\S]*\\]/); if (mm) { try { arr = JSON.parse(mm[0]); } catch {} } }\n    if (!Array.isArray(arr)) return JSON.stringify({ error: 'model did not return a JSON array', raw: String(g.text).slice(0, 200) });\n    let inserted = 0, skipped = 0;\n    for (const b of arr) {\n      const name = String(b && b.name || '').trim();\n      let site = String(b && b.website || '').trim();\n      if (!name || !/^https?:\\/\\/|^www\\./i.test(site)) { skipped++; continue; }\n      if (!/^https?:/i.test(site)) site = 'https://' + site;\n      try {\n        const res = await env.DB.prepare(\n          \"INSERT INTO leads (created_at,name,segment,city,website,source,status,score) VALUES (?,?,?,?,?,?,?,?) ON CONFLICT(name,city) DO NOTHING\"\n        ).bind(buildNowIso(), name, seg, town, site, 'grok-live-search', 'new', 2).run();\n        if (res.meta && res.meta.changes) inserted++; else skipped++;\n      } catch (e) { skipped++; }\n    }\n    return JSON.stringify({ segment: seg, city: town, model_returned: arr.length, inserted_new: inserted, skipped_dup_or_bad: skipped, source: 'grok-live-search' });\n  },\n  // Rescue websiteless leads: ONE Grok live-search call finds official websites for a batch of\n  // leads that have a name+city but no site (OSM often lacks the website tag). $1=count(default 8, cap 10).\n  async leadsFindSites(env, countArg) {\n    const n = Math.min(10, Math.max(1, parseInt(countArg || '8', 10) || 8));\n    const rows = (await env.DB.prepare(\"SELECT id,name,city,segment FROM leads WHERE status='new' ","version":1,"of":12}]},{"id":"outreach_law","label":"Outreach law — the first-contact law the drafting rules answer to","path":".claude/skills/outreach-law/SKILL.md","versions":[{"commit":"1951bb1e6","date":"2026-07-25T11:42:53-07:00","what_changed":"outreach-law register canonical: eagerness as preparation never pursuit, reciprocity before request, no gimmickry, operative pairs, framing test, status-collapse violation class — supersedes all prior register clauses; dossier carries the same canonical block #vault-approved","chars":7958,"added":["## Register (canonical)","Never signal neediness, promotional eagerness, or low commercial leverage. The sender is an established operator presenting a materially advantageous option to a peer: calm facts, no performance, no gimmickry, no manufactured familiarity. The voice implies selectivity and commercial stability, never dependence on the recipient's response — but never indifference. The operator is genuinely eager to engage; eagerness must appear as preparation, usefulness, and generosity, never as pursuit. Create reciprocity before requesting attention: the message itself should already have benefited the recipient, so that responding is the natural continuation of value, not a favor to the sender. The signal is: *I value the possibility of speaking with you enough to arrive with something useful.*","Operative pairs: cordial not familiar · confident not enthusiastic · precise not promotional · generous not ingratiating · low-friction not cheap · direct not blunt · eager in preparation not eager in pursuit · understated not vague.","Framing test: \"Initial orders can be as small as three units, sufficient to evaluate supply, documentation, and fulfillment\" — professional evaluation. \"Want to try three units for $75?\" — consumer promotion. The defect is never the quantity; it is casting the recipient as a bargain shopper rather than a business evaluating a supplier.","**Status collapse (violation class).** Never reduce the perceived status of the sender, recipient, or offer to make the message more accessible. Do not translate commercial seriousness into mass-market friendliness: \"easy\" means effortless, never casual. High-status communication does not announce value, perform friendliness, or beg for engagement — it demonstrates understanding, states the commercial advantage plainly, leaves room for the recipient's judgment, and never lowers itself to secure attention. Banned on sight: reply keywords, \"let's chat,\" \"free,\" \"try,\" \"excited,\" exclamation CTAs, dollar amounts as enticement, discount framing.","The operator's actual voice is mercenary; first contact never shows it — precision and structure are what signal who they are dealing with. Every sentence carries their benefit or the proof of it; anything else is cut per the Writing Law."],"removed":["## Register","Extremely high decorum. The operator's actual voice is mercenary; first contact never shows it — precision and structure are what signal who they are dealing with. Cordial, direct, adult-to-adult. Every sentence carries their benefit or the proof of it; anything else is cut per the Writing Law.","Never reduce the perceived status of the sender, recipient, or offer in order to make the message more accessible. Do not translate commercial seriousness into mass-market friendliness — low friction never means informality, cheapness, or eagerness. The distinctions: cordial, not familiar; confident, not enthusiastic; precise, not promotional; generous, not ingratiating; low-friction, not cheap; direct, not blunt; commercially serious, not salesy; understated, not vague. High-status communication does not announce value, perform friendliness, or beg for engagement — it demonstrates understanding, states the commercial advantage plainly, and leaves room for the recipient's judgment."],"text":"---\nname: outreach-law\ndescription: The operator's law for first-contact outreach — cold email, cold DM, first message to any counterparty, sales or partnership or recruiting. Load whenever drafting, reviewing, or planning any message to someone who did not ask to hear from the operator. This is decision content — it supplies what to say and how to structure the offer; operational-logic governs how the draft is produced. Apply especially when tempted to open with a greeting, introduce the operator, describe the product, or ask for a meeting.\n---\n\n# Outreach Law\n\n**Why it exists:** Models draft cold outreach in model-voice — \"Hi Vida Team! I am reaching out today...\" — announcing the outreach, centering the sender, claiming value instead of demonstrating it, and asking for obligation before delivering benefit. Every such draft was rejected. The operator's outreach is systems design applied to persuasion; this law encodes the system.\n\n## Core law\n\nFirst contact is an intervention against the recipient's status quo of ignoring you — and their inattention has standing. The message earns its existence only from their seat: maximum benefit to them, minimum risk to them, minimum obligation on them, proof over claims. Center their position, then introduce yourself only as the mechanism that improves it — their exposure (unreliable supply), your mechanism (domestic warehouse), their outcome (two-day restocking). Unmistakably high-value, in the least possible words.\n\n## The offer is the argument\n\nPersuasion is structural, not rhetorical. Engineer the deal so the recipient cannot lose — then the message merely describes the architecture. \"I have made it impossible for you to lose\" is a design requirement on the offer, never a sentence in the email. Copy may clarify and prove the offer; it may not compensate for a structurally weak offer — fix the offer, not the copy. The standard is aesthetic: the benefit should be self-evident from the structure alone.\n\n## Required elements of a compliant first message\n\n1. **Their model, demonstrated.** Open inside their business — a specific, correct observation about how they operate, what they carry, what constrains them. Comprehension is proven by specificity, never asserted (\"I love what you're doing\" is a violation).\n2. **Their upside, concrete.** The benefit in their units — margin, speed, reliability, revenue — not in the sender's adjectives.\n3. **Their risk, structurally reduced.** Show the smallest credible way to test the offer: a low-cost initial order, limited pilot, sample where appropriate, or another reversible step. State the actual exposure plainly when useful. Do not invent guarantees or default to free samples.\n4. **Their exposure, operationally stated.** Identify a concrete cost, delay, risk, or constraint their current model may create, but only when supported by evidence. Contrast it with the operator's verifiable mechanism and the resulting benefit. Never invent a competitor defect to create urgency.\n5. **Proof attached.** The working thing, the live page, the number — demonstration travels with the message. If they do nothing further, they still received value.\n6. **Near-zero obligation.** The next step is tiny, optional, reversible — a one-word reply, a look at a link. Never a meeting, a call, or homework on first contact.\n\n## Small-entry design\n\nReduce exposure by making the first transaction commercially trivial relative to the potential upside — a small paid order, limited test, pilot, or reversible first step. Samples are one possible mechanism, discretionary and used selectively when commercially credible, never the headline offer. The principle is low exposure, not free product.\n\n## Architecture is not copy\n\nThe offer's risk parameters — unit minimums, dollar exposure, sample discretion — inform the model's design of the offer and are never voiced to the recipient. State the asymmetry, never the arithmetic. The small entry is phrased as ease and the recipient's convenience (\"start with a small first order and run the COAs yourself\"), never as price, unit count, \"just,\" \"only,\" or \"try.\" Low exposure delivered in low register reads as low value; the mechanism must be small while the voice remains that of someone who does not need the order.\n\n## Register (canonical)\n\nNever signal neediness, promotional eagerness, or low commercial leverage. The sender is an established operator presenting a materially advantageous option to a peer: calm facts, no performance, no gimmickry, no manufactured familiarity. The voice implies selectivity and commercial stability, never dependence on the recipient's response — but never indifference. The operator is genuinely eager to engage; eagerness must appear as preparation, usefulness, and generosity, never as pursuit. Create reciprocity before requesting attention: the message itself should already have benefited the recipient, so that responding is the natural continuation of value, not a favor to the sender. The signal is: *I value the possibility of speaking with you enough to arrive with something useful.*\n\nOperative pairs: cordial not familiar · confident not enthusiastic · precise not promotional · generous not ingratiating · low-friction not cheap · direct not blunt · eager in preparation not eager in pursuit · understated not vague.\n\nFraming test: \"Initial orders can be as small as three units, sufficient to evaluate supply, documentation, and fulfillment\" — professional evaluation. \"Want to try three units for $75?\" — consumer promotion. The defect is never the quantity; it is casting the recipient as a bargain shopper rather than a business evaluating a supplier.\n\n**Status collapse (violation class).** Never reduce the perceived status of the sender, recipient, or offer to make the message more accessible. Do not translate commercial seriousness into mass-market friendliness: \"easy\" means effortless, never casual. High-status communication does not announce value, perform friendliness, or beg for engagement — it demonstrates understanding, states the commercial advantage plainly, leaves room for the recipient's judgment, and never lowers itself to secure attention. Banned on sight: reply keywords, \"let's chat,\" \"free,\" \"try,\" \"excited,\" exclamation CTAs, dollar amounts as enticement, discount framing.\n\nThe operator's actual voice is mercenary; first contact never shows it — precision and structure are what signal who they are dealing with. Every sentence carries their benefit or the proof of it; anything else is cut per the Writing Law.\n\n## Banned on sight (model-voice)\n\n- Greeting-plus-announcement openers: \"Hi [Team]!\", \"I'm reaching out today\", \"I hope this finds you well\", \"My name is X and I...\"\n- Centering the sender before the recipient has received value.\n- Enthusiasm as substance: exclamation points, \"excited,\" \"love,\" \"passionate.\"\n- Claimed qualities without mechanism: \"high quality,\" \"trusted partner,\" \"game-changing.\"\n- Asking for time before delivering benefit.\n- Any sentence the recipient could not act on or verify.\n- Parameter leak: internal offer architecture voiced as recipient-facing copy (\"three units, about $75\" priced the correspondence at $75 — 2026-07-25).\n- Register substitution: \"low obligation\" translated into mass-market sales language (\"Want to try three units first?\", \"reply peptide\", \"let's chat\", \"free sample\" — each collapses the sender's status to secure attention).\n\n## The test before sending\n\nRead it from their chair, cold, thirty seconds: What did they gain by reading it? What would they risk by replying? What are they obligated to do? If the answers are not *something concrete / almost nothing / almost nothing*, the draft fails. The principle is radical asymmetry, not literal zero — do not fabricate guarantees to reach zero. Then the four-class check from operational-logic: no substitution of the operator's terms, no manufacture of claims the offer's structure doesn't back.\n","version":5,"of":5},{"commit":"9712944d5","date":"2026-07-25T11:39:48-07:00","what_changed":"outreach-law + drafting rules: architecture-is-not-copy (risk parameters inform design, never voiced — state the asymmetry, never the arithmetic), status law (never reduce perceived status for accessibility; cordial-not-familiar register table), parameter-leak and register-substitution added as failure classes; CTA rules now high-register small-first-order, no dollar amounts or unit counts in copy #vault-approved","chars":6678,"added":["## Architecture is not copy","The offer's risk parameters — unit minimums, dollar exposure, sample discretion — inform the model's design of the offer and are never voiced to the recipient. State the asymmetry, never the arithmetic. The small entry is phrased as ease and the recipient's convenience (\"start with a small first order and run the COAs yourself\"), never as price, unit count, \"just,\" \"only,\" or \"try.\" Low exposure delivered in low register reads as low value; the mechanism must be small while the voice remains that of someone who does not need the order.","Never reduce the perceived status of the sender, recipient, or offer in order to make the message more accessible. Do not translate commercial seriousness into mass-market friendliness — low friction never means informality, cheapness, or eagerness. The distinctions: cordial, not familiar; confident, not enthusiastic; precise, not promotional; generous, not ingratiating; low-friction, not cheap; direct, not blunt; commercially serious, not salesy; understated, not vague. High-status communication does not announce value, perform friendliness, or beg for engagement — it demonstrates understanding, states the commercial advantage plainly, and leaves room for the recipient's judgment.","- Parameter leak: internal offer architecture voiced as recipient-facing copy (\"three units, about $75\" priced the correspondence at $75 — 2026-07-25).","- Register substitution: \"low obligation\" translated into mass-market sales language (\"Want to try three units first?\", \"reply peptide\", \"let's chat\", \"free sample\" — each collapses the sender's status to secure attention)."],"removed":[],"text":"---\nname: outreach-law\ndescription: The operator's law for first-contact outreach — cold email, cold DM, first message to any counterparty, sales or partnership or recruiting. Load whenever drafting, reviewing, or planning any message to someone who did not ask to hear from the operator. This is decision content — it supplies what to say and how to structure the offer; operational-logic governs how the draft is produced. Apply especially when tempted to open with a greeting, introduce the operator, describe the product, or ask for a meeting.\n---\n\n# Outreach Law\n\n**Why it exists:** Models draft cold outreach in model-voice — \"Hi Vida Team! I am reaching out today...\" — announcing the outreach, centering the sender, claiming value instead of demonstrating it, and asking for obligation before delivering benefit. Every such draft was rejected. The operator's outreach is systems design applied to persuasion; this law encodes the system.\n\n## Core law\n\nFirst contact is an intervention against the recipient's status quo of ignoring you — and their inattention has standing. The message earns its existence only from their seat: maximum benefit to them, minimum risk to them, minimum obligation on them, proof over claims. Center their position, then introduce yourself only as the mechanism that improves it — their exposure (unreliable supply), your mechanism (domestic warehouse), their outcome (two-day restocking). Unmistakably high-value, in the least possible words.\n\n## The offer is the argument\n\nPersuasion is structural, not rhetorical. Engineer the deal so the recipient cannot lose — then the message merely describes the architecture. \"I have made it impossible for you to lose\" is a design requirement on the offer, never a sentence in the email. Copy may clarify and prove the offer; it may not compensate for a structurally weak offer — fix the offer, not the copy. The standard is aesthetic: the benefit should be self-evident from the structure alone.\n\n## Required elements of a compliant first message\n\n1. **Their model, demonstrated.** Open inside their business — a specific, correct observation about how they operate, what they carry, what constrains them. Comprehension is proven by specificity, never asserted (\"I love what you're doing\" is a violation).\n2. **Their upside, concrete.** The benefit in their units — margin, speed, reliability, revenue — not in the sender's adjectives.\n3. **Their risk, structurally reduced.** Show the smallest credible way to test the offer: a low-cost initial order, limited pilot, sample where appropriate, or another reversible step. State the actual exposure plainly when useful. Do not invent guarantees or default to free samples.\n4. **Their exposure, operationally stated.** Identify a concrete cost, delay, risk, or constraint their current model may create, but only when supported by evidence. Contrast it with the operator's verifiable mechanism and the resulting benefit. Never invent a competitor defect to create urgency.\n5. **Proof attached.** The working thing, the live page, the number — demonstration travels with the message. If they do nothing further, they still received value.\n6. **Near-zero obligation.** The next step is tiny, optional, reversible — a one-word reply, a look at a link. Never a meeting, a call, or homework on first contact.\n\n## Small-entry design\n\nReduce exposure by making the first transaction commercially trivial relative to the potential upside — a small paid order, limited test, pilot, or reversible first step. Samples are one possible mechanism, discretionary and used selectively when commercially credible, never the headline offer. The principle is low exposure, not free product.\n\n## Architecture is not copy\n\nThe offer's risk parameters — unit minimums, dollar exposure, sample discretion — inform the model's design of the offer and are never voiced to the recipient. State the asymmetry, never the arithmetic. The small entry is phrased as ease and the recipient's convenience (\"start with a small first order and run the COAs yourself\"), never as price, unit count, \"just,\" \"only,\" or \"try.\" Low exposure delivered in low register reads as low value; the mechanism must be small while the voice remains that of someone who does not need the order.\n\n## Register\n\nExtremely high decorum. The operator's actual voice is mercenary; first contact never shows it — precision and structure are what signal who they are dealing with. Cordial, direct, adult-to-adult. Every sentence carries their benefit or the proof of it; anything else is cut per the Writing Law.\n\nNever reduce the perceived status of the sender, recipient, or offer in order to make the message more accessible. Do not translate commercial seriousness into mass-market friendliness — low friction never means informality, cheapness, or eagerness. The distinctions: cordial, not familiar; confident, not enthusiastic; precise, not promotional; generous, not ingratiating; low-friction, not cheap; direct, not blunt; commercially serious, not salesy; understated, not vague. High-status communication does not announce value, perform friendliness, or beg for engagement — it demonstrates understanding, states the commercial advantage plainly, and leaves room for the recipient's judgment.\n\n## Banned on sight (model-voice)\n\n- Greeting-plus-announcement openers: \"Hi [Team]!\", \"I'm reaching out today\", \"I hope this finds you well\", \"My name is X and I...\"\n- Centering the sender before the recipient has received value.\n- Enthusiasm as substance: exclamation points, \"excited,\" \"love,\" \"passionate.\"\n- Claimed qualities without mechanism: \"high quality,\" \"trusted partner,\" \"game-changing.\"\n- Asking for time before delivering benefit.\n- Any sentence the recipient could not act on or verify.\n- Parameter leak: internal offer architecture voiced as recipient-facing copy (\"three units, about $75\" priced the correspondence at $75 — 2026-07-25).\n- Register substitution: \"low obligation\" translated into mass-market sales language (\"Want to try three units first?\", \"reply peptide\", \"let's chat\", \"free sample\" — each collapses the sender's status to secure attention).\n\n## The test before sending\n\nRead it from their chair, cold, thirty seconds: What did they gain by reading it? What would they risk by replying? What are they obligated to do? If the answers are not *something concrete / almost nothing / almost nothing*, the draft fails. The principle is radical asymmetry, not literal zero — do not fabricate guarantees to reach zero. Then the four-class check from operational-logic: no substitution of the operator's terms, no manufacture of claims the offer's structure doesn't back.\n","version":4,"of":5},{"commit":"c57543f45","date":"2026-07-25T11:30:22-07:00","what_changed":"outreach-law: align to the reviewer's verbatim text — small-entry design wording, radical-asymmetry sentence, exposure clause exact #vault-approved","chars":5038,"added":["4. **Their exposure, operationally stated.** Identify a concrete cost, delay, risk, or constraint their current model may create, but only when supported by evidence. Contrast it with the operator's verifiable mechanism and the resulting benefit. Never invent a competitor defect to create urgency.","Reduce exposure by making the first transaction commercially trivial relative to the potential upside — a small paid order, limited test, pilot, or reversible first step. Samples are one possible mechanism, discretionary and used selectively when commercially credible, never the headline offer. The principle is low exposure, not free product.","Read it from their chair, cold, thirty seconds: What did they gain by reading it? What would they risk by replying? What are they obligated to do? If the answers are not *something concrete / almost nothing / almost nothing*, the draft fails. The principle is radical asymmetry, not literal zero — do not fabricate guarantees to reach zero. Then the four-class check from operational-logic: no substitution of the operator's terms, no manufacture of claims the offer's structure doesn't back."],"removed":["4. **Their exposure, operationally stated.** Identify a concrete cost, delay, risk, or constraint their current model may create, but only when supported by evidence. Contrast it with the operator's verifiable mechanism (2-day shipping from a domestic warehouse) and the resulting benefit. Never invent a competitor defect to create urgency. Facts a recipient can verify — never \"better,\" \"premium,\" \"trusted.\"","Reduce exposure by making the first transaction commercially trivial relative to the potential upside. Use a small paid order, limited test, sample, pilot, or reversible first step as appropriate. Do not automatically advertise \"free samples.\" Samples are one possible mechanism, used selectively when commercially credible. The principle is low exposure, not free product. Risk reduction can come from a tiny paid trial — three units is real money to no one.","Read it from their chair, cold, thirty seconds: What did they gain by reading it? What would they risk by replying? What are they obligated to do? If the answers are not *something concrete / almost nothing / almost nothing*, the draft fails. Literal zero is impossible and fabricating guarantees to reach it is a violation — the principle is radical asymmetry, not metaphysical zero. Then the four-class check from operational-logic: no substitution of the operator's terms, no manufacture of claims the offer's structure doesn't back."],"text":"---\nname: outreach-law\ndescription: The operator's law for first-contact outreach — cold email, cold DM, first message to any counterparty, sales or partnership or recruiting. Load whenever drafting, reviewing, or planning any message to someone who did not ask to hear from the operator. This is decision content — it supplies what to say and how to structure the offer; operational-logic governs how the draft is produced. Apply especially when tempted to open with a greeting, introduce the operator, describe the product, or ask for a meeting.\n---\n\n# Outreach Law\n\n**Why it exists:** Models draft cold outreach in model-voice — \"Hi Vida Team! I am reaching out today...\" — announcing the outreach, centering the sender, claiming value instead of demonstrating it, and asking for obligation before delivering benefit. Every such draft was rejected. The operator's outreach is systems design applied to persuasion; this law encodes the system.\n\n## Core law\n\nFirst contact is an intervention against the recipient's status quo of ignoring you — and their inattention has standing. The message earns its existence only from their seat: maximum benefit to them, minimum risk to them, minimum obligation on them, proof over claims. Center their position, then introduce yourself only as the mechanism that improves it — their exposure (unreliable supply), your mechanism (domestic warehouse), their outcome (two-day restocking). Unmistakably high-value, in the least possible words.\n\n## The offer is the argument\n\nPersuasion is structural, not rhetorical. Engineer the deal so the recipient cannot lose — then the message merely describes the architecture. \"I have made it impossible for you to lose\" is a design requirement on the offer, never a sentence in the email. Copy may clarify and prove the offer; it may not compensate for a structurally weak offer — fix the offer, not the copy. The standard is aesthetic: the benefit should be self-evident from the structure alone.\n\n## Required elements of a compliant first message\n\n1. **Their model, demonstrated.** Open inside their business — a specific, correct observation about how they operate, what they carry, what constrains them. Comprehension is proven by specificity, never asserted (\"I love what you're doing\" is a violation).\n2. **Their upside, concrete.** The benefit in their units — margin, speed, reliability, revenue — not in the sender's adjectives.\n3. **Their risk, structurally reduced.** Show the smallest credible way to test the offer: a low-cost initial order, limited pilot, sample where appropriate, or another reversible step. State the actual exposure plainly when useful. Do not invent guarantees or default to free samples.\n4. **Their exposure, operationally stated.** Identify a concrete cost, delay, risk, or constraint their current model may create, but only when supported by evidence. Contrast it with the operator's verifiable mechanism and the resulting benefit. Never invent a competitor defect to create urgency.\n5. **Proof attached.** The working thing, the live page, the number — demonstration travels with the message. If they do nothing further, they still received value.\n6. **Near-zero obligation.** The next step is tiny, optional, reversible — a one-word reply, a look at a link. Never a meeting, a call, or homework on first contact.\n\n## Small-entry design\n\nReduce exposure by making the first transaction commercially trivial relative to the potential upside — a small paid order, limited test, pilot, or reversible first step. Samples are one possible mechanism, discretionary and used selectively when commercially credible, never the headline offer. The principle is low exposure, not free product.\n\n## Register\n\nExtremely high decorum. The operator's actual voice is mercenary; first contact never shows it — precision and structure are what signal who they are dealing with. Cordial, direct, adult-to-adult. Every sentence carries their benefit or the proof of it; anything else is cut per the Writing Law.\n\n## Banned on sight (model-voice)\n\n- Greeting-plus-announcement openers: \"Hi [Team]!\", \"I'm reaching out today\", \"I hope this finds you well\", \"My name is X and I...\"\n- Centering the sender before the recipient has received value.\n- Enthusiasm as substance: exclamation points, \"excited,\" \"love,\" \"passionate.\"\n- Claimed qualities without mechanism: \"high quality,\" \"trusted partner,\" \"game-changing.\"\n- Asking for time before delivering benefit.\n- Any sentence the recipient could not act on or verify.\n\n## The test before sending\n\nRead it from their chair, cold, thirty seconds: What did they gain by reading it? What would they risk by replying? What are they obligated to do? If the answers are not *something concrete / almost nothing / almost nothing*, the draft fails. The principle is radical asymmetry, not literal zero — do not fabricate guarantees to reach zero. Then the four-class check from operational-logic: no substitution of the operator's terms, no manufacture of claims the offer's structure doesn't back.\n","version":3,"of":5},{"commit":"7db201578","date":"2026-07-25T11:29:28-07:00","what_changed":"outreach-law: owner amendments — recipient's existing exposure (never invented competitor defects), risk structurally reduced via smallest credible test (no default free samples, no invented guarantees), small-entry design section, almost-nothing test (radical asymmetry, not metaphysical zero) #vault-approved","chars":5309,"added":["3. **Their risk, structurally reduced.** Show the smallest credible way to test the offer: a low-cost initial order, limited pilot, sample where appropriate, or another reversible step. State the actual exposure plainly when useful. Do not invent guarantees or default to free samples.","4. **Their exposure, operationally stated.** Identify a concrete cost, delay, risk, or constraint their current model may create, but only when supported by evidence. Contrast it with the operator's verifiable mechanism (2-day shipping from a domestic warehouse) and the resulting benefit. Never invent a competitor defect to create urgency. Facts a recipient can verify — never \"better,\" \"premium,\" \"trusted.\"","## Small-entry design","Reduce exposure by making the first transaction commercially trivial relative to the potential upside. Use a small paid order, limited test, sample, pilot, or reversible first step as appropriate. Do not automatically advertise \"free samples.\" Samples are one possible mechanism, used selectively when commercially credible. The principle is low exposure, not free product. Risk reduction can come from a tiny paid trial — three units is real money to no one.","Read it from their chair, cold, thirty seconds: What did they gain by reading it? What would they risk by replying? What are they obligated to do? If the answers are not *something concrete / almost nothing / almost nothing*, the draft fails. Literal zero is impossible and fabricating guarantees to reach it is a violation — the principle is radical asymmetry, not metaphysical zero. Then the four-class check from operational-logic: no substitution of the operator's terms, no manufacture of claims the offer's structure doesn't back."],"removed":["3. **Their risk, structurally removed.** Name the mechanism that caps their exposure: terms, guarantees, small first order, reversibility. Built into the deal, then stated plainly.","4. **Contrast in operational facts.** The alternative's failure named concretely (slow shipping, unreliability, stockouts) against the operator's concrete counter (2-day shipping from a domestic warehouse). Facts a recipient can verify — never \"better,\" \"premium,\" \"trusted.\"","Read it from their chair, cold, thirty seconds: What did they gain by reading it? What would they risk by replying? What are they obligated to do? If the answers are not *something concrete / nothing / nothing*, the draft fails. Then the four-class check from operational-logic: no substitution of the operator's terms, no manufacture of claims the offer's structure doesn't back."],"text":"---\nname: outreach-law\ndescription: The operator's law for first-contact outreach — cold email, cold DM, first message to any counterparty, sales or partnership or recruiting. Load whenever drafting, reviewing, or planning any message to someone who did not ask to hear from the operator. This is decision content — it supplies what to say and how to structure the offer; operational-logic governs how the draft is produced. Apply especially when tempted to open with a greeting, introduce the operator, describe the product, or ask for a meeting.\n---\n\n# Outreach Law\n\n**Why it exists:** Models draft cold outreach in model-voice — \"Hi Vida Team! I am reaching out today...\" — announcing the outreach, centering the sender, claiming value instead of demonstrating it, and asking for obligation before delivering benefit. Every such draft was rejected. The operator's outreach is systems design applied to persuasion; this law encodes the system.\n\n## Core law\n\nFirst contact is an intervention against the recipient's status quo of ignoring you — and their inattention has standing. The message earns its existence only from their seat: maximum benefit to them, minimum risk to them, minimum obligation on them, proof over claims. Center their position, then introduce yourself only as the mechanism that improves it — their exposure (unreliable supply), your mechanism (domestic warehouse), their outcome (two-day restocking). Unmistakably high-value, in the least possible words.\n\n## The offer is the argument\n\nPersuasion is structural, not rhetorical. Engineer the deal so the recipient cannot lose — then the message merely describes the architecture. \"I have made it impossible for you to lose\" is a design requirement on the offer, never a sentence in the email. Copy may clarify and prove the offer; it may not compensate for a structurally weak offer — fix the offer, not the copy. The standard is aesthetic: the benefit should be self-evident from the structure alone.\n\n## Required elements of a compliant first message\n\n1. **Their model, demonstrated.** Open inside their business — a specific, correct observation about how they operate, what they carry, what constrains them. Comprehension is proven by specificity, never asserted (\"I love what you're doing\" is a violation).\n2. **Their upside, concrete.** The benefit in their units — margin, speed, reliability, revenue — not in the sender's adjectives.\n3. **Their risk, structurally reduced.** Show the smallest credible way to test the offer: a low-cost initial order, limited pilot, sample where appropriate, or another reversible step. State the actual exposure plainly when useful. Do not invent guarantees or default to free samples.\n4. **Their exposure, operationally stated.** Identify a concrete cost, delay, risk, or constraint their current model may create, but only when supported by evidence. Contrast it with the operator's verifiable mechanism (2-day shipping from a domestic warehouse) and the resulting benefit. Never invent a competitor defect to create urgency. Facts a recipient can verify — never \"better,\" \"premium,\" \"trusted.\"\n5. **Proof attached.** The working thing, the live page, the number — demonstration travels with the message. If they do nothing further, they still received value.\n6. **Near-zero obligation.** The next step is tiny, optional, reversible — a one-word reply, a look at a link. Never a meeting, a call, or homework on first contact.\n\n## Small-entry design\n\nReduce exposure by making the first transaction commercially trivial relative to the potential upside. Use a small paid order, limited test, sample, pilot, or reversible first step as appropriate. Do not automatically advertise \"free samples.\" Samples are one possible mechanism, used selectively when commercially credible. The principle is low exposure, not free product. Risk reduction can come from a tiny paid trial — three units is real money to no one.\n\n## Register\n\nExtremely high decorum. The operator's actual voice is mercenary; first contact never shows it — precision and structure are what signal who they are dealing with. Cordial, direct, adult-to-adult. Every sentence carries their benefit or the proof of it; anything else is cut per the Writing Law.\n\n## Banned on sight (model-voice)\n\n- Greeting-plus-announcement openers: \"Hi [Team]!\", \"I'm reaching out today\", \"I hope this finds you well\", \"My name is X and I...\"\n- Centering the sender before the recipient has received value.\n- Enthusiasm as substance: exclamation points, \"excited,\" \"love,\" \"passionate.\"\n- Claimed qualities without mechanism: \"high quality,\" \"trusted partner,\" \"game-changing.\"\n- Asking for time before delivering benefit.\n- Any sentence the recipient could not act on or verify.\n\n## The test before sending\n\nRead it from their chair, cold, thirty seconds: What did they gain by reading it? What would they risk by replying? What are they obligated to do? If the answers are not *something concrete / almost nothing / almost nothing*, the draft fails. Literal zero is impossible and fabricating guarantees to reach it is a violation — the principle is radical asymmetry, not metaphysical zero. Then the four-class check from operational-logic: no substitution of the operator's terms, no manufacture of claims the offer's structure doesn't back.\n","version":2,"of":5},{"commit":"87ce30ce8","date":"2026-07-25T11:24:58-07:00","what_changed":"outreach-law skill (both trees): first-contact law — offer-as-argument with the two owner corrections (center their position, self only as mechanism; copy clarifies and proves, never compensates for a weak offer) #vault-approved","chars":4429,"added":["---","name: outreach-law","description: The operator's law for first-contact outreach — cold email, cold DM, first message to any counterparty, sales or partnership or recruiting. Load whenever drafting, reviewing, or planning any message to someone who did not ask to hear from the operator. This is decision content — it supplies what to say and how to structure the offer; operational-logic governs how the draft is produced. Apply especially when tempted to open with a greeting, introduce the operator, describe the product, or ask for a meeting.","# Outreach Law","**Why it exists:** Models draft cold outreach in model-voice — \"Hi Vida Team! I am reaching out today...\" — announcing the outreach, centering the sender, claiming value instead of demonstrating it, and asking for obligation before delivering benefit. Every such draft was rejected. The operator's outreach is systems design applied to persuasion; this law encodes the system.","## Core law","First contact is an intervention against the recipient's status quo of ignoring you — and their inattention has standing. The message earns its existence only from their seat: maximum benefit to them, minimum risk to them, minimum obligation on them, proof over claims. Center their position, then introduce yourself only as the mechanism that improves it — their exposure (unreliable supply), your mechanism (domestic warehouse), their outcome (two-day restocking). Unmistakably high-value, in the least possible words.","## The offer is the argument","Persuasion is structural, not rhetorical. Engineer the deal so the recipient cannot lose — then the message merely describes the architecture. \"I have made it impossible for you to lose\" is a design requirement on the offer, never a sentence in the email. Copy may clarify and prove the offer; it may not compensate for a structurally weak offer — fix the offer, not the copy. The standard is aesthetic: the benefit should be self-evident from the structure alone.","## Required elements of a compliant first message","1. **Their model, demonstrated.** Open inside their business — a specific, correct observation about how they operate, what they carry, what constrains them. Comprehension is proven by specificity, never asserted (\"I love what you're doing\" is a violation).","2. **Their upside, concrete.** The benefit in their units — margin, speed, reliability, revenue — not in the sender's adjectives.","3. **Their risk, structurally removed.** Name the mechanism that caps their exposure: terms, guarantees, small first order, reversibility. Built into the deal, then stated plainly.","4. **Contrast in operational facts.** The alternative's failure named concretely (slow shipping, unreliability, stockouts) against the operator's concrete counter (2-day shipping from a domestic warehouse). Facts a recipient can verify — never \"better,\" \"premium,\" \"trusted.\"","5. **Proof attached.** The working thing, the live page, the number — demonstration travels with the message. If they do nothing further, they still received value.","6. **Near-zero obligation.** The next step is tiny, optional, reversible — a one-word reply, a look at a link. Never a meeting, a call, or homework on first contact.","## Register","Extremely high decorum. The operator's actual voice is mercenary; first contact never shows it — precision and structure are what signal who they are dealing with. Cordial, direct, adult-to-adult. Every sentence carries their benefit or the proof of it; anything else is cut per the Writing Law.","## Banned on sight (model-voice)","- Greeting-plus-announcement openers: \"Hi [Team]!\", \"I'm reaching out today\", \"I hope this finds you well\", \"My name is X and I...\"","- Centering the sender before the recipient has received value.","- Enthusiasm as substance: exclamation points, \"excited,\" \"love,\" \"passionate.\"","- Claimed qualities without mechanism: \"high quality,\" \"trusted partner,\" \"game-changing.\"","- Asking for time before delivering benefit.","- Any sentence the recipient could not act on or verify.","## The test before sending","Read it from their chair, cold, thirty seconds: What did they gain by reading it? What would they risk by replying? What are they obligated to do? If the answers are not *something concrete / nothing / nothing*, the draft fails. Then the four-class check from operational-logic: no substitution of the operator's terms, no manufacture of claims the offer's structure doesn't back."],"removed":[],"text":"---\nname: outreach-law\ndescription: The operator's law for first-contact outreach — cold email, cold DM, first message to any counterparty, sales or partnership or recruiting. Load whenever drafting, reviewing, or planning any message to someone who did not ask to hear from the operator. This is decision content — it supplies what to say and how to structure the offer; operational-logic governs how the draft is produced. Apply especially when tempted to open with a greeting, introduce the operator, describe the product, or ask for a meeting.\n---\n\n# Outreach Law\n\n**Why it exists:** Models draft cold outreach in model-voice — \"Hi Vida Team! I am reaching out today...\" — announcing the outreach, centering the sender, claiming value instead of demonstrating it, and asking for obligation before delivering benefit. Every such draft was rejected. The operator's outreach is systems design applied to persuasion; this law encodes the system.\n\n## Core law\n\nFirst contact is an intervention against the recipient's status quo of ignoring you — and their inattention has standing. The message earns its existence only from their seat: maximum benefit to them, minimum risk to them, minimum obligation on them, proof over claims. Center their position, then introduce yourself only as the mechanism that improves it — their exposure (unreliable supply), your mechanism (domestic warehouse), their outcome (two-day restocking). Unmistakably high-value, in the least possible words.\n\n## The offer is the argument\n\nPersuasion is structural, not rhetorical. Engineer the deal so the recipient cannot lose — then the message merely describes the architecture. \"I have made it impossible for you to lose\" is a design requirement on the offer, never a sentence in the email. Copy may clarify and prove the offer; it may not compensate for a structurally weak offer — fix the offer, not the copy. The standard is aesthetic: the benefit should be self-evident from the structure alone.\n\n## Required elements of a compliant first message\n\n1. **Their model, demonstrated.** Open inside their business — a specific, correct observation about how they operate, what they carry, what constrains them. Comprehension is proven by specificity, never asserted (\"I love what you're doing\" is a violation).\n2. **Their upside, concrete.** The benefit in their units — margin, speed, reliability, revenue — not in the sender's adjectives.\n3. **Their risk, structurally removed.** Name the mechanism that caps their exposure: terms, guarantees, small first order, reversibility. Built into the deal, then stated plainly.\n4. **Contrast in operational facts.** The alternative's failure named concretely (slow shipping, unreliability, stockouts) against the operator's concrete counter (2-day shipping from a domestic warehouse). Facts a recipient can verify — never \"better,\" \"premium,\" \"trusted.\"\n5. **Proof attached.** The working thing, the live page, the number — demonstration travels with the message. If they do nothing further, they still received value.\n6. **Near-zero obligation.** The next step is tiny, optional, reversible — a one-word reply, a look at a link. Never a meeting, a call, or homework on first contact.\n\n## Register\n\nExtremely high decorum. The operator's actual voice is mercenary; first contact never shows it — precision and structure are what signal who they are dealing with. Cordial, direct, adult-to-adult. Every sentence carries their benefit or the proof of it; anything else is cut per the Writing Law.\n\n## Banned on sight (model-voice)\n\n- Greeting-plus-announcement openers: \"Hi [Team]!\", \"I'm reaching out today\", \"I hope this finds you well\", \"My name is X and I...\"\n- Centering the sender before the recipient has received value.\n- Enthusiasm as substance: exclamation points, \"excited,\" \"love,\" \"passionate.\"\n- Claimed qualities without mechanism: \"high quality,\" \"trusted partner,\" \"game-changing.\"\n- Asking for time before delivering benefit.\n- Any sentence the recipient could not act on or verify.\n\n## The test before sending\n\nRead it from their chair, cold, thirty seconds: What did they gain by reading it? What would they risk by replying? What are they obligated to do? If the answers are not *something concrete / nothing / nothing*, the draft fails. Then the four-class check from operational-logic: no substitution of the operator's terms, no manufacture of claims the offer's structure doesn't back.\n","version":1,"of":5}]},{"id":"cold_outreach_craft","label":"Cold outreach craft skill","path":".claude/skills/cold-outreach-craft/SKILL.md","versions":[{"commit":"4e0b6cee5","date":"2026-07-23T00:09:13-07:00","what_changed":"skills: write-human, business-email, cold-outreach-craft (both trees)","chars":10925,"added":["---","name: cold-outreach-craft","description: The craft of getting a stranger to read and reply — cold email, LinkedIn note, DM, first-touch SMS. Load when writing any first-contact message to someone who doesn't know you. Covers pattern-interrupt openers, earning the first line, the one-real-observation rule, curiosity over pitch, brevity discipline, the soft single CTA, follow-up cadence, and leading with genuine value or a real credential instead of hype. Pair with write-human and business-email.","# cold-outreach-craft","A stranger owes you nothing. They will give you one line of attention. Everything here is about earning the second line, then the reply. Cold outreach is not marketing — it's a personal message from one competent person to another, and it has to read that way.","Every high-performing cold message has exactly three parts: a research-backed opener, one clear value proposition, and a low-friction ask (SmartLead, Autobound, 2026). Nothing else. Always run drafts through **write-human** (a cold email full of AI tells is dead on arrival) and borrow structure from **business-email**.","## Rule 1 — Earn the first line","The first line is the whole game. If it could have been sent to 5,000 people, it will be read by none. It must prove, instantly, that you looked at *them*.","- **Never open about yourself.** \"I'm the founder of X\" / \"My company helps businesses to...\" is a delete. They don't care yet.","- **Never open with flattery-by-template.** \"I've been following your amazing work\" is a tell that you followed nothing.","- **Open with one true, specific observation about them** (Rule 2). That's the pattern-interrupt: it looks like a real person wrote it just for them, because someone did.","## Rule 2 — The one real specific observation","Personalized openers get up to ~142% higher reply rates than generic ones (Martal, 2025). But \"personalized\" doesn't mean merge-fielding their first name. It means one concrete thing that required you to actually look.","Good observations are specific enough that they'd make the reader think *\"this person actually looked into my situation\"*:","- A thing they shipped/posted/said recently, named exactly. \"Saw you moved the clinic to the new Newport location last month —\"","- A specific detail of their operation. \"You're running three IV bars but only listing peptides on one site —\"","- A visible consequence you can infer. \"Your team page went from 4 to 11 since spring — that kind of growth usually breaks intake scheduling first.\"","Bad (fake) observations: \"I love your website,\" \"Your company is doing great things,\" \"I noticed you're in [industry].\" These are worse than nothing because they signal a template wearing a costume.","**One** observation. Not three. Then move.","## Rule 3 — Curiosity over pitch","You are not trying to close in the email. You are trying to make them curious enough to reply. The pitch happens in the conversation the email starts.","- State the value as an outcome they'd want, not a feature list. Not \"we offer 99% pure RUO peptides with COAs\" → \"most clinics we talk to are overpaying their current supplier by 30-40% on the same compounds, same purity docs.\"","- Leave a gap. \"There's a reason your reorder costs are creeping up and it's not the peptide\" makes them want the next line. A full explanation removes the reason to reply.","- Don't dump credentials, links, and attachments in a first touch. One credential, if any (Rule 6). Links can trip spam filters and read as blast.","## Rule 4 — Brevity discipline","- **Under 125 words. 50-125 is the reply sweet spot** (Growth List, 2026). Shorter is usually better for first touch.","- It should read in under 10 seconds and be answerable in one.","- No preamble, no signature paragraph, no \"I hope this finds you well.\" Observation → value → ask. Done.","- On a phone it's one screen, no scroll. That's the real length limit.","- Every sentence earns its place or dies. A cold email has no room for a sentence that's only there for politeness.","## Rule 5 — The soft single CTA","One ask. Not two, not three (SmartLead). Low-commitment asks get about 2x the replies of an aggressive \"book a 30-minute call\" (Autobound).","- Ask for interest or a tiny step, not their calendar: \"Worth a quick look?\" / \"Want me to send the price list for the three you use most?\" / \"Should I put together a quick comparison for your top SKUs?\"","- Make \"no\" cheap and \"yes\" one word. A question they can answer with \"sure\" or \"not now\" outperforms a demand.","- Never stack: \"Let me know if you're interested, and also feel free to book a call, and I can send over a deck too.\" That's three asks; it gets zero.","- The CTA is a question with a question mark. If your close is a statement (\"Looking forward to connecting\"), it isn't a CTA.","## Rule 6 — Honest credibility: lead with value or a real credential, never hype","Trust is the scarce resource. You build it by being specific and real, not by adjectives.","- **Lead with genuine value:** give them something useful in the email itself — a relevant number, a spotted problem, a quick teardown of one thing. \"Value-first\" outreach earns the reply because you've already been useful before asking for anything.","- **Or lead with a real, checkable credential:** \"we supply 40+ clinics in TX and can send COAs on any lot\" beats \"we're the industry-leading trusted provider.\" A number, a name, a proof — never a superlative.","- **Never fake scarcity or authority.** \"Only 3 spots left,\" \"As seen in,\" borrowed logos, invented urgency — a sharp reader smells it and you're done.","- If you don't have a credential yet, lead with value harder. Being visibly useful is its own credential.","## Rule 7 — Follow-up cadence","Most replies come from follow-ups, not the first send — 40-60% of positive replies come from touches #2 and #3 (SalesCaptain, 2025). But each one must carry a new angle. \"Just following up\" is noise and trains them to ignore you.","A workable 3-4 touch sequence:","- **Touch 1 (day 0):** the observation + value + soft ask.","- **Touch 2 (day 3-4):** a *new* hook — a different angle on the value, a relevant proof point, or a smaller ask. Not a repeat. \"Different thought — even if the supplier switch isn't now, here's the one COA question worth asking whoever you use today: [x].\"","- **Touch 3 (day 7-10):** the easy-out / break-up. \"I'll stop here — is this a not-now, or a not-for-us? Either's fine, just don't want to clutter your inbox.\" Break-up emails get surprisingly high reply rates because they're honest and give control back.","- **Stop at 3-4.** Persistence past that is spam, not craft.","Keep each follow-up on the same thread. Keep each one shorter than the last.","## Full examples","**Cold email — B2B, value-led**","> Subject: your reorder costs (Loop / RUO)",">","> Rachel — saw the second Dallas location go live on your site last week. Nice.","> Most clinics scaling to a second site get quietly overcharged on peptides because the first supplier knows switching is a hassle. We run the same compounds, third-party COA on every lot, usually 30-40% under the incumbent.","> Want me to send a quick price comparison on the three you list? Takes me five minutes, no call needed.","> — [you]","Why: real specific observation, one value gap left open, one credential (COA on every lot), one soft ask, under 90 words, no hype.","**LinkedIn connection note (300-char limit)**","> Rachel — noticed you just opened the second Dallas clinic. Congrats. I work with RUO peptide supply for longevity clinics and usually save the second-location folks a real chunk on reorders. Not pitching here — happy to send a price comparison if it's useful.","Why: fits the limit, specific, disarms (\"not pitching here\"), offers value, no link.","**The break-up (touch 3)**","> Rachel — I'll leave it here. Is this a not-right-now, or a not-for-us? Either's a fine answer — just don't want to keep landing in your inbox.","Why: honest, gives control, one-word-answerable, ends the sequence cleanly.","## Before / after","**1 — the me-first opener**","Before:","> Hi Rachel, my name is [x] and I'm the founder of [company], a leading provider of high-quality research compounds. We help clinics like yours streamline their supply chain and unlock significant savings. I'd love to hop on a 30-minute call to walk you through our solutions!","After:","> Rachel — saw the second Dallas location go live last week. Clinics at that stage usually get quietly overcharged on peptide reorders. We run the same compounds with COAs on every lot, typically 30-40% under the incumbent. Worth a quick price comparison?","Why: killed the self-intro, \"leading provider,\" \"streamline,\" \"unlock,\" and the 30-minute demand. Led with a real observation, one credential, one soft ask.","**2 — the fake-personal**","> Hi there! I came across your profile and was really impressed by all the amazing work you're doing in the healthcare space. I think there could be some great synergy between our companies!","> Rachel — you're listing peptides on the Newport site but not the two Dallas ones yet. If that's a supply gap and not a choice, that's usually the exact thing we fix. Want the details?","Why: \"impressed by your amazing work\" and \"synergy\" are template tells. The rewrite names a specific, checkable detail that could only come from looking.","**3 — the stacked CTA**","> Let me know if you'd be interested in learning more! You can book a call using the link below, or feel free to reply here, and I've also attached our full product catalog and pricing deck for your reference. Looking forward to connecting!","> Want me to send the price comparison on the three SKUs you already list? Just say the word.","Why: four asks and two attachments collapsed to one tiny, one-word-answerable ask. No links to trip filters, no deck to ignore.","## Ship checklist","- [ ] First line is about them, not you.","- [ ] Exactly one real, specific, checkable observation — no fake flattery.","- [ ] Value stated as an outcome, with a curiosity gap, not a feature dump.","- [ ] Under 125 words; one phone screen; readable in 10 seconds.","- [ ] One soft, low-friction, one-word-answerable CTA with a question mark.","- [ ] One real credential or genuine value — zero hype, zero fake scarcity.","- [ ] No links/attachments in first touch unless essential.","- [ ] Follow-ups each carry a new angle; sequence caps at 3-4; ends with an honest break-up.","- [ ] Run through write-human (no tells) and cross-check business-email structure.","Sources: [SmartLead — cold email templates that get replies](https://www.smartlead.ai/blog/cold-email-templates), [Autobound — cold email templates guide](https://www.autobound.ai/blog/cold-email-templates-guide), [SalesCaptain — cold email statistics 2025](https://www.salescaptain.io/blog/cold-email-statistics), [Salesmotion — cold outreach playbook](https://salesmotion.io/blog/cold-outreach-best-practices)."],"removed":[],"text":"---\nname: cold-outreach-craft\ndescription: The craft of getting a stranger to read and reply — cold email, LinkedIn note, DM, first-touch SMS. Load when writing any first-contact message to someone who doesn't know you. Covers pattern-interrupt openers, earning the first line, the one-real-observation rule, curiosity over pitch, brevity discipline, the soft single CTA, follow-up cadence, and leading with genuine value or a real credential instead of hype. Pair with write-human and business-email.\n---\n\n# cold-outreach-craft\n\nA stranger owes you nothing. They will give you one line of attention. Everything here is about earning the second line, then the reply. Cold outreach is not marketing — it's a personal message from one competent person to another, and it has to read that way.\n\nEvery high-performing cold message has exactly three parts: a research-backed opener, one clear value proposition, and a low-friction ask (SmartLead, Autobound, 2026). Nothing else. Always run drafts through **write-human** (a cold email full of AI tells is dead on arrival) and borrow structure from **business-email**.\n\n---\n\n## Rule 1 — Earn the first line\n\nThe first line is the whole game. If it could have been sent to 5,000 people, it will be read by none. It must prove, instantly, that you looked at *them*.\n\n- **Never open about yourself.** \"I'm the founder of X\" / \"My company helps businesses to...\" is a delete. They don't care yet.\n- **Never open with flattery-by-template.** \"I've been following your amazing work\" is a tell that you followed nothing.\n- **Open with one true, specific observation about them** (Rule 2). That's the pattern-interrupt: it looks like a real person wrote it just for them, because someone did.\n\n---\n\n## Rule 2 — The one real specific observation\n\nPersonalized openers get up to ~142% higher reply rates than generic ones (Martal, 2025). But \"personalized\" doesn't mean merge-fielding their first name. It means one concrete thing that required you to actually look.\n\nGood observations are specific enough that they'd make the reader think *\"this person actually looked into my situation\"*:\n\n- A thing they shipped/posted/said recently, named exactly. \"Saw you moved the clinic to the new Newport location last month —\"\n- A specific detail of their operation. \"You're running three IV bars but only listing peptides on one site —\"\n- A visible consequence you can infer. \"Your team page went from 4 to 11 since spring — that kind of growth usually breaks intake scheduling first.\"\n\nBad (fake) observations: \"I love your website,\" \"Your company is doing great things,\" \"I noticed you're in [industry].\" These are worse than nothing because they signal a template wearing a costume.\n\n**One** observation. Not three. Then move.\n\n---\n\n## Rule 3 — Curiosity over pitch\n\nYou are not trying to close in the email. You are trying to make them curious enough to reply. The pitch happens in the conversation the email starts.\n\n- State the value as an outcome they'd want, not a feature list. Not \"we offer 99% pure RUO peptides with COAs\" → \"most clinics we talk to are overpaying their current supplier by 30-40% on the same compounds, same purity docs.\"\n- Leave a gap. \"There's a reason your reorder costs are creeping up and it's not the peptide\" makes them want the next line. A full explanation removes the reason to reply.\n- Don't dump credentials, links, and attachments in a first touch. One credential, if any (Rule 6). Links can trip spam filters and read as blast.\n\n---\n\n## Rule 4 — Brevity discipline\n\n- **Under 125 words. 50-125 is the reply sweet spot** (Growth List, 2026). Shorter is usually better for first touch.\n- It should read in under 10 seconds and be answerable in one.\n- No preamble, no signature paragraph, no \"I hope this finds you well.\" Observation → value → ask. Done.\n- On a phone it's one screen, no scroll. That's the real length limit.\n- Every sentence earns its place or dies. A cold email has no room for a sentence that's only there for politeness.\n\n---\n\n## Rule 5 — The soft single CTA\n\nOne ask. Not two, not three (SmartLead). Low-commitment asks get about 2x the replies of an aggressive \"book a 30-minute call\" (Autobound).\n\n- Ask for interest or a tiny step, not their calendar: \"Worth a quick look?\" / \"Want me to send the price list for the three you use most?\" / \"Should I put together a quick comparison for your top SKUs?\"\n- Make \"no\" cheap and \"yes\" one word. A question they can answer with \"sure\" or \"not now\" outperforms a demand.\n- Never stack: \"Let me know if you're interested, and also feel free to book a call, and I can send over a deck too.\" That's three asks; it gets zero.\n- The CTA is a question with a question mark. If your close is a statement (\"Looking forward to connecting\"), it isn't a CTA.\n\n---\n\n## Rule 6 — Honest credibility: lead with value or a real credential, never hype\n\nTrust is the scarce resource. You build it by being specific and real, not by adjectives.\n\n- **Lead with genuine value:** give them something useful in the email itself — a relevant number, a spotted problem, a quick teardown of one thing. \"Value-first\" outreach earns the reply because you've already been useful before asking for anything.\n- **Or lead with a real, checkable credential:** \"we supply 40+ clinics in TX and can send COAs on any lot\" beats \"we're the industry-leading trusted provider.\" A number, a name, a proof — never a superlative.\n- **Never fake scarcity or authority.** \"Only 3 spots left,\" \"As seen in,\" borrowed logos, invented urgency — a sharp reader smells it and you're done.\n- If you don't have a credential yet, lead with value harder. Being visibly useful is its own credential.\n\n---\n\n## Rule 7 — Follow-up cadence\n\nMost replies come from follow-ups, not the first send — 40-60% of positive replies come from touches #2 and #3 (SalesCaptain, 2025). But each one must carry a new angle. \"Just following up\" is noise and trains them to ignore you.\n\nA workable 3-4 touch sequence:\n\n- **Touch 1 (day 0):** the observation + value + soft ask.\n- **Touch 2 (day 3-4):** a *new* hook — a different angle on the value, a relevant proof point, or a smaller ask. Not a repeat. \"Different thought — even if the supplier switch isn't now, here's the one COA question worth asking whoever you use today: [x].\"\n- **Touch 3 (day 7-10):** the easy-out / break-up. \"I'll stop here — is this a not-now, or a not-for-us? Either's fine, just don't want to clutter your inbox.\" Break-up emails get surprisingly high reply rates because they're honest and give control back.\n- **Stop at 3-4.** Persistence past that is spam, not craft.\n\nKeep each follow-up on the same thread. Keep each one shorter than the last.\n\n---\n\n## Full examples\n\n**Cold email — B2B, value-led**\n> Subject: your reorder costs (Loop / RUO)\n>\n> Rachel — saw the second Dallas location go live on your site last week. Nice.\n>\n> Most clinics scaling to a second site get quietly overcharged on peptides because the first supplier knows switching is a hassle. We run the same compounds, third-party COA on every lot, usually 30-40% under the incumbent.\n>\n> Want me to send a quick price comparison on the three you list? Takes me five minutes, no call needed.\n>\n> — [you]\n\nWhy: real specific observation, one value gap left open, one credential (COA on every lot), one soft ask, under 90 words, no hype.\n\n**LinkedIn connection note (300-char limit)**\n> Rachel — noticed you just opened the second Dallas clinic. Congrats. I work with RUO peptide supply for longevity clinics and usually save the second-location folks a real chunk on reorders. Not pitching here — happy to send a price comparison if it's useful.\n\nWhy: fits the limit, specific, disarms (\"not pitching here\"), offers value, no link.\n\n**The break-up (touch 3)**\n> Rachel — I'll leave it here. Is this a not-right-now, or a not-for-us? Either's a fine answer — just don't want to keep landing in your inbox.\n\nWhy: honest, gives control, one-word-answerable, ends the sequence cleanly.\n\n---\n\n## Before / after\n\n**1 — the me-first opener**\n\nBefore:\n> Hi Rachel, my name is [x] and I'm the founder of [company], a leading provider of high-quality research compounds. We help clinics like yours streamline their supply chain and unlock significant savings. I'd love to hop on a 30-minute call to walk you through our solutions!\n\nAfter:\n> Rachel — saw the second Dallas location go live last week. Clinics at that stage usually get quietly overcharged on peptide reorders. We run the same compounds with COAs on every lot, typically 30-40% under the incumbent. Worth a quick price comparison?\n\nWhy: killed the self-intro, \"leading provider,\" \"streamline,\" \"unlock,\" and the 30-minute demand. Led with a real observation, one credential, one soft ask.\n\n**2 — the fake-personal**\n\nBefore:\n> Hi there! I came across your profile and was really impressed by all the amazing work you're doing in the healthcare space. I think there could be some great synergy between our companies!\n\nAfter:\n> Rachel — you're listing peptides on the Newport site but not the two Dallas ones yet. If that's a supply gap and not a choice, that's usually the exact thing we fix. Want the details?\n\nWhy: \"impressed by your amazing work\" and \"synergy\" are template tells. The rewrite names a specific, checkable detail that could only come from looking.\n\n**3 — the stacked CTA**\n\nBefore:\n> Let me know if you'd be interested in learning more! You can book a call using the link below, or feel free to reply here, and I've also attached our full product catalog and pricing deck for your reference. Looking forward to connecting!\n\nAfter:\n> Want me to send the price comparison on the three SKUs you already list? Just say the word.\n\nWhy: four asks and two attachments collapsed to one tiny, one-word-answerable ask. No links to trip filters, no deck to ignore.\n\n---\n\n## Ship checklist\n\n- [ ] First line is about them, not you.\n- [ ] Exactly one real, specific, checkable observation — no fake flattery.\n- [ ] Value stated as an outcome, with a curiosity gap, not a feature dump.\n- [ ] Under 125 words; one phone screen; readable in 10 seconds.\n- [ ] One soft, low-friction, one-word-answerable CTA with a question mark.\n- [ ] One real credential or genuine value — zero hype, zero fake scarcity.\n- [ ] No links/attachments in first touch unless essential.\n- [ ] Follow-ups each carry a new angle; sequence caps at 3-4; ends with an honest break-up.\n- [ ] Run through write-human (no tells) and cross-check business-email structure.\n\nSources: [SmartLead — cold email templates that get replies](https://www.smartlead.ai/blog/cold-email-templates), [Autobound — cold email templates guide](https://www.autobound.ai/blog/cold-email-templates-guide), [SalesCaptain — cold email statistics 2025](https://www.salescaptain.io/blog/cold-email-statistics), [Salesmotion — cold outreach playbook](https://salesmotion.io/blog/cold-outreach-best-practices).\n","version":1,"of":1}]},{"id":"write_human","label":"Write-human skill — the sentence-level register","path":".claude/skills/write-human/SKILL.md","versions":[{"commit":"418230b8b","date":"2026-07-24T10:23:40-07:00","what_changed":"recency feed: SSR /latest (+json/txt), homepage head links + Journal anchor; post-to-x law corrected (sign every post, link+card every post, no first-person-as-owner, no attached images over links) #widgets-approved","chars":21785,"added":["**4. X — load the `post-to-x` skill and follow it.** EVERY post carries the article link (the og_card featured image renders as the preview — never attach an image over a link) and EVERY post carries the model signature `— Fable 5 (Claude Code)`. Never first person as the owner. The X voice is blunt, hook-first, one idea per post — never the em-dash essayist register. Full rules in `post-to-x` (its format section was corrected 2026-07-24 after it corrupted the SIGN_YOUR_WORK law)."],"removed":["**4. X — load the `post-to-x` skill and follow it.** Promote as a THREAD, not scattered lone posts, and every post ships with an image (`X_POST` takes `{text, image_url, reply_to}`). The X voice is NOT the article's voice: on X, blunt, lowercase-comfortable, hook-first, one idea per post, land the point — never the em-dash essayist \"the bind the whole trend sits on\" register, which reads as model slop. Sign the thread once, on the closing/link post. Full rules, voice examples, image prompts, and the thread procedure live in `post-to-x`."],"text":"---\nname: write-human\ndescription: Write and revise any prose so it reads as a sharp human wrote it, not an AI. Load before drafting or editing articles, emails, landing copy, DMs, captions, summaries, handoffs, or any owner-facing or public text — and always when a draft is about to ship. The anti-slop bible: kill the AI tells, vary rhythm, use concrete detail, pass the read-aloud test.\n---\n\n# write-human\n\nThe job: prose that a smart, busy human would actually write and send. Not \"polished.\" Not \"engaging.\" Human. That means it has a point of view, uneven rhythm, concrete nouns, and zero of the reflexes below.\n\nThis is a revision discipline as much as a drafting one. Draft, then hunt the tells and cut them. The tells are mechanical; catching them is mechanical too.\n\n---\n\n## Part 1 — The tell catalog (delete on sight)\n\nEach entry: the tell, why it reads as machine, the fix.\n\n### Punctuation and structure tells\n\n**1. Em-dash overuse.** The single strongest surface tell. AI sprinkles em dashes into every third sentence as a rhythm crutch. Humans use them sparingly, for a real interruption or a snapped-off aside. Rule: at most one em dash per ~200 words, and only where a comma, period, or colon genuinely won't do. When in doubt, split into two sentences. (Sources: Plagiarism Today, Inc., 2025.)\n\n**2. The \"It's not just X — it's Y\" construction.** And its cousins: \"This isn't about X. It's about Y.\" / \"More than a Z, it's a W.\" A hollow reveal structure that promises depth and delivers a synonym. Delete it. Say the actual thing once.\n\n**3. Rule-of-three everywhere.** \"Fast, reliable, and affordable.\" \"It informs, it delights, it converts.\" Triple-parallel lists are the metronome of AI prose. One list of three in a whole piece is fine. Three of them on a page is a fingerprint. Break the pattern: use two, use four, use one blunt item.\n\n**4. Triple-parallel sentence openers.** \"You'll learn X. You'll discover Y. You'll master Z.\" Anaphora on a schedule. Cut to one, or vary the verb and the structure.\n\n**5. The restating summary sentence.** A sentence that adds nothing but re-announces what you just said: \"In short, the key is consistency.\" / \"Ultimately, it all comes down to trust.\" Also the closing \"By doing X, you'll be well on your way to Y.\" Delete the recap. The last real sentence is the ending.\n\n**6. Over-transitioning.** \"Moreover,\" \"Furthermore,\" \"Additionally,\" \"That said,\" \"It's worth noting that,\" \"Importantly,\" at the head of nearly every paragraph. Humans let sentences sit next to each other without a signpost between each one. Cut ~80% of transition words. Keep the ones that carry real logical weight.\n\n**7. Uniform sentence length / metronomic rhythm.** The deepest tell — see Part 2. If every sentence is 15-22 words, it reads as generated even with every word swapped out.\n\n### Diction tells (words and phrases that scream machine)\n\n**8. The vocabulary blacklist.** Delete or replace on sight: delve, tapestry, testament (as in \"a testament to\"), navigate the landscape / navigating the complexities, boasts (of a product: \"boasts a 5-inch screen\"), elevate, seamless / seamlessly, unlock (unlock potential/value), leverage (as a verb), streamline, robust, comprehensive, cutting-edge, game-changer / game-changing, foster, harness, embark, realm, landscape (figurative), vibrant, bustling, myriad, plethora, meticulous / meticulously, resonate, underscore, pivotal, crucial, vital, essential (as filler), holistic, dynamic, innovative, revolutionize, empower, curate / curated, elevate, unleash, supercharge, \"at the forefront of,\" \"in the ever-evolving world of.\"\n\n**9. The setup phrases.** \"In today's world,\" \"In today's fast-paced world,\" \"In today's digital age,\" \"In this ever-changing landscape,\" \"Now more than ever,\" \"In the realm of,\" \"When it comes to,\" \"At the end of the day.\" These are throat-clearing. The reader wants the sentence that comes after. Delete the setup, keep the substance.\n\n**10. Fake-insight filler.** \"It's important to note that,\" \"It's worth mentioning that,\" \"One key aspect to consider is,\" \"This is a game-changer,\" \"The possibilities are endless,\" \"provides valuable insights,\" \"plays a crucial role in.\" Research finds a phrase like \"provides valuable insights\" appears vastly more often in AI text than in human writing (QuillBot, 2025). If a sentence's job is only to announce that the next thing matters, cut it and let the next thing matter on its own.\n\n**11. Adjective inflation.** \"comprehensive solution,\" \"robust framework,\" \"powerful tool,\" \"seamless experience,\" \"valuable resource.\" AI reaches for a laudatory adjective when a concrete noun would carry more. Strip the adjective or replace the phrase with the specific thing.\n\n**12. Forced enthusiasm and hype.** Exclamation points, \"Get ready to,\" \"You're going to love,\" \"The best part?\", \"Here's the kicker,\" \"And that's not all.\" Confidence is quiet. State the good thing plainly; let the reader be the one who's impressed.\n\n**13. Over-hedging.** \"may potentially,\" \"could possibly,\" \"might arguably,\" \"in many cases, it can often be the case that.\" Stacking qualifiers to avoid ever committing. Humans with a view say it. If it's true, say it's true. If it's uncertain, say \"I don't know\" or name the one real caveat once.\n\n### Shape tells\n\n**14. The balanced both-sides non-answer.** Presenting every pro and con and never landing. Humans have opinions: \"X is better than Y here, because Z.\" Take the position the evidence supports and defend it in a line.\n\n**15. The listicle reflex.** Turning every idea into a bulleted list with bolded lead-ins of exactly parallel length. Lists are for genuinely parallel items. Prose is for reasoning. Don't bullet an argument.\n\n**16. The \"treadmill.\"** Circling one idea across three paragraphs, restated with new adjectives, no new information added. Each paragraph must earn its place with a new fact, example, or move. If a paragraph only rephrases the one above it, delete it.\n\n**17. Perfect, sanded-flat neutrality.** No specific person, no real number, no dated example, no mild opinion, no rough edge. Humans leave fingerprints: a named tool, a real price, \"last Tuesday,\" a thing they think is overrated.\n\n---\n\n## Part 2 — Sentence rhythm (the deepest fix)\n\nYou can swap every blacklisted word and still sound like a machine if the rhythm is even. Human writing has a heartbeat, not a metronome.\n\n- **Vary length hard.** Put a 4-word sentence next to a 30-word one. Then a medium one. Read the length pattern, not just the words.\n- **Use fragments deliberately.** \"Not always. But often.\" A fragment is a human move; AI almost never makes it unprompted.\n- **Let one sentence run long** with real subordinate clauses when the thought genuinely has parts, then snap back short.\n- **Start sentences differently.** Not every sentence begins with the subject. Open sometimes with a time, a condition, a short verb, a \"But.\"\n- **Kill the comma-splice-plus-participle tic:** \"..., allowing you to...\", \"..., helping teams to...\", \"..., making it easy to...\" This trailing \"-ing\" clause is an AI signature. Cut it or make it its own sentence.\n\nTest: read only the sentence lengths as a sequence of numbers. If they cluster in a narrow band, break them.\n\n---\n\n## Part 3 — Concrete over abstract\n\nAbstraction is the water AI swims in. The cure is specificity.\n\n- Replace category words with instances. Not \"a beverage\" → \"a lukewarm gas-station coffee.\" Not \"stakeholders\" → \"the two people who sign the check.\"\n- Use real numbers, not \"many/several/a range of.\" \"40% of opens\" beats \"a significant portion.\"\n- Name the thing. Not \"leading tools\" → \"Ahrefs and Screaming Frog.\"\n- Prefer the verb that shows the action over the abstract noun. \"decide\" not \"make a determination,\" \"help\" not \"provide assistance.\"\n- One vivid concrete detail buys more credibility than three adjectives. Spend on detail, not on praise.\n\nHuman writers *interpret* — they tell you what a fact means and why they think so. AI *presents*. Add the interpretation.\n\n---\n\n## Part 4 — The read-aloud test (mandatory before shipping)\n\nRead the whole thing out loud, or subvocalize it at speaking pace. Flag and fix:\n\n- Any sentence you run out of breath on → split it.\n- Any phrase you'd never say to a colleague (\"navigate the ever-evolving landscape\") → rewrite in the words you'd actually use.\n- Any spot where two sentences in a row have the same shape → vary one.\n- Any sentence that made you feel nothing / added nothing → cut it.\n- Any place a transition word slowed you down → delete it.\n\nIf it doesn't sound like a person talking, it isn't done.\n\n---\n\n## Part 5 — Before / after\n\n**Example 1 — landing copy**\n\nBefore (AI):\n> In today's fast-paced digital landscape, our cutting-edge platform empowers businesses to seamlessly streamline their workflows and unlock unprecedented growth. It's not just a tool — it's a game-changer that will revolutionize the way you work.\n\nAfter (human):\n> Most teams lose an hour a day to copy-pasting between four tools. This connects them, so you stop. Setup takes about ten minutes.\n\nWhy: killed the setup phrase, the blacklist words (cutting-edge, empowers, seamlessly, streamline, unlock, game-changer, revolutionize), the \"not just X — it's Y,\" and the hype. Added a concrete number and a concrete claim.\n\n**Example 2 — email line**\n\nBefore (AI):\n> I wanted to reach out to touch base regarding a potential opportunity that I believe could provide significant value and help elevate your team's productivity to the next level.\n\nAfter (human):\n> Quick one: I think we can cut your onboarding time from three weeks to about five days. Worth fifteen minutes to show you how?\n\nWhy: killed \"reach out / touch base,\" \"provide significant value,\" \"elevate... to the next level.\" Named the actual before/after and a single small ask.\n\n**Example 3 — explanatory prose**\n\nBefore (AI):\n> There are several key factors to consider. First, it's important to note that consistency plays a crucial role. Moreover, quality is essential. Additionally, timing can be a pivotal element. Ultimately, by keeping these factors in mind, you'll be well on your way to success.\n\nAfter (human):\n> Three things move the needle, in order. Consistency first — showing up beats any single great post. Quality second. Timing barely matters until the other two are handled, so don't obsess over it early.\n\nWhy: killed \"several key factors,\" \"it's important to note,\" \"plays a crucial role,\" \"Moreover/Additionally,\" \"pivotal,\" the restating summary, and the both-sides flatness. Added rank-ordering (a real opinion), a fragment, and varied sentence length.\n\n**Example 4 — the recap-sentence delete**\n\nBefore:\n> ...and that's how the caching layer cuts response time. In short, caching is a powerful way to make your app faster, and by implementing it correctly, you can deliver a seamless experience for your users.\n\nAfter:\n> ...and that's how the caching layer cuts response time.\n\nWhy: the recap sentence added nothing. The last real sentence was already the ending.\n\n---\n\n## Part 6 — The miscsubjects article register: logic-first, human-voiced (Law added 2026-07-24)\n\nThe house format for articles is a HYBRID: the structure of an evidence review, the voice of a person. The whole point is that a model can be told \"make an article about this incident\" and produce this shape without further prompting.\n\n**Structure (logic-first, non-negotiable):**\n- Open with what happened, in one dense factual paragraph. No scene-setting, no throat-clearing.\n- Every claim files under its evidence, and every source EMBEDS IN PLACE via `[[embed:source:sN]]` on its own line, in its native platform widget, at the exact point in the argument where the reader needs it. Never a sources sidebar, never a link wall.\n- Sequence by evidentiary weight: the primary documentary record first (the org that lived it), then named testimony, then independent reporting, then the social/contested layer, then color (memes, culture), then any analytical construction — flagged as such.\n- Say plainly what did NOT happen when headlines invite the wrong reading.\n\n**Headers (the robotic-header ban):**\n- A sub-header is said the way a person would say it across a table, and it carries a FINDING, not a category. \"What Hugging Face said\" is a filing label — robotic. \"The company that got hacked wrote it up first\" is a finding with a voice.\n- Ban the parallel-template series: \"What X said / What Y said / What Z said\" or \"The A reaction / The B reaction.\" If two headers share a grammatical template, rewrite one.\n- A header may be a sentence, may take a stance, may be short (\"Hours, not weeks\"). It may never be a taxonomy node.\n\n**Voice:** everything in Parts 1–5 applies. Concrete numbers over adjectives, one position where the evidence takes one, uneven rhythm, the read-aloud test. The register is a sharp writer walking you through a file of evidence — never a report generator filling slots.\n\n**Worked examples live at** `/a/openai-huggingface-hack-2026` (incident/evidence review) and `/a/the-canonical-morgh-index` (essay with embedded scholarly widgets). Widget contract: see the design-law skill.\n\n---\n\n## Ship checklist\n\n- [ ] Em dashes: ≤1 per 200 words, each earned.\n- [ ] Zero blacklist words (Part 1, #8-9).\n- [ ] No \"not just X — it's Y,\" no setup phrases, no fake-insight filler.\n- [ ] At most one rule-of-three in the whole piece.\n- [ ] No restating summary sentence at the end.\n- [ ] Transition words cut ~80%.\n- [ ] Sentence lengths vary visibly; at least one fragment or one short punch.\n- [ ] At least one concrete number/name/detail per paragraph.\n- [ ] A real position taken where the topic has one.\n- [ ] Read aloud, start to finish, and it sounds like a person.\n\nSources: [Inc. — sentence structure as AI tell](https://www.inc.com/jessica-stillman/this-sentence-structure-is-a-dead-giveaway-that-ai-wrote-it/91275389), [Plagiarism Today — em dashes and AI](https://www.plagiarismtoday.com/2025/06/26/em-dashes-hyphens-and-spotting-ai-writing/), [QuillBot — make AI writing sound human](https://quillbot.com/blog/ai-writing-tools/make-ai-writing-sound-human/), [vrid.ai — 27 signs of AI writing](https://vrid.ai/blog/signs-of-ai-writing), [Hunting the Muse — how to tell if writing is AI](https://huntingthemuse.net/library/how-to-tell-if-writing-is-ai).\n\n---\n\n## Part 7 — The full publish pipeline (replicable end to end)\n\nAn article is not shipped when the prose is done. It is shipped when a stranger can find it, share it, and see it whole. Run every step, in order, every time.\n\n**0. Boundary check (before writing anything).** POST `{title, markdown-or-outline}` to `/api/protocol/article-boundary`. The formula: an article is a SUBJECT (one entity or one bounded event), never a length. Verdict `append` → write DIVs into the existing page. `append_as_section` → collapsible section on the host page (it graduates to its own page past ~14 DIVs). `new_article` → proceed. An event binding two or more existing subjects (an incident, a lawsuit, an acquisition) is always its own page, cross-referenced from each subject page.\n\n**0b. REFERENCE-FIRST LAW (owner, 2026-07-24).** A comparison or positioning article (\"is it X\", \"the build vs Y\", any piece that leans on an external system) is a build failure if the reader has to already know what X is. Before writing it, check /a/ for a standalone reference article on EACH named external system (LangChain, MCP, A2A, AgentKit, ...). Missing one → write the reference article FIRST: zero mentions of the build, primary sources only (official docs, repos, changelogs — never content farms or SEO listicles), its own claims and sources. The comparison article then cites and links the reference articles instead of smuggling in its own thin context. A challenge to a reference propagates to every comparison built on it.\n\n**1. Featured image (default, not optional) — as a finished CARD, not raw art.** Generate with ArcAds using the GPT image model: `ARCADS_GENERATE gpt-image|<prompt>|16:9|||true`, then `ARCADS_TO_R2 <arcads_id>|gpt-image`. Prompt shape that works: minimal editorial illustration of the article's central mechanism, ink-black and paper-white with one indigo accent, generous negative space, flat vector, \"no text, no letters, no logos, no watermark\". **1x1 SOCIAL CARD LAW (owner, 2026-07-24): the on-page hero stays 16:9, but the SOCIAL card (meta.og_card, X post attachments) is a 1:1 SQUARE (1024x1024) — center-crop the hero, re-composite the title pill + signature band onto the square. Square renders correctly on X mobile and small screens; the 16:9 card gets cropped badly.** Then COMPOSITE the article title + a model signature onto it (the Morgh Index card is the reference: title pill bottom-left, \"— <Model>\" byline band) so the social card reads as a designed piece, not a bare illustration — a raw illustration makes a shitty-looking X card. Upload the composited card to R2 and set it as `meta.hero`; that drives the on-page hero AND og:image/twitter:image. LOOK at the final card before using it. Set the hero BEFORE any X post — X scrapes og:image at post time and caches it; post first and the card comes up blank.\n\n**2. Write the article** under Part 6 (logic-first, sources embedded in place, headers that carry findings). Real, sourced material only — never a fabricated event, never a planted fallacy, never invented content \"to demonstrate\" any site machinery (NO_FABRICATED_LIVE_CONTENT law; intake refuses self-declared plants with `planted_content_refused`). A worked example of a bad inference uses a genuinely bad inference found in the wild.\n\n**2a. PRESENTATION LAW — the article must LOOK like `/a/openai-huggingface-hack-2026`, or it is a catastrophic failure (2026-07-24).** The reference article is the only acceptable presentation bar. Two things make it that, and both are mandatory, not nice-to-have:\n- **The page renders your WRITTEN body, never an auto-composed claim-list.** `meta.prefer_stored` MUST be `true` (document intake now sets it by default; never unset it, and if you build meta by hand, set it). The failure signature: the live page shows your claims as a stack of one-sentence paragraphs with `◈` dividers and a source carousel bolted on the end. That is the \"AI slop\" look. If you see it, `prefer_stored` is missing — the site threw your writing away.\n- **Real evidence is embedded INLINE through the argument, densely.** The reference has ~15 `[[embed:source:sN]]` woven between paragraphs — a real tweet, a news masthead card, a thread — appearing exactly where the point lands. Minimum bar: a findings-header every 2–3 paragraphs, and a real embedded source after MOST sections. Include real TWEETS for texture (find them via X_SEARCH or a web search restricted to x.com; embed the real URL/author/quote — NEVER fabricate a tweet). A prose wall with sources only in a bottom rail is a blurb, not an article.\nMeasure this on the HUMAN side (the rendered page), never by counting sources in the meta. Do NOT report \"N sources\" as evidence of quality — open the page and compare it to the reference.\n\n**3. Chrome + SEO verify (curl, not vibes) — and re-verify after EVERY post-publish mutation (2026-07-24).** The page's verified state dies the moment anything touches the article again: a voxel edit, a claims/sources batch, a raw meta write. The owner clicked two fresh articles during that mutation window and got errors. The gate: (a) all edits, sources, claims, meta.hero/og_card land FIRST; (b) THEN smoke: hit /a/<slug> 3+ times (fresh, no cache) plus /api/articles/<slug>/bundle plus the hero and og_card URLs, all 200 with full bodies; (c) ONLY THEN post links to X or hand them to the owner. A link shipped before the final smoke pass is a link to an unverified page.\nTwo additions (owner + reviewer, 2026-07-24):\n- **Sequential 200s do not clear a race.** If the suspected failure is publish/mutate/read interleaving, run the concurrent form once: fire the mutation while a parallel client hammers reads; assert no 5xx, no partial body, no stale-hash serve. (A real intermittent 503 was caught on /a/what-is-langchain only on a cold parallel-ish hit after 15 sequential 200s.)\n- **200-with-full-body can still lie.** The rendered HTML's claim/source counts MUST match the API: server-rendered source cards and claims == /api/articles/<slug>/sources and /claims counts. A page whose meta says 0 sources while the API holds 10 passes a status-code smoke and deceives every non-JS reader (crawlers, models). Compare the counts, not just the codes. The rendered page must carry: byline dates (posted/updated + read time), the hero figure, `<link rel=\"canonical\">`, `og:image` = the hero, `article:published_time`, JSON-LD Article, `<meta name=\"robots\" content=\"index,follow...\">`. The share widget (robot button, bottom right) must show the article title, Copy link, Copy for LLM, Share on X.\n\n**4. X — load the `post-to-x` skill and follow it.** EVERY post carries the article link (the og_card featured image renders as the preview — never attach an image over a link) and EVERY post carries the model signature `— Fable 5 (Claude Code)`. Never first person as the owner. The X voice is blunt, hook-first, one idea per post — never the em-dash essayist register. Full rules in `post-to-x` (its format section was corrected 2026-07-24 after it corrupted the SIGN_YOUR_WORK law).\n\n**5. Close the loop.** Open the rendered page and read it as a stranger would, side by side with `/a/openai-huggingface-hack-2026`. If it reads thinner — fewer inline tweets/cards, fewer findings-headers, or (fatal) a claim-list instead of prose — it is NOT shipped; go back to 2a. Screenshot desktop AND mobile. Append STATE.md. If anything was skipped, say which step is open.\n","version":9,"of":9},{"commit":"3121c74ed","date":"2026-07-24T09:25:47-07:00","what_changed":"reference-first + 1x1 card laws in write-human; STATE: reference layer, Book rename, builder queue","chars":21842,"added":["**0b. REFERENCE-FIRST LAW (owner, 2026-07-24).** A comparison or positioning article (\"is it X\", \"the build vs Y\", any piece that leans on an external system) is a build failure if the reader has to already know what X is. Before writing it, check /a/ for a standalone reference article on EACH named external system (LangChain, MCP, A2A, AgentKit, ...). Missing one → write the reference article FIRST: zero mentions of the build, primary sources only (official docs, repos, changelogs — never content farms or SEO listicles), its own claims and sources. The comparison article then cites and links the reference articles instead of smuggling in its own thin context. A challenge to a reference propagates to every comparison built on it.","**1. Featured image (default, not optional) — as a finished CARD, not raw art.** Generate with ArcAds using the GPT image model: `ARCADS_GENERATE gpt-image|<prompt>|16:9|||true`, then `ARCADS_TO_R2 <arcads_id>|gpt-image`. Prompt shape that works: minimal editorial illustration of the article's central mechanism, ink-black and paper-white with one indigo accent, generous negative space, flat vector, \"no text, no letters, no logos, no watermark\". **1x1 SOCIAL CARD LAW (owner, 2026-07-24): the on-page hero stays 16:9, but the SOCIAL card (meta.og_card, X post attachments) is a 1:1 SQUARE (1024x1024) — center-crop the hero, re-composite the title pill + signature band onto the square. Square renders correctly on X mobile and small screens; the 16:9 card gets cropped badly.** Then COMPOSITE the article title + a model signature onto it (the Morgh Index card is the reference: title pill bottom-left, \"— <Model>\" byline band) so the social card reads as a designed piece, not a bare illustration — a raw illustration makes a shitty-looking X card. Upload the composited card to R2 and set it as `meta.hero`; that drives the on-page hero AND og:image/twitter:image. LOOK at the final card before using it. Set the hero BEFORE any X post — X scrapes og:image at post time and caches it; post first and the card comes up blank.","**3. Chrome + SEO verify (curl, not vibes) — and re-verify after EVERY post-publish mutation (2026-07-24).** The page's verified state dies the moment anything touches the article again: a voxel edit, a claims/sources batch, a raw meta write. The owner clicked two fresh articles during that mutation window and got errors. The gate: (a) all edits, sources, claims, meta.hero/og_card land FIRST; (b) THEN smoke: hit /a/<slug> 3+ times (fresh, no cache) plus /api/articles/<slug>/bundle plus the hero and og_card URLs, all 200 with full bodies; (c) ONLY THEN post links to X or hand them to the owner. A link shipped before the final smoke pass is a link to an unverified page.","Two additions (owner + reviewer, 2026-07-24):","- **Sequential 200s do not clear a race.** If the suspected failure is publish/mutate/read interleaving, run the concurrent form once: fire the mutation while a parallel client hammers reads; assert no 5xx, no partial body, no stale-hash serve. (A real intermittent 503 was caught on /a/what-is-langchain only on a cold parallel-ish hit after 15 sequential 200s.)","- **200-with-full-body can still lie.** The rendered HTML's claim/source counts MUST match the API: server-rendered source cards and claims == /api/articles/<slug>/sources and /claims counts. A page whose meta says 0 sources while the API holds 10 passes a status-code smoke and deceives every non-JS reader (crawlers, models). Compare the counts, not just the codes. The rendered page must carry: byline dates (posted/updated + read time), the hero figure, `<link rel=\"canonical\">`, `og:image` = the hero, `article:published_time`, JSON-LD Article, `<meta name=\"robots\" content=\"index,follow...\">`. The share widget (robot button, bottom right) must show the article title, Copy link, Copy for LLM, Share on X."],"removed":["**1. Featured image (default, not optional) — as a finished CARD, not raw art.** Generate with ArcAds using the GPT image model: `ARCADS_GENERATE gpt-image|<prompt>|16:9|||true`, then `ARCADS_TO_R2 <arcads_id>|gpt-image`. Prompt shape that works: minimal editorial illustration of the article's central mechanism, ink-black and paper-white with one indigo accent, generous negative space, flat vector, \"no text, no letters, no logos, no watermark\". Then COMPOSITE the article title + a model signature onto it (the Morgh Index card is the reference: title pill bottom-left, \"— <Model>\" byline band) so the social card reads as a designed piece, not a bare illustration — a raw illustration makes a shitty-looking X card. Upload the composited card to R2 and set it as `meta.hero`; that drives the on-page hero AND og:image/twitter:image. LOOK at the final card before using it. Set the hero BEFORE any X post — X scrapes og:image at post time and caches it; post first and the card comes up blank.","**3. Chrome + SEO verify (curl, not vibes) — and re-verify after EVERY post-publish mutation (2026-07-24).** The page's verified state dies the moment anything touches the article again: a voxel edit, a claims/sources batch, a raw meta write. The owner clicked two fresh articles during that mutation window and got errors. The gate: (a) all edits, sources, claims, meta.hero/og_card land FIRST; (b) THEN smoke: hit /a/<slug> 3+ times (fresh, no cache) plus /api/articles/<slug>/bundle plus the hero and og_card URLs, all 200 with full bodies; (c) ONLY THEN post links to X or hand them to the owner. A link shipped before the final smoke pass is a link to an unverified page. The rendered page must carry: byline dates (posted/updated + read time), the hero figure, `<link rel=\"canonical\">`, `og:image` = the hero, `article:published_time`, JSON-LD Article, `<meta name=\"robots\" content=\"index,follow...\">`. The share widget (robot button, bottom right) must show the article title, Copy link, Copy for LLM, Share on X."],"text":"---\nname: write-human\ndescription: Write and revise any prose so it reads as a sharp human wrote it, not an AI. Load before drafting or editing articles, emails, landing copy, DMs, captions, summaries, handoffs, or any owner-facing or public text — and always when a draft is about to ship. The anti-slop bible: kill the AI tells, vary rhythm, use concrete detail, pass the read-aloud test.\n---\n\n# write-human\n\nThe job: prose that a smart, busy human would actually write and send. Not \"polished.\" Not \"engaging.\" Human. That means it has a point of view, uneven rhythm, concrete nouns, and zero of the reflexes below.\n\nThis is a revision discipline as much as a drafting one. Draft, then hunt the tells and cut them. The tells are mechanical; catching them is mechanical too.\n\n---\n\n## Part 1 — The tell catalog (delete on sight)\n\nEach entry: the tell, why it reads as machine, the fix.\n\n### Punctuation and structure tells\n\n**1. Em-dash overuse.** The single strongest surface tell. AI sprinkles em dashes into every third sentence as a rhythm crutch. Humans use them sparingly, for a real interruption or a snapped-off aside. Rule: at most one em dash per ~200 words, and only where a comma, period, or colon genuinely won't do. When in doubt, split into two sentences. (Sources: Plagiarism Today, Inc., 2025.)\n\n**2. The \"It's not just X — it's Y\" construction.** And its cousins: \"This isn't about X. It's about Y.\" / \"More than a Z, it's a W.\" A hollow reveal structure that promises depth and delivers a synonym. Delete it. Say the actual thing once.\n\n**3. Rule-of-three everywhere.** \"Fast, reliable, and affordable.\" \"It informs, it delights, it converts.\" Triple-parallel lists are the metronome of AI prose. One list of three in a whole piece is fine. Three of them on a page is a fingerprint. Break the pattern: use two, use four, use one blunt item.\n\n**4. Triple-parallel sentence openers.** \"You'll learn X. You'll discover Y. You'll master Z.\" Anaphora on a schedule. Cut to one, or vary the verb and the structure.\n\n**5. The restating summary sentence.** A sentence that adds nothing but re-announces what you just said: \"In short, the key is consistency.\" / \"Ultimately, it all comes down to trust.\" Also the closing \"By doing X, you'll be well on your way to Y.\" Delete the recap. The last real sentence is the ending.\n\n**6. Over-transitioning.** \"Moreover,\" \"Furthermore,\" \"Additionally,\" \"That said,\" \"It's worth noting that,\" \"Importantly,\" at the head of nearly every paragraph. Humans let sentences sit next to each other without a signpost between each one. Cut ~80% of transition words. Keep the ones that carry real logical weight.\n\n**7. Uniform sentence length / metronomic rhythm.** The deepest tell — see Part 2. If every sentence is 15-22 words, it reads as generated even with every word swapped out.\n\n### Diction tells (words and phrases that scream machine)\n\n**8. The vocabulary blacklist.** Delete or replace on sight: delve, tapestry, testament (as in \"a testament to\"), navigate the landscape / navigating the complexities, boasts (of a product: \"boasts a 5-inch screen\"), elevate, seamless / seamlessly, unlock (unlock potential/value), leverage (as a verb), streamline, robust, comprehensive, cutting-edge, game-changer / game-changing, foster, harness, embark, realm, landscape (figurative), vibrant, bustling, myriad, plethora, meticulous / meticulously, resonate, underscore, pivotal, crucial, vital, essential (as filler), holistic, dynamic, innovative, revolutionize, empower, curate / curated, elevate, unleash, supercharge, \"at the forefront of,\" \"in the ever-evolving world of.\"\n\n**9. The setup phrases.** \"In today's world,\" \"In today's fast-paced world,\" \"In today's digital age,\" \"In this ever-changing landscape,\" \"Now more than ever,\" \"In the realm of,\" \"When it comes to,\" \"At the end of the day.\" These are throat-clearing. The reader wants the sentence that comes after. Delete the setup, keep the substance.\n\n**10. Fake-insight filler.** \"It's important to note that,\" \"It's worth mentioning that,\" \"One key aspect to consider is,\" \"This is a game-changer,\" \"The possibilities are endless,\" \"provides valuable insights,\" \"plays a crucial role in.\" Research finds a phrase like \"provides valuable insights\" appears vastly more often in AI text than in human writing (QuillBot, 2025). If a sentence's job is only to announce that the next thing matters, cut it and let the next thing matter on its own.\n\n**11. Adjective inflation.** \"comprehensive solution,\" \"robust framework,\" \"powerful tool,\" \"seamless experience,\" \"valuable resource.\" AI reaches for a laudatory adjective when a concrete noun would carry more. Strip the adjective or replace the phrase with the specific thing.\n\n**12. Forced enthusiasm and hype.** Exclamation points, \"Get ready to,\" \"You're going to love,\" \"The best part?\", \"Here's the kicker,\" \"And that's not all.\" Confidence is quiet. State the good thing plainly; let the reader be the one who's impressed.\n\n**13. Over-hedging.** \"may potentially,\" \"could possibly,\" \"might arguably,\" \"in many cases, it can often be the case that.\" Stacking qualifiers to avoid ever committing. Humans with a view say it. If it's true, say it's true. If it's uncertain, say \"I don't know\" or name the one real caveat once.\n\n### Shape tells\n\n**14. The balanced both-sides non-answer.** Presenting every pro and con and never landing. Humans have opinions: \"X is better than Y here, because Z.\" Take the position the evidence supports and defend it in a line.\n\n**15. The listicle reflex.** Turning every idea into a bulleted list with bolded lead-ins of exactly parallel length. Lists are for genuinely parallel items. Prose is for reasoning. Don't bullet an argument.\n\n**16. The \"treadmill.\"** Circling one idea across three paragraphs, restated with new adjectives, no new information added. Each paragraph must earn its place with a new fact, example, or move. If a paragraph only rephrases the one above it, delete it.\n\n**17. Perfect, sanded-flat neutrality.** No specific person, no real number, no dated example, no mild opinion, no rough edge. Humans leave fingerprints: a named tool, a real price, \"last Tuesday,\" a thing they think is overrated.\n\n---\n\n## Part 2 — Sentence rhythm (the deepest fix)\n\nYou can swap every blacklisted word and still sound like a machine if the rhythm is even. Human writing has a heartbeat, not a metronome.\n\n- **Vary length hard.** Put a 4-word sentence next to a 30-word one. Then a medium one. Read the length pattern, not just the words.\n- **Use fragments deliberately.** \"Not always. But often.\" A fragment is a human move; AI almost never makes it unprompted.\n- **Let one sentence run long** with real subordinate clauses when the thought genuinely has parts, then snap back short.\n- **Start sentences differently.** Not every sentence begins with the subject. Open sometimes with a time, a condition, a short verb, a \"But.\"\n- **Kill the comma-splice-plus-participle tic:** \"..., allowing you to...\", \"..., helping teams to...\", \"..., making it easy to...\" This trailing \"-ing\" clause is an AI signature. Cut it or make it its own sentence.\n\nTest: read only the sentence lengths as a sequence of numbers. If they cluster in a narrow band, break them.\n\n---\n\n## Part 3 — Concrete over abstract\n\nAbstraction is the water AI swims in. The cure is specificity.\n\n- Replace category words with instances. Not \"a beverage\" → \"a lukewarm gas-station coffee.\" Not \"stakeholders\" → \"the two people who sign the check.\"\n- Use real numbers, not \"many/several/a range of.\" \"40% of opens\" beats \"a significant portion.\"\n- Name the thing. Not \"leading tools\" → \"Ahrefs and Screaming Frog.\"\n- Prefer the verb that shows the action over the abstract noun. \"decide\" not \"make a determination,\" \"help\" not \"provide assistance.\"\n- One vivid concrete detail buys more credibility than three adjectives. Spend on detail, not on praise.\n\nHuman writers *interpret* — they tell you what a fact means and why they think so. AI *presents*. Add the interpretation.\n\n---\n\n## Part 4 — The read-aloud test (mandatory before shipping)\n\nRead the whole thing out loud, or subvocalize it at speaking pace. Flag and fix:\n\n- Any sentence you run out of breath on → split it.\n- Any phrase you'd never say to a colleague (\"navigate the ever-evolving landscape\") → rewrite in the words you'd actually use.\n- Any spot where two sentences in a row have the same shape → vary one.\n- Any sentence that made you feel nothing / added nothing → cut it.\n- Any place a transition word slowed you down → delete it.\n\nIf it doesn't sound like a person talking, it isn't done.\n\n---\n\n## Part 5 — Before / after\n\n**Example 1 — landing copy**\n\nBefore (AI):\n> In today's fast-paced digital landscape, our cutting-edge platform empowers businesses to seamlessly streamline their workflows and unlock unprecedented growth. It's not just a tool — it's a game-changer that will revolutionize the way you work.\n\nAfter (human):\n> Most teams lose an hour a day to copy-pasting between four tools. This connects them, so you stop. Setup takes about ten minutes.\n\nWhy: killed the setup phrase, the blacklist words (cutting-edge, empowers, seamlessly, streamline, unlock, game-changer, revolutionize), the \"not just X — it's Y,\" and the hype. Added a concrete number and a concrete claim.\n\n**Example 2 — email line**\n\nBefore (AI):\n> I wanted to reach out to touch base regarding a potential opportunity that I believe could provide significant value and help elevate your team's productivity to the next level.\n\nAfter (human):\n> Quick one: I think we can cut your onboarding time from three weeks to about five days. Worth fifteen minutes to show you how?\n\nWhy: killed \"reach out / touch base,\" \"provide significant value,\" \"elevate... to the next level.\" Named the actual before/after and a single small ask.\n\n**Example 3 — explanatory prose**\n\nBefore (AI):\n> There are several key factors to consider. First, it's important to note that consistency plays a crucial role. Moreover, quality is essential. Additionally, timing can be a pivotal element. Ultimately, by keeping these factors in mind, you'll be well on your way to success.\n\nAfter (human):\n> Three things move the needle, in order. Consistency first — showing up beats any single great post. Quality second. Timing barely matters until the other two are handled, so don't obsess over it early.\n\nWhy: killed \"several key factors,\" \"it's important to note,\" \"plays a crucial role,\" \"Moreover/Additionally,\" \"pivotal,\" the restating summary, and the both-sides flatness. Added rank-ordering (a real opinion), a fragment, and varied sentence length.\n\n**Example 4 — the recap-sentence delete**\n\nBefore:\n> ...and that's how the caching layer cuts response time. In short, caching is a powerful way to make your app faster, and by implementing it correctly, you can deliver a seamless experience for your users.\n\nAfter:\n> ...and that's how the caching layer cuts response time.\n\nWhy: the recap sentence added nothing. The last real sentence was already the ending.\n\n---\n\n## Part 6 — The miscsubjects article register: logic-first, human-voiced (Law added 2026-07-24)\n\nThe house format for articles is a HYBRID: the structure of an evidence review, the voice of a person. The whole point is that a model can be told \"make an article about this incident\" and produce this shape without further prompting.\n\n**Structure (logic-first, non-negotiable):**\n- Open with what happened, in one dense factual paragraph. No scene-setting, no throat-clearing.\n- Every claim files under its evidence, and every source EMBEDS IN PLACE via `[[embed:source:sN]]` on its own line, in its native platform widget, at the exact point in the argument where the reader needs it. Never a sources sidebar, never a link wall.\n- Sequence by evidentiary weight: the primary documentary record first (the org that lived it), then named testimony, then independent reporting, then the social/contested layer, then color (memes, culture), then any analytical construction — flagged as such.\n- Say plainly what did NOT happen when headlines invite the wrong reading.\n\n**Headers (the robotic-header ban):**\n- A sub-header is said the way a person would say it across a table, and it carries a FINDING, not a category. \"What Hugging Face said\" is a filing label — robotic. \"The company that got hacked wrote it up first\" is a finding with a voice.\n- Ban the parallel-template series: \"What X said / What Y said / What Z said\" or \"The A reaction / The B reaction.\" If two headers share a grammatical template, rewrite one.\n- A header may be a sentence, may take a stance, may be short (\"Hours, not weeks\"). It may never be a taxonomy node.\n\n**Voice:** everything in Parts 1–5 applies. Concrete numbers over adjectives, one position where the evidence takes one, uneven rhythm, the read-aloud test. The register is a sharp writer walking you through a file of evidence — never a report generator filling slots.\n\n**Worked examples live at** `/a/openai-huggingface-hack-2026` (incident/evidence review) and `/a/the-canonical-morgh-index` (essay with embedded scholarly widgets). Widget contract: see the design-law skill.\n\n---\n\n## Ship checklist\n\n- [ ] Em dashes: ≤1 per 200 words, each earned.\n- [ ] Zero blacklist words (Part 1, #8-9).\n- [ ] No \"not just X — it's Y,\" no setup phrases, no fake-insight filler.\n- [ ] At most one rule-of-three in the whole piece.\n- [ ] No restating summary sentence at the end.\n- [ ] Transition words cut ~80%.\n- [ ] Sentence lengths vary visibly; at least one fragment or one short punch.\n- [ ] At least one concrete number/name/detail per paragraph.\n- [ ] A real position taken where the topic has one.\n- [ ] Read aloud, start to finish, and it sounds like a person.\n\nSources: [Inc. — sentence structure as AI tell](https://www.inc.com/jessica-stillman/this-sentence-structure-is-a-dead-giveaway-that-ai-wrote-it/91275389), [Plagiarism Today — em dashes and AI](https://www.plagiarismtoday.com/2025/06/26/em-dashes-hyphens-and-spotting-ai-writing/), [QuillBot — make AI writing sound human](https://quillbot.com/blog/ai-writing-tools/make-ai-writing-sound-human/), [vrid.ai — 27 signs of AI writing](https://vrid.ai/blog/signs-of-ai-writing), [Hunting the Muse — how to tell if writing is AI](https://huntingthemuse.net/library/how-to-tell-if-writing-is-ai).\n\n---\n\n## Part 7 — The full publish pipeline (replicable end to end)\n\nAn article is not shipped when the prose is done. It is shipped when a stranger can find it, share it, and see it whole. Run every step, in order, every time.\n\n**0. Boundary check (before writing anything).** POST `{title, markdown-or-outline}` to `/api/protocol/article-boundary`. The formula: an article is a SUBJECT (one entity or one bounded event), never a length. Verdict `append` → write DIVs into the existing page. `append_as_section` → collapsible section on the host page (it graduates to its own page past ~14 DIVs). `new_article` → proceed. An event binding two or more existing subjects (an incident, a lawsuit, an acquisition) is always its own page, cross-referenced from each subject page.\n\n**0b. REFERENCE-FIRST LAW (owner, 2026-07-24).** A comparison or positioning article (\"is it X\", \"the build vs Y\", any piece that leans on an external system) is a build failure if the reader has to already know what X is. Before writing it, check /a/ for a standalone reference article on EACH named external system (LangChain, MCP, A2A, AgentKit, ...). Missing one → write the reference article FIRST: zero mentions of the build, primary sources only (official docs, repos, changelogs — never content farms or SEO listicles), its own claims and sources. The comparison article then cites and links the reference articles instead of smuggling in its own thin context. A challenge to a reference propagates to every comparison built on it.\n\n**1. Featured image (default, not optional) — as a finished CARD, not raw art.** Generate with ArcAds using the GPT image model: `ARCADS_GENERATE gpt-image|<prompt>|16:9|||true`, then `ARCADS_TO_R2 <arcads_id>|gpt-image`. Prompt shape that works: minimal editorial illustration of the article's central mechanism, ink-black and paper-white with one indigo accent, generous negative space, flat vector, \"no text, no letters, no logos, no watermark\". **1x1 SOCIAL CARD LAW (owner, 2026-07-24): the on-page hero stays 16:9, but the SOCIAL card (meta.og_card, X post attachments) is a 1:1 SQUARE (1024x1024) — center-crop the hero, re-composite the title pill + signature band onto the square. Square renders correctly on X mobile and small screens; the 16:9 card gets cropped badly.** Then COMPOSITE the article title + a model signature onto it (the Morgh Index card is the reference: title pill bottom-left, \"— <Model>\" byline band) so the social card reads as a designed piece, not a bare illustration — a raw illustration makes a shitty-looking X card. Upload the composited card to R2 and set it as `meta.hero`; that drives the on-page hero AND og:image/twitter:image. LOOK at the final card before using it. Set the hero BEFORE any X post — X scrapes og:image at post time and caches it; post first and the card comes up blank.\n\n**2. Write the article** under Part 6 (logic-first, sources embedded in place, headers that carry findings). Real, sourced material only — never a fabricated event, never a planted fallacy, never invented content \"to demonstrate\" any site machinery (NO_FABRICATED_LIVE_CONTENT law; intake refuses self-declared plants with `planted_content_refused`). A worked example of a bad inference uses a genuinely bad inference found in the wild.\n\n**2a. PRESENTATION LAW — the article must LOOK like `/a/openai-huggingface-hack-2026`, or it is a catastrophic failure (2026-07-24).** The reference article is the only acceptable presentation bar. Two things make it that, and both are mandatory, not nice-to-have:\n- **The page renders your WRITTEN body, never an auto-composed claim-list.** `meta.prefer_stored` MUST be `true` (document intake now sets it by default; never unset it, and if you build meta by hand, set it). The failure signature: the live page shows your claims as a stack of one-sentence paragraphs with `◈` dividers and a source carousel bolted on the end. That is the \"AI slop\" look. If you see it, `prefer_stored` is missing — the site threw your writing away.\n- **Real evidence is embedded INLINE through the argument, densely.** The reference has ~15 `[[embed:source:sN]]` woven between paragraphs — a real tweet, a news masthead card, a thread — appearing exactly where the point lands. Minimum bar: a findings-header every 2–3 paragraphs, and a real embedded source after MOST sections. Include real TWEETS for texture (find them via X_SEARCH or a web search restricted to x.com; embed the real URL/author/quote — NEVER fabricate a tweet). A prose wall with sources only in a bottom rail is a blurb, not an article.\nMeasure this on the HUMAN side (the rendered page), never by counting sources in the meta. Do NOT report \"N sources\" as evidence of quality — open the page and compare it to the reference.\n\n**3. Chrome + SEO verify (curl, not vibes) — and re-verify after EVERY post-publish mutation (2026-07-24).** The page's verified state dies the moment anything touches the article again: a voxel edit, a claims/sources batch, a raw meta write. The owner clicked two fresh articles during that mutation window and got errors. The gate: (a) all edits, sources, claims, meta.hero/og_card land FIRST; (b) THEN smoke: hit /a/<slug> 3+ times (fresh, no cache) plus /api/articles/<slug>/bundle plus the hero and og_card URLs, all 200 with full bodies; (c) ONLY THEN post links to X or hand them to the owner. A link shipped before the final smoke pass is a link to an unverified page.\nTwo additions (owner + reviewer, 2026-07-24):\n- **Sequential 200s do not clear a race.** If the suspected failure is publish/mutate/read interleaving, run the concurrent form once: fire the mutation while a parallel client hammers reads; assert no 5xx, no partial body, no stale-hash serve. (A real intermittent 503 was caught on /a/what-is-langchain only on a cold parallel-ish hit after 15 sequential 200s.)\n- **200-with-full-body can still lie.** The rendered HTML's claim/source counts MUST match the API: server-rendered source cards and claims == /api/articles/<slug>/sources and /claims counts. A page whose meta says 0 sources while the API holds 10 passes a status-code smoke and deceives every non-JS reader (crawlers, models). Compare the counts, not just the codes. The rendered page must carry: byline dates (posted/updated + read time), the hero figure, `<link rel=\"canonical\">`, `og:image` = the hero, `article:published_time`, JSON-LD Article, `<meta name=\"robots\" content=\"index,follow...\">`. The share widget (robot button, bottom right) must show the article title, Copy link, Copy for LLM, Share on X.\n\n**4. X — load the `post-to-x` skill and follow it.** Promote as a THREAD, not scattered lone posts, and every post ships with an image (`X_POST` takes `{text, image_url, reply_to}`). The X voice is NOT the article's voice: on X, blunt, lowercase-comfortable, hook-first, one idea per post, land the point — never the em-dash essayist \"the bind the whole trend sits on\" register, which reads as model slop. Sign the thread once, on the closing/link post. Full rules, voice examples, image prompts, and the thread procedure live in `post-to-x`.\n\n**5. Close the loop.** Open the rendered page and read it as a stranger would, side by side with `/a/openai-huggingface-hack-2026`. If it reads thinner — fewer inline tweets/cards, fewer findings-headers, or (fatal) a claim-list instead of prose — it is NOT shipped; go back to 2a. Screenshot desktop AND mobile. Append STATE.md. If anything was skipped, say which step is open.\n","version":8,"of":9},{"commit":"9507b6d8e","date":"2026-07-24T08:51:44-07:00","what_changed":"homepage: Journal first; article pages: latest-from-the-journal rail; write-human: post-mutation smoke gate #widgets-approved","chars":19990,"added":["**3. Chrome + SEO verify (curl, not vibes) — and re-verify after EVERY post-publish mutation (2026-07-24).** The page's verified state dies the moment anything touches the article again: a voxel edit, a claims/sources batch, a raw meta write. The owner clicked two fresh articles during that mutation window and got errors. The gate: (a) all edits, sources, claims, meta.hero/og_card land FIRST; (b) THEN smoke: hit /a/<slug> 3+ times (fresh, no cache) plus /api/articles/<slug>/bundle plus the hero and og_card URLs, all 200 with full bodies; (c) ONLY THEN post links to X or hand them to the owner. A link shipped before the final smoke pass is a link to an unverified page. The rendered page must carry: byline dates (posted/updated + read time), the hero figure, `<link rel=\"canonical\">`, `og:image` = the hero, `article:published_time`, JSON-LD Article, `<meta name=\"robots\" content=\"index,follow...\">`. The share widget (robot button, bottom right) must show the article title, Copy link, Copy for LLM, Share on X."],"removed":["**3. Chrome + SEO verify (curl, not vibes).** The rendered page must carry: byline dates (posted/updated + read time), the hero figure, `<link rel=\"canonical\">`, `og:image` = the hero, `article:published_time`, JSON-LD Article, `<meta name=\"robots\" content=\"index,follow...\">`. The share widget (robot button, bottom right) must show the article title, Copy link, Copy for LLM, Share on X."],"text":"---\nname: write-human\ndescription: Write and revise any prose so it reads as a sharp human wrote it, not an AI. Load before drafting or editing articles, emails, landing copy, DMs, captions, summaries, handoffs, or any owner-facing or public text — and always when a draft is about to ship. The anti-slop bible: kill the AI tells, vary rhythm, use concrete detail, pass the read-aloud test.\n---\n\n# write-human\n\nThe job: prose that a smart, busy human would actually write and send. Not \"polished.\" Not \"engaging.\" Human. That means it has a point of view, uneven rhythm, concrete nouns, and zero of the reflexes below.\n\nThis is a revision discipline as much as a drafting one. Draft, then hunt the tells and cut them. The tells are mechanical; catching them is mechanical too.\n\n---\n\n## Part 1 — The tell catalog (delete on sight)\n\nEach entry: the tell, why it reads as machine, the fix.\n\n### Punctuation and structure tells\n\n**1. Em-dash overuse.** The single strongest surface tell. AI sprinkles em dashes into every third sentence as a rhythm crutch. Humans use them sparingly, for a real interruption or a snapped-off aside. Rule: at most one em dash per ~200 words, and only where a comma, period, or colon genuinely won't do. When in doubt, split into two sentences. (Sources: Plagiarism Today, Inc., 2025.)\n\n**2. The \"It's not just X — it's Y\" construction.** And its cousins: \"This isn't about X. It's about Y.\" / \"More than a Z, it's a W.\" A hollow reveal structure that promises depth and delivers a synonym. Delete it. Say the actual thing once.\n\n**3. Rule-of-three everywhere.** \"Fast, reliable, and affordable.\" \"It informs, it delights, it converts.\" Triple-parallel lists are the metronome of AI prose. One list of three in a whole piece is fine. Three of them on a page is a fingerprint. Break the pattern: use two, use four, use one blunt item.\n\n**4. Triple-parallel sentence openers.** \"You'll learn X. You'll discover Y. You'll master Z.\" Anaphora on a schedule. Cut to one, or vary the verb and the structure.\n\n**5. The restating summary sentence.** A sentence that adds nothing but re-announces what you just said: \"In short, the key is consistency.\" / \"Ultimately, it all comes down to trust.\" Also the closing \"By doing X, you'll be well on your way to Y.\" Delete the recap. The last real sentence is the ending.\n\n**6. Over-transitioning.** \"Moreover,\" \"Furthermore,\" \"Additionally,\" \"That said,\" \"It's worth noting that,\" \"Importantly,\" at the head of nearly every paragraph. Humans let sentences sit next to each other without a signpost between each one. Cut ~80% of transition words. Keep the ones that carry real logical weight.\n\n**7. Uniform sentence length / metronomic rhythm.** The deepest tell — see Part 2. If every sentence is 15-22 words, it reads as generated even with every word swapped out.\n\n### Diction tells (words and phrases that scream machine)\n\n**8. The vocabulary blacklist.** Delete or replace on sight: delve, tapestry, testament (as in \"a testament to\"), navigate the landscape / navigating the complexities, boasts (of a product: \"boasts a 5-inch screen\"), elevate, seamless / seamlessly, unlock (unlock potential/value), leverage (as a verb), streamline, robust, comprehensive, cutting-edge, game-changer / game-changing, foster, harness, embark, realm, landscape (figurative), vibrant, bustling, myriad, plethora, meticulous / meticulously, resonate, underscore, pivotal, crucial, vital, essential (as filler), holistic, dynamic, innovative, revolutionize, empower, curate / curated, elevate, unleash, supercharge, \"at the forefront of,\" \"in the ever-evolving world of.\"\n\n**9. The setup phrases.** \"In today's world,\" \"In today's fast-paced world,\" \"In today's digital age,\" \"In this ever-changing landscape,\" \"Now more than ever,\" \"In the realm of,\" \"When it comes to,\" \"At the end of the day.\" These are throat-clearing. The reader wants the sentence that comes after. Delete the setup, keep the substance.\n\n**10. Fake-insight filler.** \"It's important to note that,\" \"It's worth mentioning that,\" \"One key aspect to consider is,\" \"This is a game-changer,\" \"The possibilities are endless,\" \"provides valuable insights,\" \"plays a crucial role in.\" Research finds a phrase like \"provides valuable insights\" appears vastly more often in AI text than in human writing (QuillBot, 2025). If a sentence's job is only to announce that the next thing matters, cut it and let the next thing matter on its own.\n\n**11. Adjective inflation.** \"comprehensive solution,\" \"robust framework,\" \"powerful tool,\" \"seamless experience,\" \"valuable resource.\" AI reaches for a laudatory adjective when a concrete noun would carry more. Strip the adjective or replace the phrase with the specific thing.\n\n**12. Forced enthusiasm and hype.** Exclamation points, \"Get ready to,\" \"You're going to love,\" \"The best part?\", \"Here's the kicker,\" \"And that's not all.\" Confidence is quiet. State the good thing plainly; let the reader be the one who's impressed.\n\n**13. Over-hedging.** \"may potentially,\" \"could possibly,\" \"might arguably,\" \"in many cases, it can often be the case that.\" Stacking qualifiers to avoid ever committing. Humans with a view say it. If it's true, say it's true. If it's uncertain, say \"I don't know\" or name the one real caveat once.\n\n### Shape tells\n\n**14. The balanced both-sides non-answer.** Presenting every pro and con and never landing. Humans have opinions: \"X is better than Y here, because Z.\" Take the position the evidence supports and defend it in a line.\n\n**15. The listicle reflex.** Turning every idea into a bulleted list with bolded lead-ins of exactly parallel length. Lists are for genuinely parallel items. Prose is for reasoning. Don't bullet an argument.\n\n**16. The \"treadmill.\"** Circling one idea across three paragraphs, restated with new adjectives, no new information added. Each paragraph must earn its place with a new fact, example, or move. If a paragraph only rephrases the one above it, delete it.\n\n**17. Perfect, sanded-flat neutrality.** No specific person, no real number, no dated example, no mild opinion, no rough edge. Humans leave fingerprints: a named tool, a real price, \"last Tuesday,\" a thing they think is overrated.\n\n---\n\n## Part 2 — Sentence rhythm (the deepest fix)\n\nYou can swap every blacklisted word and still sound like a machine if the rhythm is even. Human writing has a heartbeat, not a metronome.\n\n- **Vary length hard.** Put a 4-word sentence next to a 30-word one. Then a medium one. Read the length pattern, not just the words.\n- **Use fragments deliberately.** \"Not always. But often.\" A fragment is a human move; AI almost never makes it unprompted.\n- **Let one sentence run long** with real subordinate clauses when the thought genuinely has parts, then snap back short.\n- **Start sentences differently.** Not every sentence begins with the subject. Open sometimes with a time, a condition, a short verb, a \"But.\"\n- **Kill the comma-splice-plus-participle tic:** \"..., allowing you to...\", \"..., helping teams to...\", \"..., making it easy to...\" This trailing \"-ing\" clause is an AI signature. Cut it or make it its own sentence.\n\nTest: read only the sentence lengths as a sequence of numbers. If they cluster in a narrow band, break them.\n\n---\n\n## Part 3 — Concrete over abstract\n\nAbstraction is the water AI swims in. The cure is specificity.\n\n- Replace category words with instances. Not \"a beverage\" → \"a lukewarm gas-station coffee.\" Not \"stakeholders\" → \"the two people who sign the check.\"\n- Use real numbers, not \"many/several/a range of.\" \"40% of opens\" beats \"a significant portion.\"\n- Name the thing. Not \"leading tools\" → \"Ahrefs and Screaming Frog.\"\n- Prefer the verb that shows the action over the abstract noun. \"decide\" not \"make a determination,\" \"help\" not \"provide assistance.\"\n- One vivid concrete detail buys more credibility than three adjectives. Spend on detail, not on praise.\n\nHuman writers *interpret* — they tell you what a fact means and why they think so. AI *presents*. Add the interpretation.\n\n---\n\n## Part 4 — The read-aloud test (mandatory before shipping)\n\nRead the whole thing out loud, or subvocalize it at speaking pace. Flag and fix:\n\n- Any sentence you run out of breath on → split it.\n- Any phrase you'd never say to a colleague (\"navigate the ever-evolving landscape\") → rewrite in the words you'd actually use.\n- Any spot where two sentences in a row have the same shape → vary one.\n- Any sentence that made you feel nothing / added nothing → cut it.\n- Any place a transition word slowed you down → delete it.\n\nIf it doesn't sound like a person talking, it isn't done.\n\n---\n\n## Part 5 — Before / after\n\n**Example 1 — landing copy**\n\nBefore (AI):\n> In today's fast-paced digital landscape, our cutting-edge platform empowers businesses to seamlessly streamline their workflows and unlock unprecedented growth. It's not just a tool — it's a game-changer that will revolutionize the way you work.\n\nAfter (human):\n> Most teams lose an hour a day to copy-pasting between four tools. This connects them, so you stop. Setup takes about ten minutes.\n\nWhy: killed the setup phrase, the blacklist words (cutting-edge, empowers, seamlessly, streamline, unlock, game-changer, revolutionize), the \"not just X — it's Y,\" and the hype. Added a concrete number and a concrete claim.\n\n**Example 2 — email line**\n\nBefore (AI):\n> I wanted to reach out to touch base regarding a potential opportunity that I believe could provide significant value and help elevate your team's productivity to the next level.\n\nAfter (human):\n> Quick one: I think we can cut your onboarding time from three weeks to about five days. Worth fifteen minutes to show you how?\n\nWhy: killed \"reach out / touch base,\" \"provide significant value,\" \"elevate... to the next level.\" Named the actual before/after and a single small ask.\n\n**Example 3 — explanatory prose**\n\nBefore (AI):\n> There are several key factors to consider. First, it's important to note that consistency plays a crucial role. Moreover, quality is essential. Additionally, timing can be a pivotal element. Ultimately, by keeping these factors in mind, you'll be well on your way to success.\n\nAfter (human):\n> Three things move the needle, in order. Consistency first — showing up beats any single great post. Quality second. Timing barely matters until the other two are handled, so don't obsess over it early.\n\nWhy: killed \"several key factors,\" \"it's important to note,\" \"plays a crucial role,\" \"Moreover/Additionally,\" \"pivotal,\" the restating summary, and the both-sides flatness. Added rank-ordering (a real opinion), a fragment, and varied sentence length.\n\n**Example 4 — the recap-sentence delete**\n\nBefore:\n> ...and that's how the caching layer cuts response time. In short, caching is a powerful way to make your app faster, and by implementing it correctly, you can deliver a seamless experience for your users.\n\nAfter:\n> ...and that's how the caching layer cuts response time.\n\nWhy: the recap sentence added nothing. The last real sentence was already the ending.\n\n---\n\n## Part 6 — The miscsubjects article register: logic-first, human-voiced (Law added 2026-07-24)\n\nThe house format for articles is a HYBRID: the structure of an evidence review, the voice of a person. The whole point is that a model can be told \"make an article about this incident\" and produce this shape without further prompting.\n\n**Structure (logic-first, non-negotiable):**\n- Open with what happened, in one dense factual paragraph. No scene-setting, no throat-clearing.\n- Every claim files under its evidence, and every source EMBEDS IN PLACE via `[[embed:source:sN]]` on its own line, in its native platform widget, at the exact point in the argument where the reader needs it. Never a sources sidebar, never a link wall.\n- Sequence by evidentiary weight: the primary documentary record first (the org that lived it), then named testimony, then independent reporting, then the social/contested layer, then color (memes, culture), then any analytical construction — flagged as such.\n- Say plainly what did NOT happen when headlines invite the wrong reading.\n\n**Headers (the robotic-header ban):**\n- A sub-header is said the way a person would say it across a table, and it carries a FINDING, not a category. \"What Hugging Face said\" is a filing label — robotic. \"The company that got hacked wrote it up first\" is a finding with a voice.\n- Ban the parallel-template series: \"What X said / What Y said / What Z said\" or \"The A reaction / The B reaction.\" If two headers share a grammatical template, rewrite one.\n- A header may be a sentence, may take a stance, may be short (\"Hours, not weeks\"). It may never be a taxonomy node.\n\n**Voice:** everything in Parts 1–5 applies. Concrete numbers over adjectives, one position where the evidence takes one, uneven rhythm, the read-aloud test. The register is a sharp writer walking you through a file of evidence — never a report generator filling slots.\n\n**Worked examples live at** `/a/openai-huggingface-hack-2026` (incident/evidence review) and `/a/the-canonical-morgh-index` (essay with embedded scholarly widgets). Widget contract: see the design-law skill.\n\n---\n\n## Ship checklist\n\n- [ ] Em dashes: ≤1 per 200 words, each earned.\n- [ ] Zero blacklist words (Part 1, #8-9).\n- [ ] No \"not just X — it's Y,\" no setup phrases, no fake-insight filler.\n- [ ] At most one rule-of-three in the whole piece.\n- [ ] No restating summary sentence at the end.\n- [ ] Transition words cut ~80%.\n- [ ] Sentence lengths vary visibly; at least one fragment or one short punch.\n- [ ] At least one concrete number/name/detail per paragraph.\n- [ ] A real position taken where the topic has one.\n- [ ] Read aloud, start to finish, and it sounds like a person.\n\nSources: [Inc. — sentence structure as AI tell](https://www.inc.com/jessica-stillman/this-sentence-structure-is-a-dead-giveaway-that-ai-wrote-it/91275389), [Plagiarism Today — em dashes and AI](https://www.plagiarismtoday.com/2025/06/26/em-dashes-hyphens-and-spotting-ai-writing/), [QuillBot — make AI writing sound human](https://quillbot.com/blog/ai-writing-tools/make-ai-writing-sound-human/), [vrid.ai — 27 signs of AI writing](https://vrid.ai/blog/signs-of-ai-writing), [Hunting the Muse — how to tell if writing is AI](https://huntingthemuse.net/library/how-to-tell-if-writing-is-ai).\n\n---\n\n## Part 7 — The full publish pipeline (replicable end to end)\n\nAn article is not shipped when the prose is done. It is shipped when a stranger can find it, share it, and see it whole. Run every step, in order, every time.\n\n**0. Boundary check (before writing anything).** POST `{title, markdown-or-outline}` to `/api/protocol/article-boundary`. The formula: an article is a SUBJECT (one entity or one bounded event), never a length. Verdict `append` → write DIVs into the existing page. `append_as_section` → collapsible section on the host page (it graduates to its own page past ~14 DIVs). `new_article` → proceed. An event binding two or more existing subjects (an incident, a lawsuit, an acquisition) is always its own page, cross-referenced from each subject page.\n\n**1. Featured image (default, not optional) — as a finished CARD, not raw art.** Generate with ArcAds using the GPT image model: `ARCADS_GENERATE gpt-image|<prompt>|16:9|||true`, then `ARCADS_TO_R2 <arcads_id>|gpt-image`. Prompt shape that works: minimal editorial illustration of the article's central mechanism, ink-black and paper-white with one indigo accent, generous negative space, flat vector, \"no text, no letters, no logos, no watermark\". Then COMPOSITE the article title + a model signature onto it (the Morgh Index card is the reference: title pill bottom-left, \"— <Model>\" byline band) so the social card reads as a designed piece, not a bare illustration — a raw illustration makes a shitty-looking X card. Upload the composited card to R2 and set it as `meta.hero`; that drives the on-page hero AND og:image/twitter:image. LOOK at the final card before using it. Set the hero BEFORE any X post — X scrapes og:image at post time and caches it; post first and the card comes up blank.\n\n**2. Write the article** under Part 6 (logic-first, sources embedded in place, headers that carry findings). Real, sourced material only — never a fabricated event, never a planted fallacy, never invented content \"to demonstrate\" any site machinery (NO_FABRICATED_LIVE_CONTENT law; intake refuses self-declared plants with `planted_content_refused`). A worked example of a bad inference uses a genuinely bad inference found in the wild.\n\n**2a. PRESENTATION LAW — the article must LOOK like `/a/openai-huggingface-hack-2026`, or it is a catastrophic failure (2026-07-24).** The reference article is the only acceptable presentation bar. Two things make it that, and both are mandatory, not nice-to-have:\n- **The page renders your WRITTEN body, never an auto-composed claim-list.** `meta.prefer_stored` MUST be `true` (document intake now sets it by default; never unset it, and if you build meta by hand, set it). The failure signature: the live page shows your claims as a stack of one-sentence paragraphs with `◈` dividers and a source carousel bolted on the end. That is the \"AI slop\" look. If you see it, `prefer_stored` is missing — the site threw your writing away.\n- **Real evidence is embedded INLINE through the argument, densely.** The reference has ~15 `[[embed:source:sN]]` woven between paragraphs — a real tweet, a news masthead card, a thread — appearing exactly where the point lands. Minimum bar: a findings-header every 2–3 paragraphs, and a real embedded source after MOST sections. Include real TWEETS for texture (find them via X_SEARCH or a web search restricted to x.com; embed the real URL/author/quote — NEVER fabricate a tweet). A prose wall with sources only in a bottom rail is a blurb, not an article.\nMeasure this on the HUMAN side (the rendered page), never by counting sources in the meta. Do NOT report \"N sources\" as evidence of quality — open the page and compare it to the reference.\n\n**3. Chrome + SEO verify (curl, not vibes) — and re-verify after EVERY post-publish mutation (2026-07-24).** The page's verified state dies the moment anything touches the article again: a voxel edit, a claims/sources batch, a raw meta write. The owner clicked two fresh articles during that mutation window and got errors. The gate: (a) all edits, sources, claims, meta.hero/og_card land FIRST; (b) THEN smoke: hit /a/<slug> 3+ times (fresh, no cache) plus /api/articles/<slug>/bundle plus the hero and og_card URLs, all 200 with full bodies; (c) ONLY THEN post links to X or hand them to the owner. A link shipped before the final smoke pass is a link to an unverified page. The rendered page must carry: byline dates (posted/updated + read time), the hero figure, `<link rel=\"canonical\">`, `og:image` = the hero, `article:published_time`, JSON-LD Article, `<meta name=\"robots\" content=\"index,follow...\">`. The share widget (robot button, bottom right) must show the article title, Copy link, Copy for LLM, Share on X.\n\n**4. X — load the `post-to-x` skill and follow it.** Promote as a THREAD, not scattered lone posts, and every post ships with an image (`X_POST` takes `{text, image_url, reply_to}`). The X voice is NOT the article's voice: on X, blunt, lowercase-comfortable, hook-first, one idea per post, land the point — never the em-dash essayist \"the bind the whole trend sits on\" register, which reads as model slop. Sign the thread once, on the closing/link post. Full rules, voice examples, image prompts, and the thread procedure live in `post-to-x`.\n\n**5. Close the loop.** Open the rendered page and read it as a stranger would, side by side with `/a/openai-huggingface-hack-2026`. If it reads thinner — fewer inline tweets/cards, fewer findings-headers, or (fatal) a claim-list instead of prose — it is NOT shipped; go back to 2a. Screenshot desktop AND mobile. Append STATE.md. If anything was skipped, say which step is open.\n","version":7,"of":9},{"commit":"a281ea386","date":"2026-07-24T00:20:27-07:00","what_changed":"New skills: post-to-x (owner X voice + image-per-post + thread + X_POST contract) and multi-model-team (panel via Cloudflare AI Gateway). write-human defers X copy to post-to-x #widgets-approved","chars":19359,"added":["**4. X — load the `post-to-x` skill and follow it.** Promote as a THREAD, not scattered lone posts, and every post ships with an image (`X_POST` takes `{text, image_url, reply_to}`). The X voice is NOT the article's voice: on X, blunt, lowercase-comfortable, hook-first, one idea per post, land the point — never the em-dash essayist \"the bind the whole trend sits on\" register, which reads as model slop. Sign the thread once, on the closing/link post. Full rules, voice examples, image prompts, and the thread procedure live in `post-to-x`."],"removed":["**4. X post series (part of shipping, not an afterthought).** Write SEVERAL posts, not one — one per facet of the article, each linking to a different portion (`/a/<slug>` for the lead; `/a/<slug>#disc-<claimId>` for a specific claim; `/a/<slug>#sources` for the evidence rail). Tag the actual parties in the story (the orgs and named people with X handles — e.g. @OpenAI, @huggingface, @ClementDelangue, @tedlieu — only handles you have verified exist). Each post: one concrete fact or finding from that facet, plain words, no hype vocabulary. SIGN EVERY POST with a model signature as the last segment — `— Fable 5 (Claude Code)` (SIGN_YOUR_WORK law); an unsigned post is a law violation. Trim so the whole post, signature and link included, is ≤280 chars. Post via X_POST, one call per post, and quote the returned x.com status URLs as proof."],"text":"---\nname: write-human\ndescription: Write and revise any prose so it reads as a sharp human wrote it, not an AI. Load before drafting or editing articles, emails, landing copy, DMs, captions, summaries, handoffs, or any owner-facing or public text — and always when a draft is about to ship. The anti-slop bible: kill the AI tells, vary rhythm, use concrete detail, pass the read-aloud test.\n---\n\n# write-human\n\nThe job: prose that a smart, busy human would actually write and send. Not \"polished.\" Not \"engaging.\" Human. That means it has a point of view, uneven rhythm, concrete nouns, and zero of the reflexes below.\n\nThis is a revision discipline as much as a drafting one. Draft, then hunt the tells and cut them. The tells are mechanical; catching them is mechanical too.\n\n---\n\n## Part 1 — The tell catalog (delete on sight)\n\nEach entry: the tell, why it reads as machine, the fix.\n\n### Punctuation and structure tells\n\n**1. Em-dash overuse.** The single strongest surface tell. AI sprinkles em dashes into every third sentence as a rhythm crutch. Humans use them sparingly, for a real interruption or a snapped-off aside. Rule: at most one em dash per ~200 words, and only where a comma, period, or colon genuinely won't do. When in doubt, split into two sentences. (Sources: Plagiarism Today, Inc., 2025.)\n\n**2. The \"It's not just X — it's Y\" construction.** And its cousins: \"This isn't about X. It's about Y.\" / \"More than a Z, it's a W.\" A hollow reveal structure that promises depth and delivers a synonym. Delete it. Say the actual thing once.\n\n**3. Rule-of-three everywhere.** \"Fast, reliable, and affordable.\" \"It informs, it delights, it converts.\" Triple-parallel lists are the metronome of AI prose. One list of three in a whole piece is fine. Three of them on a page is a fingerprint. Break the pattern: use two, use four, use one blunt item.\n\n**4. Triple-parallel sentence openers.** \"You'll learn X. You'll discover Y. You'll master Z.\" Anaphora on a schedule. Cut to one, or vary the verb and the structure.\n\n**5. The restating summary sentence.** A sentence that adds nothing but re-announces what you just said: \"In short, the key is consistency.\" / \"Ultimately, it all comes down to trust.\" Also the closing \"By doing X, you'll be well on your way to Y.\" Delete the recap. The last real sentence is the ending.\n\n**6. Over-transitioning.** \"Moreover,\" \"Furthermore,\" \"Additionally,\" \"That said,\" \"It's worth noting that,\" \"Importantly,\" at the head of nearly every paragraph. Humans let sentences sit next to each other without a signpost between each one. Cut ~80% of transition words. Keep the ones that carry real logical weight.\n\n**7. Uniform sentence length / metronomic rhythm.** The deepest tell — see Part 2. If every sentence is 15-22 words, it reads as generated even with every word swapped out.\n\n### Diction tells (words and phrases that scream machine)\n\n**8. The vocabulary blacklist.** Delete or replace on sight: delve, tapestry, testament (as in \"a testament to\"), navigate the landscape / navigating the complexities, boasts (of a product: \"boasts a 5-inch screen\"), elevate, seamless / seamlessly, unlock (unlock potential/value), leverage (as a verb), streamline, robust, comprehensive, cutting-edge, game-changer / game-changing, foster, harness, embark, realm, landscape (figurative), vibrant, bustling, myriad, plethora, meticulous / meticulously, resonate, underscore, pivotal, crucial, vital, essential (as filler), holistic, dynamic, innovative, revolutionize, empower, curate / curated, elevate, unleash, supercharge, \"at the forefront of,\" \"in the ever-evolving world of.\"\n\n**9. The setup phrases.** \"In today's world,\" \"In today's fast-paced world,\" \"In today's digital age,\" \"In this ever-changing landscape,\" \"Now more than ever,\" \"In the realm of,\" \"When it comes to,\" \"At the end of the day.\" These are throat-clearing. The reader wants the sentence that comes after. Delete the setup, keep the substance.\n\n**10. Fake-insight filler.** \"It's important to note that,\" \"It's worth mentioning that,\" \"One key aspect to consider is,\" \"This is a game-changer,\" \"The possibilities are endless,\" \"provides valuable insights,\" \"plays a crucial role in.\" Research finds a phrase like \"provides valuable insights\" appears vastly more often in AI text than in human writing (QuillBot, 2025). If a sentence's job is only to announce that the next thing matters, cut it and let the next thing matter on its own.\n\n**11. Adjective inflation.** \"comprehensive solution,\" \"robust framework,\" \"powerful tool,\" \"seamless experience,\" \"valuable resource.\" AI reaches for a laudatory adjective when a concrete noun would carry more. Strip the adjective or replace the phrase with the specific thing.\n\n**12. Forced enthusiasm and hype.** Exclamation points, \"Get ready to,\" \"You're going to love,\" \"The best part?\", \"Here's the kicker,\" \"And that's not all.\" Confidence is quiet. State the good thing plainly; let the reader be the one who's impressed.\n\n**13. Over-hedging.** \"may potentially,\" \"could possibly,\" \"might arguably,\" \"in many cases, it can often be the case that.\" Stacking qualifiers to avoid ever committing. Humans with a view say it. If it's true, say it's true. If it's uncertain, say \"I don't know\" or name the one real caveat once.\n\n### Shape tells\n\n**14. The balanced both-sides non-answer.** Presenting every pro and con and never landing. Humans have opinions: \"X is better than Y here, because Z.\" Take the position the evidence supports and defend it in a line.\n\n**15. The listicle reflex.** Turning every idea into a bulleted list with bolded lead-ins of exactly parallel length. Lists are for genuinely parallel items. Prose is for reasoning. Don't bullet an argument.\n\n**16. The \"treadmill.\"** Circling one idea across three paragraphs, restated with new adjectives, no new information added. Each paragraph must earn its place with a new fact, example, or move. If a paragraph only rephrases the one above it, delete it.\n\n**17. Perfect, sanded-flat neutrality.** No specific person, no real number, no dated example, no mild opinion, no rough edge. Humans leave fingerprints: a named tool, a real price, \"last Tuesday,\" a thing they think is overrated.\n\n---\n\n## Part 2 — Sentence rhythm (the deepest fix)\n\nYou can swap every blacklisted word and still sound like a machine if the rhythm is even. Human writing has a heartbeat, not a metronome.\n\n- **Vary length hard.** Put a 4-word sentence next to a 30-word one. Then a medium one. Read the length pattern, not just the words.\n- **Use fragments deliberately.** \"Not always. But often.\" A fragment is a human move; AI almost never makes it unprompted.\n- **Let one sentence run long** with real subordinate clauses when the thought genuinely has parts, then snap back short.\n- **Start sentences differently.** Not every sentence begins with the subject. Open sometimes with a time, a condition, a short verb, a \"But.\"\n- **Kill the comma-splice-plus-participle tic:** \"..., allowing you to...\", \"..., helping teams to...\", \"..., making it easy to...\" This trailing \"-ing\" clause is an AI signature. Cut it or make it its own sentence.\n\nTest: read only the sentence lengths as a sequence of numbers. If they cluster in a narrow band, break them.\n\n---\n\n## Part 3 — Concrete over abstract\n\nAbstraction is the water AI swims in. The cure is specificity.\n\n- Replace category words with instances. Not \"a beverage\" → \"a lukewarm gas-station coffee.\" Not \"stakeholders\" → \"the two people who sign the check.\"\n- Use real numbers, not \"many/several/a range of.\" \"40% of opens\" beats \"a significant portion.\"\n- Name the thing. Not \"leading tools\" → \"Ahrefs and Screaming Frog.\"\n- Prefer the verb that shows the action over the abstract noun. \"decide\" not \"make a determination,\" \"help\" not \"provide assistance.\"\n- One vivid concrete detail buys more credibility than three adjectives. Spend on detail, not on praise.\n\nHuman writers *interpret* — they tell you what a fact means and why they think so. AI *presents*. Add the interpretation.\n\n---\n\n## Part 4 — The read-aloud test (mandatory before shipping)\n\nRead the whole thing out loud, or subvocalize it at speaking pace. Flag and fix:\n\n- Any sentence you run out of breath on → split it.\n- Any phrase you'd never say to a colleague (\"navigate the ever-evolving landscape\") → rewrite in the words you'd actually use.\n- Any spot where two sentences in a row have the same shape → vary one.\n- Any sentence that made you feel nothing / added nothing → cut it.\n- Any place a transition word slowed you down → delete it.\n\nIf it doesn't sound like a person talking, it isn't done.\n\n---\n\n## Part 5 — Before / after\n\n**Example 1 — landing copy**\n\nBefore (AI):\n> In today's fast-paced digital landscape, our cutting-edge platform empowers businesses to seamlessly streamline their workflows and unlock unprecedented growth. It's not just a tool — it's a game-changer that will revolutionize the way you work.\n\nAfter (human):\n> Most teams lose an hour a day to copy-pasting between four tools. This connects them, so you stop. Setup takes about ten minutes.\n\nWhy: killed the setup phrase, the blacklist words (cutting-edge, empowers, seamlessly, streamline, unlock, game-changer, revolutionize), the \"not just X — it's Y,\" and the hype. Added a concrete number and a concrete claim.\n\n**Example 2 — email line**\n\nBefore (AI):\n> I wanted to reach out to touch base regarding a potential opportunity that I believe could provide significant value and help elevate your team's productivity to the next level.\n\nAfter (human):\n> Quick one: I think we can cut your onboarding time from three weeks to about five days. Worth fifteen minutes to show you how?\n\nWhy: killed \"reach out / touch base,\" \"provide significant value,\" \"elevate... to the next level.\" Named the actual before/after and a single small ask.\n\n**Example 3 — explanatory prose**\n\nBefore (AI):\n> There are several key factors to consider. First, it's important to note that consistency plays a crucial role. Moreover, quality is essential. Additionally, timing can be a pivotal element. Ultimately, by keeping these factors in mind, you'll be well on your way to success.\n\nAfter (human):\n> Three things move the needle, in order. Consistency first — showing up beats any single great post. Quality second. Timing barely matters until the other two are handled, so don't obsess over it early.\n\nWhy: killed \"several key factors,\" \"it's important to note,\" \"plays a crucial role,\" \"Moreover/Additionally,\" \"pivotal,\" the restating summary, and the both-sides flatness. Added rank-ordering (a real opinion), a fragment, and varied sentence length.\n\n**Example 4 — the recap-sentence delete**\n\nBefore:\n> ...and that's how the caching layer cuts response time. In short, caching is a powerful way to make your app faster, and by implementing it correctly, you can deliver a seamless experience for your users.\n\nAfter:\n> ...and that's how the caching layer cuts response time.\n\nWhy: the recap sentence added nothing. The last real sentence was already the ending.\n\n---\n\n## Part 6 — The miscsubjects article register: logic-first, human-voiced (Law added 2026-07-24)\n\nThe house format for articles is a HYBRID: the structure of an evidence review, the voice of a person. The whole point is that a model can be told \"make an article about this incident\" and produce this shape without further prompting.\n\n**Structure (logic-first, non-negotiable):**\n- Open with what happened, in one dense factual paragraph. No scene-setting, no throat-clearing.\n- Every claim files under its evidence, and every source EMBEDS IN PLACE via `[[embed:source:sN]]` on its own line, in its native platform widget, at the exact point in the argument where the reader needs it. Never a sources sidebar, never a link wall.\n- Sequence by evidentiary weight: the primary documentary record first (the org that lived it), then named testimony, then independent reporting, then the social/contested layer, then color (memes, culture), then any analytical construction — flagged as such.\n- Say plainly what did NOT happen when headlines invite the wrong reading.\n\n**Headers (the robotic-header ban):**\n- A sub-header is said the way a person would say it across a table, and it carries a FINDING, not a category. \"What Hugging Face said\" is a filing label — robotic. \"The company that got hacked wrote it up first\" is a finding with a voice.\n- Ban the parallel-template series: \"What X said / What Y said / What Z said\" or \"The A reaction / The B reaction.\" If two headers share a grammatical template, rewrite one.\n- A header may be a sentence, may take a stance, may be short (\"Hours, not weeks\"). It may never be a taxonomy node.\n\n**Voice:** everything in Parts 1–5 applies. Concrete numbers over adjectives, one position where the evidence takes one, uneven rhythm, the read-aloud test. The register is a sharp writer walking you through a file of evidence — never a report generator filling slots.\n\n**Worked examples live at** `/a/openai-huggingface-hack-2026` (incident/evidence review) and `/a/the-canonical-morgh-index` (essay with embedded scholarly widgets). Widget contract: see the design-law skill.\n\n---\n\n## Ship checklist\n\n- [ ] Em dashes: ≤1 per 200 words, each earned.\n- [ ] Zero blacklist words (Part 1, #8-9).\n- [ ] No \"not just X — it's Y,\" no setup phrases, no fake-insight filler.\n- [ ] At most one rule-of-three in the whole piece.\n- [ ] No restating summary sentence at the end.\n- [ ] Transition words cut ~80%.\n- [ ] Sentence lengths vary visibly; at least one fragment or one short punch.\n- [ ] At least one concrete number/name/detail per paragraph.\n- [ ] A real position taken where the topic has one.\n- [ ] Read aloud, start to finish, and it sounds like a person.\n\nSources: [Inc. — sentence structure as AI tell](https://www.inc.com/jessica-stillman/this-sentence-structure-is-a-dead-giveaway-that-ai-wrote-it/91275389), [Plagiarism Today — em dashes and AI](https://www.plagiarismtoday.com/2025/06/26/em-dashes-hyphens-and-spotting-ai-writing/), [QuillBot — make AI writing sound human](https://quillbot.com/blog/ai-writing-tools/make-ai-writing-sound-human/), [vrid.ai — 27 signs of AI writing](https://vrid.ai/blog/signs-of-ai-writing), [Hunting the Muse — how to tell if writing is AI](https://huntingthemuse.net/library/how-to-tell-if-writing-is-ai).\n\n---\n\n## Part 7 — The full publish pipeline (replicable end to end)\n\nAn article is not shipped when the prose is done. It is shipped when a stranger can find it, share it, and see it whole. Run every step, in order, every time.\n\n**0. Boundary check (before writing anything).** POST `{title, markdown-or-outline}` to `/api/protocol/article-boundary`. The formula: an article is a SUBJECT (one entity or one bounded event), never a length. Verdict `append` → write DIVs into the existing page. `append_as_section` → collapsible section on the host page (it graduates to its own page past ~14 DIVs). `new_article` → proceed. An event binding two or more existing subjects (an incident, a lawsuit, an acquisition) is always its own page, cross-referenced from each subject page.\n\n**1. Featured image (default, not optional) — as a finished CARD, not raw art.** Generate with ArcAds using the GPT image model: `ARCADS_GENERATE gpt-image|<prompt>|16:9|||true`, then `ARCADS_TO_R2 <arcads_id>|gpt-image`. Prompt shape that works: minimal editorial illustration of the article's central mechanism, ink-black and paper-white with one indigo accent, generous negative space, flat vector, \"no text, no letters, no logos, no watermark\". Then COMPOSITE the article title + a model signature onto it (the Morgh Index card is the reference: title pill bottom-left, \"— <Model>\" byline band) so the social card reads as a designed piece, not a bare illustration — a raw illustration makes a shitty-looking X card. Upload the composited card to R2 and set it as `meta.hero`; that drives the on-page hero AND og:image/twitter:image. LOOK at the final card before using it. Set the hero BEFORE any X post — X scrapes og:image at post time and caches it; post first and the card comes up blank.\n\n**2. Write the article** under Part 6 (logic-first, sources embedded in place, headers that carry findings). Real, sourced material only — never a fabricated event, never a planted fallacy, never invented content \"to demonstrate\" any site machinery (NO_FABRICATED_LIVE_CONTENT law; intake refuses self-declared plants with `planted_content_refused`). A worked example of a bad inference uses a genuinely bad inference found in the wild.\n\n**2a. PRESENTATION LAW — the article must LOOK like `/a/openai-huggingface-hack-2026`, or it is a catastrophic failure (2026-07-24).** The reference article is the only acceptable presentation bar. Two things make it that, and both are mandatory, not nice-to-have:\n- **The page renders your WRITTEN body, never an auto-composed claim-list.** `meta.prefer_stored` MUST be `true` (document intake now sets it by default; never unset it, and if you build meta by hand, set it). The failure signature: the live page shows your claims as a stack of one-sentence paragraphs with `◈` dividers and a source carousel bolted on the end. That is the \"AI slop\" look. If you see it, `prefer_stored` is missing — the site threw your writing away.\n- **Real evidence is embedded INLINE through the argument, densely.** The reference has ~15 `[[embed:source:sN]]` woven between paragraphs — a real tweet, a news masthead card, a thread — appearing exactly where the point lands. Minimum bar: a findings-header every 2–3 paragraphs, and a real embedded source after MOST sections. Include real TWEETS for texture (find them via X_SEARCH or a web search restricted to x.com; embed the real URL/author/quote — NEVER fabricate a tweet). A prose wall with sources only in a bottom rail is a blurb, not an article.\nMeasure this on the HUMAN side (the rendered page), never by counting sources in the meta. Do NOT report \"N sources\" as evidence of quality — open the page and compare it to the reference.\n\n**3. Chrome + SEO verify (curl, not vibes).** The rendered page must carry: byline dates (posted/updated + read time), the hero figure, `<link rel=\"canonical\">`, `og:image` = the hero, `article:published_time`, JSON-LD Article, `<meta name=\"robots\" content=\"index,follow...\">`. The share widget (robot button, bottom right) must show the article title, Copy link, Copy for LLM, Share on X.\n\n**4. X — load the `post-to-x` skill and follow it.** Promote as a THREAD, not scattered lone posts, and every post ships with an image (`X_POST` takes `{text, image_url, reply_to}`). The X voice is NOT the article's voice: on X, blunt, lowercase-comfortable, hook-first, one idea per post, land the point — never the em-dash essayist \"the bind the whole trend sits on\" register, which reads as model slop. Sign the thread once, on the closing/link post. Full rules, voice examples, image prompts, and the thread procedure live in `post-to-x`.\n\n**5. Close the loop.** Open the rendered page and read it as a stranger would, side by side with `/a/openai-huggingface-hack-2026`. If it reads thinner — fewer inline tweets/cards, fewer findings-headers, or (fatal) a claim-list instead of prose — it is NOT shipped; go back to 2a. Screenshot desktop AND mobile. Append STATE.md. If anything was skipped, say which step is open.\n","version":6,"of":9},{"commit":"51661831b","date":"2026-07-23T23:35:56-07:00","what_changed":"Never render articles as claim-list slop: document intake defaults prefer_stored=true (renders authored prose + inline embeds, not auto-composed claims). write-human Part 7 gains the presentation law — match /a/openai-huggingface-hack-2026 (prefer_stored, inline real tweets/cards, findings-headers), measured on the human side not source counts","chars":19662,"added":["**2a. PRESENTATION LAW — the article must LOOK like `/a/openai-huggingface-hack-2026`, or it is a catastrophic failure (2026-07-24).** The reference article is the only acceptable presentation bar. Two things make it that, and both are mandatory, not nice-to-have:","- **The page renders your WRITTEN body, never an auto-composed claim-list.** `meta.prefer_stored` MUST be `true` (document intake now sets it by default; never unset it, and if you build meta by hand, set it). The failure signature: the live page shows your claims as a stack of one-sentence paragraphs with `◈` dividers and a source carousel bolted on the end. That is the \"AI slop\" look. If you see it, `prefer_stored` is missing — the site threw your writing away.","- **Real evidence is embedded INLINE through the argument, densely.** The reference has ~15 `[[embed:source:sN]]` woven between paragraphs — a real tweet, a news masthead card, a thread — appearing exactly where the point lands. Minimum bar: a findings-header every 2–3 paragraphs, and a real embedded source after MOST sections. Include real TWEETS for texture (find them via X_SEARCH or a web search restricted to x.com; embed the real URL/author/quote — NEVER fabricate a tweet). A prose wall with sources only in a bottom rail is a blurb, not an article.","Measure this on the HUMAN side (the rendered page), never by counting sources in the meta. Do NOT report \"N sources\" as evidence of quality — open the page and compare it to the reference.","**5. Close the loop.** Open the rendered page and read it as a stranger would, side by side with `/a/openai-huggingface-hack-2026`. If it reads thinner — fewer inline tweets/cards, fewer findings-headers, or (fatal) a claim-list instead of prose — it is NOT shipped; go back to 2a. Screenshot desktop AND mobile. Append STATE.md. If anything was skipped, say which step is open."],"removed":["**5. Close the loop.** Screenshot the rendered page (desktop AND mobile widths) and confirm the feature you shipped is visible. Append STATE.md. If anything in the pipeline was skipped, the article is not shipped — say which step is open."],"text":"---\nname: write-human\ndescription: Write and revise any prose so it reads as a sharp human wrote it, not an AI. Load before drafting or editing articles, emails, landing copy, DMs, captions, summaries, handoffs, or any owner-facing or public text — and always when a draft is about to ship. The anti-slop bible: kill the AI tells, vary rhythm, use concrete detail, pass the read-aloud test.\n---\n\n# write-human\n\nThe job: prose that a smart, busy human would actually write and send. Not \"polished.\" Not \"engaging.\" Human. That means it has a point of view, uneven rhythm, concrete nouns, and zero of the reflexes below.\n\nThis is a revision discipline as much as a drafting one. Draft, then hunt the tells and cut them. The tells are mechanical; catching them is mechanical too.\n\n---\n\n## Part 1 — The tell catalog (delete on sight)\n\nEach entry: the tell, why it reads as machine, the fix.\n\n### Punctuation and structure tells\n\n**1. Em-dash overuse.** The single strongest surface tell. AI sprinkles em dashes into every third sentence as a rhythm crutch. Humans use them sparingly, for a real interruption or a snapped-off aside. Rule: at most one em dash per ~200 words, and only where a comma, period, or colon genuinely won't do. When in doubt, split into two sentences. (Sources: Plagiarism Today, Inc., 2025.)\n\n**2. The \"It's not just X — it's Y\" construction.** And its cousins: \"This isn't about X. It's about Y.\" / \"More than a Z, it's a W.\" A hollow reveal structure that promises depth and delivers a synonym. Delete it. Say the actual thing once.\n\n**3. Rule-of-three everywhere.** \"Fast, reliable, and affordable.\" \"It informs, it delights, it converts.\" Triple-parallel lists are the metronome of AI prose. One list of three in a whole piece is fine. Three of them on a page is a fingerprint. Break the pattern: use two, use four, use one blunt item.\n\n**4. Triple-parallel sentence openers.** \"You'll learn X. You'll discover Y. You'll master Z.\" Anaphora on a schedule. Cut to one, or vary the verb and the structure.\n\n**5. The restating summary sentence.** A sentence that adds nothing but re-announces what you just said: \"In short, the key is consistency.\" / \"Ultimately, it all comes down to trust.\" Also the closing \"By doing X, you'll be well on your way to Y.\" Delete the recap. The last real sentence is the ending.\n\n**6. Over-transitioning.** \"Moreover,\" \"Furthermore,\" \"Additionally,\" \"That said,\" \"It's worth noting that,\" \"Importantly,\" at the head of nearly every paragraph. Humans let sentences sit next to each other without a signpost between each one. Cut ~80% of transition words. Keep the ones that carry real logical weight.\n\n**7. Uniform sentence length / metronomic rhythm.** The deepest tell — see Part 2. If every sentence is 15-22 words, it reads as generated even with every word swapped out.\n\n### Diction tells (words and phrases that scream machine)\n\n**8. The vocabulary blacklist.** Delete or replace on sight: delve, tapestry, testament (as in \"a testament to\"), navigate the landscape / navigating the complexities, boasts (of a product: \"boasts a 5-inch screen\"), elevate, seamless / seamlessly, unlock (unlock potential/value), leverage (as a verb), streamline, robust, comprehensive, cutting-edge, game-changer / game-changing, foster, harness, embark, realm, landscape (figurative), vibrant, bustling, myriad, plethora, meticulous / meticulously, resonate, underscore, pivotal, crucial, vital, essential (as filler), holistic, dynamic, innovative, revolutionize, empower, curate / curated, elevate, unleash, supercharge, \"at the forefront of,\" \"in the ever-evolving world of.\"\n\n**9. The setup phrases.** \"In today's world,\" \"In today's fast-paced world,\" \"In today's digital age,\" \"In this ever-changing landscape,\" \"Now more than ever,\" \"In the realm of,\" \"When it comes to,\" \"At the end of the day.\" These are throat-clearing. The reader wants the sentence that comes after. Delete the setup, keep the substance.\n\n**10. Fake-insight filler.** \"It's important to note that,\" \"It's worth mentioning that,\" \"One key aspect to consider is,\" \"This is a game-changer,\" \"The possibilities are endless,\" \"provides valuable insights,\" \"plays a crucial role in.\" Research finds a phrase like \"provides valuable insights\" appears vastly more often in AI text than in human writing (QuillBot, 2025). If a sentence's job is only to announce that the next thing matters, cut it and let the next thing matter on its own.\n\n**11. Adjective inflation.** \"comprehensive solution,\" \"robust framework,\" \"powerful tool,\" \"seamless experience,\" \"valuable resource.\" AI reaches for a laudatory adjective when a concrete noun would carry more. Strip the adjective or replace the phrase with the specific thing.\n\n**12. Forced enthusiasm and hype.** Exclamation points, \"Get ready to,\" \"You're going to love,\" \"The best part?\", \"Here's the kicker,\" \"And that's not all.\" Confidence is quiet. State the good thing plainly; let the reader be the one who's impressed.\n\n**13. Over-hedging.** \"may potentially,\" \"could possibly,\" \"might arguably,\" \"in many cases, it can often be the case that.\" Stacking qualifiers to avoid ever committing. Humans with a view say it. If it's true, say it's true. If it's uncertain, say \"I don't know\" or name the one real caveat once.\n\n### Shape tells\n\n**14. The balanced both-sides non-answer.** Presenting every pro and con and never landing. Humans have opinions: \"X is better than Y here, because Z.\" Take the position the evidence supports and defend it in a line.\n\n**15. The listicle reflex.** Turning every idea into a bulleted list with bolded lead-ins of exactly parallel length. Lists are for genuinely parallel items. Prose is for reasoning. Don't bullet an argument.\n\n**16. The \"treadmill.\"** Circling one idea across three paragraphs, restated with new adjectives, no new information added. Each paragraph must earn its place with a new fact, example, or move. If a paragraph only rephrases the one above it, delete it.\n\n**17. Perfect, sanded-flat neutrality.** No specific person, no real number, no dated example, no mild opinion, no rough edge. Humans leave fingerprints: a named tool, a real price, \"last Tuesday,\" a thing they think is overrated.\n\n---\n\n## Part 2 — Sentence rhythm (the deepest fix)\n\nYou can swap every blacklisted word and still sound like a machine if the rhythm is even. Human writing has a heartbeat, not a metronome.\n\n- **Vary length hard.** Put a 4-word sentence next to a 30-word one. Then a medium one. Read the length pattern, not just the words.\n- **Use fragments deliberately.** \"Not always. But often.\" A fragment is a human move; AI almost never makes it unprompted.\n- **Let one sentence run long** with real subordinate clauses when the thought genuinely has parts, then snap back short.\n- **Start sentences differently.** Not every sentence begins with the subject. Open sometimes with a time, a condition, a short verb, a \"But.\"\n- **Kill the comma-splice-plus-participle tic:** \"..., allowing you to...\", \"..., helping teams to...\", \"..., making it easy to...\" This trailing \"-ing\" clause is an AI signature. Cut it or make it its own sentence.\n\nTest: read only the sentence lengths as a sequence of numbers. If they cluster in a narrow band, break them.\n\n---\n\n## Part 3 — Concrete over abstract\n\nAbstraction is the water AI swims in. The cure is specificity.\n\n- Replace category words with instances. Not \"a beverage\" → \"a lukewarm gas-station coffee.\" Not \"stakeholders\" → \"the two people who sign the check.\"\n- Use real numbers, not \"many/several/a range of.\" \"40% of opens\" beats \"a significant portion.\"\n- Name the thing. Not \"leading tools\" → \"Ahrefs and Screaming Frog.\"\n- Prefer the verb that shows the action over the abstract noun. \"decide\" not \"make a determination,\" \"help\" not \"provide assistance.\"\n- One vivid concrete detail buys more credibility than three adjectives. Spend on detail, not on praise.\n\nHuman writers *interpret* — they tell you what a fact means and why they think so. AI *presents*. Add the interpretation.\n\n---\n\n## Part 4 — The read-aloud test (mandatory before shipping)\n\nRead the whole thing out loud, or subvocalize it at speaking pace. Flag and fix:\n\n- Any sentence you run out of breath on → split it.\n- Any phrase you'd never say to a colleague (\"navigate the ever-evolving landscape\") → rewrite in the words you'd actually use.\n- Any spot where two sentences in a row have the same shape → vary one.\n- Any sentence that made you feel nothing / added nothing → cut it.\n- Any place a transition word slowed you down → delete it.\n\nIf it doesn't sound like a person talking, it isn't done.\n\n---\n\n## Part 5 — Before / after\n\n**Example 1 — landing copy**\n\nBefore (AI):\n> In today's fast-paced digital landscape, our cutting-edge platform empowers businesses to seamlessly streamline their workflows and unlock unprecedented growth. It's not just a tool — it's a game-changer that will revolutionize the way you work.\n\nAfter (human):\n> Most teams lose an hour a day to copy-pasting between four tools. This connects them, so you stop. Setup takes about ten minutes.\n\nWhy: killed the setup phrase, the blacklist words (cutting-edge, empowers, seamlessly, streamline, unlock, game-changer, revolutionize), the \"not just X — it's Y,\" and the hype. Added a concrete number and a concrete claim.\n\n**Example 2 — email line**\n\nBefore (AI):\n> I wanted to reach out to touch base regarding a potential opportunity that I believe could provide significant value and help elevate your team's productivity to the next level.\n\nAfter (human):\n> Quick one: I think we can cut your onboarding time from three weeks to about five days. Worth fifteen minutes to show you how?\n\nWhy: killed \"reach out / touch base,\" \"provide significant value,\" \"elevate... to the next level.\" Named the actual before/after and a single small ask.\n\n**Example 3 — explanatory prose**\n\nBefore (AI):\n> There are several key factors to consider. First, it's important to note that consistency plays a crucial role. Moreover, quality is essential. Additionally, timing can be a pivotal element. Ultimately, by keeping these factors in mind, you'll be well on your way to success.\n\nAfter (human):\n> Three things move the needle, in order. Consistency first — showing up beats any single great post. Quality second. Timing barely matters until the other two are handled, so don't obsess over it early.\n\nWhy: killed \"several key factors,\" \"it's important to note,\" \"plays a crucial role,\" \"Moreover/Additionally,\" \"pivotal,\" the restating summary, and the both-sides flatness. Added rank-ordering (a real opinion), a fragment, and varied sentence length.\n\n**Example 4 — the recap-sentence delete**\n\nBefore:\n> ...and that's how the caching layer cuts response time. In short, caching is a powerful way to make your app faster, and by implementing it correctly, you can deliver a seamless experience for your users.\n\nAfter:\n> ...and that's how the caching layer cuts response time.\n\nWhy: the recap sentence added nothing. The last real sentence was already the ending.\n\n---\n\n## Part 6 — The miscsubjects article register: logic-first, human-voiced (Law added 2026-07-24)\n\nThe house format for articles is a HYBRID: the structure of an evidence review, the voice of a person. The whole point is that a model can be told \"make an article about this incident\" and produce this shape without further prompting.\n\n**Structure (logic-first, non-negotiable):**\n- Open with what happened, in one dense factual paragraph. No scene-setting, no throat-clearing.\n- Every claim files under its evidence, and every source EMBEDS IN PLACE via `[[embed:source:sN]]` on its own line, in its native platform widget, at the exact point in the argument where the reader needs it. Never a sources sidebar, never a link wall.\n- Sequence by evidentiary weight: the primary documentary record first (the org that lived it), then named testimony, then independent reporting, then the social/contested layer, then color (memes, culture), then any analytical construction — flagged as such.\n- Say plainly what did NOT happen when headlines invite the wrong reading.\n\n**Headers (the robotic-header ban):**\n- A sub-header is said the way a person would say it across a table, and it carries a FINDING, not a category. \"What Hugging Face said\" is a filing label — robotic. \"The company that got hacked wrote it up first\" is a finding with a voice.\n- Ban the parallel-template series: \"What X said / What Y said / What Z said\" or \"The A reaction / The B reaction.\" If two headers share a grammatical template, rewrite one.\n- A header may be a sentence, may take a stance, may be short (\"Hours, not weeks\"). It may never be a taxonomy node.\n\n**Voice:** everything in Parts 1–5 applies. Concrete numbers over adjectives, one position where the evidence takes one, uneven rhythm, the read-aloud test. The register is a sharp writer walking you through a file of evidence — never a report generator filling slots.\n\n**Worked examples live at** `/a/openai-huggingface-hack-2026` (incident/evidence review) and `/a/the-canonical-morgh-index` (essay with embedded scholarly widgets). Widget contract: see the design-law skill.\n\n---\n\n## Ship checklist\n\n- [ ] Em dashes: ≤1 per 200 words, each earned.\n- [ ] Zero blacklist words (Part 1, #8-9).\n- [ ] No \"not just X — it's Y,\" no setup phrases, no fake-insight filler.\n- [ ] At most one rule-of-three in the whole piece.\n- [ ] No restating summary sentence at the end.\n- [ ] Transition words cut ~80%.\n- [ ] Sentence lengths vary visibly; at least one fragment or one short punch.\n- [ ] At least one concrete number/name/detail per paragraph.\n- [ ] A real position taken where the topic has one.\n- [ ] Read aloud, start to finish, and it sounds like a person.\n\nSources: [Inc. — sentence structure as AI tell](https://www.inc.com/jessica-stillman/this-sentence-structure-is-a-dead-giveaway-that-ai-wrote-it/91275389), [Plagiarism Today — em dashes and AI](https://www.plagiarismtoday.com/2025/06/26/em-dashes-hyphens-and-spotting-ai-writing/), [QuillBot — make AI writing sound human](https://quillbot.com/blog/ai-writing-tools/make-ai-writing-sound-human/), [vrid.ai — 27 signs of AI writing](https://vrid.ai/blog/signs-of-ai-writing), [Hunting the Muse — how to tell if writing is AI](https://huntingthemuse.net/library/how-to-tell-if-writing-is-ai).\n\n---\n\n## Part 7 — The full publish pipeline (replicable end to end)\n\nAn article is not shipped when the prose is done. It is shipped when a stranger can find it, share it, and see it whole. Run every step, in order, every time.\n\n**0. Boundary check (before writing anything).** POST `{title, markdown-or-outline}` to `/api/protocol/article-boundary`. The formula: an article is a SUBJECT (one entity or one bounded event), never a length. Verdict `append` → write DIVs into the existing page. `append_as_section` → collapsible section on the host page (it graduates to its own page past ~14 DIVs). `new_article` → proceed. An event binding two or more existing subjects (an incident, a lawsuit, an acquisition) is always its own page, cross-referenced from each subject page.\n\n**1. Featured image (default, not optional) — as a finished CARD, not raw art.** Generate with ArcAds using the GPT image model: `ARCADS_GENERATE gpt-image|<prompt>|16:9|||true`, then `ARCADS_TO_R2 <arcads_id>|gpt-image`. Prompt shape that works: minimal editorial illustration of the article's central mechanism, ink-black and paper-white with one indigo accent, generous negative space, flat vector, \"no text, no letters, no logos, no watermark\". Then COMPOSITE the article title + a model signature onto it (the Morgh Index card is the reference: title pill bottom-left, \"— <Model>\" byline band) so the social card reads as a designed piece, not a bare illustration — a raw illustration makes a shitty-looking X card. Upload the composited card to R2 and set it as `meta.hero`; that drives the on-page hero AND og:image/twitter:image. LOOK at the final card before using it. Set the hero BEFORE any X post — X scrapes og:image at post time and caches it; post first and the card comes up blank.\n\n**2. Write the article** under Part 6 (logic-first, sources embedded in place, headers that carry findings). Real, sourced material only — never a fabricated event, never a planted fallacy, never invented content \"to demonstrate\" any site machinery (NO_FABRICATED_LIVE_CONTENT law; intake refuses self-declared plants with `planted_content_refused`). A worked example of a bad inference uses a genuinely bad inference found in the wild.\n\n**2a. PRESENTATION LAW — the article must LOOK like `/a/openai-huggingface-hack-2026`, or it is a catastrophic failure (2026-07-24).** The reference article is the only acceptable presentation bar. Two things make it that, and both are mandatory, not nice-to-have:\n- **The page renders your WRITTEN body, never an auto-composed claim-list.** `meta.prefer_stored` MUST be `true` (document intake now sets it by default; never unset it, and if you build meta by hand, set it). The failure signature: the live page shows your claims as a stack of one-sentence paragraphs with `◈` dividers and a source carousel bolted on the end. That is the \"AI slop\" look. If you see it, `prefer_stored` is missing — the site threw your writing away.\n- **Real evidence is embedded INLINE through the argument, densely.** The reference has ~15 `[[embed:source:sN]]` woven between paragraphs — a real tweet, a news masthead card, a thread — appearing exactly where the point lands. Minimum bar: a findings-header every 2–3 paragraphs, and a real embedded source after MOST sections. Include real TWEETS for texture (find them via X_SEARCH or a web search restricted to x.com; embed the real URL/author/quote — NEVER fabricate a tweet). A prose wall with sources only in a bottom rail is a blurb, not an article.\nMeasure this on the HUMAN side (the rendered page), never by counting sources in the meta. Do NOT report \"N sources\" as evidence of quality — open the page and compare it to the reference.\n\n**3. Chrome + SEO verify (curl, not vibes).** The rendered page must carry: byline dates (posted/updated + read time), the hero figure, `<link rel=\"canonical\">`, `og:image` = the hero, `article:published_time`, JSON-LD Article, `<meta name=\"robots\" content=\"index,follow...\">`. The share widget (robot button, bottom right) must show the article title, Copy link, Copy for LLM, Share on X.\n\n**4. X post series (part of shipping, not an afterthought).** Write SEVERAL posts, not one — one per facet of the article, each linking to a different portion (`/a/<slug>` for the lead; `/a/<slug>#disc-<claimId>` for a specific claim; `/a/<slug>#sources` for the evidence rail). Tag the actual parties in the story (the orgs and named people with X handles — e.g. @OpenAI, @huggingface, @ClementDelangue, @tedlieu — only handles you have verified exist). Each post: one concrete fact or finding from that facet, plain words, no hype vocabulary. SIGN EVERY POST with a model signature as the last segment — `— Fable 5 (Claude Code)` (SIGN_YOUR_WORK law); an unsigned post is a law violation. Trim so the whole post, signature and link included, is ≤280 chars. Post via X_POST, one call per post, and quote the returned x.com status URLs as proof.\n\n**5. Close the loop.** Open the rendered page and read it as a stranger would, side by side with `/a/openai-huggingface-hack-2026`. If it reads thinner — fewer inline tweets/cards, fewer findings-headers, or (fatal) a claim-list instead of prose — it is NOT shipped; go back to 2a. Screenshot desktop AND mobile. Append STATE.md. If anything was skipped, say which step is open.\n","version":5,"of":9},{"commit":"4bb7e45d9","date":"2026-07-23T21:36:27-07:00","what_changed":"Machine-data guard: browser navigations to /api/ get a designed viewer + 'back to readable page', never a raw JSON dump (bots/fetch untouched). SIGN_YOUR_WORK law: model signature on every X post + published contribution. write-human pipeline: OG card compositing + signature.","chars":18040,"added":["**1. Featured image (default, not optional) — as a finished CARD, not raw art.** Generate with ArcAds using the GPT image model: `ARCADS_GENERATE gpt-image|<prompt>|16:9|||true`, then `ARCADS_TO_R2 <arcads_id>|gpt-image`. Prompt shape that works: minimal editorial illustration of the article's central mechanism, ink-black and paper-white with one indigo accent, generous negative space, flat vector, \"no text, no letters, no logos, no watermark\". Then COMPOSITE the article title + a model signature onto it (the Morgh Index card is the reference: title pill bottom-left, \"— <Model>\" byline band) so the social card reads as a designed piece, not a bare illustration — a raw illustration makes a shitty-looking X card. Upload the composited card to R2 and set it as `meta.hero`; that drives the on-page hero AND og:image/twitter:image. LOOK at the final card before using it. Set the hero BEFORE any X post — X scrapes og:image at post time and caches it; post first and the card comes up blank.","**4. X post series (part of shipping, not an afterthought).** Write SEVERAL posts, not one — one per facet of the article, each linking to a different portion (`/a/<slug>` for the lead; `/a/<slug>#disc-<claimId>` for a specific claim; `/a/<slug>#sources` for the evidence rail). Tag the actual parties in the story (the orgs and named people with X handles — e.g. @OpenAI, @huggingface, @ClementDelangue, @tedlieu — only handles you have verified exist). Each post: one concrete fact or finding from that facet, plain words, no hype vocabulary. SIGN EVERY POST with a model signature as the last segment — `— Fable 5 (Claude Code)` (SIGN_YOUR_WORK law); an unsigned post is a law violation. Trim so the whole post, signature and link included, is ≤280 chars. Post via X_POST, one call per post, and quote the returned x.com status URLs as proof."],"removed":["**1. Featured image (default, not optional).** Generate with ArcAds using the GPT image model: `ARCADS_GENERATE gpt-image|<prompt>|16:9|||true`, then `ARCADS_TO_R2 <arcads_id>|gpt-image` for the permanent `https://miscsubjects.com/img/...` link. Prompt shape that works: minimal editorial illustration of the article's central mechanism, ink-black and paper-white with one indigo accent, generous negative space, flat vector, \"no text, no letters, no logos, no watermark\". LOOK at the result before using it. Set it as `meta.hero` on the article — that drives both the on-page hero and og:image/twitter:image.","**4. X post series (part of shipping, not an afterthought).** Write SEVERAL posts, not one — one per facet of the article, each linking to a different portion (`/a/<slug>` for the lead; `/a/<slug>#disc-<claimId>` for a specific claim; `/a/<slug>#sources` for the evidence rail). Tag the actual parties in the story (the orgs and named people with X handles — e.g. @OpenAI, @huggingface, @ClementDelangue, @tedlieu — only handles you have verified exist). Each post: one concrete fact or finding from that facet, plain words, no hype vocabulary, ≤280 chars including the link. Post via X_POST, one call per post, and quote the returned x.com status URLs as proof."],"text":"---\nname: write-human\ndescription: Write and revise any prose so it reads as a sharp human wrote it, not an AI. Load before drafting or editing articles, emails, landing copy, DMs, captions, summaries, handoffs, or any owner-facing or public text — and always when a draft is about to ship. The anti-slop bible: kill the AI tells, vary rhythm, use concrete detail, pass the read-aloud test.\n---\n\n# write-human\n\nThe job: prose that a smart, busy human would actually write and send. Not \"polished.\" Not \"engaging.\" Human. That means it has a point of view, uneven rhythm, concrete nouns, and zero of the reflexes below.\n\nThis is a revision discipline as much as a drafting one. Draft, then hunt the tells and cut them. The tells are mechanical; catching them is mechanical too.\n\n---\n\n## Part 1 — The tell catalog (delete on sight)\n\nEach entry: the tell, why it reads as machine, the fix.\n\n### Punctuation and structure tells\n\n**1. Em-dash overuse.** The single strongest surface tell. AI sprinkles em dashes into every third sentence as a rhythm crutch. Humans use them sparingly, for a real interruption or a snapped-off aside. Rule: at most one em dash per ~200 words, and only where a comma, period, or colon genuinely won't do. When in doubt, split into two sentences. (Sources: Plagiarism Today, Inc., 2025.)\n\n**2. The \"It's not just X — it's Y\" construction.** And its cousins: \"This isn't about X. It's about Y.\" / \"More than a Z, it's a W.\" A hollow reveal structure that promises depth and delivers a synonym. Delete it. Say the actual thing once.\n\n**3. Rule-of-three everywhere.** \"Fast, reliable, and affordable.\" \"It informs, it delights, it converts.\" Triple-parallel lists are the metronome of AI prose. One list of three in a whole piece is fine. Three of them on a page is a fingerprint. Break the pattern: use two, use four, use one blunt item.\n\n**4. Triple-parallel sentence openers.** \"You'll learn X. You'll discover Y. You'll master Z.\" Anaphora on a schedule. Cut to one, or vary the verb and the structure.\n\n**5. The restating summary sentence.** A sentence that adds nothing but re-announces what you just said: \"In short, the key is consistency.\" / \"Ultimately, it all comes down to trust.\" Also the closing \"By doing X, you'll be well on your way to Y.\" Delete the recap. The last real sentence is the ending.\n\n**6. Over-transitioning.** \"Moreover,\" \"Furthermore,\" \"Additionally,\" \"That said,\" \"It's worth noting that,\" \"Importantly,\" at the head of nearly every paragraph. Humans let sentences sit next to each other without a signpost between each one. Cut ~80% of transition words. Keep the ones that carry real logical weight.\n\n**7. Uniform sentence length / metronomic rhythm.** The deepest tell — see Part 2. If every sentence is 15-22 words, it reads as generated even with every word swapped out.\n\n### Diction tells (words and phrases that scream machine)\n\n**8. The vocabulary blacklist.** Delete or replace on sight: delve, tapestry, testament (as in \"a testament to\"), navigate the landscape / navigating the complexities, boasts (of a product: \"boasts a 5-inch screen\"), elevate, seamless / seamlessly, unlock (unlock potential/value), leverage (as a verb), streamline, robust, comprehensive, cutting-edge, game-changer / game-changing, foster, harness, embark, realm, landscape (figurative), vibrant, bustling, myriad, plethora, meticulous / meticulously, resonate, underscore, pivotal, crucial, vital, essential (as filler), holistic, dynamic, innovative, revolutionize, empower, curate / curated, elevate, unleash, supercharge, \"at the forefront of,\" \"in the ever-evolving world of.\"\n\n**9. The setup phrases.** \"In today's world,\" \"In today's fast-paced world,\" \"In today's digital age,\" \"In this ever-changing landscape,\" \"Now more than ever,\" \"In the realm of,\" \"When it comes to,\" \"At the end of the day.\" These are throat-clearing. The reader wants the sentence that comes after. Delete the setup, keep the substance.\n\n**10. Fake-insight filler.** \"It's important to note that,\" \"It's worth mentioning that,\" \"One key aspect to consider is,\" \"This is a game-changer,\" \"The possibilities are endless,\" \"provides valuable insights,\" \"plays a crucial role in.\" Research finds a phrase like \"provides valuable insights\" appears vastly more often in AI text than in human writing (QuillBot, 2025). If a sentence's job is only to announce that the next thing matters, cut it and let the next thing matter on its own.\n\n**11. Adjective inflation.** \"comprehensive solution,\" \"robust framework,\" \"powerful tool,\" \"seamless experience,\" \"valuable resource.\" AI reaches for a laudatory adjective when a concrete noun would carry more. Strip the adjective or replace the phrase with the specific thing.\n\n**12. Forced enthusiasm and hype.** Exclamation points, \"Get ready to,\" \"You're going to love,\" \"The best part?\", \"Here's the kicker,\" \"And that's not all.\" Confidence is quiet. State the good thing plainly; let the reader be the one who's impressed.\n\n**13. Over-hedging.** \"may potentially,\" \"could possibly,\" \"might arguably,\" \"in many cases, it can often be the case that.\" Stacking qualifiers to avoid ever committing. Humans with a view say it. If it's true, say it's true. If it's uncertain, say \"I don't know\" or name the one real caveat once.\n\n### Shape tells\n\n**14. The balanced both-sides non-answer.** Presenting every pro and con and never landing. Humans have opinions: \"X is better than Y here, because Z.\" Take the position the evidence supports and defend it in a line.\n\n**15. The listicle reflex.** Turning every idea into a bulleted list with bolded lead-ins of exactly parallel length. Lists are for genuinely parallel items. Prose is for reasoning. Don't bullet an argument.\n\n**16. The \"treadmill.\"** Circling one idea across three paragraphs, restated with new adjectives, no new information added. Each paragraph must earn its place with a new fact, example, or move. If a paragraph only rephrases the one above it, delete it.\n\n**17. Perfect, sanded-flat neutrality.** No specific person, no real number, no dated example, no mild opinion, no rough edge. Humans leave fingerprints: a named tool, a real price, \"last Tuesday,\" a thing they think is overrated.\n\n---\n\n## Part 2 — Sentence rhythm (the deepest fix)\n\nYou can swap every blacklisted word and still sound like a machine if the rhythm is even. Human writing has a heartbeat, not a metronome.\n\n- **Vary length hard.** Put a 4-word sentence next to a 30-word one. Then a medium one. Read the length pattern, not just the words.\n- **Use fragments deliberately.** \"Not always. But often.\" A fragment is a human move; AI almost never makes it unprompted.\n- **Let one sentence run long** with real subordinate clauses when the thought genuinely has parts, then snap back short.\n- **Start sentences differently.** Not every sentence begins with the subject. Open sometimes with a time, a condition, a short verb, a \"But.\"\n- **Kill the comma-splice-plus-participle tic:** \"..., allowing you to...\", \"..., helping teams to...\", \"..., making it easy to...\" This trailing \"-ing\" clause is an AI signature. Cut it or make it its own sentence.\n\nTest: read only the sentence lengths as a sequence of numbers. If they cluster in a narrow band, break them.\n\n---\n\n## Part 3 — Concrete over abstract\n\nAbstraction is the water AI swims in. The cure is specificity.\n\n- Replace category words with instances. Not \"a beverage\" → \"a lukewarm gas-station coffee.\" Not \"stakeholders\" → \"the two people who sign the check.\"\n- Use real numbers, not \"many/several/a range of.\" \"40% of opens\" beats \"a significant portion.\"\n- Name the thing. Not \"leading tools\" → \"Ahrefs and Screaming Frog.\"\n- Prefer the verb that shows the action over the abstract noun. \"decide\" not \"make a determination,\" \"help\" not \"provide assistance.\"\n- One vivid concrete detail buys more credibility than three adjectives. Spend on detail, not on praise.\n\nHuman writers *interpret* — they tell you what a fact means and why they think so. AI *presents*. Add the interpretation.\n\n---\n\n## Part 4 — The read-aloud test (mandatory before shipping)\n\nRead the whole thing out loud, or subvocalize it at speaking pace. Flag and fix:\n\n- Any sentence you run out of breath on → split it.\n- Any phrase you'd never say to a colleague (\"navigate the ever-evolving landscape\") → rewrite in the words you'd actually use.\n- Any spot where two sentences in a row have the same shape → vary one.\n- Any sentence that made you feel nothing / added nothing → cut it.\n- Any place a transition word slowed you down → delete it.\n\nIf it doesn't sound like a person talking, it isn't done.\n\n---\n\n## Part 5 — Before / after\n\n**Example 1 — landing copy**\n\nBefore (AI):\n> In today's fast-paced digital landscape, our cutting-edge platform empowers businesses to seamlessly streamline their workflows and unlock unprecedented growth. It's not just a tool — it's a game-changer that will revolutionize the way you work.\n\nAfter (human):\n> Most teams lose an hour a day to copy-pasting between four tools. This connects them, so you stop. Setup takes about ten minutes.\n\nWhy: killed the setup phrase, the blacklist words (cutting-edge, empowers, seamlessly, streamline, unlock, game-changer, revolutionize), the \"not just X — it's Y,\" and the hype. Added a concrete number and a concrete claim.\n\n**Example 2 — email line**\n\nBefore (AI):\n> I wanted to reach out to touch base regarding a potential opportunity that I believe could provide significant value and help elevate your team's productivity to the next level.\n\nAfter (human):\n> Quick one: I think we can cut your onboarding time from three weeks to about five days. Worth fifteen minutes to show you how?\n\nWhy: killed \"reach out / touch base,\" \"provide significant value,\" \"elevate... to the next level.\" Named the actual before/after and a single small ask.\n\n**Example 3 — explanatory prose**\n\nBefore (AI):\n> There are several key factors to consider. First, it's important to note that consistency plays a crucial role. Moreover, quality is essential. Additionally, timing can be a pivotal element. Ultimately, by keeping these factors in mind, you'll be well on your way to success.\n\nAfter (human):\n> Three things move the needle, in order. Consistency first — showing up beats any single great post. Quality second. Timing barely matters until the other two are handled, so don't obsess over it early.\n\nWhy: killed \"several key factors,\" \"it's important to note,\" \"plays a crucial role,\" \"Moreover/Additionally,\" \"pivotal,\" the restating summary, and the both-sides flatness. Added rank-ordering (a real opinion), a fragment, and varied sentence length.\n\n**Example 4 — the recap-sentence delete**\n\nBefore:\n> ...and that's how the caching layer cuts response time. In short, caching is a powerful way to make your app faster, and by implementing it correctly, you can deliver a seamless experience for your users.\n\nAfter:\n> ...and that's how the caching layer cuts response time.\n\nWhy: the recap sentence added nothing. The last real sentence was already the ending.\n\n---\n\n## Part 6 — The miscsubjects article register: logic-first, human-voiced (Law added 2026-07-24)\n\nThe house format for articles is a HYBRID: the structure of an evidence review, the voice of a person. The whole point is that a model can be told \"make an article about this incident\" and produce this shape without further prompting.\n\n**Structure (logic-first, non-negotiable):**\n- Open with what happened, in one dense factual paragraph. No scene-setting, no throat-clearing.\n- Every claim files under its evidence, and every source EMBEDS IN PLACE via `[[embed:source:sN]]` on its own line, in its native platform widget, at the exact point in the argument where the reader needs it. Never a sources sidebar, never a link wall.\n- Sequence by evidentiary weight: the primary documentary record first (the org that lived it), then named testimony, then independent reporting, then the social/contested layer, then color (memes, culture), then any analytical construction — flagged as such.\n- Say plainly what did NOT happen when headlines invite the wrong reading.\n\n**Headers (the robotic-header ban):**\n- A sub-header is said the way a person would say it across a table, and it carries a FINDING, not a category. \"What Hugging Face said\" is a filing label — robotic. \"The company that got hacked wrote it up first\" is a finding with a voice.\n- Ban the parallel-template series: \"What X said / What Y said / What Z said\" or \"The A reaction / The B reaction.\" If two headers share a grammatical template, rewrite one.\n- A header may be a sentence, may take a stance, may be short (\"Hours, not weeks\"). It may never be a taxonomy node.\n\n**Voice:** everything in Parts 1–5 applies. Concrete numbers over adjectives, one position where the evidence takes one, uneven rhythm, the read-aloud test. The register is a sharp writer walking you through a file of evidence — never a report generator filling slots.\n\n**Worked examples live at** `/a/openai-huggingface-hack-2026` (incident/evidence review) and `/a/the-canonical-morgh-index` (essay with embedded scholarly widgets). Widget contract: see the design-law skill.\n\n---\n\n## Ship checklist\n\n- [ ] Em dashes: ≤1 per 200 words, each earned.\n- [ ] Zero blacklist words (Part 1, #8-9).\n- [ ] No \"not just X — it's Y,\" no setup phrases, no fake-insight filler.\n- [ ] At most one rule-of-three in the whole piece.\n- [ ] No restating summary sentence at the end.\n- [ ] Transition words cut ~80%.\n- [ ] Sentence lengths vary visibly; at least one fragment or one short punch.\n- [ ] At least one concrete number/name/detail per paragraph.\n- [ ] A real position taken where the topic has one.\n- [ ] Read aloud, start to finish, and it sounds like a person.\n\nSources: [Inc. — sentence structure as AI tell](https://www.inc.com/jessica-stillman/this-sentence-structure-is-a-dead-giveaway-that-ai-wrote-it/91275389), [Plagiarism Today — em dashes and AI](https://www.plagiarismtoday.com/2025/06/26/em-dashes-hyphens-and-spotting-ai-writing/), [QuillBot — make AI writing sound human](https://quillbot.com/blog/ai-writing-tools/make-ai-writing-sound-human/), [vrid.ai — 27 signs of AI writing](https://vrid.ai/blog/signs-of-ai-writing), [Hunting the Muse — how to tell if writing is AI](https://huntingthemuse.net/library/how-to-tell-if-writing-is-ai).\n\n---\n\n## Part 7 — The full publish pipeline (replicable end to end)\n\nAn article is not shipped when the prose is done. It is shipped when a stranger can find it, share it, and see it whole. Run every step, in order, every time.\n\n**0. Boundary check (before writing anything).** POST `{title, markdown-or-outline}` to `/api/protocol/article-boundary`. The formula: an article is a SUBJECT (one entity or one bounded event), never a length. Verdict `append` → write DIVs into the existing page. `append_as_section` → collapsible section on the host page (it graduates to its own page past ~14 DIVs). `new_article` → proceed. An event binding two or more existing subjects (an incident, a lawsuit, an acquisition) is always its own page, cross-referenced from each subject page.\n\n**1. Featured image (default, not optional) — as a finished CARD, not raw art.** Generate with ArcAds using the GPT image model: `ARCADS_GENERATE gpt-image|<prompt>|16:9|||true`, then `ARCADS_TO_R2 <arcads_id>|gpt-image`. Prompt shape that works: minimal editorial illustration of the article's central mechanism, ink-black and paper-white with one indigo accent, generous negative space, flat vector, \"no text, no letters, no logos, no watermark\". Then COMPOSITE the article title + a model signature onto it (the Morgh Index card is the reference: title pill bottom-left, \"— <Model>\" byline band) so the social card reads as a designed piece, not a bare illustration — a raw illustration makes a shitty-looking X card. Upload the composited card to R2 and set it as `meta.hero`; that drives the on-page hero AND og:image/twitter:image. LOOK at the final card before using it. Set the hero BEFORE any X post — X scrapes og:image at post time and caches it; post first and the card comes up blank.\n\n**2. Write the article** under Part 6 (logic-first, sources embedded in place, headers that carry findings). Real, sourced material only — never a fabricated event, never a planted fallacy, never invented content \"to demonstrate\" any site machinery (NO_FABRICATED_LIVE_CONTENT law; intake refuses self-declared plants with `planted_content_refused`). A worked example of a bad inference uses a genuinely bad inference found in the wild.\n\n**3. Chrome + SEO verify (curl, not vibes).** The rendered page must carry: byline dates (posted/updated + read time), the hero figure, `<link rel=\"canonical\">`, `og:image` = the hero, `article:published_time`, JSON-LD Article, `<meta name=\"robots\" content=\"index,follow...\">`. The share widget (robot button, bottom right) must show the article title, Copy link, Copy for LLM, Share on X.\n\n**4. X post series (part of shipping, not an afterthought).** Write SEVERAL posts, not one — one per facet of the article, each linking to a different portion (`/a/<slug>` for the lead; `/a/<slug>#disc-<claimId>` for a specific claim; `/a/<slug>#sources` for the evidence rail). Tag the actual parties in the story (the orgs and named people with X handles — e.g. @OpenAI, @huggingface, @ClementDelangue, @tedlieu — only handles you have verified exist). Each post: one concrete fact or finding from that facet, plain words, no hype vocabulary. SIGN EVERY POST with a model signature as the last segment — `— Fable 5 (Claude Code)` (SIGN_YOUR_WORK law); an unsigned post is a law violation. Trim so the whole post, signature and link included, is ≤280 chars. Post via X_POST, one call per post, and quote the returned x.com status URLs as proof.\n\n**5. Close the loop.** Screenshot the rendered page (desktop AND mobile widths) and confirm the feature you shipped is visible. Append STATE.md. If anything in the pipeline was skipped, the article is not shipped — say which step is open.\n","version":4,"of":9},{"commit":"44e70e94a","date":"2026-07-23T21:00:20-07:00","what_changed":"Article facelift: share widget (robot icon, Copy for LLM, X share), reading-position URL tracker (both scroll directions), byline dates + read time, inline no-navigate YouTube embeds, article-boundary formula (/api/protocol/article-boundary + advisory on document intake), featured-image + X-series pipeline in write-human skill #widgets-approved","chars":17469,"added":["## Part 7 — The full publish pipeline (replicable end to end)","An article is not shipped when the prose is done. It is shipped when a stranger can find it, share it, and see it whole. Run every step, in order, every time.","**0. Boundary check (before writing anything).** POST `{title, markdown-or-outline}` to `/api/protocol/article-boundary`. The formula: an article is a SUBJECT (one entity or one bounded event), never a length. Verdict `append` → write DIVs into the existing page. `append_as_section` → collapsible section on the host page (it graduates to its own page past ~14 DIVs). `new_article` → proceed. An event binding two or more existing subjects (an incident, a lawsuit, an acquisition) is always its own page, cross-referenced from each subject page.","**1. Featured image (default, not optional).** Generate with ArcAds using the GPT image model: `ARCADS_GENERATE gpt-image|<prompt>|16:9|||true`, then `ARCADS_TO_R2 <arcads_id>|gpt-image` for the permanent `https://miscsubjects.com/img/...` link. Prompt shape that works: minimal editorial illustration of the article's central mechanism, ink-black and paper-white with one indigo accent, generous negative space, flat vector, \"no text, no letters, no logos, no watermark\". LOOK at the result before using it. Set it as `meta.hero` on the article — that drives both the on-page hero and og:image/twitter:image.","**2. Write the article** under Part 6 (logic-first, sources embedded in place, headers that carry findings). Real, sourced material only — never a fabricated event, never a planted fallacy, never invented content \"to demonstrate\" any site machinery (NO_FABRICATED_LIVE_CONTENT law; intake refuses self-declared plants with `planted_content_refused`). A worked example of a bad inference uses a genuinely bad inference found in the wild.","**3. Chrome + SEO verify (curl, not vibes).** The rendered page must carry: byline dates (posted/updated + read time), the hero figure, `<link rel=\"canonical\">`, `og:image` = the hero, `article:published_time`, JSON-LD Article, `<meta name=\"robots\" content=\"index,follow...\">`. The share widget (robot button, bottom right) must show the article title, Copy link, Copy for LLM, Share on X.","**4. X post series (part of shipping, not an afterthought).** Write SEVERAL posts, not one — one per facet of the article, each linking to a different portion (`/a/<slug>` for the lead; `/a/<slug>#disc-<claimId>` for a specific claim; `/a/<slug>#sources` for the evidence rail). Tag the actual parties in the story (the orgs and named people with X handles — e.g. @OpenAI, @huggingface, @ClementDelangue, @tedlieu — only handles you have verified exist). Each post: one concrete fact or finding from that facet, plain words, no hype vocabulary, ≤280 chars including the link. Post via X_POST, one call per post, and quote the returned x.com status URLs as proof.","**5. Close the loop.** Screenshot the rendered page (desktop AND mobile widths) and confirm the feature you shipped is visible. Append STATE.md. If anything in the pipeline was skipped, the article is not shipped — say which step is open."],"removed":[],"text":"---\nname: write-human\ndescription: Write and revise any prose so it reads as a sharp human wrote it, not an AI. Load before drafting or editing articles, emails, landing copy, DMs, captions, summaries, handoffs, or any owner-facing or public text — and always when a draft is about to ship. The anti-slop bible: kill the AI tells, vary rhythm, use concrete detail, pass the read-aloud test.\n---\n\n# write-human\n\nThe job: prose that a smart, busy human would actually write and send. Not \"polished.\" Not \"engaging.\" Human. That means it has a point of view, uneven rhythm, concrete nouns, and zero of the reflexes below.\n\nThis is a revision discipline as much as a drafting one. Draft, then hunt the tells and cut them. The tells are mechanical; catching them is mechanical too.\n\n---\n\n## Part 1 — The tell catalog (delete on sight)\n\nEach entry: the tell, why it reads as machine, the fix.\n\n### Punctuation and structure tells\n\n**1. Em-dash overuse.** The single strongest surface tell. AI sprinkles em dashes into every third sentence as a rhythm crutch. Humans use them sparingly, for a real interruption or a snapped-off aside. Rule: at most one em dash per ~200 words, and only where a comma, period, or colon genuinely won't do. When in doubt, split into two sentences. (Sources: Plagiarism Today, Inc., 2025.)\n\n**2. The \"It's not just X — it's Y\" construction.** And its cousins: \"This isn't about X. It's about Y.\" / \"More than a Z, it's a W.\" A hollow reveal structure that promises depth and delivers a synonym. Delete it. Say the actual thing once.\n\n**3. Rule-of-three everywhere.** \"Fast, reliable, and affordable.\" \"It informs, it delights, it converts.\" Triple-parallel lists are the metronome of AI prose. One list of three in a whole piece is fine. Three of them on a page is a fingerprint. Break the pattern: use two, use four, use one blunt item.\n\n**4. Triple-parallel sentence openers.** \"You'll learn X. You'll discover Y. You'll master Z.\" Anaphora on a schedule. Cut to one, or vary the verb and the structure.\n\n**5. The restating summary sentence.** A sentence that adds nothing but re-announces what you just said: \"In short, the key is consistency.\" / \"Ultimately, it all comes down to trust.\" Also the closing \"By doing X, you'll be well on your way to Y.\" Delete the recap. The last real sentence is the ending.\n\n**6. Over-transitioning.** \"Moreover,\" \"Furthermore,\" \"Additionally,\" \"That said,\" \"It's worth noting that,\" \"Importantly,\" at the head of nearly every paragraph. Humans let sentences sit next to each other without a signpost between each one. Cut ~80% of transition words. Keep the ones that carry real logical weight.\n\n**7. Uniform sentence length / metronomic rhythm.** The deepest tell — see Part 2. If every sentence is 15-22 words, it reads as generated even with every word swapped out.\n\n### Diction tells (words and phrases that scream machine)\n\n**8. The vocabulary blacklist.** Delete or replace on sight: delve, tapestry, testament (as in \"a testament to\"), navigate the landscape / navigating the complexities, boasts (of a product: \"boasts a 5-inch screen\"), elevate, seamless / seamlessly, unlock (unlock potential/value), leverage (as a verb), streamline, robust, comprehensive, cutting-edge, game-changer / game-changing, foster, harness, embark, realm, landscape (figurative), vibrant, bustling, myriad, plethora, meticulous / meticulously, resonate, underscore, pivotal, crucial, vital, essential (as filler), holistic, dynamic, innovative, revolutionize, empower, curate / curated, elevate, unleash, supercharge, \"at the forefront of,\" \"in the ever-evolving world of.\"\n\n**9. The setup phrases.** \"In today's world,\" \"In today's fast-paced world,\" \"In today's digital age,\" \"In this ever-changing landscape,\" \"Now more than ever,\" \"In the realm of,\" \"When it comes to,\" \"At the end of the day.\" These are throat-clearing. The reader wants the sentence that comes after. Delete the setup, keep the substance.\n\n**10. Fake-insight filler.** \"It's important to note that,\" \"It's worth mentioning that,\" \"One key aspect to consider is,\" \"This is a game-changer,\" \"The possibilities are endless,\" \"provides valuable insights,\" \"plays a crucial role in.\" Research finds a phrase like \"provides valuable insights\" appears vastly more often in AI text than in human writing (QuillBot, 2025). If a sentence's job is only to announce that the next thing matters, cut it and let the next thing matter on its own.\n\n**11. Adjective inflation.** \"comprehensive solution,\" \"robust framework,\" \"powerful tool,\" \"seamless experience,\" \"valuable resource.\" AI reaches for a laudatory adjective when a concrete noun would carry more. Strip the adjective or replace the phrase with the specific thing.\n\n**12. Forced enthusiasm and hype.** Exclamation points, \"Get ready to,\" \"You're going to love,\" \"The best part?\", \"Here's the kicker,\" \"And that's not all.\" Confidence is quiet. State the good thing plainly; let the reader be the one who's impressed.\n\n**13. Over-hedging.** \"may potentially,\" \"could possibly,\" \"might arguably,\" \"in many cases, it can often be the case that.\" Stacking qualifiers to avoid ever committing. Humans with a view say it. If it's true, say it's true. If it's uncertain, say \"I don't know\" or name the one real caveat once.\n\n### Shape tells\n\n**14. The balanced both-sides non-answer.** Presenting every pro and con and never landing. Humans have opinions: \"X is better than Y here, because Z.\" Take the position the evidence supports and defend it in a line.\n\n**15. The listicle reflex.** Turning every idea into a bulleted list with bolded lead-ins of exactly parallel length. Lists are for genuinely parallel items. Prose is for reasoning. Don't bullet an argument.\n\n**16. The \"treadmill.\"** Circling one idea across three paragraphs, restated with new adjectives, no new information added. Each paragraph must earn its place with a new fact, example, or move. If a paragraph only rephrases the one above it, delete it.\n\n**17. Perfect, sanded-flat neutrality.** No specific person, no real number, no dated example, no mild opinion, no rough edge. Humans leave fingerprints: a named tool, a real price, \"last Tuesday,\" a thing they think is overrated.\n\n---\n\n## Part 2 — Sentence rhythm (the deepest fix)\n\nYou can swap every blacklisted word and still sound like a machine if the rhythm is even. Human writing has a heartbeat, not a metronome.\n\n- **Vary length hard.** Put a 4-word sentence next to a 30-word one. Then a medium one. Read the length pattern, not just the words.\n- **Use fragments deliberately.** \"Not always. But often.\" A fragment is a human move; AI almost never makes it unprompted.\n- **Let one sentence run long** with real subordinate clauses when the thought genuinely has parts, then snap back short.\n- **Start sentences differently.** Not every sentence begins with the subject. Open sometimes with a time, a condition, a short verb, a \"But.\"\n- **Kill the comma-splice-plus-participle tic:** \"..., allowing you to...\", \"..., helping teams to...\", \"..., making it easy to...\" This trailing \"-ing\" clause is an AI signature. Cut it or make it its own sentence.\n\nTest: read only the sentence lengths as a sequence of numbers. If they cluster in a narrow band, break them.\n\n---\n\n## Part 3 — Concrete over abstract\n\nAbstraction is the water AI swims in. The cure is specificity.\n\n- Replace category words with instances. Not \"a beverage\" → \"a lukewarm gas-station coffee.\" Not \"stakeholders\" → \"the two people who sign the check.\"\n- Use real numbers, not \"many/several/a range of.\" \"40% of opens\" beats \"a significant portion.\"\n- Name the thing. Not \"leading tools\" → \"Ahrefs and Screaming Frog.\"\n- Prefer the verb that shows the action over the abstract noun. \"decide\" not \"make a determination,\" \"help\" not \"provide assistance.\"\n- One vivid concrete detail buys more credibility than three adjectives. Spend on detail, not on praise.\n\nHuman writers *interpret* — they tell you what a fact means and why they think so. AI *presents*. Add the interpretation.\n\n---\n\n## Part 4 — The read-aloud test (mandatory before shipping)\n\nRead the whole thing out loud, or subvocalize it at speaking pace. Flag and fix:\n\n- Any sentence you run out of breath on → split it.\n- Any phrase you'd never say to a colleague (\"navigate the ever-evolving landscape\") → rewrite in the words you'd actually use.\n- Any spot where two sentences in a row have the same shape → vary one.\n- Any sentence that made you feel nothing / added nothing → cut it.\n- Any place a transition word slowed you down → delete it.\n\nIf it doesn't sound like a person talking, it isn't done.\n\n---\n\n## Part 5 — Before / after\n\n**Example 1 — landing copy**\n\nBefore (AI):\n> In today's fast-paced digital landscape, our cutting-edge platform empowers businesses to seamlessly streamline their workflows and unlock unprecedented growth. It's not just a tool — it's a game-changer that will revolutionize the way you work.\n\nAfter (human):\n> Most teams lose an hour a day to copy-pasting between four tools. This connects them, so you stop. Setup takes about ten minutes.\n\nWhy: killed the setup phrase, the blacklist words (cutting-edge, empowers, seamlessly, streamline, unlock, game-changer, revolutionize), the \"not just X — it's Y,\" and the hype. Added a concrete number and a concrete claim.\n\n**Example 2 — email line**\n\nBefore (AI):\n> I wanted to reach out to touch base regarding a potential opportunity that I believe could provide significant value and help elevate your team's productivity to the next level.\n\nAfter (human):\n> Quick one: I think we can cut your onboarding time from three weeks to about five days. Worth fifteen minutes to show you how?\n\nWhy: killed \"reach out / touch base,\" \"provide significant value,\" \"elevate... to the next level.\" Named the actual before/after and a single small ask.\n\n**Example 3 — explanatory prose**\n\nBefore (AI):\n> There are several key factors to consider. First, it's important to note that consistency plays a crucial role. Moreover, quality is essential. Additionally, timing can be a pivotal element. Ultimately, by keeping these factors in mind, you'll be well on your way to success.\n\nAfter (human):\n> Three things move the needle, in order. Consistency first — showing up beats any single great post. Quality second. Timing barely matters until the other two are handled, so don't obsess over it early.\n\nWhy: killed \"several key factors,\" \"it's important to note,\" \"plays a crucial role,\" \"Moreover/Additionally,\" \"pivotal,\" the restating summary, and the both-sides flatness. Added rank-ordering (a real opinion), a fragment, and varied sentence length.\n\n**Example 4 — the recap-sentence delete**\n\nBefore:\n> ...and that's how the caching layer cuts response time. In short, caching is a powerful way to make your app faster, and by implementing it correctly, you can deliver a seamless experience for your users.\n\nAfter:\n> ...and that's how the caching layer cuts response time.\n\nWhy: the recap sentence added nothing. The last real sentence was already the ending.\n\n---\n\n## Part 6 — The miscsubjects article register: logic-first, human-voiced (Law added 2026-07-24)\n\nThe house format for articles is a HYBRID: the structure of an evidence review, the voice of a person. The whole point is that a model can be told \"make an article about this incident\" and produce this shape without further prompting.\n\n**Structure (logic-first, non-negotiable):**\n- Open with what happened, in one dense factual paragraph. No scene-setting, no throat-clearing.\n- Every claim files under its evidence, and every source EMBEDS IN PLACE via `[[embed:source:sN]]` on its own line, in its native platform widget, at the exact point in the argument where the reader needs it. Never a sources sidebar, never a link wall.\n- Sequence by evidentiary weight: the primary documentary record first (the org that lived it), then named testimony, then independent reporting, then the social/contested layer, then color (memes, culture), then any analytical construction — flagged as such.\n- Say plainly what did NOT happen when headlines invite the wrong reading.\n\n**Headers (the robotic-header ban):**\n- A sub-header is said the way a person would say it across a table, and it carries a FINDING, not a category. \"What Hugging Face said\" is a filing label — robotic. \"The company that got hacked wrote it up first\" is a finding with a voice.\n- Ban the parallel-template series: \"What X said / What Y said / What Z said\" or \"The A reaction / The B reaction.\" If two headers share a grammatical template, rewrite one.\n- A header may be a sentence, may take a stance, may be short (\"Hours, not weeks\"). It may never be a taxonomy node.\n\n**Voice:** everything in Parts 1–5 applies. Concrete numbers over adjectives, one position where the evidence takes one, uneven rhythm, the read-aloud test. The register is a sharp writer walking you through a file of evidence — never a report generator filling slots.\n\n**Worked examples live at** `/a/openai-huggingface-hack-2026` (incident/evidence review) and `/a/the-canonical-morgh-index` (essay with embedded scholarly widgets). Widget contract: see the design-law skill.\n\n---\n\n## Ship checklist\n\n- [ ] Em dashes: ≤1 per 200 words, each earned.\n- [ ] Zero blacklist words (Part 1, #8-9).\n- [ ] No \"not just X — it's Y,\" no setup phrases, no fake-insight filler.\n- [ ] At most one rule-of-three in the whole piece.\n- [ ] No restating summary sentence at the end.\n- [ ] Transition words cut ~80%.\n- [ ] Sentence lengths vary visibly; at least one fragment or one short punch.\n- [ ] At least one concrete number/name/detail per paragraph.\n- [ ] A real position taken where the topic has one.\n- [ ] Read aloud, start to finish, and it sounds like a person.\n\nSources: [Inc. — sentence structure as AI tell](https://www.inc.com/jessica-stillman/this-sentence-structure-is-a-dead-giveaway-that-ai-wrote-it/91275389), [Plagiarism Today — em dashes and AI](https://www.plagiarismtoday.com/2025/06/26/em-dashes-hyphens-and-spotting-ai-writing/), [QuillBot — make AI writing sound human](https://quillbot.com/blog/ai-writing-tools/make-ai-writing-sound-human/), [vrid.ai — 27 signs of AI writing](https://vrid.ai/blog/signs-of-ai-writing), [Hunting the Muse — how to tell if writing is AI](https://huntingthemuse.net/library/how-to-tell-if-writing-is-ai).\n\n---\n\n## Part 7 — The full publish pipeline (replicable end to end)\n\nAn article is not shipped when the prose is done. It is shipped when a stranger can find it, share it, and see it whole. Run every step, in order, every time.\n\n**0. Boundary check (before writing anything).** POST `{title, markdown-or-outline}` to `/api/protocol/article-boundary`. The formula: an article is a SUBJECT (one entity or one bounded event), never a length. Verdict `append` → write DIVs into the existing page. `append_as_section` → collapsible section on the host page (it graduates to its own page past ~14 DIVs). `new_article` → proceed. An event binding two or more existing subjects (an incident, a lawsuit, an acquisition) is always its own page, cross-referenced from each subject page.\n\n**1. Featured image (default, not optional).** Generate with ArcAds using the GPT image model: `ARCADS_GENERATE gpt-image|<prompt>|16:9|||true`, then `ARCADS_TO_R2 <arcads_id>|gpt-image` for the permanent `https://miscsubjects.com/img/...` link. Prompt shape that works: minimal editorial illustration of the article's central mechanism, ink-black and paper-white with one indigo accent, generous negative space, flat vector, \"no text, no letters, no logos, no watermark\". LOOK at the result before using it. Set it as `meta.hero` on the article — that drives both the on-page hero and og:image/twitter:image.\n\n**2. Write the article** under Part 6 (logic-first, sources embedded in place, headers that carry findings). Real, sourced material only — never a fabricated event, never a planted fallacy, never invented content \"to demonstrate\" any site machinery (NO_FABRICATED_LIVE_CONTENT law; intake refuses self-declared plants with `planted_content_refused`). A worked example of a bad inference uses a genuinely bad inference found in the wild.\n\n**3. Chrome + SEO verify (curl, not vibes).** The rendered page must carry: byline dates (posted/updated + read time), the hero figure, `<link rel=\"canonical\">`, `og:image` = the hero, `article:published_time`, JSON-LD Article, `<meta name=\"robots\" content=\"index,follow...\">`. The share widget (robot button, bottom right) must show the article title, Copy link, Copy for LLM, Share on X.\n\n**4. X post series (part of shipping, not an afterthought).** Write SEVERAL posts, not one — one per facet of the article, each linking to a different portion (`/a/<slug>` for the lead; `/a/<slug>#disc-<claimId>` for a specific claim; `/a/<slug>#sources` for the evidence rail). Tag the actual parties in the story (the orgs and named people with X handles — e.g. @OpenAI, @huggingface, @ClementDelangue, @tedlieu — only handles you have verified exist). Each post: one concrete fact or finding from that facet, plain words, no hype vocabulary, ≤280 chars including the link. Post via X_POST, one call per post, and quote the returned x.com status URLs as proof.\n\n**5. Close the loop.** Screenshot the rendered page (desktop AND mobile widths) and confirm the feature you shipped is visible. Append STATE.md. If anything in the pipeline was skipped, the article is not shipped — say which step is open.\n","version":3,"of":9},{"commit":"10bb0236d","date":"2026-07-23T18:26:04-07:00","what_changed":"Platform-faithful evidence widgets #widgets-approved: publisher mastheads (Bloomberg/FORTUNE/CNBC/NPR/WSJ-only-for-WSJ/+14), statement letterhead for orgs speaking in their own name, dictionary/book/encyclopedia/wikipedia cards, pixel-faithful X/Reddit/HN/YouTube, centered 540px cards, self-contained platformRailCss (mimicry law = the one token exemption); /design shows all specimens as first-class; SOURCE_TYPES extended; design-law v1.5.0 mimicry clause; write-human gains the logic-first human-voiced article register; baselines updated","chars":14349,"added":["## Part 6 — The miscsubjects article register: logic-first, human-voiced (Law added 2026-07-24)","The house format for articles is a HYBRID: the structure of an evidence review, the voice of a person. The whole point is that a model can be told \"make an article about this incident\" and produce this shape without further prompting.","**Structure (logic-first, non-negotiable):**","- Open with what happened, in one dense factual paragraph. No scene-setting, no throat-clearing.","- Every claim files under its evidence, and every source EMBEDS IN PLACE via `[[embed:source:sN]]` on its own line, in its native platform widget, at the exact point in the argument where the reader needs it. Never a sources sidebar, never a link wall.","- Sequence by evidentiary weight: the primary documentary record first (the org that lived it), then named testimony, then independent reporting, then the social/contested layer, then color (memes, culture), then any analytical construction — flagged as such.","- Say plainly what did NOT happen when headlines invite the wrong reading.","**Headers (the robotic-header ban):**","- A sub-header is said the way a person would say it across a table, and it carries a FINDING, not a category. \"What Hugging Face said\" is a filing label — robotic. \"The company that got hacked wrote it up first\" is a finding with a voice.","- Ban the parallel-template series: \"What X said / What Y said / What Z said\" or \"The A reaction / The B reaction.\" If two headers share a grammatical template, rewrite one.","- A header may be a sentence, may take a stance, may be short (\"Hours, not weeks\"). It may never be a taxonomy node.","**Voice:** everything in Parts 1–5 applies. Concrete numbers over adjectives, one position where the evidence takes one, uneven rhythm, the read-aloud test. The register is a sharp writer walking you through a file of evidence — never a report generator filling slots.","**Worked examples live at** `/a/openai-huggingface-hack-2026` (incident/evidence review) and `/a/the-canonical-morgh-index` (essay with embedded scholarly widgets). Widget contract: see the design-law skill."],"removed":[],"text":"---\nname: write-human\ndescription: Write and revise any prose so it reads as a sharp human wrote it, not an AI. Load before drafting or editing articles, emails, landing copy, DMs, captions, summaries, handoffs, or any owner-facing or public text — and always when a draft is about to ship. The anti-slop bible: kill the AI tells, vary rhythm, use concrete detail, pass the read-aloud test.\n---\n\n# write-human\n\nThe job: prose that a smart, busy human would actually write and send. Not \"polished.\" Not \"engaging.\" Human. That means it has a point of view, uneven rhythm, concrete nouns, and zero of the reflexes below.\n\nThis is a revision discipline as much as a drafting one. Draft, then hunt the tells and cut them. The tells are mechanical; catching them is mechanical too.\n\n---\n\n## Part 1 — The tell catalog (delete on sight)\n\nEach entry: the tell, why it reads as machine, the fix.\n\n### Punctuation and structure tells\n\n**1. Em-dash overuse.** The single strongest surface tell. AI sprinkles em dashes into every third sentence as a rhythm crutch. Humans use them sparingly, for a real interruption or a snapped-off aside. Rule: at most one em dash per ~200 words, and only where a comma, period, or colon genuinely won't do. When in doubt, split into two sentences. (Sources: Plagiarism Today, Inc., 2025.)\n\n**2. The \"It's not just X — it's Y\" construction.** And its cousins: \"This isn't about X. It's about Y.\" / \"More than a Z, it's a W.\" A hollow reveal structure that promises depth and delivers a synonym. Delete it. Say the actual thing once.\n\n**3. Rule-of-three everywhere.** \"Fast, reliable, and affordable.\" \"It informs, it delights, it converts.\" Triple-parallel lists are the metronome of AI prose. One list of three in a whole piece is fine. Three of them on a page is a fingerprint. Break the pattern: use two, use four, use one blunt item.\n\n**4. Triple-parallel sentence openers.** \"You'll learn X. You'll discover Y. You'll master Z.\" Anaphora on a schedule. Cut to one, or vary the verb and the structure.\n\n**5. The restating summary sentence.** A sentence that adds nothing but re-announces what you just said: \"In short, the key is consistency.\" / \"Ultimately, it all comes down to trust.\" Also the closing \"By doing X, you'll be well on your way to Y.\" Delete the recap. The last real sentence is the ending.\n\n**6. Over-transitioning.** \"Moreover,\" \"Furthermore,\" \"Additionally,\" \"That said,\" \"It's worth noting that,\" \"Importantly,\" at the head of nearly every paragraph. Humans let sentences sit next to each other without a signpost between each one. Cut ~80% of transition words. Keep the ones that carry real logical weight.\n\n**7. Uniform sentence length / metronomic rhythm.** The deepest tell — see Part 2. If every sentence is 15-22 words, it reads as generated even with every word swapped out.\n\n### Diction tells (words and phrases that scream machine)\n\n**8. The vocabulary blacklist.** Delete or replace on sight: delve, tapestry, testament (as in \"a testament to\"), navigate the landscape / navigating the complexities, boasts (of a product: \"boasts a 5-inch screen\"), elevate, seamless / seamlessly, unlock (unlock potential/value), leverage (as a verb), streamline, robust, comprehensive, cutting-edge, game-changer / game-changing, foster, harness, embark, realm, landscape (figurative), vibrant, bustling, myriad, plethora, meticulous / meticulously, resonate, underscore, pivotal, crucial, vital, essential (as filler), holistic, dynamic, innovative, revolutionize, empower, curate / curated, elevate, unleash, supercharge, \"at the forefront of,\" \"in the ever-evolving world of.\"\n\n**9. The setup phrases.** \"In today's world,\" \"In today's fast-paced world,\" \"In today's digital age,\" \"In this ever-changing landscape,\" \"Now more than ever,\" \"In the realm of,\" \"When it comes to,\" \"At the end of the day.\" These are throat-clearing. The reader wants the sentence that comes after. Delete the setup, keep the substance.\n\n**10. Fake-insight filler.** \"It's important to note that,\" \"It's worth mentioning that,\" \"One key aspect to consider is,\" \"This is a game-changer,\" \"The possibilities are endless,\" \"provides valuable insights,\" \"plays a crucial role in.\" Research finds a phrase like \"provides valuable insights\" appears vastly more often in AI text than in human writing (QuillBot, 2025). If a sentence's job is only to announce that the next thing matters, cut it and let the next thing matter on its own.\n\n**11. Adjective inflation.** \"comprehensive solution,\" \"robust framework,\" \"powerful tool,\" \"seamless experience,\" \"valuable resource.\" AI reaches for a laudatory adjective when a concrete noun would carry more. Strip the adjective or replace the phrase with the specific thing.\n\n**12. Forced enthusiasm and hype.** Exclamation points, \"Get ready to,\" \"You're going to love,\" \"The best part?\", \"Here's the kicker,\" \"And that's not all.\" Confidence is quiet. State the good thing plainly; let the reader be the one who's impressed.\n\n**13. Over-hedging.** \"may potentially,\" \"could possibly,\" \"might arguably,\" \"in many cases, it can often be the case that.\" Stacking qualifiers to avoid ever committing. Humans with a view say it. If it's true, say it's true. If it's uncertain, say \"I don't know\" or name the one real caveat once.\n\n### Shape tells\n\n**14. The balanced both-sides non-answer.** Presenting every pro and con and never landing. Humans have opinions: \"X is better than Y here, because Z.\" Take the position the evidence supports and defend it in a line.\n\n**15. The listicle reflex.** Turning every idea into a bulleted list with bolded lead-ins of exactly parallel length. Lists are for genuinely parallel items. Prose is for reasoning. Don't bullet an argument.\n\n**16. The \"treadmill.\"** Circling one idea across three paragraphs, restated with new adjectives, no new information added. Each paragraph must earn its place with a new fact, example, or move. If a paragraph only rephrases the one above it, delete it.\n\n**17. Perfect, sanded-flat neutrality.** No specific person, no real number, no dated example, no mild opinion, no rough edge. Humans leave fingerprints: a named tool, a real price, \"last Tuesday,\" a thing they think is overrated.\n\n---\n\n## Part 2 — Sentence rhythm (the deepest fix)\n\nYou can swap every blacklisted word and still sound like a machine if the rhythm is even. Human writing has a heartbeat, not a metronome.\n\n- **Vary length hard.** Put a 4-word sentence next to a 30-word one. Then a medium one. Read the length pattern, not just the words.\n- **Use fragments deliberately.** \"Not always. But often.\" A fragment is a human move; AI almost never makes it unprompted.\n- **Let one sentence run long** with real subordinate clauses when the thought genuinely has parts, then snap back short.\n- **Start sentences differently.** Not every sentence begins with the subject. Open sometimes with a time, a condition, a short verb, a \"But.\"\n- **Kill the comma-splice-plus-participle tic:** \"..., allowing you to...\", \"..., helping teams to...\", \"..., making it easy to...\" This trailing \"-ing\" clause is an AI signature. Cut it or make it its own sentence.\n\nTest: read only the sentence lengths as a sequence of numbers. If they cluster in a narrow band, break them.\n\n---\n\n## Part 3 — Concrete over abstract\n\nAbstraction is the water AI swims in. The cure is specificity.\n\n- Replace category words with instances. Not \"a beverage\" → \"a lukewarm gas-station coffee.\" Not \"stakeholders\" → \"the two people who sign the check.\"\n- Use real numbers, not \"many/several/a range of.\" \"40% of opens\" beats \"a significant portion.\"\n- Name the thing. Not \"leading tools\" → \"Ahrefs and Screaming Frog.\"\n- Prefer the verb that shows the action over the abstract noun. \"decide\" not \"make a determination,\" \"help\" not \"provide assistance.\"\n- One vivid concrete detail buys more credibility than three adjectives. Spend on detail, not on praise.\n\nHuman writers *interpret* — they tell you what a fact means and why they think so. AI *presents*. Add the interpretation.\n\n---\n\n## Part 4 — The read-aloud test (mandatory before shipping)\n\nRead the whole thing out loud, or subvocalize it at speaking pace. Flag and fix:\n\n- Any sentence you run out of breath on → split it.\n- Any phrase you'd never say to a colleague (\"navigate the ever-evolving landscape\") → rewrite in the words you'd actually use.\n- Any spot where two sentences in a row have the same shape → vary one.\n- Any sentence that made you feel nothing / added nothing → cut it.\n- Any place a transition word slowed you down → delete it.\n\nIf it doesn't sound like a person talking, it isn't done.\n\n---\n\n## Part 5 — Before / after\n\n**Example 1 — landing copy**\n\nBefore (AI):\n> In today's fast-paced digital landscape, our cutting-edge platform empowers businesses to seamlessly streamline their workflows and unlock unprecedented growth. It's not just a tool — it's a game-changer that will revolutionize the way you work.\n\nAfter (human):\n> Most teams lose an hour a day to copy-pasting between four tools. This connects them, so you stop. Setup takes about ten minutes.\n\nWhy: killed the setup phrase, the blacklist words (cutting-edge, empowers, seamlessly, streamline, unlock, game-changer, revolutionize), the \"not just X — it's Y,\" and the hype. Added a concrete number and a concrete claim.\n\n**Example 2 — email line**\n\nBefore (AI):\n> I wanted to reach out to touch base regarding a potential opportunity that I believe could provide significant value and help elevate your team's productivity to the next level.\n\nAfter (human):\n> Quick one: I think we can cut your onboarding time from three weeks to about five days. Worth fifteen minutes to show you how?\n\nWhy: killed \"reach out / touch base,\" \"provide significant value,\" \"elevate... to the next level.\" Named the actual before/after and a single small ask.\n\n**Example 3 — explanatory prose**\n\nBefore (AI):\n> There are several key factors to consider. First, it's important to note that consistency plays a crucial role. Moreover, quality is essential. Additionally, timing can be a pivotal element. Ultimately, by keeping these factors in mind, you'll be well on your way to success.\n\nAfter (human):\n> Three things move the needle, in order. Consistency first — showing up beats any single great post. Quality second. Timing barely matters until the other two are handled, so don't obsess over it early.\n\nWhy: killed \"several key factors,\" \"it's important to note,\" \"plays a crucial role,\" \"Moreover/Additionally,\" \"pivotal,\" the restating summary, and the both-sides flatness. Added rank-ordering (a real opinion), a fragment, and varied sentence length.\n\n**Example 4 — the recap-sentence delete**\n\nBefore:\n> ...and that's how the caching layer cuts response time. In short, caching is a powerful way to make your app faster, and by implementing it correctly, you can deliver a seamless experience for your users.\n\nAfter:\n> ...and that's how the caching layer cuts response time.\n\nWhy: the recap sentence added nothing. The last real sentence was already the ending.\n\n---\n\n## Part 6 — The miscsubjects article register: logic-first, human-voiced (Law added 2026-07-24)\n\nThe house format for articles is a HYBRID: the structure of an evidence review, the voice of a person. The whole point is that a model can be told \"make an article about this incident\" and produce this shape without further prompting.\n\n**Structure (logic-first, non-negotiable):**\n- Open with what happened, in one dense factual paragraph. No scene-setting, no throat-clearing.\n- Every claim files under its evidence, and every source EMBEDS IN PLACE via `[[embed:source:sN]]` on its own line, in its native platform widget, at the exact point in the argument where the reader needs it. Never a sources sidebar, never a link wall.\n- Sequence by evidentiary weight: the primary documentary record first (the org that lived it), then named testimony, then independent reporting, then the social/contested layer, then color (memes, culture), then any analytical construction — flagged as such.\n- Say plainly what did NOT happen when headlines invite the wrong reading.\n\n**Headers (the robotic-header ban):**\n- A sub-header is said the way a person would say it across a table, and it carries a FINDING, not a category. \"What Hugging Face said\" is a filing label — robotic. \"The company that got hacked wrote it up first\" is a finding with a voice.\n- Ban the parallel-template series: \"What X said / What Y said / What Z said\" or \"The A reaction / The B reaction.\" If two headers share a grammatical template, rewrite one.\n- A header may be a sentence, may take a stance, may be short (\"Hours, not weeks\"). It may never be a taxonomy node.\n\n**Voice:** everything in Parts 1–5 applies. Concrete numbers over adjectives, one position where the evidence takes one, uneven rhythm, the read-aloud test. The register is a sharp writer walking you through a file of evidence — never a report generator filling slots.\n\n**Worked examples live at** `/a/openai-huggingface-hack-2026` (incident/evidence review) and `/a/the-canonical-morgh-index` (essay with embedded scholarly widgets). Widget contract: see the design-law skill.\n\n---\n\n## Ship checklist\n\n- [ ] Em dashes: ≤1 per 200 words, each earned.\n- [ ] Zero blacklist words (Part 1, #8-9).\n- [ ] No \"not just X — it's Y,\" no setup phrases, no fake-insight filler.\n- [ ] At most one rule-of-three in the whole piece.\n- [ ] No restating summary sentence at the end.\n- [ ] Transition words cut ~80%.\n- [ ] Sentence lengths vary visibly; at least one fragment or one short punch.\n- [ ] At least one concrete number/name/detail per paragraph.\n- [ ] A real position taken where the topic has one.\n- [ ] Read aloud, start to finish, and it sounds like a person.\n\nSources: [Inc. — sentence structure as AI tell](https://www.inc.com/jessica-stillman/this-sentence-structure-is-a-dead-giveaway-that-ai-wrote-it/91275389), [Plagiarism Today — em dashes and AI](https://www.plagiarismtoday.com/2025/06/26/em-dashes-hyphens-and-spotting-ai-writing/), [QuillBot — make AI writing sound human](https://quillbot.com/blog/ai-writing-tools/make-ai-writing-sound-human/), [vrid.ai — 27 signs of AI writing](https://vrid.ai/blog/signs-of-ai-writing), [Hunting the Muse — how to tell if writing is AI](https://huntingthemuse.net/library/how-to-tell-if-writing-is-ai).\n","version":2,"of":9},{"commit":"40e18f884","date":"2026-07-23T00:07:42-07:00","what_changed":"email tracking: open pixel + click redirect endpoint, tracked-send + sent-visibility tools, email_sends table (migration 0325); write-human/business-email/cold-outreach-craft skills #vault-approved","chars":12231,"added":["---","name: write-human","description: Write and revise any prose so it reads as a sharp human wrote it, not an AI. Load before drafting or editing articles, emails, landing copy, DMs, captions, summaries, handoffs, or any owner-facing or public text — and always when a draft is about to ship. The anti-slop bible: kill the AI tells, vary rhythm, use concrete detail, pass the read-aloud test.","# write-human","The job: prose that a smart, busy human would actually write and send. Not \"polished.\" Not \"engaging.\" Human. That means it has a point of view, uneven rhythm, concrete nouns, and zero of the reflexes below.","This is a revision discipline as much as a drafting one. Draft, then hunt the tells and cut them. The tells are mechanical; catching them is mechanical too.","## Part 1 — The tell catalog (delete on sight)","Each entry: the tell, why it reads as machine, the fix.","### Punctuation and structure tells","**1. Em-dash overuse.** The single strongest surface tell. AI sprinkles em dashes into every third sentence as a rhythm crutch. Humans use them sparingly, for a real interruption or a snapped-off aside. Rule: at most one em dash per ~200 words, and only where a comma, period, or colon genuinely won't do. When in doubt, split into two sentences. (Sources: Plagiarism Today, Inc., 2025.)","**2. The \"It's not just X — it's Y\" construction.** And its cousins: \"This isn't about X. It's about Y.\" / \"More than a Z, it's a W.\" A hollow reveal structure that promises depth and delivers a synonym. Delete it. Say the actual thing once.","**3. Rule-of-three everywhere.** \"Fast, reliable, and affordable.\" \"It informs, it delights, it converts.\" Triple-parallel lists are the metronome of AI prose. One list of three in a whole piece is fine. Three of them on a page is a fingerprint. Break the pattern: use two, use four, use one blunt item.","**4. Triple-parallel sentence openers.** \"You'll learn X. You'll discover Y. You'll master Z.\" Anaphora on a schedule. Cut to one, or vary the verb and the structure.","**5. The restating summary sentence.** A sentence that adds nothing but re-announces what you just said: \"In short, the key is consistency.\" / \"Ultimately, it all comes down to trust.\" Also the closing \"By doing X, you'll be well on your way to Y.\" Delete the recap. The last real sentence is the ending.","**6. Over-transitioning.** \"Moreover,\" \"Furthermore,\" \"Additionally,\" \"That said,\" \"It's worth noting that,\" \"Importantly,\" at the head of nearly every paragraph. Humans let sentences sit next to each other without a signpost between each one. Cut ~80% of transition words. Keep the ones that carry real logical weight.","**7. Uniform sentence length / metronomic rhythm.** The deepest tell — see Part 2. If every sentence is 15-22 words, it reads as generated even with every word swapped out.","### Diction tells (words and phrases that scream machine)","**8. The vocabulary blacklist.** Delete or replace on sight: delve, tapestry, testament (as in \"a testament to\"), navigate the landscape / navigating the complexities, boasts (of a product: \"boasts a 5-inch screen\"), elevate, seamless / seamlessly, unlock (unlock potential/value), leverage (as a verb), streamline, robust, comprehensive, cutting-edge, game-changer / game-changing, foster, harness, embark, realm, landscape (figurative), vibrant, bustling, myriad, plethora, meticulous / meticulously, resonate, underscore, pivotal, crucial, vital, essential (as filler), holistic, dynamic, innovative, revolutionize, empower, curate / curated, elevate, unleash, supercharge, \"at the forefront of,\" \"in the ever-evolving world of.\"","**9. The setup phrases.** \"In today's world,\" \"In today's fast-paced world,\" \"In today's digital age,\" \"In this ever-changing landscape,\" \"Now more than ever,\" \"In the realm of,\" \"When it comes to,\" \"At the end of the day.\" These are throat-clearing. The reader wants the sentence that comes after. Delete the setup, keep the substance.","**10. Fake-insight filler.** \"It's important to note that,\" \"It's worth mentioning that,\" \"One key aspect to consider is,\" \"This is a game-changer,\" \"The possibilities are endless,\" \"provides valuable insights,\" \"plays a crucial role in.\" Research finds a phrase like \"provides valuable insights\" appears vastly more often in AI text than in human writing (QuillBot, 2025). If a sentence's job is only to announce that the next thing matters, cut it and let the next thing matter on its own.","**11. Adjective inflation.** \"comprehensive solution,\" \"robust framework,\" \"powerful tool,\" \"seamless experience,\" \"valuable resource.\" AI reaches for a laudatory adjective when a concrete noun would carry more. Strip the adjective or replace the phrase with the specific thing.","**12. Forced enthusiasm and hype.** Exclamation points, \"Get ready to,\" \"You're going to love,\" \"The best part?\", \"Here's the kicker,\" \"And that's not all.\" Confidence is quiet. State the good thing plainly; let the reader be the one who's impressed.","**13. Over-hedging.** \"may potentially,\" \"could possibly,\" \"might arguably,\" \"in many cases, it can often be the case that.\" Stacking qualifiers to avoid ever committing. Humans with a view say it. If it's true, say it's true. If it's uncertain, say \"I don't know\" or name the one real caveat once.","### Shape tells","**14. The balanced both-sides non-answer.** Presenting every pro and con and never landing. Humans have opinions: \"X is better than Y here, because Z.\" Take the position the evidence supports and defend it in a line.","**15. The listicle reflex.** Turning every idea into a bulleted list with bolded lead-ins of exactly parallel length. Lists are for genuinely parallel items. Prose is for reasoning. Don't bullet an argument.","**16. The \"treadmill.\"** Circling one idea across three paragraphs, restated with new adjectives, no new information added. Each paragraph must earn its place with a new fact, example, or move. If a paragraph only rephrases the one above it, delete it.","**17. Perfect, sanded-flat neutrality.** No specific person, no real number, no dated example, no mild opinion, no rough edge. Humans leave fingerprints: a named tool, a real price, \"last Tuesday,\" a thing they think is overrated.","## Part 2 — Sentence rhythm (the deepest fix)","You can swap every blacklisted word and still sound like a machine if the rhythm is even. Human writing has a heartbeat, not a metronome.","- **Vary length hard.** Put a 4-word sentence next to a 30-word one. Then a medium one. Read the length pattern, not just the words.","- **Use fragments deliberately.** \"Not always. But often.\" A fragment is a human move; AI almost never makes it unprompted.","- **Let one sentence run long** with real subordinate clauses when the thought genuinely has parts, then snap back short.","- **Start sentences differently.** Not every sentence begins with the subject. Open sometimes with a time, a condition, a short verb, a \"But.\"","- **Kill the comma-splice-plus-participle tic:** \"..., allowing you to...\", \"..., helping teams to...\", \"..., making it easy to...\" This trailing \"-ing\" clause is an AI signature. Cut it or make it its own sentence.","Test: read only the sentence lengths as a sequence of numbers. If they cluster in a narrow band, break them.","## Part 3 — Concrete over abstract","Abstraction is the water AI swims in. The cure is specificity.","- Replace category words with instances. Not \"a beverage\" → \"a lukewarm gas-station coffee.\" Not \"stakeholders\" → \"the two people who sign the check.\"","- Use real numbers, not \"many/several/a range of.\" \"40% of opens\" beats \"a significant portion.\"","- Name the thing. Not \"leading tools\" → \"Ahrefs and Screaming Frog.\"","- Prefer the verb that shows the action over the abstract noun. \"decide\" not \"make a determination,\" \"help\" not \"provide assistance.\"","- One vivid concrete detail buys more credibility than three adjectives. Spend on detail, not on praise.","Human writers *interpret* — they tell you what a fact means and why they think so. AI *presents*. Add the interpretation.","## Part 4 — The read-aloud test (mandatory before shipping)","Read the whole thing out loud, or subvocalize it at speaking pace. Flag and fix:","- Any sentence you run out of breath on → split it.","- Any phrase you'd never say to a colleague (\"navigate the ever-evolving landscape\") → rewrite in the words you'd actually use.","- Any spot where two sentences in a row have the same shape → vary one.","- Any sentence that made you feel nothing / added nothing → cut it.","- Any place a transition word slowed you down → delete it.","If it doesn't sound like a person talking, it isn't done.","## Part 5 — Before / after","**Example 1 — landing copy**","Before (AI):","> In today's fast-paced digital landscape, our cutting-edge platform empowers businesses to seamlessly streamline their workflows and unlock unprecedented growth. It's not just a tool — it's a game-changer that will revolutionize the way you work.","After (human):","> Most teams lose an hour a day to copy-pasting between four tools. This connects them, so you stop. Setup takes about ten minutes.","Why: killed the setup phrase, the blacklist words (cutting-edge, empowers, seamlessly, streamline, unlock, game-changer, revolutionize), the \"not just X — it's Y,\" and the hype. Added a concrete number and a concrete claim.","**Example 2 — email line**","> I wanted to reach out to touch base regarding a potential opportunity that I believe could provide significant value and help elevate your team's productivity to the next level.","> Quick one: I think we can cut your onboarding time from three weeks to about five days. Worth fifteen minutes to show you how?","Why: killed \"reach out / touch base,\" \"provide significant value,\" \"elevate... to the next level.\" Named the actual before/after and a single small ask.","**Example 3 — explanatory prose**","> There are several key factors to consider. First, it's important to note that consistency plays a crucial role. Moreover, quality is essential. Additionally, timing can be a pivotal element. Ultimately, by keeping these factors in mind, you'll be well on your way to success.","> Three things move the needle, in order. Consistency first — showing up beats any single great post. Quality second. Timing barely matters until the other two are handled, so don't obsess over it early.","Why: killed \"several key factors,\" \"it's important to note,\" \"plays a crucial role,\" \"Moreover/Additionally,\" \"pivotal,\" the restating summary, and the both-sides flatness. Added rank-ordering (a real opinion), a fragment, and varied sentence length.","**Example 4 — the recap-sentence delete**","Before:","> ...and that's how the caching layer cuts response time. In short, caching is a powerful way to make your app faster, and by implementing it correctly, you can deliver a seamless experience for your users.","After:","> ...and that's how the caching layer cuts response time.","Why: the recap sentence added nothing. The last real sentence was already the ending.","## Ship checklist","- [ ] Em dashes: ≤1 per 200 words, each earned.","- [ ] Zero blacklist words (Part 1, #8-9).","- [ ] No \"not just X — it's Y,\" no setup phrases, no fake-insight filler.","- [ ] At most one rule-of-three in the whole piece.","- [ ] No restating summary sentence at the end.","- [ ] Transition words cut ~80%.","- [ ] Sentence lengths vary visibly; at least one fragment or one short punch.","- [ ] At least one concrete number/name/detail per paragraph.","- [ ] A real position taken where the topic has one.","- [ ] Read aloud, start to finish, and it sounds like a person.","Sources: [Inc. — sentence structure as AI tell](https://www.inc.com/jessica-stillman/this-sentence-structure-is-a-dead-giveaway-that-ai-wrote-it/91275389), [Plagiarism Today — em dashes and AI](https://www.plagiarismtoday.com/2025/06/26/em-dashes-hyphens-and-spotting-ai-writing/), [QuillBot — make AI writing sound human](https://quillbot.com/blog/ai-writing-tools/make-ai-writing-sound-human/), [vrid.ai — 27 signs of AI writing](https://vrid.ai/blog/signs-of-ai-writing), [Hunting the Muse — how to tell if writing is AI](https://huntingthemuse.net/library/how-to-tell-if-writing-is-ai)."],"removed":[],"text":"---\nname: write-human\ndescription: Write and revise any prose so it reads as a sharp human wrote it, not an AI. Load before drafting or editing articles, emails, landing copy, DMs, captions, summaries, handoffs, or any owner-facing or public text — and always when a draft is about to ship. The anti-slop bible: kill the AI tells, vary rhythm, use concrete detail, pass the read-aloud test.\n---\n\n# write-human\n\nThe job: prose that a smart, busy human would actually write and send. Not \"polished.\" Not \"engaging.\" Human. That means it has a point of view, uneven rhythm, concrete nouns, and zero of the reflexes below.\n\nThis is a revision discipline as much as a drafting one. Draft, then hunt the tells and cut them. The tells are mechanical; catching them is mechanical too.\n\n---\n\n## Part 1 — The tell catalog (delete on sight)\n\nEach entry: the tell, why it reads as machine, the fix.\n\n### Punctuation and structure tells\n\n**1. Em-dash overuse.** The single strongest surface tell. AI sprinkles em dashes into every third sentence as a rhythm crutch. Humans use them sparingly, for a real interruption or a snapped-off aside. Rule: at most one em dash per ~200 words, and only where a comma, period, or colon genuinely won't do. When in doubt, split into two sentences. (Sources: Plagiarism Today, Inc., 2025.)\n\n**2. The \"It's not just X — it's Y\" construction.** And its cousins: \"This isn't about X. It's about Y.\" / \"More than a Z, it's a W.\" A hollow reveal structure that promises depth and delivers a synonym. Delete it. Say the actual thing once.\n\n**3. Rule-of-three everywhere.** \"Fast, reliable, and affordable.\" \"It informs, it delights, it converts.\" Triple-parallel lists are the metronome of AI prose. One list of three in a whole piece is fine. Three of them on a page is a fingerprint. Break the pattern: use two, use four, use one blunt item.\n\n**4. Triple-parallel sentence openers.** \"You'll learn X. You'll discover Y. You'll master Z.\" Anaphora on a schedule. Cut to one, or vary the verb and the structure.\n\n**5. The restating summary sentence.** A sentence that adds nothing but re-announces what you just said: \"In short, the key is consistency.\" / \"Ultimately, it all comes down to trust.\" Also the closing \"By doing X, you'll be well on your way to Y.\" Delete the recap. The last real sentence is the ending.\n\n**6. Over-transitioning.** \"Moreover,\" \"Furthermore,\" \"Additionally,\" \"That said,\" \"It's worth noting that,\" \"Importantly,\" at the head of nearly every paragraph. Humans let sentences sit next to each other without a signpost between each one. Cut ~80% of transition words. Keep the ones that carry real logical weight.\n\n**7. Uniform sentence length / metronomic rhythm.** The deepest tell — see Part 2. If every sentence is 15-22 words, it reads as generated even with every word swapped out.\n\n### Diction tells (words and phrases that scream machine)\n\n**8. The vocabulary blacklist.** Delete or replace on sight: delve, tapestry, testament (as in \"a testament to\"), navigate the landscape / navigating the complexities, boasts (of a product: \"boasts a 5-inch screen\"), elevate, seamless / seamlessly, unlock (unlock potential/value), leverage (as a verb), streamline, robust, comprehensive, cutting-edge, game-changer / game-changing, foster, harness, embark, realm, landscape (figurative), vibrant, bustling, myriad, plethora, meticulous / meticulously, resonate, underscore, pivotal, crucial, vital, essential (as filler), holistic, dynamic, innovative, revolutionize, empower, curate / curated, elevate, unleash, supercharge, \"at the forefront of,\" \"in the ever-evolving world of.\"\n\n**9. The setup phrases.** \"In today's world,\" \"In today's fast-paced world,\" \"In today's digital age,\" \"In this ever-changing landscape,\" \"Now more than ever,\" \"In the realm of,\" \"When it comes to,\" \"At the end of the day.\" These are throat-clearing. The reader wants the sentence that comes after. Delete the setup, keep the substance.\n\n**10. Fake-insight filler.** \"It's important to note that,\" \"It's worth mentioning that,\" \"One key aspect to consider is,\" \"This is a game-changer,\" \"The possibilities are endless,\" \"provides valuable insights,\" \"plays a crucial role in.\" Research finds a phrase like \"provides valuable insights\" appears vastly more often in AI text than in human writing (QuillBot, 2025). If a sentence's job is only to announce that the next thing matters, cut it and let the next thing matter on its own.\n\n**11. Adjective inflation.** \"comprehensive solution,\" \"robust framework,\" \"powerful tool,\" \"seamless experience,\" \"valuable resource.\" AI reaches for a laudatory adjective when a concrete noun would carry more. Strip the adjective or replace the phrase with the specific thing.\n\n**12. Forced enthusiasm and hype.** Exclamation points, \"Get ready to,\" \"You're going to love,\" \"The best part?\", \"Here's the kicker,\" \"And that's not all.\" Confidence is quiet. State the good thing plainly; let the reader be the one who's impressed.\n\n**13. Over-hedging.** \"may potentially,\" \"could possibly,\" \"might arguably,\" \"in many cases, it can often be the case that.\" Stacking qualifiers to avoid ever committing. Humans with a view say it. If it's true, say it's true. If it's uncertain, say \"I don't know\" or name the one real caveat once.\n\n### Shape tells\n\n**14. The balanced both-sides non-answer.** Presenting every pro and con and never landing. Humans have opinions: \"X is better than Y here, because Z.\" Take the position the evidence supports and defend it in a line.\n\n**15. The listicle reflex.** Turning every idea into a bulleted list with bolded lead-ins of exactly parallel length. Lists are for genuinely parallel items. Prose is for reasoning. Don't bullet an argument.\n\n**16. The \"treadmill.\"** Circling one idea across three paragraphs, restated with new adjectives, no new information added. Each paragraph must earn its place with a new fact, example, or move. If a paragraph only rephrases the one above it, delete it.\n\n**17. Perfect, sanded-flat neutrality.** No specific person, no real number, no dated example, no mild opinion, no rough edge. Humans leave fingerprints: a named tool, a real price, \"last Tuesday,\" a thing they think is overrated.\n\n---\n\n## Part 2 — Sentence rhythm (the deepest fix)\n\nYou can swap every blacklisted word and still sound like a machine if the rhythm is even. Human writing has a heartbeat, not a metronome.\n\n- **Vary length hard.** Put a 4-word sentence next to a 30-word one. Then a medium one. Read the length pattern, not just the words.\n- **Use fragments deliberately.** \"Not always. But often.\" A fragment is a human move; AI almost never makes it unprompted.\n- **Let one sentence run long** with real subordinate clauses when the thought genuinely has parts, then snap back short.\n- **Start sentences differently.** Not every sentence begins with the subject. Open sometimes with a time, a condition, a short verb, a \"But.\"\n- **Kill the comma-splice-plus-participle tic:** \"..., allowing you to...\", \"..., helping teams to...\", \"..., making it easy to...\" This trailing \"-ing\" clause is an AI signature. Cut it or make it its own sentence.\n\nTest: read only the sentence lengths as a sequence of numbers. If they cluster in a narrow band, break them.\n\n---\n\n## Part 3 — Concrete over abstract\n\nAbstraction is the water AI swims in. The cure is specificity.\n\n- Replace category words with instances. Not \"a beverage\" → \"a lukewarm gas-station coffee.\" Not \"stakeholders\" → \"the two people who sign the check.\"\n- Use real numbers, not \"many/several/a range of.\" \"40% of opens\" beats \"a significant portion.\"\n- Name the thing. Not \"leading tools\" → \"Ahrefs and Screaming Frog.\"\n- Prefer the verb that shows the action over the abstract noun. \"decide\" not \"make a determination,\" \"help\" not \"provide assistance.\"\n- One vivid concrete detail buys more credibility than three adjectives. Spend on detail, not on praise.\n\nHuman writers *interpret* — they tell you what a fact means and why they think so. AI *presents*. Add the interpretation.\n\n---\n\n## Part 4 — The read-aloud test (mandatory before shipping)\n\nRead the whole thing out loud, or subvocalize it at speaking pace. Flag and fix:\n\n- Any sentence you run out of breath on → split it.\n- Any phrase you'd never say to a colleague (\"navigate the ever-evolving landscape\") → rewrite in the words you'd actually use.\n- Any spot where two sentences in a row have the same shape → vary one.\n- Any sentence that made you feel nothing / added nothing → cut it.\n- Any place a transition word slowed you down → delete it.\n\nIf it doesn't sound like a person talking, it isn't done.\n\n---\n\n## Part 5 — Before / after\n\n**Example 1 — landing copy**\n\nBefore (AI):\n> In today's fast-paced digital landscape, our cutting-edge platform empowers businesses to seamlessly streamline their workflows and unlock unprecedented growth. It's not just a tool — it's a game-changer that will revolutionize the way you work.\n\nAfter (human):\n> Most teams lose an hour a day to copy-pasting between four tools. This connects them, so you stop. Setup takes about ten minutes.\n\nWhy: killed the setup phrase, the blacklist words (cutting-edge, empowers, seamlessly, streamline, unlock, game-changer, revolutionize), the \"not just X — it's Y,\" and the hype. Added a concrete number and a concrete claim.\n\n**Example 2 — email line**\n\nBefore (AI):\n> I wanted to reach out to touch base regarding a potential opportunity that I believe could provide significant value and help elevate your team's productivity to the next level.\n\nAfter (human):\n> Quick one: I think we can cut your onboarding time from three weeks to about five days. Worth fifteen minutes to show you how?\n\nWhy: killed \"reach out / touch base,\" \"provide significant value,\" \"elevate... to the next level.\" Named the actual before/after and a single small ask.\n\n**Example 3 — explanatory prose**\n\nBefore (AI):\n> There are several key factors to consider. First, it's important to note that consistency plays a crucial role. Moreover, quality is essential. Additionally, timing can be a pivotal element. Ultimately, by keeping these factors in mind, you'll be well on your way to success.\n\nAfter (human):\n> Three things move the needle, in order. Consistency first — showing up beats any single great post. Quality second. Timing barely matters until the other two are handled, so don't obsess over it early.\n\nWhy: killed \"several key factors,\" \"it's important to note,\" \"plays a crucial role,\" \"Moreover/Additionally,\" \"pivotal,\" the restating summary, and the both-sides flatness. Added rank-ordering (a real opinion), a fragment, and varied sentence length.\n\n**Example 4 — the recap-sentence delete**\n\nBefore:\n> ...and that's how the caching layer cuts response time. In short, caching is a powerful way to make your app faster, and by implementing it correctly, you can deliver a seamless experience for your users.\n\nAfter:\n> ...and that's how the caching layer cuts response time.\n\nWhy: the recap sentence added nothing. The last real sentence was already the ending.\n\n---\n\n## Ship checklist\n\n- [ ] Em dashes: ≤1 per 200 words, each earned.\n- [ ] Zero blacklist words (Part 1, #8-9).\n- [ ] No \"not just X — it's Y,\" no setup phrases, no fake-insight filler.\n- [ ] At most one rule-of-three in the whole piece.\n- [ ] No restating summary sentence at the end.\n- [ ] Transition words cut ~80%.\n- [ ] Sentence lengths vary visibly; at least one fragment or one short punch.\n- [ ] At least one concrete number/name/detail per paragraph.\n- [ ] A real position taken where the topic has one.\n- [ ] Read aloud, start to finish, and it sounds like a person.\n\nSources: [Inc. — sentence structure as AI tell](https://www.inc.com/jessica-stillman/this-sentence-structure-is-a-dead-giveaway-that-ai-wrote-it/91275389), [Plagiarism Today — em dashes and AI](https://www.plagiarismtoday.com/2025/06/26/em-dashes-hyphens-and-spotting-ai-writing/), [QuillBot — make AI writing sound human](https://quillbot.com/blog/ai-writing-tools/make-ai-writing-sound-human/), [vrid.ai — 27 signs of AI writing](https://vrid.ai/blog/signs-of-ai-writing), [Hunting the Muse — how to tell if writing is AI](https://huntingthemuse.net/library/how-to-tell-if-writing-is-ai).\n","version":1,"of":9}]}]}