-- decision-constitution@1.2.0: the exhaustive-citation rule (C8) baked in — the version test
-- in /a/auditable-reasoning-audited proved it is the single change that converges independent
-- models onto an identical clause signature, which is what let the seal reach APPROVE
-- (inv_bq7bp4l78t). Object: functions/_lib/decision_constitution.js. No row shape change.
UPDATE directory SET updated_at = datetime('now') WHERE key = 'DECISION_CONSTITUTION';
