-- Self-test group identity (documented in code; no D1 schema change).
-- ButterCup (BLOOIO_API_KEY_BUTTERCUP / [PHONE]) = questions only.
-- Pepper (BLOOIO_API_KEY_PEPPERUP / [BUILD_PHONE]) = answers only.
-- ROUTER dispatch is in-process; webhook must not re-route TEST prompts in grp_d21e1ea99f8a4ea0.
SELECT 1;