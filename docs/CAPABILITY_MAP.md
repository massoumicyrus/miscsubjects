# CAPABILITY_MAP — recursive surface discovery

Generated 2026-06-13 by run-wild workflow wf_6a5a6aa4-975. Read-only discovery. 5 surfaces.
Classify: read | write | destructive | spend | send | unknown. Wire = directory-row template.

## Installed CLIs on macOS (Darwin 25.4.0), home /Users/owner. 15/15 target CLIs present. Versions: claude 2.1.165, codex-cli 0.139.0, gemini 0.46.0, grok 1.0.1, gh 2.93.0, wrangler 4.98.0, git 2.50.1 (Apple), rg 15.1.0, fd 10.4.2, ffmpeg 8.1.1, pandoc 3.9.0.2, tesseract 5.5.2, sqlite3 3.51.0, bun 1.3.14, shortcuts (no --version, subcommands run/list/view/sign). Timeout enforced via perl alarm (no timeout/gtimeout binary present). All probes read-only: --version/--help only; sqlite3 mapped via piped .help/.quit (never left interactive).
counts: `{"cli_present":15,"cli_target":15,"cli_missing":0,"total_ops":73}`

**gaps:** No timeout/gtimeout binary on this Mac; enforced the 5s cap with a perl alarm wrapper instead.; shortcuts rejects --version (Unknown option); version unknown, but the binary works and exposes run/list/view/sign.; sqlite3 --help would drop into interactive mode; mapped via piped .help/.quit (read-only) instead of --help.; Agentic CLIs (claude -p, codex exec, gemini -p, grok -p, bun run, bun x, shortcuts run, gh api, sqlite3) are classify=unknown: read-shaped invocation but can perform writes/spend depending on prompt, args, or the shortcut/SQL/script body. No CLI errored and none required auth to print --help/--version.

**notes:** All 15 target CLIs are installed and responded to --version and --help (shortcuts via subcommand help; sqlite3 via piped .help). Recursed one level on the four richest: gh (api/auth/repo/pr/issue/release/run/workflow/secret/search), wrangler (deploy/dev/d1/kv/r2/secret/tail/pages/versions/vectorize/whoami), git (status/log/diff/add/commit/push/pull/fetch/checkout/branch/reset/clone), claude (agents/auth/mcp/plugin/project/ultrareview/update/doctor + -p print/--json-schema). Every probe was read-only: only --version/--help (5s perl-alarm cap), never started interactive mode, no writes/sends/spends/deploys. No secrets surfaced in any --help output, so no redaction was needed. classify values: read = pure inspection; write = mutates local/remote state; destructive = git reset (--hard) can discard work; unknown = invocation reads but the operation's effect depends on args/prompt/SQL/script (flagged rather than guessed). No spend/send op was conclusively identified from help text alone — billing/messaging effects of agentic CLIs and shortcuts run are subsumed under unknown. Useful op count total_ops=73 spans the items[] rows above.

---

## https://miscsubjects.com/api/directory — public dispatch directory, 331 rows, JSON {count, rows:[{key,type,target,auth,content,category,allowed_categories,seq,enabled,planner_visible,planner_rank,input_schema,examples,updated_at}]}. No `sensitive` field exists; classification derived from key/target verbs, embedded HTTP methods (inline `METHOD url` and `target_map:{...,"method":...}`), and category. Every row enabled=1. Invoke any row via dispatch {key, body}. fn = JS function (camelCase target), http = outbound HTTP (often a target_map of multiple methods), agent = LLM persona on a model, flow = orchestration with empty target. fn/http are summarized by category (counts + member keys + per-class breakdown in detail); all 15 agents and 37 flows listed individually.
counts: `{"by_type":{"fn":117,"http":162,"agent":15,"flow":37},"by_category":{"uncategorized_null":44,"cli":29,"util":16,"agent":15,"cloudflare":15,"local":12,"capability":11,"phone":11,"directory":10,"google":9,"pages":9,"stripe":9,"flow":8,"computer":8,"arcads":7,"kv":7,"builder":6,"grok":6,"self_mod":5,"openai":5,"settings":5,"threads":5,"articles":4,"desktop":4,"d1":4,"llm":4,"r2":4,"session":4,"ai":4,"security":4,"blooio":3,"log":3,"content":3,"file":3,"rest_meta":3,"repo":3,"voice":3,"tasks":2,"agents":2,"docs":2,"loopdata":2,"meta":2,"sheets":2,"web":2,"audit":1,"bigcommerce":1,"expert":1,"github":1,"cloaker":1,"klaviyo":1,"asset":1,"mcp":1,"queue":1,"channel":1,"triplewhale":1,"twochat":1},"by_classify":{"write":200,"read":93,"send":27,"destructive":8,"spend":3,"unknown_remaining_after_override":0},"sensitive_count":0,"enabled_count":331,"disabled_count":0,"note_on_classify":"Derived, not from API. destructive(8 distinct row keys): PAGES_DELETE, STRIPE_WRITE, BUILDER_DELETE, KV_DEL, DEL_ROW, R2_DEL, WATCH_RULE_DELETE, INVALIDATE_DIR_SNAPSHOT. spend(3): SEND_NAMED_INVOICE, SEND_PEPTIDE_INVOICE, STRIPE_CATALOG_SYNC (plus META/TW proxies and STRIPE_WRITE are spend-capable). Multi-method target_map rows ARTICLES/BLOOIO/CF carry GET+POST+PATCH+DELETE — counted as write but each is delete-capable."}`

---

## /Users/owner/miscsubjects-pages/docs/api/ — captured vendor API specs (8 vendor dirs)
counts: `{"vendors":8,"vendor_dirs_present":6,"vendor_dirs_empty":3,"arcads_paths":63,"arcads_operations":76,"arcads_schemas":73,"arcads_params":80,"arcads_request_bodies":32,"arcads_method_get":29,"arcads_method_post":35,"arcads_method_put":5,"arcads_method_delete":7,"live_directory_rows_total":337,"live_http_rows_total":198,"grok_ref_endpoints":17,"openai_ref_endpoints":46}`

| name | kind | class | detail | wire |
|---|---|---|---|---|
| arcads | api | write | OpenAPI 3.0.0 spec at /Users/owner/miscsubjects-pages/docs/api/arcads/openapi-spec.json (103,887 bytes). 63 paths / 76 operations (35 POST, 29 | http rows per endpoint (one directory row per path+verb, type='http', target='VERB https://external-api.arcads.ai/<path>', auth=he |
| stripe | api | spend | Docs dir /Users/owner/miscsubjects-pages/docs/api/stripe is EMPTY (no openapi/llms/md captured). Stripe coverage exists only as live directory | http rows per endpoint (already 45 live rows / ~30 distinct endpoints wired to api.stripe.com); GET-only per standing rule |
| klaviyo | api | send | Docs dir /Users/owner/miscsubjects-pages/docs/api/klaviyo does NOT exist among the 8 captured dirs (only arcads, blooio, gemini, grok, openai, | http rows per endpoint (1 live row / ~2 endpoints wired to a.klaviyo.com); no captured spec to expand from |
| twochat/2chat | api | send | Docs dir /Users/owner/miscsubjects-pages/docs/api/twochat is EMPTY (no openapi/llms/md). 2Chat WhatsApp/SMS coverage exists only as live direc | http rows per endpoint (10 live rows / ~9 endpoints wired to api.p.2chat.io); no captured spec under docs/api to expand from |
| grok | api | spend | llms-full.txt at /Users/owner/miscsubjects-pages/docs/api/grok/llms-full.txt (1,173,723 bytes / 33,105 lines) + 21 split REST-reference sectio | http rows per endpoint (3 live rows wired to api.x.ai; ~14 more endpoints in the captured reference are unwired) |
| openai | api | spend | llms-full.txt at /Users/owner/miscsubjects-pages/docs/api/openai/llms-full.txt + openai/sections split files. ~46 distinct /v1 endpoints refer | http rows per endpoint (only 1 live row wired to api.openai.com; ~45 referenced endpoints unwired) |
| blooio | api | send | Docs dir /Users/owner/miscsubjects-pages/docs/api/blooio is EMPTY (no openapi/llms/md captured). Blooio iMessage webhook/send coverage exists  | http rows per endpoint (already 20 live rows / ~33 endpoints wired to backend.blooio.com); no captured spec under docs/api |
| gemini | api | spend | Docs dir /Users/owner/miscsubjects-pages/docs/api/gemini is EMPTY (no openapi/llms/md captured). Gemini (generativelanguage.googleapis.com) ha | http rows per endpoint (0 wired, 0 captured spec — fully unwired; needs a captured spec or llms.txt before rows can be authored) |

**gaps:** No captured spec for stripe or klaviyo under docs/api (no dir at all); their coverage inferred only from live directory http rows, not from a docs artifact.; blooio, gemini, twochat docs dirs are empty — vendor counts treat them as docs-present-but-empty; gemini has neither spec nor live rows (fully dark).; Live directory counts are reconstructed from migrations/*.sql (latest-wins, parsing INSERT/DELETE) — no local D1 sqlite exists under .wrangler (only KV/cache), so counts are not read from a live DB. Bundled target_map rows pack multiple endpoints into one key, so live_rows undercounts true endpoint coverage; distinct-endpoint figures are approximate (regex path-normalization).; grok/openai endpoint counts come from prose llms-full.txt + section .md, not a machine OpenAPI spec, so distinct-endpoint counts (17/46) are extraction approximations, not authoritative operation counts.; arcads openapi-spec.json has empty servers[] — base URL (external-api.arcads.ai) taken from openapi-docs.md/live rows, not the spec itself.

**notes:** Captured-spec material: only arcads has a real machine-readable OpenAPI spec (openapi-spec.json: 63 paths / 76 ops / 73 schemas / 80 params, HTTP Basic auth). grok and openai have large captured llms-full.txt + split section .md endpoint references (grok ~17 api.x.ai/v1 endpoints across 21 REST-reference files; openai ~46 /v1 endpoints, both Bearer auth), but NO OpenAPI JSON. blooio, gemini, twochat/2chat docs dirs exist but are EMPTY; klaviyo + stripe have no docs/api dir captured at all.  Wired vs docs (live directory = D1 directory table, type IN fn/http/agent/flow, reconstructed from migrations/*.sql latest-wins: 337 total rows, 198 http rows): - WIRED (live http rows / distinct endpoints): stripe 45/~30, blooio 20/~33, 2chat 10/~9, grok 3/3, arcads 2/8 (bundled via ARCADS_ROUTES/ARCADS_PRODUCTS target_map), klaviyo 1/2, openai 1/1. - UNWIRED = material gain:   1. arcads — 76-operation OpenAPI spec captured but only ~8 endpoints wired via 2 bundled rows; ~68 operations (audio-driven, b-roll, omnihuman, presets/*, folders, products, file-upload presign, elevenlabs, assets) are docs-only. Largest single expansion off a clean machine spec.   2. openai — ~46 endpoints in captured llms-full, only 1 wired. ~45 unwired.   3. grok — ~17 endpoints in captured reference, 3 wired. ~14 unwired.   4. gemini — 0 wired AND 0 captured spec; fully dark surface, needs a spec/llms.txt capture first (probe an llms.txt before authoring rows). Stripe writes remain forbidden by standing rule (GET-only) regardless of wiring. All classifications: arcads=write (mixed POST/PUT/DELETE creative generation), stripe=spend, klaviyo/2chat/blooio=send, grok/openai/gemini=spend (paid token/credit inference). READ-ONLY honored throughout: jq/grep/python parse only, no CLI run, no HTTP, no secret printed (auth shown as $ARCADS_BASIC_AUTH / Bearer <REDACTED> placeholders as stored).

---

**gaps:** sdef CLI is non-functional: `/usr/bin/sdef` is an xcode-select shim that exits 1 ('tool sdef requires Xcode, but active developer directory /Library/Developer/CommandLineTools is a command line tools instance'); full Xcode is NOT installed (no /Applications/Xcode.app) and CommandLineTools ships no real sdef/sdp binary. Worked around by parsing each app's bundled .sdef in Contents/Resources directly (equivalent source XML). Install Xcode to restore the CLI.; Dictionary counts are app-specific-suite only for the 4 apps using xi:include (Messages +1, Notes +1, Safari +1, System Events +2). The included Standard Suite / Text Suite (generic open/close/count/delete/duplicate/exists/make/move/print/quit/save, ~8-11 commands) was NOT merged into the counts because the included files were not resolved/parsed. Music and Chrome embed the standard suite inline so their counts are complete. To get fully-merged totals, resolve the xi:include hrefs (point at /System/Library/ScriptingDefinitions/CocoaStandard.sdef and the app's bundled suite files).; Alfred 5 is installed and powerpack-licensed but has ZERO workflows. The path specified in the task (~/Library/Application Support/Alfred/Alfred.alfredpreferences/workflows) does not exist; Alfred.alfredpreferences is a package whose only child is preferences/, and preferences/local/<synchash>/ holds only a 'hotkey' settings dir. No user.workflow.* bundles found anywhere under the Alfred support tree. Gallery/workflows is also empty. Nothing to enumerate.; Per-shortcut action classification (read/write/send/destructive) was NOT resolved — `shortcuts view`/export was not run (would have surfaced each shortcut's action graph). Classifications above are inferred from names only; 'Door Lock'/'Gate code'/'Home' imply HomeKit physical-destructive and 'Send Email' implies send, but the action contents are unverified.

**notes:** Method: `shortcuts list` (41, matches expected). For dictionaries, `sdef <app>` failed on all 6 (Xcode-required shim), so each app's bundled Contents/Resources/*.sdef was parsed with a Python ElementTree namespace-agnostic walk counting <command>/<class>/<xi:include> — not a single grep. Alfred located at /Applications/Alfred 5.app with prefs under ~/Library/Application Support/Alfred (NOT the task's stated workflows path); zero workflows present. All operations read-only: only `ls`/`find`/`mdfind`/`shortcuts list`/file reads/XML parse — no shortcut was run, no AppleScript executed, no app launched, no JS dispatched. Control primitives noted (osascript keystroke/click/notification, open -a, URL schemes) but not invoked. Highest-risk surface: System Events (keystroke/click drives any GUI; log out/restart/shut down) and Chrome/Safari execute/do-JavaScript (arbitrary page code) — all classified destructive/write and currently gated (Accessibility for keystroke, Allow-JS-from-Apple-Events for browser JS).

---

## Live Claude Code MCP layer — connected servers as MCP_IMPORT execution surfaces. Confirmed via ToolSearch schema fetches (Notion, Klaviyo, Chrome both bridges, registry) + deferred-tool name inventory (Preview, scheduled-tasks, Figma, Drive, Semantic Scholar, Typefully). Server UUIDs in tool prefixes are the import handles.
counts: `{"servers":11,"write_capable":8,"read_only":3,"send_or_publish_capable":3,"browser_automation_surfaces":3}`

| name | kind | class | detail | wire |
|---|---|---|---|---|
| Notion | mcp | write | UUID 7a003c5e-67f6-457d-95a1-63f271ee4992. read+write. Tools: notion-search (semantic over workspace + connected Slack/Drive/GitHub/Jira/Teams/SharePo | MCP_IMPORT server=7a003c5e-... then MCP_TOOL_CALL rows: one row per tool. Read rows (notion-search/fetch/query-database-view) safe |
| Klaviyo | mcp | send | UUID d07cb3b1-c887-4c4f-8c7e-ba318882194f. read+write+send. READ: get_campaigns/get_campaign, get_flows/get_flow, get_lists/get_list, get_segments/get | MCP_IMPORT server=d07cb3b1-... then one MCP_TOOL_CALL row per endpoint; every row carries model=claude param. Classify rows: repor |
| Control_Chrome | mcp | write | read+write (arbitrary JS = anything the logged-in session can do). Tools: open_url, navigate (go_back/go_forward), list_tabs, get_current_tab, switch_ | MCP_IMPORT (Control_Chrome bridge name, no uuid prefix) then MCP_TOOL_CALL rows: get_page_content/list_tabs/get_current_tab = read |
| Claude_in_Chrome | mcp | write | read+write, full UI automation incl. submit/purchase/post. Named bridge (no uuid prefix). Tools: navigate, browser_batch (multi-step one round trip),  | MCP_IMPORT (bridge name) then MCP_TOOL_CALL rows. Read rows: get_page_text, read_page, find, read_network_requests, read_console_m |
| Claude_Preview | mcp | write | read+write but sandboxed (no real credentials/funds). Headless preview browser, not the user's real Chrome session. Tools: preview_start/stop/list, pr | MCP_IMPORT (bridge name) then MCP_TOOL_CALL rows; lower blast radius than Claude_in_Chrome since no logged-in session. Read rows:  |
| scheduled-tasks | mcp | write | read+write, creates persistent standing config. Tools: create_scheduled_task (cron or one-time fireAt, writes {taskId}/SKILL.md under ~/.claude/schedu | MCP_IMPORT then MCP_TOOL_CALL rows: list_scheduled_tasks = read; create/update = write (standing config -> confirm per CLAUDE.md p |
| Google Drive | mcp | write | read+write. UUID 1e473c35-51d0-4876-b1a3-7fbe29aaddb9. READ: search_files (structured query DSL), read_file_content, download_file_content, get_file_m | MCP_IMPORT server=1e473c35-... then MCP_TOOL_CALL rows. Read rows (search_files/read_file_content/get_file_metadata) safe. create_ |
| Figma | mcp | write | read+write. UUID 4383a6db-422e-4a87-b236-000bf04417ed. READ: whoami, get_metadata, get_screenshot, get_design_context, get_variable_defs, get_librarie | MCP_IMPORT server=4383a6db-... then MCP_TOOL_CALL rows. Read rows: get_screenshot/get_metadata/get_variable_defs/search_design_sys |
| Typefully | mcp | send | read+write+publish. UUID acf0dd56-550c-4790-8e17-103bff40264a. READ: get_me, list_drafts/get_draft, get_queue/get_queue_schedule, list_social_sets/get | MCP_IMPORT server=acf0dd56-... then MCP_TOOL_CALL rows. Read rows: get_me, list_drafts, get_queue, *_analytics_*. Publish rows: cr |
| Semantic Scholar / PubMed article lookup | mcp | read | read-only. UUID 4d0ef0c2-dc52-4331-8a9d-6ccaeb8da5b5 (PubMed/PMC-backed article server). Tools: search_articles (PubMed syntax/NL, date filters), get_ | MCP_IMPORT server=4d0ef0c2-... then MCP_TOOL_CALL rows, all read. NL 'find peer-reviewed studies on BPC-157 oral bioavailability'  |
| mcp-registry | mcp | read | read + user-gated install UI. Meta-server. Tools: search_mcp_registry (find connectors by keyword, returns connected status), suggest_connectors (rend | MCP_IMPORT registry then MCP_TOOL_CALL rows: search_mcp_registry/list_connectors = read. suggest_connectors renders Connect button |

**notes:** Top 3 to import first for real-world business execution. (1) Klaviyo (d07cb3b1): the only connected surface that directly performs the core DTC revenue motion - reads campaign/flow/segment performance AND can create+send email/SMS and mutate profile consent. Highest direct-money leverage; also the one most needing hard send-gates and the BigCommerce-owns-revenue reconciliation caveat. (2) Claude_in_Chrome: universal fallback executor - anything with no API (BigCommerce admin, Meta Ads Manager, Triple Whale, bank/vendor portals) is reachable through the user's authenticated Chrome via form_input/computer/browser_batch, making it the broadest real-world-action surface; pair every write row with confirm/prohibited tiers since a misfired click can send/spend/delete. (3) Notion (7a003c5e): the system of record and cross-tool search hub - backs the Loop content MATRIX, the factual peptide DB, and the audit ledgers the standing-orders law requires, and notion-search reaches Slack/Drive/GitHub/Jira too, so one import yields both the writable data board and a federated read across the other stacks. Runner-up: scheduled-tasks turns any of the above into a durable cadence (recurring reporting pulls, model-input nudges) which the end-of-turn-sync and reporting laws call for. Import-layer classification rule: reports/get_*/search_* rows are read and may auto-run; create_campaign/subscribe (Klaviyo), Typefully publish/schedule, any Chrome click-to-submit, and all standing-config writes (scheduled-tasks create/update, Notion data-source DDL) must carry per-action confirm; OAuth grant / ACL change / fund movement stay prohibited-tier regardless of connector.

---

