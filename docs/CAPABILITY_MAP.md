# CAPABILITY_MAP — recursive surface discovery

Generated 2026-06-13 by run-wild workflow wf_6a5a6aa4-975. Read-only discovery. 5 surfaces.
Classify: read | write | destructive | spend | send | unknown. Wire = directory-row template.

## Surfaces (index)
- Installed CLIs on macOS (Darwin 25.4.0), home /Users/owner. 15/15 target CLIs present. Versions: claude 2.1.165, codex-cli  (items=72)
- https://miscsubjects.com/api/directory — public dispatch directory, 331 rows, JSON {count, rows:[{key,type,target,auth,content,cat  (items=116)
- /Users/owner/miscsubjects-pages/docs/api/ — captured vendor API specs (8 vendor dirs)  (items=8)
- Mac automation surface for user [OWNER_HANDLE] (Darwin 25.4.0). Three control layers enumerated read-only: (1) macOS Shortcuts via   (items=9)
- Live Claude Code MCP layer — connected servers as MCP_IMPORT execution surfaces. Confirmed via ToolSearch schema fetches (Notion,   (items=11)

## Installed CLIs on macOS (Darwin 25.4.0), home /Users/owner. 15/15 target CLIs present. Versions: claude 2.1.165, codex-cli 0.139.0, gemini 0.46.0, grok 1.0.1, gh 2.93.0, wrangler 4.98.0, git 2.50.1 (Apple), rg 15.1.0, fd 10.4.2, ffmpeg 8.1.1, pandoc 3.9.0.2, tesseract 5.5.2, sqlite3 3.51.0, bun 1.3.14, shortcuts (no --version, subcommands run/list/view/sign). Timeout enforced via perl alarm (no timeout/gtimeout binary present). All probes read-only: --version/--help only; sqlite3 mapped via piped .help/.quit (never left interactive).
counts: `{"cli_present":15,"cli_target":15,"cli_missing":0,"total_ops":73}`

| name | kind | class | detail | wire |
|---|---|---|---|---|
| claude --help | cli | read | Top-level Claude Code CLI; interactive by default, -p/--print for non-interactive output, --json-schema for structured output | row CLI_CLAUDE type=fn target=/exec cmd=claude args=["--help"] |
| claude -p | cli | unknown | Headless one-shot prompt to Claude; prints model output (read-shaped, but the agent can call tools that write) | row CLI_CLAUDE_PRINT type=fn target=/exec cmd=claude args=["-p","<prompt>"] |
| claude agents | cli | write | Manage background agents | row CLI_CLAUDE_AGENTS type=fn target=/exec cmd=claude args=["agents"] |
| claude auth | cli | write | Manage Claude Code authentication (login/token) | row CLI_CLAUDE_AUTH type=fn target=/exec cmd=claude args=["auth"] |
| claude mcp | cli | write | Configure and manage MCP servers for Claude Code | row CLI_CLAUDE_MCP type=fn target=/exec cmd=claude args=["mcp"] |
| claude plugin | cli | write | Manage Claude Code plugins (install/list) | row CLI_CLAUDE_PLUGIN type=fn target=/exec cmd=claude args=["plugin"] |
| claude project | cli | write | Manage Claude Code project state | row CLI_CLAUDE_PROJECT type=fn target=/exec cmd=claude args=["project"] |
| claude ultrareview | cli | read | Cloud-hosted multi-agent code review of current branch/PR; prints findings | row CLI_CLAUDE_ULTRAREVIEW type=fn target=/exec cmd=claude args=["ultrareview"] |
| claude update | cli | write | Check for Claude Code updates and install if available | row CLI_CLAUDE_UPDATE type=fn target=/exec cmd=claude args=["update"] |
| claude doctor | cli | read | Check health of the Claude Code auto-updater; spawns stdio MCP servers from .mcp.json | row CLI_CLAUDE_DOCTOR type=fn target=/exec cmd=claude args=["doctor"] |
| codex exec | cli | unknown | Run Codex agent non-interactively on a prompt (alias e); can execute model-generated shell commands | row CLI_CODEX_EXEC type=fn target=/exec cmd=codex args=["exec","<prompt>"] |
| codex review | cli | read | Run a Codex code review non-interactively | row CLI_CODEX_REVIEW type=fn target=/exec cmd=codex args=["review"] |
| codex login | cli | write | Manage Codex login / store auth credentials | row CLI_CODEX_LOGIN type=fn target=/exec cmd=codex args=["login"] |
| codex mcp | cli | write | Manage external MCP servers for Codex | row CLI_CODEX_MCP type=fn target=/exec cmd=codex args=["mcp"] |
| codex apply | cli | write | Apply the latest Codex-produced diff as git apply to working tree (alias a) | row CLI_CODEX_APPLY type=fn target=/exec cmd=codex args=["apply"] |
| codex doctor | cli | read | Diagnose local Codex install, config, auth, runtime health | row CLI_CODEX_DOCTOR type=fn target=/exec cmd=codex args=["doctor"] |
| codex update | cli | write | Update Codex CLI to latest version | row CLI_CODEX_UPDATE type=fn target=/exec cmd=codex args=["update"] |
| gemini -p | cli | unknown | Gemini CLI headless prompt (non-interactive); -o json for structured output | row CLI_GEMINI_PROMPT type=fn target=/exec cmd=gemini args=["-p","<prompt>"] |
| gemini mcp | cli | write | Manage Gemini CLI MCP servers | row CLI_GEMINI_MCP type=fn target=/exec cmd=gemini args=["mcp"] |
| gemini extensions | cli | write | Manage Gemini CLI extensions | row CLI_GEMINI_EXT type=fn target=/exec cmd=gemini args=["extensions"] |
| gemini skills | cli | write | Manage Gemini agent skills | row CLI_GEMINI_SKILLS type=fn target=/exec cmd=gemini args=["skills"] |
| grok -p | cli | unknown | Grok conversational CLI headless single-prompt mode; honor reasoning_effort=none per the owner law | row CLI_GROK_PROMPT type=fn target=/exec cmd=grok args=["-p","<prompt>"] |
| grok git | cli | write | Grok-assisted git operations | row CLI_GROK_GIT type=fn target=/exec cmd=grok args=["git"] |
| grok mcp | cli | write | Manage Grok CLI MCP servers | row CLI_GROK_MCP type=fn target=/exec cmd=grok args=["mcp"] |
| gh api | cli | unknown | Authenticated GitHub REST/GraphQL request; method defaults GET, -X POST/etc for writes | row CLI_GH_API type=fn target=/exec cmd=gh args=["api","<endpoint>"] |
| gh auth | cli | write | Authenticate gh and git with GitHub (login/token/status) | row CLI_GH_AUTH type=fn target=/exec cmd=gh args=["auth","status"] |
| gh repo | cli | unknown | Manage GitHub repos (clone/create/view/delete subcommands) | row CLI_GH_REPO type=fn target=/exec cmd=gh args=["repo","view"] |
| gh pr | cli | unknown | Manage pull requests (list/view read; create/merge/close write) | row CLI_GH_PR type=fn target=/exec cmd=gh args=["pr","list"] |
| gh issue | cli | unknown | Manage issues (list/view read; create/close write) | row CLI_GH_ISSUE type=fn target=/exec cmd=gh args=["issue","list"] |
| gh release | cli | unknown | Manage releases (list/view read; create/delete write) | row CLI_GH_RELEASE type=fn target=/exec cmd=gh args=["release","list"] |
| gh run | cli | unknown | View/watch GitHub Actions workflow runs (rerun/cancel write) | row CLI_GH_RUN type=fn target=/exec cmd=gh args=["run","list"] |
| gh workflow | cli | unknown | List/view workflows; run triggers a workflow_dispatch | row CLI_GH_WORKFLOW type=fn target=/exec cmd=gh args=["workflow","list"] |
| gh secret | cli | write | Manage GitHub Actions/Codespaces secrets (set/delete write; list read) | row CLI_GH_SECRET type=fn target=/exec cmd=gh args=["secret","list"] |
| gh search | cli | read | Search repos/issues/PRs/code across GitHub | row CLI_GH_SEARCH type=fn target=/exec cmd=gh args=["search","repos","<q>"] |
| wrangler deploy | cli | write | Deploy a Worker to Cloudflare | row CLI_WR_DEPLOY type=fn target=/exec cmd=wrangler args=["deploy"] |
| wrangler dev | cli | read | Start a local dev server for a Worker | row CLI_WR_DEV type=fn target=/exec cmd=wrangler args=["dev"] |
| wrangler d1 | cli | unknown | Manage D1 databases (create/list/info read; execute can write) | row CLI_WR_D1 type=fn target=/exec cmd=wrangler args=["d1","list"] |
| wrangler kv | cli | unknown | Manage KV namespaces/keys/bulk (get/list read; put/delete write) | row CLI_WR_KV type=fn target=/exec cmd=wrangler args=["kv","namespace","list"] |
| wrangler r2 | cli | unknown | Manage R2 buckets/objects/sql (list read; put/delete write) | row CLI_WR_R2 type=fn target=/exec cmd=wrangler args=["r2","bucket","list"] |
| wrangler secret | cli | write | Manage Worker secrets (put/delete write; list read) | row CLI_WR_SECRET type=fn target=/exec cmd=wrangler args=["secret","list"] |
| wrangler tail | cli | read | Live-tail a Worker's logs | row CLI_WR_TAIL type=fn target=/exec cmd=wrangler args=["tail","<worker>"] |
| wrangler pages | cli | unknown | Configure Cloudflare Pages (dev/project read; deploy write) | row CLI_WR_PAGES type=fn target=/exec cmd=wrangler args=["pages","project","list"] |
| wrangler versions | cli | unknown | List/view/upload/deploy Worker Versions | row CLI_WR_VERSIONS type=fn target=/exec cmd=wrangler args=["versions","list"] |
| wrangler vectorize | cli | unknown | Manage Vectorize indexes (get/list read; create/delete write) | row CLI_WR_VECTORIZE type=fn target=/exec cmd=wrangler args=["vectorize","list"] |
| wrangler whoami | cli | read | Retrieve authenticated Cloudflare user info | row CLI_WR_WHOAMI type=fn target=/exec cmd=wrangler args=["whoami"] |
| git status | cli | read | Show working tree status | row CLI_GIT_STATUS type=fn target=/exec cmd=git args=["status"] |
| git log | cli | read | Show commit logs | row CLI_GIT_LOG type=fn target=/exec cmd=git args=["log"] |
| git diff | cli | read | Show changes between commits/tree | row CLI_GIT_DIFF type=fn target=/exec cmd=git args=["diff"] |
| git add | cli | write | Stage file contents to the index | row CLI_GIT_ADD type=fn target=/exec cmd=git args=["add","<pathspec>"] |
| git commit | cli | write | Record staged changes to the repository | row CLI_GIT_COMMIT type=fn target=/exec cmd=git args=["commit","-m","<msg>"] |
| git push | cli | write | Update remote refs with local commits | row CLI_GIT_PUSH type=fn target=/exec cmd=git args=["push"] |
| git pull | cli | write | Fetch and integrate from a remote/branch | row CLI_GIT_PULL type=fn target=/exec cmd=git args=["pull"] |
| git fetch | cli | write | Download objects/refs from another repo | row CLI_GIT_FETCH type=fn target=/exec cmd=git args=["fetch"] |
| git checkout | cli | write | Switch branches / restore tree files | row CLI_GIT_CHECKOUT type=fn target=/exec cmd=git args=["checkout","<branch>"] |
| git branch | cli | unknown | List/create/delete branches | row CLI_GIT_BRANCH type=fn target=/exec cmd=git args=["branch"] |
| git reset | cli | destructive | Reset HEAD; --hard discards working-tree changes | row CLI_GIT_RESET type=fn target=/exec cmd=git args=["reset"] |
| git clone | cli | write | Clone a repository into a new directory | row CLI_GIT_CLONE type=fn target=/exec cmd=git args=["clone","<repo>"] |
| rg | cli | read | Recursively regex-search files (PCRE2 enabled); --files lists, --type-list types | row CLI_RG type=fn target=/exec cmd=rg args=["<pattern>","<path>"] |
| fd | cli | read | Find filesystem entries by regex/glob; -x can exec commands per match | row CLI_FD type=fn target=/exec cmd=fd args=["<pattern>","<path>"] |
| ffmpeg | cli | write | Transcode/convert audio/video; writes output files (-i in, out path) | row CLI_FFMPEG type=fn target=/exec cmd=ffmpeg args=["-i","<in>","<out>"] |
| pandoc | cli | write | Convert documents between formats; -o writes output file | row CLI_PANDOC type=fn target=/exec cmd=pandoc args=["-f","<from>","-t","<to>","-o","<out>","<in>"] |
| tesseract | cli | write | OCR an image to text/output base; --list-langs read-only | row CLI_TESSERACT type=fn target=/exec cmd=tesseract args=["<image>","<outbase>"] |
| sqlite3 | cli | unknown | SQLite shell; SELECT/.dump read, INSERT/UPDATE/DELETE/.import write to the db file | row CLI_SQLITE3 type=fn target=/exec cmd=sqlite3 args=["<db>","<sql>"] |
| bun run | cli | unknown | Execute a JS/TS file or package.json script with Bun runtime | row CLI_BUN_RUN type=fn target=/exec cmd=bun args=["run","<file-or-script>"] |
| bun install | cli | write | Install package.json dependencies (writes node_modules/lockfile) | row CLI_BUN_INSTALL type=fn target=/exec cmd=bun args=["install"] |
| bun add | cli | write | Add a dependency to package.json | row CLI_BUN_ADD type=fn target=/exec cmd=bun args=["add","<pkg>"] |
| bun test | cli | read | Run unit tests with Bun | row CLI_BUN_TEST type=fn target=/exec cmd=bun args=["test"] |
| bun x | cli | unknown | Execute a package binary, installing if needed (bunx) | row CLI_BUN_X type=fn target=/exec cmd=bun args=["x","<pkg>"] |
| shortcuts list | cli | read | List macOS Shortcuts (--folders, --show-identifiers) | row CLI_SHORTCUTS_LIST type=fn target=/exec cmd=shortcuts args=["list"] |
| shortcuts run | cli | unknown | Run a macOS Shortcut by name/identifier; arbitrary side effects depending on the shortcut | row CLI_SHORTCUTS_RUN type=fn target=/exec cmd=shortcuts args=["run","<name>"] |
| shortcuts view | cli | read | Open a shortcut in the Shortcuts app | row CLI_SHORTCUTS_VIEW type=fn target=/exec cmd=shortcuts args=["view","<name>"] |
| shortcuts sign | cli | write | Sign a shortcut file (writes signed output) | row CLI_SHORTCUTS_SIGN type=fn target=/exec cmd=shortcuts args=["sign"] |

**gaps:** No timeout/gtimeout binary on this Mac; enforced the 5s cap with a perl alarm wrapper instead.; shortcuts rejects --version (Unknown option); version unknown, but the binary works and exposes run/list/view/sign.; sqlite3 --help would drop into interactive mode; mapped via piped .help/.quit (read-only) instead of --help.; Agentic CLIs (claude -p, codex exec, gemini -p, grok -p, bun run, bun x, shortcuts run, gh api, sqlite3) are classify=unknown: read-shaped invocation but can perform writes/spend depending on prompt, args, or the shortcut/SQL/script body. No CLI errored and none required auth to print --help/--version.

**notes:** All 15 target CLIs are installed and responded to --version and --help (shortcuts via subcommand help; sqlite3 via piped .help). Recursed one level on the four richest: gh (api/auth/repo/pr/issue/release/run/workflow/secret/search), wrangler (deploy/dev/d1/kv/r2/secret/tail/pages/versions/vectorize/whoami), git (status/log/diff/add/commit/push/pull/fetch/checkout/branch/reset/clone), claude (agents/auth/mcp/plugin/project/ultrareview/update/doctor + -p print/--json-schema). Every probe was read-only: only --version/--help (5s perl-alarm cap), never started interactive mode, no writes/sends/spends/deploys. No secrets surfaced in any --help output, so no redaction was needed. classify values: read = pure inspection; write = mutates local/remote state; destructive = git reset (--hard) can discard work; unknown = invocation reads but the operation's effect depends on args/prompt/SQL/script (flagged rather than guessed). No spend/send op was conclusively identified from help text alone — billing/messaging effects of agentic CLIs and shortcuts run are subsumed under unknown. Useful op count total_ops=73 spans the items[] rows above.

---

## https://miscsubjects.com/api/directory — public dispatch directory, 331 rows, JSON {count, rows:[{key,type,target,auth,content,category,allowed_categories,seq,enabled,planner_visible,planner_rank,input_schema,examples,updated_at}]}. No `sensitive` field exists; classification derived from key/target verbs, embedded HTTP methods (inline `METHOD url` and `target_map:{...,"method":...}`), and category. Every row enabled=1. Invoke any row via dispatch {key, body}. fn = JS function (camelCase target), http = outbound HTTP (often a target_map of multiple methods), agent = LLM persona on a model, flow = orchestration with empty target. fn/http are summarized by category (counts + member keys + per-class breakdown in detail); all 15 agents and 37 flows listed individually.
counts: `{"by_type":{"fn":117,"http":162,"agent":15,"flow":37},"by_category":{"uncategorized_null":44,"cli":29,"util":16,"agent":15,"cloudflare":15,"local":12,"capability":11,"phone":11,"directory":10,"google":9,"pages":9,"stripe":9,"flow":8,"computer":8,"arcads":7,"kv":7,"builder":6,"grok":6,"self_mod":5,"openai":5,"settings":5,"threads":5,"articles":4,"desktop":4,"d1":4,"llm":4,"r2":4,"session":4,"ai":4,"security":4,"blooio":3,"log":3,"content":3,"file":3,"rest_meta":3,"repo":3,"voice":3,"tasks":2,"agents":2,"docs":2,"loopdata":2,"meta":2,"sheets":2,"web":2,"audit":1,"bigcommerce":1,"expert":1,"github":1,"cloaker":1,"klaviyo":1,"asset":1,"mcp":1,"queue":1,"channel":1,"triplewhale":1,"twochat":1},"by_classify":{"write":200,"read":93,"send":27,"destructive":8,"spend":3,"unknown_remaining_after_override":0},"sensitive_count":0,"enabled_count":331,"disabled_count":0,"note_on_classify":"Derived, not from API. destructive(8 distinct row keys): PAGES_DELETE, STRIPE_WRITE, BUILDER_DELETE, KV_DEL, DEL_ROW, R2_DEL, WATCH_RULE_DELETE, INVALIDATE_DIR_SNAPSHOT. spend(3): SEND_NAMED_INVOICE, SEND_PEPTIDE_INVOICE, STRIPE_CATALOG_SYNC (plus META/TW proxies and STRIPE_WRITE are spend-capable). Multi-method target_map rows ARTICLES/BLOOIO/CF carry GET+POST+PATCH+DELETE — counted as write but each is delete-capable."}`

| name | kind | class | detail | wire |
|---|---|---|---|---|
| ROUTER | agent | write | category=agent target=grok-4.3 (top-level NL router, planner_visible=0 rank=100; emits [KEY]args[/KEY] dispatch tags) | dispatch {key:'ROUTER', body:{message,...}} |
| ARCADS | agent | write | category=agent target=grok-4.3 (creative/ArcAds ad-generation persona) | dispatch {key:'ARCADS', body:{input}} |
| ASK_CLAUDE | agent | write | category=agent target=claude-fable-5 (cross-check / second-opinion model) | dispatch {key:'ASK_CLAUDE', body:{prompt}} |
| ASK_GEMINI | agent | write | category=agent target=gemini-2.5-flash | dispatch {key:'ASK_GEMINI', body:{prompt}} |
| ASK_GPT | agent | write | category=agent target=gpt-4o | dispatch {key:'ASK_GPT', body:{prompt}} |
| ASK_KIMI | agent | write | category=agent target=kimi-k2.6 | dispatch {key:'ASK_KIMI', body:{prompt}} |
| BUILDER | agent | write | category=agents target=grok-4.3 planner_visible=1 (self-build/row-authoring agent) | dispatch {key:'BUILDER', body:{input}} |
| CF_EXPERT | agent | write | category=expert target=grok-4.3 planner_visible=1 rank=9 (Cloudflare domain expert) | dispatch {key:'CF_EXPERT', body:{question}} |
| GW_DEEPSEEK | agent | write | category=agent target=gw:openai/gpt-4.1-mini (AI-Gateway-routed model) | dispatch {key:'GW_DEEPSEEK', body:{prompt}} |
| GW_FABLE | agent | write | category=agent target=gw:openai/gpt-4.1-mini | dispatch {key:'GW_FABLE', body:{prompt}} |
| GW_LLAMA | agent | write | category=agent target=gw:@cf/meta/llama-3.3-70b-instruct-fp8-fast | dispatch {key:'GW_LLAMA', body:{prompt}} |
| OPS | agent | write | category=agent target=grok-4.3 (ops/infra persona; hands heavy CLI work to [TERMINUS] which has no row) | dispatch {key:'OPS', body:{input}} |
| PLANNER | agent | write | category=agents target=grok-4.3 planner_visible=1 (multi-step planner over planner_visible rows) | dispatch {key:'PLANNER', body:{goal}} |
| VOICE | agent | write | category=agent target=grok-4.3 (voice/TTS-facing persona) | dispatch {key:'VOICE', body:{input}} |
| XAI_CHAT | agent | write | category=agent target=grok-build-0.1 (raw xAI chat passthrough) | dispatch {key:'XAI_CHAT', body:{messages}} |
| ARTICLE_GET | flow | read | category=articles target=(empty/orchestration); fetch a single article | dispatch {key:'ARTICLE_GET', body:{id}} |
| BLOOIO_FINISH | flow | write | category=flow; completes a Blooio iMessage turn (delivery side) | dispatch {key:'BLOOIO_FINISH', body:{...}} |
| BLOOIO_LOGS_TAIL | flow | read | category=log; tail Blooio webhook logs | dispatch {key:'BLOOIO_LOGS_TAIL', body:{n}} |
| BLOOIO_TURN_PHASE_A | flow | write | category=flow; first phase of an inbound iMessage turn (parse+route) | dispatch {key:'BLOOIO_TURN_PHASE_A', body:{event}} |
| CONTENT_GET | flow | read | category=content; fetch one loop-content record | dispatch {key:'CONTENT_GET', body:{id}} |
| CONTENT_LIST | flow | read | category=content; list loop-content records | dispatch {key:'CONTENT_LIST', body:{filter}} |
| CONTENT_SEARCH | flow | read | category=content; search loop-content | dispatch {key:'CONTENT_SEARCH', body:{q}} |
| DELIVER_PENDING_ASSETS | flow | send | category=flow; delivers queued assets out to a channel (send side-effect) | dispatch {key:'DELIVER_PENDING_ASSETS', body:{...}} |
| DIRECTORY_GET | flow | read | category=directory; fetch one directory row | dispatch {key:'DIRECTORY_GET', body:{key}} |
| DIRECTORY_LIST | flow | read | category=directory; list directory rows | dispatch {key:'DIRECTORY_LIST', body:{filter}} |
| DIRECT_EXEC_IF_PREFIX | flow | write | category=flow; if message has a [KEY] prefix, dispatch that row directly (bypasses router) | dispatch {key:'DIRECT_EXEC_IF_PREFIX', body:{text}} |
| FIDELITY_REPORT | flow | read | category=None; audit/fidelity report over rows | dispatch {key:'FIDELITY_REPORT', body:{}} |
| GROK_LEDGER_TAIL | flow | read | category=log; tail Grok credit/usage ledger | dispatch {key:'GROK_LEDGER_TAIL', body:{n}} |
| LOG_TAIL | flow | read | category=log; tail general event log | dispatch {key:'LOG_TAIL', body:{n}} |
| META_CAPI_EVENT | flow | send | category=meta; sends a Meta Conversions-API event (outbound to Facebook) | dispatch {key:'META_CAPI_EVENT', body:{event}} |
| PAGES_GET | flow | read | category=pages; fetch one page record | dispatch {key:'PAGES_GET', body:{slug}} |
| PAGES_LIST | flow | read | category=pages; list pages | dispatch {key:'PAGES_LIST', body:{}} |
| PAGES_VERSIONS | flow | read | category=pages; list version history for a page | dispatch {key:'PAGES_VERSIONS', body:{slug}} |
| PHONE_CLIPBOARD_HANDLE | flow | send | category=phone; handle inbound clipboard share from phone shortcut | dispatch {key:'PHONE_CLIPBOARD_HANDLE', body:{payload}} |
| PHONE_LOCATION_HANDLE | flow | send | category=phone; handle inbound location share | dispatch {key:'PHONE_LOCATION_HANDLE', body:{payload}} |
| PHONE_NOTIFY | flow | send | category=phone; push a notification to the phone | dispatch {key:'PHONE_NOTIFY', body:{text}} |
| PHONE_SHARE_IMAGE_HANDLE | flow | send | category=phone; handle inbound image share | dispatch {key:'PHONE_SHARE_IMAGE_HANDLE', body:{payload}} |
| PHONE_SHARE_TEXT_HANDLE | flow | send | category=phone; handle inbound text share | dispatch {key:'PHONE_SHARE_TEXT_HANDLE', body:{payload}} |
| PHONE_SHARE_URL_HANDLE | flow | send | category=phone; handle inbound URL share | dispatch {key:'PHONE_SHARE_URL_HANDLE', body:{payload}} |
| PHONE_VOICE_NOTE_HANDLE | flow | send | category=phone; handle inbound voice note (STT) | dispatch {key:'PHONE_VOICE_NOTE_HANDLE', body:{payload}} |
| REPO_ABSORB | flow | write | category=repo; ingest/absorb an external repo's capabilities into the directory | dispatch {key:'REPO_ABSORB', body:{repo}} |
| SERVE_PAGE | flow | read | category=pages; render/serve a stored page | dispatch {key:'SERVE_PAGE', body:{slug}} |
| SETTINGS_GET | flow | read | category=settings; read one setting | dispatch {key:'SETTINGS_GET', body:{key}} |
| SETTINGS_LIST | flow | read | category=settings; list settings | dispatch {key:'SETTINGS_LIST', body:{}} |
| SHEETS_SYNC_ALL | flow | write | category=sheets; sync all data to Google Sheets (writes external sheet) | dispatch {key:'SHEETS_SYNC_ALL', body:{}} |
| SHORTCUT_RUN | flow | send | category=phone; trigger an iOS Shortcut on the phone (remote action/send) | dispatch {key:'SHORTCUT_RUN', body:{name,args}} |
| STORE_AND_LOG_REF | flow | write | category=flow; store a reference image to R2/Drive and log the row | dispatch {key:'STORE_AND_LOG_REF', body:{image}} |
| STRIPE_PEPTIDES | flow | read | category=stripe; peptide catalog lookup for invoicing (read of Stripe products) | dispatch {key:'STRIPE_PEPTIDES', body:{}} |
| STRIPE_SKUS_BY_NAME | flow | read | category=stripe; resolve a Stripe SKU/price by product name | dispatch {key:'STRIPE_SKUS_BY_NAME', body:{name}} |
| STRIPE_SKUS_LIST | flow | read | category=stripe; list Stripe SKUs/prices | dispatch {key:'STRIPE_SKUS_LIST', body:{}} |
| TASKS_LIST | flow | read | category=tasks; list tasks | dispatch {key:'TASKS_LIST', body:{}} |
| WEBHOOK_INTAKE | flow | send | category=flow; generic inbound webhook intake/dispatch entrypoint | dispatch {key:'WEBHOOK_INTAKE', body:{event}} |
| http:cli | http | write | 29 rows [write:29] arbitrary CLI execution, each POST https://agent.cannibal.capital/exec :: CLI_AWS, CLI_BREW, CLI_BUN, CLI_CLASP, CLI_CURL_LOCAL, CL | dispatch {key:'CLI_<TOOL>', body:{args/cmd}} |
| http:uncategorized | http | write | 40 rows [write:37 read:3] mostly CLI_* exec aliases + AI-gateway raw + deploy: AIG_LIST(read), AIG_RAW, CLI_AIDER, CLI_CLAUDE_CODE, CLI_CODEX, CLI_GEM | dispatch {key:'<KEY>', body:{args}} |
| http:cloudflare | http | write | 15 rows [write:8 read:4 send:3] CF API: BROWSER_JSON, BROWSER_LINKS, BROWSER_MARKDOWN, BROWSER_PDF, BROWSER_SCRAPE, BROWSER_SCREENSHOT (POST browser-r | dispatch {key:'BROWSER_*/CF_*', body:{...}} |
| http:local | http | write | 12 rows [write:11 send:1] local Mac control via exec: LOCAL_AIRDROP, LOCAL_BATTERY, LOCAL_DICTATE_TO_PHONE, LOCAL_FOCUS, LOCAL_NETWORK, LOCAL_NOTIFY(s | dispatch {key:'LOCAL_*', body:{args}} |
| http:google | http | write | 9 rows [write:9] all POST $AIRUNNER_WEB_APP_URL (Apps Script proxy): APPS_SCRIPT_RUN, GOOGLE_CALENDAR_CREATE, GOOGLE_CALENDAR_LIST, GOOGLE_DRIVE_GET,  | dispatch {key:'GOOGLE_*', body:{op,args}} |
| http:computer | http | write | 8 rows [write:8] OS-level computer-use keystroke/click on the Mac: LOCAL_ACTIVATE, LOCAL_APPS, LOCAL_FRONTMOST, LOCAL_KEYCODE, LOCAL_KEYSTROKE, LOCAL_ | dispatch {key:'LOCAL_*', body:{key/coords}} |
| http:grok | http | write | 6 rows [write:4 read:2] xAI media: GROK_IMAGE, GROK_IMAGE_EDIT, GROK_MODELS(read), GROK_TTS, GROK_VIDEO_GET(read), GROK_VIDEO_START | dispatch {key:'GROK_*', body:{prompt}} |
| http:pages | http | destructive | 5 rows [write:3 destructive:1 read:1] page CRUD incl PAGES_DELETE(destructive): PAGES_CREATE, PAGES_PUT, PAGE_GET(read), PAGE_PATCH, PAGES_DELETE | dispatch {key:'PAGES_*', body:{slug,...}} |
| http:articles | http | write | 3 rows [write:2 read:1] ARTICLES (target_map GET/POST/PATCH/DELETE aggregator — full CRUD incl delete), ART_GET(read GET), ART_PATCH(PATCH) | dispatch {key:'ARTICLES', body:{op,id}} |
| http:desktop | http | write | 3 rows [write:3] headless browser automation via POST agent.cannibal.capital/exec: BROWSER_FETCH, BROWSER_PLAYWRIGHT, BROWSER_USE (drives a real brows | dispatch {key:'BROWSER_USE', body:{task}} |
| http:directory | http | write | 3 rows [read:2 write:1] DIR_GET(read), DIR_LIST(read), DIR_PATCH(write — edits a directory row over HTTP) | dispatch {key:'DIR_PATCH', body:{key,patch}} |
| http:file | http | write | 3 rows [read:2 write:1] FILE_GET(read), FILE_LIST(read), FILE_PUT(write) | dispatch {key:'FILE_PUT', body:{path,content}} |
| http:llm | http | write | 3 rows [write:2 read:1] GEMINI_GENERATE, GW_MODELS(read), PANEL (multi-model adversarial panel) | dispatch {key:'PANEL', body:{prompt}} |
| http:rest_meta | http | write | 3 rows [read:2 write:1] INV(read inventory), SHORT_GET(read), SHORT_EDIT(write shortlink) | dispatch {key:'SHORT_EDIT', body:{...}} |
| http:blooio | http | send | 2 rows [write:1 send:1] BLOOIO (target_map GET/POST chats/messages aggregator), SEND_IMAGE_BLOOIO(send image via Blooio iMessage) | dispatch {key:'BLOOIO/SEND_IMAGE_BLOOIO', body:{...}} |
| http:settings | http | write | 2 rows [read:1 write:1] SET_GET(read), SET_PUT(write) | dispatch {key:'SET_PUT', body:{key,value}} |
| http:stripe | http | destructive | 2 rows [read:1 destructive:1] STRIPE_READ(GET), STRIPE_WRITE(POST/PUT/DELETE — billing mutations incl delete; spend-capable) | dispatch {key:'STRIPE_WRITE', body:{method,path,body}} |
| http:arcads | http | write | 1 row [write:1] ARCADS_ROUTES (target_map of external-api.arcads.ai endpoints: actors/generate/upload) | dispatch {key:'ARCADS_ROUTES', body:{route,args}} |
| http:bigcommerce | http | read | 1 row [read:1] BC (target_map GET orders/products — source-of-truth orders+revenue, read only here) | dispatch {key:'BC', body:{route}} |
| http:github | http | write | 1 row [write:1] GITHUB (GitHub API proxy — can write repos/PRs) | dispatch {key:'GITHUB', body:{method,path,body}} |
| http:cloaker | http | write | 1 row [write:1] JCI_CLASSIFY (cloaker classify endpoint) | dispatch {key:'JCI_CLASSIFY', body:{...}} |
| http:klaviyo | http | read | 1 row [read:1] KLAVIYO (profiles+events source of truth, read) | dispatch {key:'KLAVIYO', body:{route}} |
| http:kv | http | read | 1 row [read:1] KV_LIST (list KV keys over HTTP) | dispatch {key:'KV_LIST', body:{prefix}} |
| http:mcp | http | write | 1 row [write:1] MCP (generic MCP server passthrough — can call arbitrary external MCP tools) | dispatch {key:'MCP', body:{server,tool,args}} |
| http:meta | http | write | 1 row [write:1] META (Meta Marketing API proxy — spend-capable per memory: spend only, NOT revenue) | dispatch {key:'META', body:{method,path,body}} |
| http:openai | http | read | 1 row [read:1] OPENAI_MODELS (list models, GET) | dispatch {key:'OPENAI_MODELS', body:{}} |
| http:phone | http | send | 1 row [send:1] PHONE_EVENTS_TAIL (tail phone events — read of an event stream, comms category) | dispatch {key:'PHONE_EVENTS_TAIL', body:{n}} |
| http:sheets | http | write | 1 row [write:1] SHEETS_SYNC_MASTER (write master Google Sheet) | dispatch {key:'SHEETS_SYNC_MASTER', body:{}} |
| http:repo | http | read | 1 row [read:1] SNAPSHOT_META (read repo snapshot metadata) | dispatch {key:'SNAPSHOT_META', body:{}} |
| http:triplewhale | http | write | 1 row [write:1] TW (Triple Whale API proxy — attribution only per memory) | dispatch {key:'TW', body:{method,path,body}} |
| http:twochat | http | send | 1 row [send:1] TWOCHAT_SEND (send via 2Chat / WhatsApp channel) | dispatch {key:'TWOCHAT_SEND', body:{to,text}} |
| fn:util | fn | write | 16 rows [read:11 write:5]: DEDUP_INSERT, HISTORY_GET, HISTORY_SET, HTTP_FETCH(generic outbound HTTP), LAST_REPLY_OF, LOWER, MEDIA_*, NOW, REGEX_PARSE, | dispatch {key:'HTTP_FETCH', body:{url,method,body}} |
| fn:capability | fn | write | 11 rows [write:5 read:6] self-capability tooling: AGENT_IMPORT, CF_API_GAPS, DISCOVER_SOURCE, EXTRACT_CAPABILITIES, GAP_REPORT, MCP_TOOL_CALL(call any | dispatch {key:'WIRE_UP_AND_INVOKE', body:{spec}} |
| fn:arcads | fn | write | 6 rows [read:3 write:3]: ARCADS_CREDITS(read), ARCADS_FIELDS, ARCADS_GENERATE(create ad), ARCADS_UPLOAD, ARCADS_VIDEO_FIELDS, ARCADS_VIDEO_GENERATE | dispatch {key:'ARCADS_GENERATE', body:{actor,script}} |
| fn:builder | fn | destructive | 6 rows [write:4 destructive:1 read:1]: BUILDER_ADD, BUILDER_DELETE(destructive — removes a row), BUILDER_DONE, BUILDER_LIST(read), BUILDER_NEXT, +1 | dispatch {key:'BUILDER_ADD/BUILDER_DELETE', body:{...}} |
| fn:kv | fn | destructive | 6 rows [write:3 destructive:1 read:2]: KV_APPEND, KV_DEL(destructive), KV_GET(read), KV_GET_JSON(read), KV_PUT, KV_PUT_JSON | dispatch {key:'KV_PUT/KV_DEL', body:{key,value}} |
| fn:self_mod | fn | destructive | 5 rows [write:4 destructive:1]: ADD_ROW, DEL_ROW(destructive — deletes a directory row), EDIT_ROW, PROMPT_APPEND, WIRE_UP_AND_INVOKE. Self-modifying d | dispatch {key:'EDIT_ROW/ADD_ROW/DEL_ROW', body:{key,...}} |
| fn:directory | fn | destructive | 5 rows [read:4 destructive:1]: CATEGORIES(read), INVALIDATE_DIR_SNAPSHOT(destructive — busts cache), MANIFEST(read), TOOLS_IN(read), TOOLS_SEARCH(read | dispatch {key:'TOOLS_SEARCH', body:{query}} |
| fn:threads | fn | write | 5 rows [write:3 read:2]: THREAD_ADD, THREAD_APPEND, THREAD_CLOSE, THREAD_GET(read), THREAD_LIST(read) | dispatch {key:'THREAD_APPEND', body:{id,msg}} |
| fn:d1 | fn | write | 4 rows [write:1 read:3]: D1_EXEC(write — arbitrary SQL exec), D1_QUERY(read SQL), D1_TO_2D_ARRAY(read), LEDGER_QUERY(read). D1_EXEC mutates the DB. | dispatch {key:'D1_EXEC', body:{sql,params}} |
| fn:openai | fn | write | 4 rows [write:4]: GEN_DUAL, OPENAI_IMAGE, OPENAI_IMAGE_EDIT, STORE_REF_IMAGE | dispatch {key:'OPENAI_IMAGE', body:{prompt}} |
| fn:r2 | fn | destructive | 4 rows [destructive:1 read:2 write:1]: R2_DEL(destructive), R2_GET(read), R2_LIST(read), R2_PUT(write) | dispatch {key:'R2_PUT/R2_DEL', body:{key,body}} |
| fn:stripe | fn | spend | 4 rows [spend:3 read:1]: SEND_NAMED_INVOICE(spend — creates+sends Stripe invoice), SEND_PEPTIDE_INVOICE(spend), STRIPE_CATALOG_SYNC(spend — writes cat | dispatch {key:'SEND_PEPTIDE_INVOICE', body:{customer,items}} |
| fn:session | fn | write | 4 rows [read:1 write:3]: SESSION_GET(read), SESSION_RESUME, SESSION_START, SESSION_UPDATE | dispatch {key:'SESSION_START', body:{...}} |
| fn:ai | fn | write | 4 rows [write:4] Workers-AI: WAI_EMBED, WAI_RUN(run any WAI model), WAI_T2I, WAI_TRANSLATE | dispatch {key:'WAI_RUN', body:{model,input}} |
| fn:security | fn | destructive | 4 rows [write:2 destructive:1 read:1]: WATCH_ACTION, WATCH_RULE_ADD, WATCH_RULE_DELETE(destructive), WATCH_RULE_LIST(read) | dispatch {key:'WATCH_RULE_ADD/WATCH_RULE_DELETE', body:{rule}} |
| fn:agent | fn | write | 3 rows [write:2 read:1]: AGENT(invoke a named agent), AGENT_LIST(read), AGENT_SPAWN(create a new agent persona) | dispatch {key:'AGENT_SPAWN', body:{name,model,prompt}} |
| fn:uncategorized | fn | write | 3 rows [read:2 write:1]: UTCNOW(read), WORLDTIME(read), __append_test__(write test row) | dispatch {key:'UTCNOW', body:{}} |
| fn:voice | fn | send | 3 rows [send:3]: VOICE_SAY, VOICE_SEND(send voice note out), VOICE_TRANSCRIBE | dispatch {key:'VOICE_SEND', body:{text,to}} |
| fn:docs | fn | read | 2 rows [read:2]: DOCS_GET(r2Get), DOCS_SEARCH | dispatch {key:'DOCS_SEARCH', body:{q}} |
| fn:loopdata | fn | write | 2 rows [read:1 write:1]: LBL_GET(read via httpFetch), LBL_POST(write via httpFetch — loop board data) | dispatch {key:'LBL_POST', body:{...}} |
| fn:phone | fn | send | 2 rows [send:2]: PHONE_APPROVAL_CREATE(push approval request to phone), PHONE_APPROVAL_RESOLVE | dispatch {key:'PHONE_APPROVAL_CREATE', body:{prompt}} |
| fn:flow | fn | send | 2 rows [write:1 send:1]: SAVE_FLOW(write), SEND_INVOICE_VIA_BLOOIO(send — delivers Stripe invoice over iMessage) | dispatch {key:'SEND_INVOICE_VIA_BLOOIO', body:{invoice,to}} |
| fn:web | fn | read | 2 rows [read:2]: WEB_FETCH, WEB_GET (read web content) | dispatch {key:'WEB_GET', body:{url}} |
| fn:audit | fn | write | 1 row [write:1]: FIDELITY_RUN (fidelityRun — runs the audit suite) | dispatch {key:'FIDELITY_RUN', body:{}} |
| fn:tasks | fn | write | 1 row [write:1]: ADDTASK (create a task) | dispatch {key:'ADDTASK', body:{title}} |
| fn:desktop | fn | unknown | 1 row [read:1]: COMPUTER_USE_REMOTE (classified read by verb, but drives remote computer-use — treat as write/UNKNOWN privilege) | dispatch {key:'COMPUTER_USE_REMOTE', body:{task}} |
| fn:llm | fn | write | 1 row [write:1]: GROK_STT (speech-to-text via Grok) | dispatch {key:'GROK_STT', body:{audio}} |
| fn:asset | fn | write | 1 row [write:1]: LOG_ASSET (log an asset row) | dispatch {key:'LOG_ASSET', body:{asset}} |
| fn:queue | fn | send | 1 row [send:1]: QUEUE_SEND (enqueue a CF Queue message — async dispatch) | dispatch {key:'QUEUE_SEND', body:{queue,msg}} |
| fn:repo | fn | read | 1 row [read:1]: REPO_SNAPSHOT (read repo snapshot) | dispatch {key:'REPO_SNAPSHOT', body:{}} |
| fn:channel | fn | send | 1 row [send:1]: SEND_BY_CHANNEL (route+send a message on the right channel — blooio/voice/twochat) | dispatch {key:'SEND_BY_CHANNEL', body:{channel,to,text}} |
| fn:settings | fn | read | 1 row [read:1]: SETTINGS_READ_KV_FIRST (read setting, KV-first) | dispatch {key:'SETTINGS_READ_KV_FIRST', body:{key}} |
| fn:blooio | fn | send | 1 row [send:1]: VERIFY_BLOOIO_SIG (verify inbound Blooio webhook HMAC signature) | dispatch {key:'VERIFY_BLOOIO_SIG', body:{sig,body}} |

**gaps:** No `sensitive` field on any row — sensitivity could not be read from the API; every classify value is derived from key/target/category and is best-effort, not authoritative.; TERMINUS referenced as a handoff agent by OPS (clause O1c: 'Heavy terminal/infra/CLI work -> hand off [TERMINUS]') and by VOICE (V5a), but NO row with key=TERMINUS exists in the directory — dangling agent reference (x4 references).; [ASK] used as a router shortcut by OPS (O2t: 'ask claude/gemini/gpt/kimi' -> [ASK]<model>|...) but there is no single ASK row; only ASK_CLAUDE/ASK_GEMINI/ASK_GPT/ASK_KIMI exist — the [ASK] dispatcher key is unresolved.; [TOOL_KEY] appears as a placeholder token in agent prompt content, not a real key.; 0 disabled rows — nothing is gated off, so every one of the 331 actions (incl. arbitrary CLI exec, computer-use, Stripe invoicing, page/row deletes, GitHub/Meta/TripleWhale proxies) is live and dispatchable.; Many fn rows route through shared targets (httpFetch, noop, agent.cannibal.capital/exec), so a single compromised exec endpoint backs ~57 CLI/local/computer/browser rows.; COMPUTER_USE_REMOTE classified 'unknown' privilege: its verb reads as read but it drives a remote computer-use agent (effectively write/destructive-capable).

**notes:** Most powerful 10 rows for real-world execution (highest blast radius), all enabled and dispatchable now: 1. CLI_* family (29 rows, e.g. CLI_SSH, CLI_GIT, CLI_DOCKER, CLI_KUBECTL, CLI_TERRAFORM, CLI_GCLOUD, CLI_AWS, CLI_WRANGLER, CLI_CLAUDE_CODE) — arbitrary remote shell exec via POST https://agent.cannibal.capital/exec. Run any binary on the host. classify=write, but unbounded (can delete/spend/deploy). 2. LOCAL_KEYSTROKE / LOCAL_UI_CLICK / LOCAL_KEYCODE (http:computer) — OS-level keyboard+mouse control of the owner's Mac; can drive any GUI app. write. 3. STRIPE_WRITE (http:stripe) — arbitrary Stripe API mutation incl DELETE; charge/invoice/refund-capable. destructive + spend. Memory forbids Stripe writes without explicit instruction. 4. SEND_PEPTIDE_INVOICE / SEND_NAMED_INVOICE / SEND_INVOICE_VIA_BLOOIO — create a real Stripe invoice and deliver it to a customer over iMessage. spend + send (money movement). 5. EDIT_ROW / ADD_ROW / DEL_ROW / WIRE_UP_AND_INVOKE (fn:self_mod, fn:capability) — the directory edits itself: add, rewrite, delete, or author-then-invoke any row. Privilege-escalation surface (can mint new powerful rows). write/destructive. 6. D1_EXEC (fn:d1) — arbitrary SQL execution against the D1 database (the source of truth for rows/ledger/sessions). write, can drop/overwrite tables. 7. CLI_WRANGLER / WRANGLER_DEPLOY + PAGES_CREATE/PAGES_PUT/PAGES_DELETE (http:pages) — deploy/overwrite/delete the live Cloudflare Pages site (production miscsubjects.com). write/destructive, public-facing outage risk. 8. GITHUB (http:github) — full GitHub API proxy: push commits, open/merge PRs, change repo settings. write. 9. SHORTCUT_RUN / PHONE_NOTIFY / SEND_BY_CHANNEL / TWOCHAT_SEND / VOICE_SEND — outbound messaging + remote iOS Shortcut trigger across iMessage/WhatsApp/voice channels. send (impersonation/comms reach). 10. ARCADS_GENERATE / ARCADS_VIDEO_GENERATE (fn:arcads) + ARCADS_ROUTES — generate paid ad creative on external-api.arcads.ai, which consumes ArcAds credits (real spend per the owner's credit-accounting rules). write/spend-capable. Honorable mention: MCP_TOOL_CALL / MCP (call arbitrary external MCP tools), HTTP_FETCH / WEB_FETCH (unrestricted outbound HTTP egress), META + TW (Meta ad-spend and Triple Whale proxies), BROWSER_USE / BROWSER_PLAYWRIGHT (autonomous browser agent). Method: fetched /api/directory (229,359 bytes, count=331, all enabled). Classified by camelCase-tokenized key+target verbs and embedded HTTP methods (inline + target_map). No `sensitive` field exists, so classify is derived. fn(117)+http(162) summarized by category; all 15 agents + 37 flows listed individually = 116 items total covering all 331 rows.

---

## /Users/owner/miscsubjects-pages/docs/api/ — captured vendor API specs (8 vendor dirs)
counts: `{"vendors":8,"vendor_dirs_present":6,"vendor_dirs_empty":3,"arcads_paths":63,"arcads_operations":76,"arcads_schemas":73,"arcads_params":80,"arcads_request_bodies":32,"arcads_method_get":29,"arcads_method_post":35,"arcads_method_put":5,"arcads_method_delete":7,"live_directory_rows_total":337,"live_http_rows_total":198,"grok_ref_endpoints":17,"openai_ref_endpoints":46}`

| name | kind | class | detail | wire |
|---|---|---|---|---|
| arcads | api | write | OpenAPI 3.0.0 spec at /Users/owner/miscsubjects-pages/docs/api/arcads/openapi-spec.json (103,887 bytes). 63 paths / 76 operations (35 POST, 29 | http rows per endpoint (one directory row per path+verb, type='http', target='VERB https://external-api.arcads.ai/<path>', auth=he |
| stripe | api | spend | Docs dir /Users/owner/miscsubjects-pages/docs/api/stripe is EMPTY (no openapi/llms/md captured). Stripe coverage exists only as live directory | http rows per endpoint (already 45 live rows / ~30 distinct endpoints wired to api.stripe.com); GET-only per standing rule |
| klaviyo | api | send | Docs dir /Users/owner/miscsubjects-pages/docs/api/klaviyo does NOT exist among the 8 captured dirs (only arcads, blooio, gemini, grok, openai, | http rows per endpoint (1 live row / ~2 endpoints wired to a.klaviyo.com); no captured spec to expand from |
| twochat/2chat | api | send | Docs dir /Users/owner/miscsubjects-pages/docs/api/twochat is EMPTY (no openapi/llms/md). 2Chat WhatsApp/SMS coverage exists only as live direc | http rows per endpoint (10 live rows / ~9 endpoints wired to api.p.2chat.io); no captured spec under docs/api to expand from |
| grok | api | spend | llms-full.txt at /Users/owner/miscsubjects-pages/docs/api/grok/llms-full.txt (1,173,723 bytes / 33,105 lines) + 21 split REST-reference sectio | http rows per endpoint (3 live rows wired to api.x.ai; ~14 more endpoints in the captured reference are unwired) |
| openai | api | spend | llms-full.txt at /Users/owner/miscsubjects-pages/docs/api/openai/llms-full.txt + openai/sections split files. ~46 distinct /v1 endpoints refer | http rows per endpoint (only 1 live row wired to api.openai.com; ~45 referenced endpoints unwired) |
| blooio | api | send | Docs dir /Users/owner/miscsubjects-pages/docs/api/blooio is EMPTY (no openapi/llms/md captured). Blooio iMessage webhook/send coverage exists  | http rows per endpoint (already 20 live rows / ~33 endpoints wired to backend.blooio.com); no captured spec under docs/api |
| gemini | api | spend | Docs dir /Users/owner/miscsubjects-pages/docs/api/gemini is EMPTY (no openapi/llms/md captured). Gemini (generativelanguage.googleapis.com) ha | http rows per endpoint (0 wired, 0 captured spec — fully unwired; needs a captured spec or llms.txt before rows can be authored) |

**gaps:** No captured spec for stripe or klaviyo under docs/api (no dir at all); their coverage inferred only from live directory http rows, not from a docs artifact.; blooio, gemini, twochat docs dirs are empty — vendor counts treat them as docs-present-but-empty; gemini has neither spec nor live rows (fully dark).; Live directory counts are reconstructed from migrations/*.sql (latest-wins, parsing INSERT/DELETE) — no local D1 sqlite exists under .wrangler (only KV/cache), so counts are not read from a live DB. Bundled target_map rows pack multiple endpoints into one key, so live_rows undercounts true endpoint coverage; distinct-endpoint figures are approximate (regex path-normalization).; grok/openai endpoint counts come from prose llms-full.txt + section .md, not a machine OpenAPI spec, so distinct-endpoint counts (17/46) are extraction approximations, not authoritative operation counts.; arcads openapi-spec.json has empty servers[] — base URL (external-api.arcads.ai) taken from openapi-docs.md/live rows, not the spec itself.

**notes:** Captured-spec material: only arcads has a real machine-readable OpenAPI spec (openapi-spec.json: 63 paths / 76 ops / 73 schemas / 80 params, HTTP Basic auth). grok and openai have large captured llms-full.txt + split section .md endpoint references (grok ~17 api.x.ai/v1 endpoints across 21 REST-reference files; openai ~46 /v1 endpoints, both Bearer auth), but NO OpenAPI JSON. blooio, gemini, twochat/2chat docs dirs exist but are EMPTY; klaviyo + stripe have no docs/api dir captured at all.  Wired vs docs (live directory = D1 directory table, type IN fn/http/agent/flow, reconstructed from migrations/*.sql latest-wins: 337 total rows, 198 http rows): - WIRED (live http rows / distinct endpoints): stripe 45/~30, blooio 20/~33, 2chat 10/~9, grok 3/3, arcads 2/8 (bundled via ARCADS_ROUTES/ARCADS_PRODUCTS target_map), klaviyo 1/2, openai 1/1. - UNWIRED = material gain:   1. arcads — 76-operation OpenAPI spec captured but only ~8 endpoints wired via 2 bundled rows; ~68 operations (audio-driven, b-roll, omnihuman, presets/*, folders, products, file-upload presign, elevenlabs, assets) are docs-only. Largest single expansion off a clean machine spec.   2. openai — ~46 endpoints in captured llms-full, only 1 wired. ~45 unwired.   3. grok — ~17 endpoints in captured reference, 3 wired. ~14 unwired.   4. gemini — 0 wired AND 0 captured spec; fully dark surface, needs a spec/llms.txt capture first (probe an llms.txt before authoring rows). Stripe writes remain forbidden by standing rule (GET-only) regardless of wiring. All classifications: arcads=write (mixed POST/PUT/DELETE creative generation), stripe=spend, klaviyo/2chat/blooio=send, grok/openai/gemini=spend (paid token/credit inference). READ-ONLY honored throughout: jq/grep/python parse only, no CLI run, no HTTP, no secret printed (auth shown as $ARCADS_BASIC_AUTH / Bearer <REDACTED> placeholders as stored).

---

## Mac automation surface for user [OWNER_HANDLE] (Darwin 25.4.0). Three control layers enumerated read-only: (1) macOS Shortcuts via `shortcuts` CLI — 41 user shortcuts confirmed. (2) AppleScript scripting dictionaries — all 6 target apps ship a bundled .sdef inside Contents/Resources, parsed directly with an XML parser (the `sdef` CLI shim is non-functional: it hard-requires full Xcode, only CommandLineTools is installed, so `sdef` exits 1 with "requires Xcode" — bundled .sdef files used instead, equivalent source). (3) Alfred 5 is installed (/Applications/Alfred 5.app, powerpack-licensed) but has ZERO workflows — no user.workflow.* bundles exist anywhere under ~/Library/Application Support/Alfred; the documented path .../Alfred.alfredpreferences/workflows does not exist (real package layout is preferences/local/<synchash>/ containing only hotkey settings). Underlying control primitives: osascript (System Events keystroke/click/key code + display notification), `open -a` to launch apps, and URL schemes. NOTE: per CLAUDE.md, Mac computer-use keystroke/click via System Events fails -1728/-1719 until /opt/homebrew/bin/node is granted Accessibility; enumeration and shell work without it.
counts: `{"shortcuts":41,"scriptable_apps":6,"alfred_workflows":0,"alfred_installed":true,"sdef_cli_functional":false}`

| name | kind | class | detail | wire |
|---|---|---|---|---|
| Shortcuts (macOS) | shortcut_runner | unknown | 41 user shortcuts. Names: Untitled Script; New Shortcut 1/2/3 + base; Copy to Clipboard (x4 variants) ; Get Clipboard; Share with Apps; Open Note; Run | `shortcuts run "<name>"` per shortcut; `shortcuts list` enumerates, `shortcuts view <name>` inspects. Each row is one shortcut; cl |
| Messages | applescript | send | App-specific sdef: 3 commands (send, login, logout) + 4 classes; +1 xi:include (Standard Suite adds generic open/close/quit). send = SEND class. login | `osascript -e 'tell application "Messages" to send "<text>" to buddy ...'`. Per CLAUDE.md WORKS law: NL iMessage from the owner's Mac  |
| Notes | applescript | write | App-specific sdef: 2 commands (open note location, show) + 4 classes (account, folder, note, attachment); +1 xi:include (Standard Suite supplies make/ | `osascript -e 'tell application "Notes" to make new note with properties {body:"..."}'`; `show` / `open note location` to navigate |
| Music | applescript | write | 31 commands, 26 classes, no xi:include (Standard Suite embedded inline). Commands: print, close, count, delete, duplicate, exists, make, move, open, r | `osascript -e 'tell application "Music" to play'` / pause / next track / set sound volume. Playback=read-ish; add/download/delete= |
| Google Chrome | applescript | write | 23 commands, 5 classes, no xi:include. Commands: save, open, close, quit, count, delete, duplicate, exists, make, move, print, reload, go back, go for | `osascript -e 'tell application "Google Chrome" to ... execute ... javascript "..."'`; open URL via `make new tab`. `execute javas |
| Safari | applescript | write | 10 app-specific commands, 3 classes; +1 xi:include (Standard Suite). Commands: add reading list item, do JavaScript, email contents, search the web, s | `osascript -e 'tell application "Safari" to do JavaScript "..." in document 1'`; `search the web`; `add reading list item`. do-Jav |
| System Events | applescript | destructive | 31 commands, 90 classes (largest dictionary); +2 xi:include. Commands: abort/begin/end transaction, connect, disconnect, start, stop, delete, move, op | `osascript -e 'tell application "System Events" to keystroke "..."'` / `key code N` / `click ...` / `keystroke` — the universal GU |
| osascript keystroke/click/notification | applescript | destructive | Control primitive, not an app dictionary. Two distinct capabilities: (a) System Events keystroke/key code/click = arbitrary GUI automation = destructi | `osascript -e '...'` shell-invokable. keystroke/click=GUI control primitive (via System Events, needs Accessibility); `display not |
| open -a / open URL scheme | applescript | write | Control primitive: app launching + URL-scheme dispatch. Lets shell trigger any registered handler without AppleScript. `shortcuts://run-shortcut?name= | `open -a "<App>"` launches an app; `open "<scheme>://..."` (e.g. `open "https://..."`, `messages://`, `music://`, `shortcuts://run |

**gaps:** sdef CLI is non-functional: `/usr/bin/sdef` is an xcode-select shim that exits 1 ('tool sdef requires Xcode, but active developer directory /Library/Developer/CommandLineTools is a command line tools instance'); full Xcode is NOT installed (no /Applications/Xcode.app) and CommandLineTools ships no real sdef/sdp binary. Worked around by parsing each app's bundled .sdef in Contents/Resources directly (equivalent source XML). Install Xcode to restore the CLI.; Dictionary counts are app-specific-suite only for the 4 apps using xi:include (Messages +1, Notes +1, Safari +1, System Events +2). The included Standard Suite / Text Suite (generic open/close/count/delete/duplicate/exists/make/move/print/quit/save, ~8-11 commands) was NOT merged into the counts because the included files were not resolved/parsed. Music and Chrome embed the standard suite inline so their counts are complete. To get fully-merged totals, resolve the xi:include hrefs (point at /System/Library/ScriptingDefinitions/CocoaStandard.sdef and the app's bundled suite files).; Alfred 5 is installed and powerpack-licensed but has ZERO workflows. The path specified in the task (~/Library/Application Support/Alfred/Alfred.alfredpreferences/workflows) does not exist; Alfred.alfredpreferences is a package whose only child is preferences/, and preferences/local/<synchash>/ holds only a 'hotkey' settings dir. No user.workflow.* bundles found anywhere under the Alfred support tree. Gallery/workflows is also empty. Nothing to enumerate.; Per-shortcut action classification (read/write/send/destructive) was NOT resolved — `shortcuts view`/export was not run (would have surfaced each shortcut's action graph). Classifications above are inferred from names only; 'Door Lock'/'Gate code'/'Home' imply HomeKit physical-destructive and 'Send Email' implies send, but the action contents are unverified.

**notes:** Method: `shortcuts list` (41, matches expected). For dictionaries, `sdef <app>` failed on all 6 (Xcode-required shim), so each app's bundled Contents/Resources/*.sdef was parsed with a Python ElementTree namespace-agnostic walk counting <command>/<class>/<xi:include> — not a single grep. Alfred located at /Applications/Alfred 5.app with prefs under ~/Library/Application Support/Alfred (NOT the task's stated workflows path); zero workflows present. All operations read-only: only `ls`/`find`/`mdfind`/`shortcuts list`/file reads/XML parse — no shortcut was run, no AppleScript executed, no app launched, no JS dispatched. Control primitives noted (osascript keystroke/click/notification, open -a, URL schemes) but not invoked. Highest-risk surface: System Events (keystroke/click drives any GUI; log out/restart/shut down) and Chrome/Safari execute/do-JavaScript (arbitrary page code) — all classified destructive/write and currently gated (Accessibility for keystroke, Allow-JS-from-Apple-Events for browser JS).

---

## Live Claude Code MCP layer — connected servers as MCP_IMPORT execution surfaces. Confirmed via ToolSearch schema fetches (Notion, Klaviyo, Chrome both bridges, registry) + deferred-tool name inventory (Preview, scheduled-tasks, Figma, Drive, Semantic Scholar, Typefully). Server UUIDs in tool prefixes are the import handles.
counts: `{"servers":11,"write_capable":8,"read_only":3,"send_or_publish_capable":3,"browser_automation_surfaces":3}`

| name | kind | class | detail | wire |
|---|---|---|---|---|
| Notion | mcp | write | UUID 7a003c5e-67f6-457d-95a1-63f271ee4992. read+write. Tools: notion-search (semantic over workspace + connected Slack/Drive/GitHub/Jira/Teams/SharePo | MCP_IMPORT server=7a003c5e-... then MCP_TOOL_CALL rows: one row per tool. Read rows (notion-search/fetch/query-database-view) safe |
| Klaviyo | mcp | send | UUID d07cb3b1-c887-4c4f-8c7e-ba318882194f. read+write+send. READ: get_campaigns/get_campaign, get_flows/get_flow, get_lists/get_list, get_segments/get | MCP_IMPORT server=d07cb3b1-... then one MCP_TOOL_CALL row per endpoint; every row carries model=claude param. Classify rows: repor |
| Control_Chrome | mcp | write | read+write (arbitrary JS = anything the logged-in session can do). Tools: open_url, navigate (go_back/go_forward), list_tabs, get_current_tab, switch_ | MCP_IMPORT (Control_Chrome bridge name, no uuid prefix) then MCP_TOOL_CALL rows: get_page_content/list_tabs/get_current_tab = read |
| Claude_in_Chrome | mcp | write | read+write, full UI automation incl. submit/purchase/post. Named bridge (no uuid prefix). Tools: navigate, browser_batch (multi-step one round trip),  | MCP_IMPORT (bridge name) then MCP_TOOL_CALL rows. Read rows: get_page_text, read_page, find, read_network_requests, read_console_m |
| Claude_Preview | mcp | write | read+write but sandboxed (no real credentials/funds). Headless preview browser, not the user's real Chrome session. Tools: preview_start/stop/list, pr | MCP_IMPORT (bridge name) then MCP_TOOL_CALL rows; lower blast radius than Claude_in_Chrome since no logged-in session. Read rows:  |
| scheduled-tasks | mcp | write | read+write, creates persistent standing config. Tools: create_scheduled_task (cron or one-time fireAt, writes {taskId}/SKILL.md under ~/.claude/schedu | MCP_IMPORT then MCP_TOOL_CALL rows: list_scheduled_tasks = read; create/update = write (standing config -> confirm per CLAUDE.md p |
| Google Drive | mcp | write | read+write. UUID 1e473c35-51d0-4876-b1a3-7fbe29aaddb9. READ: search_files (structured query DSL), read_file_content, download_file_content, get_file_m | MCP_IMPORT server=1e473c35-... then MCP_TOOL_CALL rows. Read rows (search_files/read_file_content/get_file_metadata) safe. create_ |
| Figma | mcp | write | read+write. UUID 4383a6db-422e-4a87-b236-000bf04417ed. READ: whoami, get_metadata, get_screenshot, get_design_context, get_variable_defs, get_librarie | MCP_IMPORT server=4383a6db-... then MCP_TOOL_CALL rows. Read rows: get_screenshot/get_metadata/get_variable_defs/search_design_sys |
| Typefully | mcp | send | read+write+publish. UUID acf0dd56-550c-4790-8e17-103bff40264a. READ: get_me, list_drafts/get_draft, get_queue/get_queue_schedule, list_social_sets/get | MCP_IMPORT server=acf0dd56-... then MCP_TOOL_CALL rows. Read rows: get_me, list_drafts, get_queue, *_analytics_*. Publish rows: cr |
| Semantic Scholar / PubMed article lookup | mcp | read | read-only. UUID 4d0ef0c2-dc52-4331-8a9d-6ccaeb8da5b5 (PubMed/PMC-backed article server). Tools: search_articles (PubMed syntax/NL, date filters), get_ | MCP_IMPORT server=4d0ef0c2-... then MCP_TOOL_CALL rows, all read. NL 'find peer-reviewed studies on BPC-157 oral bioavailability'  |
| mcp-registry | mcp | read | read + user-gated install UI. Meta-server. Tools: search_mcp_registry (find connectors by keyword, returns connected status), suggest_connectors (rend | MCP_IMPORT registry then MCP_TOOL_CALL rows: search_mcp_registry/list_connectors = read. suggest_connectors renders Connect button |

**gaps:** Claude_in_Chrome / Claude_Preview / Control_Chrome server UUIDs were not exposed in tool prefixes (named bridges, no uuid__ prefix); MCP_IMPORT must key off the bridge name, not a UUID, for these three.; No full per-server schema dump was run for Claude_Preview, Figma, Typefully, Drive, Semantic Scholar; their tool lists come from the deferred-tool name inventory plus one or two fetched schemas each. Exact required-param shapes for unfetched write tools (e.g. Typefully queue_put_queue_schedule, Figma use_figma) are not yet confirmed.; Per the READ-ONLY constraint I invoked no server tool, so no connector was confirmed actually authenticated/reachable this session - connectivity is asserted from the connected-tool listing, not a live ping.; Whether each UUID maps to one or multiple physical accounts (e.g. multiple Klaviyo stores, multiple Figma orgs/Typefully social sets) is unknown; identity calls (whoami, get_me, get_account_details, notion-get-teams) would confirm but are unrun.; Account scope per connector (which Notion workspace, which Klaviyo store, which Drive, which social sets) not enumerated.

**notes:** Top 3 to import first for real-world business execution. (1) Klaviyo (d07cb3b1): the only connected surface that directly performs the core DTC revenue motion - reads campaign/flow/segment performance AND can create+send email/SMS and mutate profile consent. Highest direct-money leverage; also the one most needing hard send-gates and the BigCommerce-owns-revenue reconciliation caveat. (2) Claude_in_Chrome: universal fallback executor - anything with no API (BigCommerce admin, Meta Ads Manager, Triple Whale, bank/vendor portals) is reachable through the user's authenticated Chrome via form_input/computer/browser_batch, making it the broadest real-world-action surface; pair every write row with confirm/prohibited tiers since a misfired click can send/spend/delete. (3) Notion (7a003c5e): the system of record and cross-tool search hub - backs the Loop content MATRIX, the factual peptide DB, and the audit ledgers the standing-orders law requires, and notion-search reaches Slack/Drive/GitHub/Jira too, so one import yields both the writable data board and a federated read across the other stacks. Runner-up: scheduled-tasks turns any of the above into a durable cadence (recurring reporting pulls, model-input nudges) which the end-of-turn-sync and reporting laws call for. Import-layer classification rule: reports/get_*/search_* rows are read and may auto-run; create_campaign/subscribe (Klaviyo), Typefully publish/schedule, any Chrome click-to-submit, and all standing-config writes (scheduled-tasks create/update, Notion data-source DDL) must carry per-action confirm; OAuth grant / ACL change / fund movement stay prohibited-tier regardless of connector.

---

